/**
 * 순서를 통째로 바꿀 때 기존 행을 잠시 밀어 두는 거리.
 *
 * ⛔ 순서 컬럼의 유일 제약은 지연 가능(deferrable)이 아니다. 두 줄의 순서를 맞바꾸면
 * **중간 상태가 반드시 그 제약을 위반한다**(공유계약 `A-5` 가 지목한 자리). 음수로 미는
 * 흔한 수법은 `CHECK (... > 0)` 이 막으므로 «위로» 민다.
 *
 * 최종 순서가 이 값보다 작아야 부딪히지 않는다 — 호출자가 요청 검증에서 함께 본다.
 * 공정 라인(`planning.routing_operation`)과 검사 항목(`quality.inspection_item_spec`)이
 * 같은 형태다: 둘 다 순서에 유일 제약이 있고, 둘 다 **NOT NULL FK 로 참조되어** 지우고
 * 다시 넣을 수 없다.
 */
export const SEQ_PARKING_OFFSET = 1_000_000;
