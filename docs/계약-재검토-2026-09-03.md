# 계약 전면 재검토 — 2026-09-03 (설계 변동 공지 `6d03a44`)

> 공지: 이 저장소 이슈 #152 「[설계 변동 공지] 2026-09-03 · 6d03a44」 · 설계 태그 `notice/20260903`
> 사본: `77a067b` → `6d03a44` (설계 커밋 42건, 2026-08-25 ~ 09-03) · 절차: `docs/agents/team-protocol.md` 규칙 5
> 직전 기록: [`계약-되돌림-mdm.md`](./계약-되돌림-mdm.md) · [`data-model/05-재검토-2026-08-28.md`](./data-model/05-재검토-2026-08-28.md)

이 라운드의 본질은 오퍼레이션 추가(+8)가 아니라 **코드 사전 연동**이다 — 계약 자리 498곳에
`x-code-key` 가 붙고(`design/schema/code-dictionary.md` 175키 · 502슬롯), 그동안 서버 시드가
임의로 정해 쓰던 코드 문자열이 설계 값으로 확정됐다. 그래서 걸리는 것의 대부분이 **값 문자열**이다.

## 1. 드리프트

| 축 | 77a067b | 6d03a44 | 비고 |
|---|---|---|---|
| 오퍼레이션 | 482 | **490** | +8 · 삭제 0 · 경로 변경 0 |
| 403 선언 오퍼레이션 | 249 | **253** | 공정 마스터 쓰기 4건 |
| `operationId` 보유 | 7 | 8 | |
| 구현 | 198 | 198 | 커버리지 198/490 · **남은 것 292** |
| 설계 변경 회차 | 93 | 91 | 설계팀이 회차 입자를 바꿨다(`변경-요약.md` 2026-08-25 ~ 09-03 = 91). 되돌아간 게 아니다 |

새 오퍼레이션 8건:

| 태그 | 오퍼레이션 | 설계 근거 |
|---|---|---|
| mdm | `POST /mdm/processes` · `GET`·`PUT /mdm/processes/{processId}` · `:activate` · `:deactivate` | `d5029b3` 공정 마스터를 W-06-01 이 흡수 |
| mdm | `GET /mdm/judgment-type-controls/{codeValueId}` | 단건 조회 신설(ETag) |
| equipment | `POST /maintenance/calibrations/{calibrationId}:clear` | `Calibration.blocksUse` 신설 — 사용 차단 해소 |
| shipment | `GET /quality/disposition-candidates` | 처분 판정 후보 목록(저장 표 없음) |

`GET /app/approval-requests` 는 새것이 아니다 — `$WorkerNoOptional` 파라미터만 늘었다.

이 PR 에서 함께 옮긴 것: `scripts/data_model/api_mapping.py` 에 `/quality/disposition-candidates` 매핑 규칙 추가
(생성기가 미매핑 경로에서 죽는다) · `docs/data-model/*` 재생성(490 중 489 매핑, 검증 PASS) ·
하드코딩 수치 482→490 / 249→253 (`contract-*.spec.ts` · `operation-permissions.spec.ts` · 주석).
`docs/server-architecture.md` 의 482·249 는 **77a067b 시점 실측**이라 그대로 둔다.

## 2. 구현한 오퍼레이션에 걸리는 것

### 2-1. 지금 깨진다 — e2e 3 스위트 · 11 테스트

런타임 요청 검증이 계약을 그대로 읽으므로 요청 측 변경은 코드를 안 고쳐도 즉시 400 이 난다.

| # | 계약 | 지금 서버 | 증거 |
|---|---|---|---|
| B-1 | `Item.lotControlled` boolean **required**(`x-source-column: lot_controlled`), `lotControlTypeCode` 삭제 | `mdm.item.lot_control_type_code VarChar(50) NOT NULL`(`prisma/schema.prisma:1721`) · `item.service.ts:19,35,123,166-170,206` · 그룹 `LOT_CONTROL_TYPE` 검증 | `test/mdm-item.e2e-spec.ts` 요청 `REQUIRED` + 응답 스키마 불일치. 픽스처 `lot_control_type_code` 9 스위트(`mdm-item`·`planning-bom:365`·`inventory-posting:71`·`logistics-putaway-rule:358` 등) |
| B-2 | `ItemExternalCode.externalSystemCode` enum `EQUIPMENT_STANDARD_IF`·`TRACKING_SYSTEM`·`UNIERP` | 코드는 검증 안 함(값 그대로 저장) | `test/mdm-item-detail.e2e-spec.ts:217-265` `'ERP'` 7곳 → enum 밖 400 |
| B-3 | `ProductionLine.lineTypeCode` enum `LINE`·`WORK_AREA`(응답에도 enum) | `equipment-group.service.ts:188` 이 `LINE_TYPE` 그룹으로 검증 — 시드 값은 이미 LINE/WORK_AREA | `test/mdm-reference.e2e-spec.ts` `GET /mdm/production-lines` 응답 검증 실패 — 원인: 같은 스위트 픽스처(`:105`)가 `line_type_code: 'ASSEMBLY'` 를 넣는다(PR B 에서 `LINE` 으로) |

### 2-2. 지금은 안 깨지지만 계약과 뜻이 다르다

| # | 계약 | 지금 서버 | 고칠 것 |
|---|---|---|---|
| S-1 | `Routing/Bom/InspectionPlanVersion.statusCode` = `MASTER_VERSION_STATUS` DRAFT·**CONFIRMED**·OBSOLETE(결정 07) | `src/planning/revision-status.ts` `CONFIRMED: 'ACTIVE'` · `src/core/document-state/transitions.ts:61,65` `'ACTIVE'` · 시드 그룹 이름 `REVISION_STATUS` | 상수·전이표 한 줄씩 + 시드 그룹 개명 + **데이터 마이그레이션** `UPDATE … SET status_code='CONFIRMED' WHERE status_code='ACTIVE'` (planning.routing · planning.bom · quality.inspection_plan_version). e2e 는 전부 상수를 거쳐 픽스처 수정 없음. **PR C 반영**(마이그레이션 20260904110000) |
| S-2 | `IntegrationMessage.statusCode` PENDING·PROCESSING·**DONE**·FAILED | `integration-message.service.ts:31-36` `COMPLETED: 'COMPLETED'` | 상수 값 + 데이터 마이그레이션 + `test/integration-message.e2e-spec.ts:166,180` |
| S-3 | `AppUser.statusCode` = `APP_USER_STATUS` **EMPLOYED**·ON_LEAVE·RESIGNED, 「⛔ ACTIVE 를 쓰지 않는다」 | `app_user.status_code DEFAULT 'ACTIVE'`(`app-user.service.ts:117-118` 이 기본값에 기댐) · `seed.ts:1442` admin `'ACTIVE'` · 시드 그룹 ACTIVE/ON_LEAVE/RETIRED | 컬럼 기본값 EMPLOYED + 시드 3값 + 기존 행 UPDATE. `test/app-user.e2e-spec.ts` 5곳. ⚠ 계약 자기모순 — §8-1. **PR D-1 반영**(마이그레이션 20260904120000 · 픽스처 34 스펙) |
| S-4 | `Terminal.terminalTypeCode` POP·MOBILE(관리웹은 등록 안 함) · `statusCode` **RUNNING**·STOPPED (둘 다 registry-system) | `terminal.service.ts:138,159,343` 그룹 검증 없음 · 시드 `TERMINAL_TYPE` ADMIN_WEB/POP/MOBILE · `TERMINAL_STATUS` NORMAL/MAINTENANCE/DISPOSED | 시드 교체 + 그룹 검증 추가(다른 마스터와 같은 패턴). `test/mdm-terminal.e2e-spec.ts` 3곳. **PR D-2 반영**(마이그레이션 20260904130000) |
| S-5 | `Mold.statusCode` → `EQUIPMENT_STATUS`(IN_SERVICE·DISPOSED) | 시드 `MOLD_STATUS` NEW/NORMAL/REPAIR/DISPOSED — 금형은 미구현 | 시드 그룹 삭제(되돌림 O-4 해소) |
| S-6 | `Worker.statusCode` `x-no-code-key` — isActive 가 정본 | 시드 `WORKER_STATUS` 만 있고 판정은 이미 isActive | 시드는 개발용으로 둔다. 작업자 ETag 주석(되돌림 §C) 는 계약이 선언했으니 지운다 |
| S-7 | `IntegrationMessage.targetTypeCode` 가 `INTERFACE_TARGET` 그룹(6자리 `x-code-key`) | `interface-definition.service.ts:28` `CONFIRMED_TARGETS` 5값 = 사전과 같음. 「막지 않고 표식만」도 같음 | 없음 — 그룹만 시드(§3) |

### 2-3. 값 문자열이 바뀐 등록부 그룹 — 구현 코드가 검증에 쓰는 것

**PR D-2 반영**(마이그레이션 20260904130000): `ITEM_TYPE`·`LOCATION_TYPE`·`WAREHOUSE_TYPE`·`WORK_ORDER_STATUS`·`WORK_SESSION_STATUS`. `LINE_TYPE`(시스템 소유 표시)·`DEPENDENCY_TYPE`(개명) 은 **PR D-4 반영**.

| 그룹 | 시드(지금) | 사전 | 검증 위치 |
|---|---|---|---|
| `ITEM_TYPE` | RAW/SEMI/FG/MDSE/DEV | RAW_MATERIAL/SEMI_FINISHED/FINISHED/MERCHANDISE | `item.service.ts` · 픽스처 `'FG'` 5 · `'RAW'` 3 · `'MATERIAL'` 1 |
| `LOCATION_TYPE` | ZONE/RACK/CELL/DOCK | RACK/FLOOR/TEMP/HOPPER/DEFAULT (TEMP·HOPPER·DEFAULT 는 화면 판정에 쓴다) | `location.service.ts:215` · 픽스처 `'RACK'`·`'BIN'` |
| `WAREHOUSE_TYPE` | 6값 | MATERIAL/PRODUCT/SPARE_PART/GENERAL | `warehouse.service.ts:219` · 픽스처 `'RAW'`·`'MATERIAL'` |
| `LINE_TYPE` → enum `CD-PRODUCTION-LINE-TYPE` | LINE/WORK_AREA | 같음 | `equipment-group.service.ts:188` — B-3 원인 확인만 |
| `DEPENDENCY_TYPE` → `ROUTING_OPERATION_DEPENDENCY_TYPE` | 3값 | 4값(+START_TO_FINISH) | `routing-operation.service.ts:165` 그룹 코드 문자열 |
| `WORK_ORDER_STATUS` | 사전 8 + BLOCKED | 8값(「진행불가」는 상태가 아니다) | 미구현 — 시드만 |
| `WORK_SESSION_STATUS` | OPEN/PAUSED/CLOSED | RUNNING/STOPPED/ENDED | 미구현 — 시드만 |

## 3. 시드 vs 코드 사전 — 전수 비교

`prisma/seed.ts` 코드 그룹 85개와 사전 175키를 그룹 코드로 맞춰 봤다. 비교 스크립트는 저장소에 넣지 않았다 —
아래 표가 결과의 정본이고, PR-D 가 시드를 고칠 때 같은 비교를 다시 한다.

| 판정 | 수 | 내용 |
|---|---|---|
| 값이 다르다 | 8 | §2-3 의 7 + `TERMINAL_TYPE`/`TERMINAL_STATUS`/`APP_USER_STATUS`(§2-2) |
| 시드에만 있다 | 22 | **PR D-4 반영**(마이그레이션 20260904140000 — 개명 2 · 그룹 은퇴 15 · `LINE_TYPE`·`SERIAL_CONTROL_TYPE`·`LOT_*_TRANSITION` 은 서버·e2e 가 읽어 유지) — 이름이 바뀐 것: `REVISION_STATUS`→`MASTER_VERSION_STATUS` · `DEPENDENCY_TYPE`→`ROUTING_OPERATION_DEPENDENCY_TYPE` · `FREQUENCY_INTERVAL_UOM`→`INSPECTION_FREQUENCY_INTERVAL_UOM`(HOUR/QUANTITY) · `INSPECTION_TYPE`(=`QUALITY_INSPECTION_TYPE` 중복). enum 으로 승격돼 그룹이 필요 없는 것: `LINE_TYPE` · `PARTNER_ROLE_TYPE`(사전 CUSTOMER/DISPOSAL/OTHER/SUBCONTRACTOR/SUPPLIER — 시드의 CARRIER 없음) · `DOCUMENT_TYPE`(출력물 9 / 물류 문서 9 로 갈라짐) · `APPROVAL_TYPE`(5 vs 8) · `APPROVER_TYPE` · `OPERATION_POLICY`(→`CD-POLICY` 5) · `RESULT_SOURCE`(POP/EQUIPMENT/MANUAL → MANUAL/IOT) · `LOT_STATUS_TRANSITION`. 사라진 것: `LOT_CONTROL_TYPE` · `MOLD_STATUS` · `WORKER_STATUS`(개발용 유지). 나머지: `SERIAL_CONTROL_TYPE` · `SAMPLING_METHOD` · `INSPECTION_DATA_TYPE` · `INSPECTION_METHOD` · `RESET_CYCLE` · `PRODUCTION_RESULT_STATUS` · `LOT_LIFECYCLE_TRANSITION` |
| 사전이 비워 둔 등록부 | 4 | `QUALITY_ZONE` · `CALIBRATION_RESULT` · `GOODS_RECEIPT_REASON` · `JUDGMENT_TYPE` — 고객이 채운다. 개발 시드는 둔다 |
| 사전에만 있다 | 40 | **PR D-3 반영**(37 그룹 · `MASTER_VERSION_STATUS` 는 PR C · 개명 대상 둘은 D-4) — 시드가 없는 그룹 — 구현이 곧 쓰는 것: `MASTER_VERSION_STATUS` · `INTEGRATION_MESSAGE_STATUS` · `INTERFACE_TARGET` · `ROUTING_OPERATION_DEPENDENCY_TYPE` · `INSPECTION_FREQUENCY_INTERVAL_UOM` · `AUDIT_EVENT_TYPE`. 나머지 34: `APPROVAL_REQUEST_STATUS` `BREAKDOWN_OCCURRENCE_STATE` `CALIBRATION_AGENCY_TYPE` `CALIBRATION_HISTORY_TYPE` `DEFECT_RECORD_SOURCE` `DEFECT_RESPONSIBILITY_TYPE` `EQUIPMENT_BREAKDOWN_STATUS` `INSPECTION_RESULT_STATUS` `INVENTORY_COUNT_STATUS` `LATE_ENTRY_REASON` `LOGISTICS_DOCUMENT_STATUS` `LOT_SOURCE_TYPE` `MAINTENANCE_ORDER_ITEM_STATUS` `MAINTENANCE_ORDER_STATUS` `MAINTENANCE_ORDER_TRIGGER_TYPE` `MAINTENANCE_RESULT_LINE_RESULT` `MATERIAL_CHANGE_REASON` `MES_CATEGORY` `MOLD_PM_TRIGGER_TYPE` `NONCONFORMANCE_SEVERITY` `NONCONFORMANCE_STATUS` `PRODUCTION_ORDER_STATUS` `PRODUCTION_PLAN_STATUS` `PRODUCTION_RESULT_CORRECT_REASON` `PUTAWAY_TASK_STATUS` `REINSPECTION_REASON` `SHIPMENT_STATUS` `STOCK_REINSTATEMENT_REASON` `STOCK_TRANSFER_REASON` `STOCK_TRANSFER_TYPE` `TOOL_TYPE` `WORK_ORDER_HOLD_REASON` `WORK_ORDER_TYPE` `WORK_SESSION_WORKER_ROLE` |
| 같다 | 51 | |

시드 정렬은 **한 PR** 로 묶되(§9 PR-D), 구현 코드가 검증에 쓰는 그룹(§2-3)은 서비스·픽스처 수정과 같은
PR 에 둔다 — 시드만 먼저 바꾸면 e2e 가 깨진다.

## 4. 새로 만들 것 (mdm 잔여 16 중 6)

| 오퍼레이션 | 형태 | 재료 |
|---|---|---|
| 공정 마스터 ×5 (`POST`·`GET`·`PUT`·`:activate`·`:deactivate`) | 표준 마스터 패턴 — `ProcessDetailResponse` + 편집 가능성(`process_id` FK 참조 수) · ETag(`version_no`)/If-Match · 403 선언 4건 · 코드 중복 400 · `processTypeCode` → `PROCESS_TYPE` 그룹 검증 | `mdm.process` 표(process_code unique · process_type_code · is_active · version_no)와 시드 `PROCESS_TYPE` 4값(MACHINING/ASSEMBLY/INSPECTION/PACKAGING = 사전) 이미 있음 |
| `GET /mdm/judgment-type-controls/{codeValueId}` | 단건 + ETag | 목록·PUT 이 이미 있다 |

## 5. 미구현 도메인에 걸리는 것 — 기록만

구현 시점에 계약을 다시 읽으므로 여기서는 「어디가 바뀌었나」만 남긴다.

| 도메인 | 바뀐 자리 |
|---|---|
| equipment | `Calibration.blocksUse`(required · 요청은 선택 default false) + `:clear` — 「열린 수리 이력」 추론 판정식을 만들지 않는다 |
| shipment | `GET /quality/disposition-candidates` — 불량창고 LOT 을 뿌리로 RETURN·PRODUCT 원천을 붙인다 |
| quality | `DefectRecordCreate.sourceCode` 신설 · `LotHoldEvent.releaseReasonCode`(`LOT_HOLD_RELEASE_REASON`, RELEASED 사건에만) · `InspectionResultCreate.statusCode` 한국어 enum 「작성중/확정」 → DRAFT/CONFIRMED **enum 해제**(그룹 `INSPECTION_RESULT_STATUS`) |
| logistics | `POST /trace/lots` 에 enum 이 붙었다(⚠ 미구현 — `contract-bindings.spec.ts` 의 `@Contract` 는 픽스처다) |
| app | `GET /app/approval-requests` `$WorkerNoOptional` |
| production | `WORK_ORDER_STATUS` 8값(BLOCKED 없음) · `WORK_SESSION_STATUS` RUNNING/STOPPED/ENDED |

## 6. `계약-되돌림-mdm.md` 항목 판정

| 항목 | 6d03a44 |
|---|---|
| 4. `externalSystemCode` enum · `QUALIFICATION_TYPE` | **풀림** — enum 3값 확정(B-2) |
| C. 작업자 ETag 미선언 | **풀림** — 계약이 선언 |
| N. 설비 유형 그룹 | **풀림** |
| O-4. 금형 상태 그룹 | **풀림** — `EQUIPMENT_STATUS` 준용 |
| S-1 / T-1 / V-1. 버전 상태 문자열 | **풀림** — `MASTER_VERSION_STATUS` CONFIRMED (S-1 수정 대상) |
| X-1. 연계 대상 값 | **풀림** — `INTERFACE_TARGET` 5값 = 구현 |
| X-4. 메시지 완료 문자열 | **풀림** — DONE (S-2 수정 대상) |
| 13 / #65 | 부분 |
| 3. `conflictCause` · 5. `certifiedBy` FK · S-2/S-3/S-4 · T-2 · U | **그대로 열림** |

`계약-되돌림-mdm.md` 머리말에 같은 표를 붙였다.

## 7. 남은 계획 — 292

| 도메인 | 남은 오퍼레이션 |
|---|---|
| mdm | 16 (공정 5 · 판정유형 단건 1 · 감사 이벤트 · 금형 8 · 예비품 import) |
| app | 46 |
| equipment | 36 |
| logistics | 94 |
| production | 52 |
| quality | 27 |
| shipment | 21 |

작업 #12 의 잔여 수치를 292 로 고친다. 완료 판정은 그대로 `contract-coverage.spec.ts` 를
`expect(implemented).toBe(registry.size)` 로 뒤집는 것.

## 8. 설계팀에 물을 것 — 문서만, 이슈는 열지 않는다

규칙 2 — 자료는 사용자를 통해 전달한다. 사용자가 「전달했고 회신 대기」라 하면 그때 이 저장소에
`[설계 요청]` 추적 이슈를 연다.

| # | 자리 | 무엇 |
|---|---|---|
| 8-1 | `AppUser.statusCode` | `"default": "ACTIVE"` 가 남아 있는데 같은 자리 설명이 「⛔ ACTIVE 를 쓰지 않는다」. 서버는 EMPLOYED 를 기본값으로 간다 — 계약의 `default` 를 EMPLOYED 로 고쳐 달라 |
| 8-2 | `Worker.certifiedBy` | 여전히 「FK 없음」(되돌림 5) |
| 8-3 | `conflictCause` | 변경 없음(되돌림 3) |
| 8-4 | `변경-요약.md` 회차 | 93 → 91 로 줄었다. 입자를 바꾼 것으로 읽었다 — 맞는지 |

## 9. 대응 — PR 분할

| PR | 내용 | 규모 |
|---|---|---|
| **A (이것)** | 계약 사본 6d03a44 · 산출물 재생성 · 수치 · 이 문서 | 생성물 제외 소량 |
| B | `item.lotControlled` — 마이그레이션 선행 커밋(`lot_controlled boolean NOT NULL DEFAULT false` + `lot_control_type_code='LOT'` 백필 · 옛 컬럼 nullable 로, 삭제는 다음 릴리스) → 서비스·뷰·`LOT_CONTROL_TYPE` 검증 제거 → 픽스처 9 스위트 + `'ERP'`→`'UNIERP'` 7곳 | 코어 아님 · ≤ 400 |
| C | `MASTER_VERSION_STATUS` CONFIRMED — 데이터 마이그레이션 선행 → `revision-status.ts` · `transitions.ts` · 시드 그룹 개명 · 픽스처 | 전표 상태기계 건드림 → **≤ 200** |
| D | 시드를 코드 사전에 맞춘다 — 값 다른 8 + 이름 바뀐 그룹 + 사전에만 있는 40 + `app_user` 기본값 EMPLOYED 마이그레이션 + 단말 그룹 검증 + `ITEM_TYPE`/`LOCATION_TYPE`/`WAREHOUSE_TYPE` 픽스처. 400줄 넘으면 「구현이 검증에 쓰는 그룹」 / 「시드만」 둘로 | 넷으로 나눔 — D-1 `app_user` EMPLOYED(#156) · D-2 마스터 유형 7 그룹(#157) · D-3 사전 등록부 37 그룹 · D-4 그룹 개명·은퇴·소유 플래그 |
| E | `INTEGRATION_MESSAGE_STATUS` DONE — 마이그레이션 + 상수 + e2e | 소량 |
| F | 공정 마스터 ×5 | 표준 마스터 1건 |
| G | 판정유형 통제 단건 GET | 소량 |

F·G 가 붙으면 데모 서버
(`omf_demo`)를 새로 올린다.
