# 기준정보 API 재작성 계획

> 근거: [ADR 0001](./adr/0001-mdm-api-rewrite-on-spec.md) · 용어: [CONTEXT.md](../CONTEXT.md)
> 계약: `omf-mes/deliverables/openapi/mdm-기준정보.json` (0.1.0 초안)

## 범위

`mdm` 태그 **23개 경로 / 46개 오퍼레이션**. `/app`(16) · `/quality`(25) ·
`/planning`(17) · `/integration`(4) · `/audit`(1)는 다음 라운드.

| 묶음 | 대상 | 오퍼 |
|---|---|---|
| 전체 CRUD | 창고, 로케이션, 코드그룹, 코드값, 부서 | 25 |
| ERP 수신본 — 부분 편집 | 품목(본체 + 환산·외부코드·BU매핑), 작업자(자격) | 13 |
| 조회 전용 | 단위, 거래처, 법인, 사업부, 공장, 라인, 공정, 설비 | 8 |

## 확정된 것

1. `src/` 전체 삭제 후 스펙 기준 재작성. `prisma/` 는 정본이므로 유지.
2. 스펙 형태(경로·메서드·봉투·헤더)는 동결. 미결 내용은 구현하며 정하고 계약
   소유자에게 되돌린다.
3. `/mdm` 응답의 int64 는 JSON 숫자.
4. 스펙 사본을 `contracts/` 에 두고 타입을 생성해, 어긋남을 컴파일러가 잡게 한다.
   사본 갱신은 사람이 하고 그 diff 가 계약 변경 알림이다.
5. PR 단위는 "한 번에 읽고 이해되는가" 기준. 계약이 태어나는 구간은 잘게,
   패턴 복제 구간은 마스터 단위로.

## 스펙과 현행의 차이 (재작성 이유)

| 축 | 스펙 | 삭제 대상 코드 |
|---|---|---|
| 식별자 | `{warehouseId}` int64 | `:warehouseCode` 문자열 |
| 수정 | `PUT` 전체 교체 | `PATCH` 부분 |
| 중지 | `POST /{id}:deactivate` → 200 + 본문 | `DELETE /{code}` → 204 |
| 목록 | `{items, page:{page,size,total}}` | `{items,total,page,size,totalPages}` |
| 상세 | `{warehouse, editability}` + 참조 건수 | Prisma 행 그대로 (snake_case) |
| 낙관적 잠금 | `If-Match` 필수 / `ETag` 응답, `version_no` 본문 비노출 | 없음 |
| 멱등 | `Idempotency-Key` 전 쓰기 필수 | `pop` 일부만 |
| 오류 | `{errors:[{scope,code,field,uniqueScope,message}]}` | Nest 기본 |

## 순서

### 건져내기 — 완료

스펙에 없고 삭제된 코드에만 있던 규칙은 [기존 구현 도메인 규칙](./기존-구현-도메인-규칙.md)
으로 옮겼다. 인증 설계 판단은 [ADR 0002](./adr/0002-self-hosted-password-auth.md).

### PR 사다리

정독 대상은 **C·D·E·F·H·I·J 일곱 개**이고 전부 200줄 안쪽이다. 나머지는 패턴
대조로 읽는다. J 를 넘기면 남은 39개 오퍼레이션에는 새 결정이 없다.

머지될 때마다 상태 칸을 갱신한다. 이 표가 진척표다.

| # | 상태 | 내용 | 오퍼 | 추정 | 성격 |
|---|---|---|---|---|---|
| A | ✅ #10 `63d6a20` | 골격 — 부팅되는 빈 Nest 앱 + Prisma 모듈 + health | – | 162 | 기계적 |
| B | ✅ #11 `3ea6b48` | 계약 사본 `contracts/` + 타입 생성 | – | 60 | 기계적 |
| — | | ↑ 생성물은 커밋하지 않고 CI·Docker 가 매번 만든다 (Prisma Client 와 같은 취급) | | | |
| C | ✅ #12 `6c56256` | `GET /mdm/warehouses` — `PageMeta` 봉투 · 응답 매퍼 · 검색/페이지 | 1 | 171+테스트 260 | 정독 |
| D | ✅ #14 | `GET /mdm/warehouses/{id}` — `ETag` · 편집 가능성 · 참조 건수 | 1 | 128+테스트 166 | 정독 |
| E | ▶ 진행 | 인증 — 자격증명·해시·로그인 | – | 273+테스트 378 | 정독 |
| F | ▶ 다음 | 가드 — 화이트리스트 보호 + 권한 데코레이터, C·D 에 적용 | – | ~150 | 정독 |
| G | | 멱등 저장소 마이그레이션 | – | ~80 | 선행 커밋 |
| H | | `POST /mdm/warehouses` — 오류 봉투 `{errors:[…]}` | 1 | ~200 | 정독 |
| I | | 멱등 인터셉터 | – | ~120 | 정독 |
| J | | `PUT /mdm/warehouses/{id}` — `If-Match` · 충돌 원인 | 1 | ~180 | 정독 |
| K | | `POST …:deactivate` | 1 | ~80 | 복제 |
| L | | 로케이션 | 5 | ~250 | 복제 |
| M | | 코드그룹 · 코드값 | 10 | ~350 | 복제 |
| N | | 부서 | 5 | ~200 | 복제 |
| O | | 조회 전용 8종 | 8 | ~250 | 복제 |
| P | | 품목 본체 + 자식 3종 | 9 | ~300 | 새 의미론 |
| Q | | 작업자 + 자격 | 4 | ~150 | 복제 |

### 넘긴 것

| 항목 | 어디로 | 나온 곳 |
|---|---|---|
| e2e 와 부팅이 프리픽스를 따로 갖는다 | C — `ValidationPipe` 도입 시 `configureApp` 으로 공유 | #10 리뷰 |
| 헬스체크 실패 시 원인이 로그에 안 남는다 | 미정 — 주기 폴링이라 로깅 여부 자체가 판단 | #10 리뷰 |
| 의존성 변경 PR 은 lockfile 을 같은 커밋에 넣는다 | 앞으로 전부 | #10 리뷰 |
| 시드가 조직 계층(법인→사업부→공장)을 만들지 않는다 | D 전 — `prisma/seed.ts` 에 최소 한 벌을 넣거나 e2e 공용 픽스처 헬퍼를 둔다 | #12 리뷰 |
| 목록 정렬 기준을 계약 소유자에게 확인 | 미정 — 되돌리기 전까지 프런트가 서버가 임의로 정한 순서에 의존하게 된다 | #12 리뷰 |
| 참조 건수가 전표 상태를 가리지 않는다 | **K** — 코드 편집 잠금에는 맞지만(취소된 전표도 그 시점 코드를 가리킨다) 사용 중지는 판단이 다르다. `countWarehouseReferences` 를 그대로 재사용하면 한 번이라도 쓰인 창고가 영영 중지되지 않는다 | #14 리뷰 |
| FK 인덱스 부재 — 창고 외 마스터 | L·N — 로케이션(19개)·부서(7개)도 같은 문제를 만난다. 참조 건수를 세기 전에 인덱스를 확인한다 | #14 리뷰 |

**인증이 C·D 뒤에 오는 이유**: 개발 중 조회 하나 확인할 때마다 토큰을 붙이는 것이
번거롭다. 무보호 구간은 읽기 전용인 C·D 둘로 한정하고, 첫 쓰기(H) 앞에 F 를 넣는다.
쓰기는 감사 컬럼 `created_by`/`updated_by` 에 인증 주체를 기록해야 하므로 인증
없이는 만들 수 없다.

## 미결 — 해당 PR 에서 정한다

| 항목 | 정할 곳 | 메모 |
|---|---|---|
| 목록 정렬 기준 | C — 잠정 확정 | 계약에 정렬이 없다. 불안정하면 페이지를 넘길 때 같은 행이 두 번 나오거나 빠지므로 유일키 `(plant_id, warehouse_code)` 로 못 박았다. 계약 소유자에게 되돌릴 항목 |
| 목록 `size` 상한 | C — 잠정 확정 | 계약에 상한이 없다. 없으면 `size=1000000` 하나로 전 테이블을 긁어가므로 200 으로 뒀다. 되돌릴 항목 |
| 참조 건수를 세는 방법 | D — 확정 | 창고는 15개 FK 컬럼(13 테이블)을 스칼라 서브쿼리 하나로 합산한다. 목록을 손으로 적되 e2e 가 `pg_constraint` 와 대조해 누락을 잡는다. 로케이션·부서도 같은 패턴을 복제한다 |
| ERP 수신본 판정 근거 | **P·Q** | 정본에 출처 컬럼이 없다. 창고는 MES 가 관리하는 마스터라 해당 없음 — 품목·작업자에서 필요하다. [기존 구현 도메인 규칙](./기존-구현-도메인-규칙.md) 에도 같은 미결로 적혀 있다 |
| 상세 조회의 404 | D — 확정 | 계약에는 200 만 있으나 없는 id 에 200 을 줄 수 없어 서버가 정의했다 |
| 편집 권한 매트릭스 (계약 §8-8 미결) | F·H | 계약 소유자에게 되돌릴 항목 |
| 멱등 키 보관 기간·재사용 정책 | G·I | |
| 충돌 원인 `erpSync`·`workerLease` 를 무엇으로 판정하나 | J | 스키마에 리스 개념이 없음 |
| 계정 잠금 | 보류 — 결정 (2026-08-05) | 실패 횟수만 센다. `locked_until` 컬럼이 이미 있어 판정 한 줄이면 붙는다. [ADR 0002](./adr/0002-self-hosted-password-auth.md) |
