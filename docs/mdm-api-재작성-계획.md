# 기준정보 API 재작성 계획

> 근거: [ADR 0001](./adr/0001-mdm-api-rewrite-on-spec.md) · 용어: [CONTEXT.md](../CONTEXT.md)
> 계약: `omf-mes/deliverables/openapi/mdm-기준정보.json` (0.1.0 초안)

## 범위

`mdm` 태그 **23개 경로 / 46개 오퍼레이션**. `/app`(16) · `/quality`(25) ·
`/planning`(17) · `/integration`(4) · `/audit`(1)는 다음 라운드.

| 묶음 | 대상 | 오퍼 |
|---|---|---|
| 전체 CRUD | 창고, 로케이션, 코드그룹, 코드값, 부서 | 25 |
| ERP 수신본 — 부분 편집 | 품목(본체 + 환산·외부코드·BU매핑), 작업자(자격) | 13 |
| 조회 전용 | 단위, 거래처, 법인, 사업부, 공장, 라인, 공정, 설비 | 8 |

## 확정된 것

1. `src/` 전체 삭제 후 스펙 기준 재작성. `prisma/` 는 정본이므로 유지.
2. 스펙 형태(경로·메서드·봉투·헤더)는 동결. 미결 내용은 구현하며 정하고 계약
   소유자에게 되돌린다.
3. `/mdm` 응답의 int64 는 JSON 숫자.
4. 스펙 사본을 `contracts/` 에 두고 타입을 생성해, 어긋남을 컴파일러가 잡게 한다.
   사본 갱신은 사람이 하고 그 diff 가 계약 변경 알림이다.
5. PR 단위는 "한 번에 읽고 이해되는가" 기준. 계약이 태어나는 구간은 잘게,
   패턴 복제 구간은 마스터 단위로.
6. (2026-08-10) 복제 구간은 더 크게 묶는다 — 창고 8 PR → 로케이션/부서 3 PR →
   조회 전용은 1 PR. 새 결정이 없으면 계약 제시 왕복을 생략하고 PR 본문에
   「선례와 다른 점」만 적는다. 결정이 생기면 그때 멈춘다.

## 스펙과 현행의 차이 (재작성 이유)

| 축 | 스펙 | 삭제 대상 코드 |
|---|---|---|
| 식별자 | `{warehouseId}` int64 | `:warehouseCode` 문자열 |
| 수정 | `PUT` 전체 교체 | `PATCH` 부분 |
| 중지 | `POST /{id}:deactivate` → 200 + 본문 | `DELETE /{code}` → 204 |
| 목록 | `{items, page:{page,size,total}}` | `{items,total,page,size,totalPages}` |
| 상세 | `{warehouse, editability}` + 참조 건수 | Prisma 행 그대로 (snake_case) |
| 낙관적 잠금 | `If-Match` 필수 / `ETag` 응답, `version_no` 본문 비노출 | 없음 |
| 멱등 | `Idempotency-Key` 전 쓰기 필수 | `pop` 일부만 |
| 오류 | `{errors:[{scope,code,field,uniqueScope,message}]}` | Nest 기본 |

## 순서

### 건져내기 — 완료

스펙에 없고 삭제된 코드에만 있던 규칙은 [기존 구현 도메인 규칙](./기존-구현-도메인-규칙.md)
으로 옮겼다. 인증 설계 판단은 [ADR 0002](./adr/0002-self-hosted-password-auth.md).

### PR 사다리

정독 대상은 **C·D·E·F·H·I·J 일곱 개**이고 전부 200줄 안쪽이다. 나머지는 패턴
대조로 읽는다. J 를 넘기면 남은 39개 오퍼레이션에는 새 결정이 없다.

머지될 때마다 상태 칸을 갱신한다. 이 표가 진척표다.

| # | 상태 | 내용 | 오퍼 | 추정 | 성격 |
|---|---|---|---|---|---|
| A | ✅ #10 `63d6a20` | 골격 — 부팅되는 빈 Nest 앱 + Prisma 모듈 + health | – | 162 | 기계적 |
| B | ✅ #11 `3ea6b48` | 계약 사본 `contracts/` + 타입 생성 | – | 60 | 기계적 |
| — | | ↑ 생성물은 커밋하지 않고 CI·Docker 가 매번 만든다 (Prisma Client 와 같은 취급) | | | |
| C | ✅ #12 `6c56256` | `GET /mdm/warehouses` — `PageMeta` 봉투 · 응답 매퍼 · 검색/페이지 | 1 | 171+테스트 260 | 정독 |
| D | ✅ #14 | `GET /mdm/warehouses/{id}` — `ETag` · 편집 가능성 · 참조 건수 | 1 | 128+테스트 166 | 정독 |
| E | ✅ #15 | 인증 — 자격증명·해시·로그인 | – | 273+테스트 378 | 정독 |
| F | ✅ #16 | 가드 — 화이트리스트 보호 + 권한 데코레이터, C·D 에 적용 | – | 161+테스트 346 | 정독 |
| G | ✅ #17 | 멱등 저장소 마이그레이션 | – | 75 | 선행 커밋 |
| H | ✅ #18 | `POST /mdm/warehouses` — 오류 봉투 `{errors:[…]}` | 1 | 320+테스트 340 | 정독 |
| I | ✅ #20 | 멱등 인터셉터 | – | 341+테스트 380 | 정독 |
| J | ✅ #21 `690ac7b` | `PUT /mdm/warehouses/{id}` — `If-Match` · 충돌 원인 | 1 | 210+테스트 250 | 정독 |
| K | ✅ #22 `f1ed4e0` | `POST …:deactivate` — 중지를 막는 것 | 1 | 110+테스트 265 | 복제 |
| K2 | ✅ #23 `84ffc97` | `POST …:activate` — 계약에 없다. 아래 미결 참조 | 1 | 85+테스트 195 | 복제 |
| L1 | ✅ #24 `675b8d8` | 로케이션 목록·상세 + FK 인덱스 26개 | 2 | 200+테스트 220 | 복제 |
| L2 | ✅ #27 `65cb741` | 로케이션 등록·수정 — **부모 순환 검사** | 2 | 280+테스트 400 | 새 의미론 |
| L3 | ✅ #28 `a048e77` | 로케이션 중지·되살리기 | 2 | 165+테스트 265 | 복제 |
| M0 | ✅ #30 `802c5cb` | 편집 잠금을 쓰기에서도 강제 (창고·로케이션) | – | 40+테스트 100 | 정독 |
| M1 | ✅ #31 `ad3ba2f` | 코드그룹·코드값 **조회 4개**. 쓰기 6개는 보류 — 아래 참조 | 4 | 230+테스트 290 | 복제 |
| N1 | ✅ #32 `3ad31e2` | 부서 목록·상세 + FK 인덱스 7개 + 페이지 DTO 공용화 | 2 | 200+테스트 200 | 복제 |
| N2 | ✅ #33 `b779af3` | 부서 등록·수정 — 순환 검사(L2 복제) | 2 | 250+테스트 330 | 복제 |
| N3 | ✅ #34 `937572f` | 부서 중지·되살리기 | 2 | 175+테스트 290 | 복제 |
| O | ✅ #35 `6b7305f` | 조회 전용 8종 — 한 PR. 새 결정 없음 | 8 | 400+테스트 300 | 복제 |
| P1 | ✅ #36 `be7c3d1` | 품목 목록·상세·수정 — ERP 수신본 | 3 | 300+테스트 290 | 복제 |
| P2 | ✅ #37 `a81f274` | 품목 자식 3종 — **컬렉션 전체 치환** | 6 | 500+테스트 400 | 새 의미론 |
| Q | ✅ #38 `446aa87` | 작업자 + 자격 — 전 필드 읽기 전용 | 4 | 330+테스트 300 | 복제 |

### 넘긴 것

| 항목 | 어디로 | 나온 곳 |
|---|---|---|
| e2e 와 부팅이 프리픽스를 따로 갖는다 | C — `ValidationPipe` 도입 시 `configureApp` 으로 공유 | #10 리뷰 |
| 헬스체크 실패 시 원인이 로그에 안 남는다 | 미정 — 주기 폴링이라 로깅 여부 자체가 판단 | #10 리뷰 |
| 의존성 변경 PR 은 lockfile 을 같은 커밋에 넣는다 | 앞으로 전부 | #10 리뷰 |
| 시드가 조직 계층(법인→사업부→공장)을 만들지 않는다 | ✅ #13 — `test/support/organization.fixture.ts` | #12 리뷰 |
| 검증 넷을 `Promise.all` 로 동시 실행 — 실패가 확실한 경우에도 전부 조회 | ✅ L2 — 그대로 둔다. **성공이 보통이고 성공은 어차피 전부 확인해야 한다** — 순차로 바꾸면 성공 경로가 왕복 5회가 된다. 낭비는 실패할 때만 생기고, 계약이 오류를 배열로 정의한 것도 한 번에 다 알려주기 위해서다 | #18 리뷰 |
| `P2002` 형태 전제를 실제 DB 로 검증 | ✅ #19 — `test/prisma-error-shape.e2e-spec.ts` | 필터의 매핑 키가 Prisma 오류 형태를 전제한다. 단위 테스트도 같은 전제를 손으로 적은 것이라, 형태가 바뀌면 코드와 테스트가 함께 옛 전제를 유지한다 | #18 리뷰 |
| 목록 정렬 기준을 계약 소유자에게 확인 | 미정 — 되돌리기 전까지 프런트가 서버가 임의로 정한 순서에 의존하게 된다 | #12 리뷰 |
| 참조 건수가 전표 상태를 가리지 않는다 | ✅ K — 중지는 참조 건수를 쓰지 않는다. `checkDeactivable` 이 **지금 살아 있는 것** 둘만 본다: 재고 잔량(수량 > 0, 행 수가 아니다)과 사용 중인 로케이션. 과거 전표 8종과 적치 규칙은 세지 않는다 | #14 리뷰 |
| FK 인덱스 부재 — 창고 외 마스터 | ✅ L1 — 로케이션은 19개가 아니라 **26개**였고 인덱스가 붙은 것은 1개였다. 25개를 달고, 창고에 남아 있던 `picking_order.warehouse_id` 도 함께 고쳤다(`ix_picking_open` 이 부분 인덱스). 「인덱스가 있다」 판정을 e2e 에 넘겼다 — 부서(7개)는 N 에서 같은 테스트가 잡는다 | #14 리뷰 |

**인증이 C·D 뒤에 오는 이유**: 개발 중 조회 하나 확인할 때마다 토큰을 붙이는 것이
번거롭다. 무보호 구간은 읽기 전용인 C·D 둘로 한정하고, 첫 쓰기(H) 앞에 F 를 넣는다.
쓰기는 감사 컬럼 `created_by`/`updated_by` 에 인증 주체를 기록해야 하므로 인증
없이는 만들 수 없다.

> 계약 소유자에게 되돌릴 것은 [계약 되돌림 문서](./계약-되돌림-mdm.md)로 추렸다.
> 아래 표는 판단 과정 전체이고, 그 문서는 상대가 답해야 할 것만 남긴 것이다.

## 미결 — 해당 PR 에서 정한다

| 항목 | 정할 곳 | 메모 |
|---|---|---|
| 목록 정렬 기준 | C — 잠정 확정 | 계약에 정렬이 없다. 불안정하면 페이지를 넘길 때 같은 행이 두 번 나오거나 빠지므로 유일키 `(plant_id, warehouse_code)` 로 못 박았다. 계약 소유자에게 되돌릴 항목 |
| 목록 `size` 상한 | C — 잠정 확정 | 계약에 상한이 없다. 없으면 `size=1000000` 하나로 전 테이블을 긁어가므로 200 으로 뒀다. 되돌릴 항목 |
| 참조 건수를 세는 방법 | D — 확정 | 창고는 15개 FK 컬럼(13 테이블)을 스칼라 서브쿼리 하나로 합산한다. 목록을 손으로 적되 e2e 가 `pg_constraint` 와 대조해 누락을 잡는다. 로케이션·부서도 같은 패턴을 복제한다 |
| ERP 수신본 판정 근거 | **P·Q** | 정본에 출처 컬럼이 없다. 창고는 MES 가 관리하는 마스터라 해당 없음 — 품목·작업자에서 필요하다. [기존 구현 도메인 규칙](./기존-구현-도메인-규칙.md) 에도 같은 미결로 적혀 있다 |
| 상세 조회의 404 | D — 확정 | 계약에는 200 만 있으나 없는 id 에 200 을 줄 수 없어 서버가 정의했다 |
| 편집 권한 매트릭스 (계약 §8-8 미결) | F — 확정 | 서버가 지어내지 않고 시드·역할 데이터가 이미 전제하는 체계를 쓴다. 조회는 `MASTER_READ` 하나, 쓰기·중지는 영역별(`MASTER_{LOGISTICS\|SYSTEM\|ORGANIZATION\|PRODUCTION\|EQUIPMENT\|QUALITY}_{WRITE\|DEACTIVATE}`). 창고·로케이션=LOGISTICS, 코드그룹·코드값=SYSTEM, 부서·작업자=ORGANIZATION, 품목=PRODUCTION |
| `must_change_password` 강제 | 비밀번호 변경 API 가 생길 때 | 지금 가드가 막으면 admin 이 아무것도 못 한다 — 변경 엔드포인트가 아직 없다. 로그인 응답의 `mustChangePassword` 로 화면이 안내한다 | 
| 멱등 키 보관 기간·재사용 정책 | G — 확정 | 키는 전역(PK=키). 24시간 보관, 인터셉터가 쓰기마다 만료분을 소량 삭제(스케줄러 의존성 없이). 같은 키·같은 지문·COMPLETED → 저장된 응답 재생 / IN_PROGRESS → 409 / 다른 지문 → 400 |
| `IN_PROGRESS` 고아 레코드 | ✅ I — 60초 경과 시 이어받음 | 첫 요청 처리 중 서버가 죽으면 그 행이 영원히 `IN_PROGRESS` 로 남아, 같은 키 재전송이 24시간 동안 409 「처리 중」을 받는다. 처리하는 주체는 이미 없다. 스키마 변경은 불필요 — `created_at` 이 N초보다 오래된 `IN_PROGRESS` 는 죽은 요청으로 보고 이어받는다 | #17 리뷰 |
| 멱등 저장소에 개인정보가 남을 수 있다 | `/app` 라운드 | 지금은 기준정보만 담겨 문제없으나, 사용자 정보를 다루는 엔드포인트가 생기면 응답 본문이 24시간 보관된다 | #17 리뷰 |
| 재생이 응답 헤더를 잃는다 | ✅ J — `response_headers` 컬럼 | 저장하는 것이 `{status, body}` 뿐이라 재생 시 `ETag` 가 빠진다. `PUT` 은 계약이 `ETag` 를 요구하므로, 재전송받은 클라이언트가 다음 쓰기를 못 한다. `response_headers` 컬럼을 더하거나 본문에 실어야 한다 | #20 리뷰 |
| 재전송이 409 「다른 사용자가 먼저 수정했습니다」로 보이는 문제 | ✅ J — e2e 로 못 박음 | 멱등 검사가 If-Match 검사보다 앞(인터셉터 → 핸들러 → 서비스)이라 자기 재전송은 저장된 응답을 받고 버전 검사에 닿지 않는다. J 의 테스트로 못 박는다. 단 **재전송에 같은 키를 쓰는 것은 클라이언트 규약**이다 |
| 충돌 원인 `erpSync`·`workerLease` 를 무엇으로 판정하나 | ✅ **해소 — 설계팀 회신 2026-09-05** | 물리 모델을 더할 일이 아니다. **충돌을 일으킨 잠금 방식이 곧 원인**이라 부르는 자리가 안다 — 낙관적 잠금(`version_no`)이 튕기면 `user`, 리스(`locked_by`)가 잡혀 있으면 `workerLease`, ERP 재동기화 경로에서 튕기면 `erpSync`. 「항상 `user`」도 사실이 아니었다 — 멱등 처리 중과 연계 메시지 점유는 이미 `workerLease` 를 낸다. `erpSync` 는 그 경로가 설계 중이라 아직 쓰는 자리가 없다. 상세: 되돌림 §3 |
| 재개가 막는 것 | K2 — 확정 | 중지가 안쪽(재고·로케이션)을 보는 것과 반대로 **위쪽**을 본다: 공장·사업부가 사용 중이어야 한다. 법인은 보지 않는다 — 창고가 직접 가리키는 것이 아니고, 법인이 꺼졌는데 공장이 켜져 있다면 공장 쪽에서 이미 깨진 데이터다. 로케이션은 함께 켜지 않는다(중지 때 하나씩 껐으므로) |
| 검사와 갱신 사이가 열려 있다 | 재고 posting | `checkDeactivable` 뒤 `applyVersioned` 앞에 재고가 들어오면 그대로 중지된다 — 재고 입고가 `warehouse.version_no` 를 건드리지 않아 낙관적 잠금이 못 잡는다. 잔량 행 잠금이나 트리거가 필요하고, 재고 posting 모습을 모르는 채 지금 고르면 다시 뜯는다. 같은 틈이 `update` 에도 있다 | #22 리뷰 |
| 미결 전표가 있는 창고를 중지할 수 있다 | 물류 모듈 | 내일 입고 예정인 창고를 닫으면 안 되므로 막아야 맞다. 지금은 판정할 수 없다 — `goods_receipt.status_code` 등은 `VarChar` 이고 어느 값이 미결인지 정하는 `*_STATUS` 코드그룹이 `mdm.code_value` 에 없다. `inventory.handling_unit` 도 같은 이유로 못 본다 |
| **재개(`:activate`)가 계약에 없다** | ⚠ K2 — 서버가 먼저 만들었다. **2026-08-10 갱신분에서 계약도 `/logistics/putaway-rules` 에 `:activate` 를 넣었다 — `mdm` 에도 넣어달라는 요청으로 바뀐다** | `:deactivate` 만 있고 `PUT` 도 `isActive` 를 받지 않아, 잘못 중지하면 API 로 되돌릴 수 없다. 창고뿐 아니라 로케이션·코드그룹·코드값·부서도 같다 — 계약 소유자에게 되돌릴 항목 |
| 중지 200 에 `ETag` 를 붙인다 | K — 확정 | 계약은 `PUT` 에만 `ETag` 를 선언했다. 재개가 생기면 그 `If-Match` 에 필요하고, 멱등 재생도 같은 헤더를 되돌려준다 |
| **공통코드 쓰기 6개를 만들지 않는다** | 결정 (2026-08-07) — 계약 소유자에게 되돌릴 항목 | `POST`·`PUT`·`:deactivate` × (코드그룹, 코드값). 공통코드는 시스템 수준 상수다 — 지금 41개 그룹·174개 값이 전부 마이그레이션과 시드로 들어왔고 앞으로도 그렇다. 그룹 코드는 `warehouse.validator.ts` 등 **소스에 문자열로 박혀 있어**, 화면에서 바꾸면 그 마스터의 등록이 전부 막힌다. 계약 자신도 「ERP 수신본이라 읽기 전용이어야 하는데 판정할 컬럼이 없다(#65)」·「등록 주체가 미결이라 잠정 허용」이라고 적었다 |
| 계약이 코드그룹을 「셀 수 있다」고 적은 것이 틀렸다 | M1 — `NOT_COUNTABLE` 로 내린다 | 셀 수 있는 것은 `code_value` **행 수**(그룹에 값이 몇 개인지)이지 `group_code` **글자를 쓰는 곳**이 아니다. 둘은 다른 값이고, 편집 잠금이 물어야 하는 것은 뒤쪽이다. 그룹 코드를 쓰는 곳은 `warehouse.validator.ts` 같은 소스 문자열이라 셀 수 없다 — 창고·로케이션은 자기를 가리키는 행 수가 「이 정체성에 얼마나 많은 이력이 매달렸나」의 대리값이 되지만, 코드그룹은 그 대리가 성립하지 않는다 |
| 편집 가능성을 알려만 주고 막지 않았다 | ✅ M0 | 상세가 `codeEditable:false` 로 판정한 뒤에도 `PUT` 이 코드 변경을 받아줬다. 계약이 「유효성 판정은 서버가 한다」(G-8)고 정했고, 화면이 여럿이 되는 순간 갈라진다. 코드를 **바꾸려 할 때만** 세므로 이름만 고치는 흔한 수정은 조회가 늘지 않는다 |
| `@db.Date` 를 로컬로 포맷해도 이 환경에서는 티가 안 난다 | ✅ M1 — 성질을 테스트한다 | UTC 자정을 양수 오프셋으로 읽으면 같은 날이 나온다. 서버 UTC·개발기 +9·하노이 +7 이 전부 양수라, 로컬 포맷으로 짜도 e2e 가 통과한다(실제로 확인했다 — 바꿔 넣으니 e2e 24개가 다 지나갔다). 단위 테스트가 날짜가 아니라 **UTC 로 포맷하는가**를 본다. `@db.Date` 41개와 `business_date` 가 같은 함정을 공유한다 |
| 부서도 「ERP 수신본이라 읽기 전용이어야 한다」고 적혀 있다(#65) | N — 그래도 쓰기를 만든다 | 공통코드와 **같은 문장**이지만 판단이 다르다. 공통코드를 뺀 진짜 이유는 수신본이어서가 아니라 **그룹 코드가 소스에 문자열로 박혀 있어** 바뀌면 그 마스터의 등록이 막히기 때문이다. 부서 코드는 소스에 없고, 조직 개편은 현장에서 일어난다. `#65` 가 정해지면 편집을 막아야 한다 |
| ERP 수신본 판정 근거 | ✅ P1 — 판정이 필요 없다 | `#65`(수신본 식별 플래그 부재)가 미결이지만 이 API 에서는 문제가 되지 않는다. `ItemUpdate` 가 원본 4열(`itemCode`·`itemName`·`itemTypeCode`·`baseUomId`)을 **아예 받지 않아** 보낼 방법이 없다 — 런타임 판정 대신 구조로 막힌다. 공통코드는 그 방법이 없어 쓰기 자체를 뺐다 |
| 품목은 참조를 세지 않는다 | ✅ P1 — 인덱스 33개가 불필요해졌다 | `mdm.item` 을 가리키는 FK 39개 중 33개에 인덱스가 없다. 창고·로케이션·부서에 인덱스를 단 것은 **편집 잠금을 위해 참조를 세려는 것**이었는데, 품목은 항상 `RECEIVED_FROM_ERP` 라 셀 일이 없다 |
| 자식 컬렉션에 동시 수정 보호가 없다 | **미결 — 계약 소유자에게 되돌릴 항목** | `item_uom_conversion`·`item_external_code`·`item_bu_item_map` 에 `version_no` 컬럼이 없고 계약도 자식 `PUT` 에 `If-Match` 를 두지 않았다. 두 사람이 같은 품목의 부속을 동시에 고치면 **나중 사람이 앞사람 것을 조용히 덮어쓴다**. 계약대로 간다(2026-08-10 결정) |
| e2e 가 드물게 `socket hang up` 으로 깨진다 | 관찰 중 (2026-08-10, 2회 관측) | 전체 실행에서 두 번 났다 — 부서 등록·수정 1건, 부서 중지·되살리기 1건. **둘 다 사업부를 중지시키는 테스트**다. 재현 시도 9회(해당 스펙 단독 6 · 전체 3) 전부 통과.<br>**배제한 것**: 연결 고갈 아님 — 전체 실행 중 `pg_stat_activity` 를 1초 간격으로 재보니 최대 5(한도 100)이고 `PrismaService` 가 `onModuleDestroy` 에서 `$disconnect` 한다.<br>**남은 가정**: supertest 가 요청마다 임시 리스너를 열고 닫는 구조라 macOS 에서 산발적 소켓 오류가 날 수 있다. 스펙마다 서버를 한 번만 띄우면 없어지겠지만 21개 파일을 고치는 일이라, 원인이 확실해지기 전에는 하지 않는다. n=2 로는 부서 스펙에 몰린 것이 우연인지 알 수 없다 |
| `IsDateString` 은 `@db.Date` 검증에 못 쓴다 | ✅ P2 — `IsDateOnly` 를 만들었다 | 확인해보니 `2026-01-01T00:00:00Z`(시각 포함)도 `2026-02-30`(없는 날)도 통과시킨다. 앞은 타임존을 태워 하루를 밀 수 있고 뒤는 DB 까지 가서 500 이 된다. 정규식으로 형태를 보고, 문자열이 왕복해서 그대로 나오는지로 실재 여부를 본다 |
| `externalSystemCode` 를 공통코드로 검증하지 못한다 | 미결 — 계약 소유자에게 되돌릴 항목 | 계약이 「공통코드」라고 적었지만 **해당 코드그룹이 `mdm.code_group` 에 없다**(`%EXTERNAL%`·`%SYSTEM%` 로 찾아도 0건). 지금은 길이만 본다. 그룹이 생기면 `code_value` 대조를 붙인다 — 창고의 `WAREHOUSE_TYPE` 과 같은 방식이다 |
| `qualificationTypeCode` 값 목록이 없다 | 미결 — 계약 소유자에게 되돌릴 항목 | 계약이 「공통코드 — 값 목록 미정 §8-5」라고 스스로 적었다. 대조할 코드그룹이 `mdm.code_value` 에 없어 형태만 본다. `externalSystemCode` 와 같은 자리다 |
| `certifiedBy` 가 무엇을 가리키는지 모른다 | 미결 (#64) | FK 가 없는 `bigint` 다 — 계약도 「무엇을 가리키는지 근거 없음 §8-2」라고 적었다. 형태만 보고 참조 검사는 하지 않는다 |
| 계정 잠금 | 보류 — 결정 (2026-08-05) | 실패 횟수만 센다. `locked_until` 컬럼이 이미 있어 판정 한 줄이면 붙는다. [ADR 0002](./adr/0002-self-hosted-password-auth.md) |
