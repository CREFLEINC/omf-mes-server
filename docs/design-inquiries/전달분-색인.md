# 설계 검토 요청 — 전달 색인 (자동 생성)

⛔ **이 파일은 손으로 고치지 않는다** — `scripts/design_inquiries/build_delivery_index.py` 가 만든다.
요청서를 더하거나 「구분」을 바꾸면 **다시 돌린다**. 표지·횡단·배포 노트는 `전달분-2026-루틴마감.md` 에 있다.

요청서 **174건** · 미회신 질의 **5건** · 손질 필요 **28건**.

## §0. 먼저 볼 것 — 답이 와야 서는 자리

⭐ 나머지는 전부 **알려 두는 것**이라 회신을 기다리지 않는다. **이 표만 막고 있다.**

| # | 제목 | 레인 |
|:-:|---|:-:|
| **30** | 폐기 출고의 승인 게이트를 걸 축이 데이터에 없다 — 화면은 「계약이 전건을 막는다」로 읽었고 서버는 «상신한 전표»만 막는다 | A |
| **122** | 불량 반출 사유를 넣을 길이 0 이다 — 응답에는 `reasonCode` 가 있는데 요청에는 없다 | C |
| **211** | 신설 `DELETE /inventory/handling-units/{handlingUnitId}` 가 **재포장 이력 때문에 영영 성립하지 않습니다** | A2 |
| **216** | 재생재 품목 행을 **누가 만드나** — 품목 생성 오퍼레이션이 **0건**이다 | A2 |
| **276** | 음수 재고를 실사 장부수량에 어떻게 보존할지 결정이 필요하다 | B |

## §1. 자재·창고 — 57건

> 입하 · 적치 · 이동 · 조정 · 실사 · 취급단위 · 재생재

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 16 | 통보 | 입고 라인에 「소유 구분」을 받는 칸이 없다 | A |
| 17 | 통보 | 적치 규칙 `capacityQty` 를 권장 판정에 어떻게 쓰나 | A |
| 22 | 통보 | `GOODS_ISSUE_DISPOSAL` 결재선의 `businessUnitId` 를 「서버가 `reasonCode` 로 파생」할 매핑이 없다 | A |
| 23 | 통보 | P/O 의 `status_code` 를 `REGISTERED` 밖으로 옮기는 오퍼레이션이 없다 — 그래서 상신 뒤가 잠기지 않는다 | A |
| 25 | 통보 | 등록한 P/O 를 다시 여는 화면이 화면 인벤토리에 없다 — 수정·라인 치환·재상신이 갈 곳이 없다 | A |
| 26 | 통보 | 입고가 소비한 입하를 «전기 완료»로 옮기는 주체가 없고, 저장된 입하를 다시 여는 화면도 없다 | A |
| 27 | 통보 | 입하 오류를 기록한 뒤 담당자가 그것을 여는 화면·필터가 없다 | A |
| 28 | 통보 | 입하 라인이 「공급사 LOT 부착」인데 `supplierLotNo` 가 비어 오는 조합 — 계약이 막지 않았고 물리는 못 받는다 | A |
| 29 | 통보 | 한 물리 공급사 LOT 이 정량분·초과분으로 갈릴 때 — `W-01-03` 대표 시나리오를 `uq_lot(plant_id, lot_no)` 이 막는다 | A |
| 30 | 질의 | 폐기 출고의 승인 게이트를 걸 축이 데이터에 없다 — 화면은 「계약이 전건을 막는다」로 읽었고 서버는 «상신한 전표»만 막는다 | A |
| 31 | 통보 | 출고 라인이 잔액 차원 두 칸(`qualityStatusCode`·`inventoryStatusCode`)을 안 싣는다 — 한 (위치·LOT) 에 재고 행이 둘이면 어느 것을 내는지 계약이 말하지 않는다 | A |
| 32 | 통보 | 취소 실행이 역트랜잭션의 영업일·시각·번호를 아무것도 안 받는다 — 같은 규약의 04 `ShipmentCancel` 은 `businessDate`·`occurredAt` 을 필수로 싣는다 | A |
| 33 | 통보 | 취소 승인이 반려되면 `CANCEL_REQUESTED` 를 되돌릴 경로가 없다 — `W-CO-09` §5-5·J-6 은 «재상신»을 전제하는데 취소 판정 순위 3 이 영구히 막는다 | A |
| 34 | 통보 | 후속 판정 축이 계약 문자와 화면에서 어긋난다 — ① `INVENTORY_TRANSACTION` 은 문서 하류(계약)인가 LOT 축(화면)인가 ② 출고의 「사용하지 않은」을 가를 축이 enum 에 없다 ③ P/O 의 후속(입하)이 enum 에 없다 | A |
| 45 | 통보 | 피킹 지시(`picking_order`/`picking_line`)를 만드는 오퍼레이션이 계약에 0건이고, 예약을 «거는» 자리도 같이 없다 — `M-01-08` 은 반쯤 열리고 M2 체인의 「피킹」 마디가 서버로 이어지지 않는다 | A |
| 46 | 통보 | `material_issue_request_line.issued_qty`(계약 required · `readOnly` · ⌜누적 출고 수량⌝)를 올리는 오퍼레이션이 계약 어디에도 없다 — 같은 «기출고»가 두 벌(`issued_qty` 칸 ↔ `goods_issue` 합계)이 되고 `ck_material_issue_line_qty` 가 무의미해진다 | A |
| 47 | 통보 | 자재 출고요청·부족 응답에 표시 라벨 칸이 0 이다 — `MaterialIssueShortageLine` 에 `itemCode`·`itemName` 없음 · `MaterialIssueRequest` 에 `workOrderNo`·도착 위치 코드/명 없음 · 같은 계약의 `PickingLine` 은 파생 9칸을 준다 | A |
| 48 | 통보 | 생산창고 입고 전표가 영원히 `REGISTERED` 이고, 수령 전표가 선 출고를 취소 판정이 못 본다 | A |
| 49 | 통보 | `ShopfloorReceiptLineCreate.issuedQty` 를 클라이언트가 보내는데 서버가 이미 아는 값이고, 다를 때 무엇을 할지 계약이 적지 않았다 | A |
| 59 | 통보 | ⭐⭐ 적치 원장의 판별자가 `STOCK_TRANSFER` 인데 그 표는 적치를 모른다 — 되돌릴 수 없는 행이 오늘부터 쌓인다 | A |
| 60 | 통보 | 임시 적치가 dead end 다 — 정위치로 되돌릴 오퍼레이션이 0건이고, 되돌릴 때 볼 사유·원장·버전이 응답에 없다 | A |
| 61 | 통보 | 취소된 입고의 적치 지시가 `PENDING` 인 채 영원히 남는다 — 닫을 값도 오퍼레이션도 없다 | A |
| 62 | 통보 | 적치를 «누가» 했는지 남을 칸이 0 이다 — `X-Worker-No` 는 required 인데 담을 곳이 담당자 축뿐이다 | A |
| 120 | 통보 | 재고 이동에 취소·역분개 경로가 0 이다 — 원장을 «두 번» 남기는데 되돌릴 오퍼레이션이 없다 | C |
| 121 | 통보 | 창고 «내» 위치 이동을 막던 물리 CHECK 가 이미 사라졌는데 계약·화면은 그것이 있다고 적는다 — 게다가 그 이동을 받을 슬라이스가 0건이다 | C |
| 122 | 질의 | 불량 반출 사유를 넣을 길이 0 이다 — 응답에는 `reasonCode` 가 있는데 요청에는 없다 | C |
| 123 | 통보 | 이동 라인 치환이 도달 불가다 — 400 이 유일한 응답이고 부르는 화면도 0건이다 | C |
| 124 | 통보 | 부분 도착한 이동을 닫는 길이 0 이고 잔여가 「운송중」 차원에 영원히 남는다 — 게다가 그 전표가 미완 목록에서 사라진다 | C |
| 125 | 통보 | 도착이 재고 상태를 무엇으로 되돌리는지 아무 문서도 적지 않았다 — 고정하면 보류 재고가 이동 한 번으로 가용이 된다 | C |
| 130 | 통보 | 조정 라인이 잔액 차원 두 칸을 안 싣는데 물리는 NOT NULL 이다 — 증(+) 라인은 되읽을 잔액 행조차 없다 | C |
| 131 | 통보 | 조정 라인의 실사 원천 칸이 물리에 없고, 그 칸에 실사 마감 판정이 달려 있다 — 계약 `x-internal-note` **넷**이 낡았다 | C |
| 132 | 통보 | 잘못 전기한 재고 조정을 되돌릴 경로가 계약에 0건이다 — 취소도 역분개도 부를 오퍼레이션이 없다 | C |
| 133 | 통보 | 조정 헤더에 공장 축이 0 이라 원장 헤더의 `plant_id` 를 라인 위치에서 역산한다 — 두 공장에 걸친 조정은 400 이다 | C |
| 134 | 통보 | 조정의 ERP 송신 토글이 갈 곳이 없다 — 건별 토글도, 그 토글과 논리곱할 「전역 설정」도 담을 자리가 0 이다 | C |
| 135 | 통보 | 조정 채번의 접두어·규칙이 없고, 기간 축으로 쓸 날짜가 등록 본문에 하나도 없다 — 하노이 새벽 등록은 «전날» 번호를 받는다 | C |
| 140 | 통보 | 재포장 이벤트를 담을 표가 계약에 없다 — 「데이터 모델 담당에게 통지 — 기다리지 않는다」가 세 곳에 그대로 살아 있어 서버가 표 둘을 신설했다 | C |
| 141 | 통보 | `handling_unit.status_code` 가 NOT NULL 인데 값 목록이 「영영 오지 않는다」로 닫혔고, 그 자리를 메운 서버 상수가 **거부 판정(409)에 쓰인다** | C |
| 142 | 통보 | 확정된 포장을 «여는» 길이 0 이다 — 되돌리는 오퍼레이션도 삭제도 없고, 두 종류의 409 도 구분되지 않는다 | C |
| 143 | 통보 | `PUT …/contents` 가 재구성 «유형»도 «여러 원 포장»도 `remarks` 도 못 받는다 — 그리고 빈 배열이 `:pack` 불변식을 사후에 깬다 | C |
| 144 | 통보 | `repack-events` 에 페이지·필터·정렬 축이 하나도 없다(한 포장의 이력이 무한히 는다) + `HU-` 채번의 기간 축이 서버 UTC 날짜가 된다 | C |
| 145 | 통보 | 모바일 → POP 2단말 스택이 끊긴다 — 재포장 이벤트를 HU 를 «가로질러» 뽑는 조회가 없고, 하위 포장을 뽑는 축도 없다 | C |
| 156 | ⚠ 미분류 | 첨부 3건을 건너뛴다 — 보관용 바이너리 저장소가 DB 밖이고, `CD-ATTACHMENT-TARGET-TYPE` 에 담을 값이 **둘** 없다 | A2 |
| 162 | ⚠ 미분류 | 재고 이동이 진행 조회에서 「전기 완료」로 **먼저** 그려지고, 적치와 겹친 판별자는 **전표 번호로만** 갈린다 | A2 |
| 163 | ⚠ 미분류 | 발행(I-27)과 포장(I-16)이 **같은 두 행**을 잠그는데 계약이 순서도 상호작용도 안 정했고, 빈 배열 치환이 라벨을 **사후에** 무효로 만든다 | A2 |
| 164 | ⚠ 미분류 | `handling_unit.status_code` 의 **세 번째 값**이 이미 저장소에 있고, 「라벨 발행 대기」의 원천이 통보 141 이후 **바뀌었다** | A2 |
| 165 | ⚠ 미분류 | 구성 치환이 **단위를 바꿀 수 있는데** 재포장 이력 라인에 단위 칸이 **0** 이라, 그 변경이 이력에 「변화 없음」으로 남는다 | A2 |
| 210 | 통보 | int64 **범위를 넘는 경로·질의 값**이 500 이었다 — 계약이 `format: int64` 를 적어 둔 자리 1456곳 | A2 |
| 211 | 질의 | 신설 `DELETE /inventory/handling-units/{handlingUnitId}` 가 **재포장 이력 때문에 영영 성립하지 않습니다** | A2 |
| 213 | 통보 | 재생재 원장에 붙일 **판별자가 없다** — 빌리면 남의 전표에 유령 라인이 붙는다 | A2 |
| 214 | 통보 | 재생재 LOT 도 **수입검사 보류**를 받는데, **IQC 생략은 쓸 수 없다** | A2 |
| 215 | 통보 | 재생재 — 마이그 **A5** 의 범위, 그리고 **등록마다 LOT·잔액 행이 하나씩 는다** | A2 |
| 216 | 질의 | 재생재 품목 행을 **누가 만드나** — 품목 생성 오퍼레이션이 **0건**이다 | A2 |
| 272 | 통보 | 실사 개시의 스냅샷·채번·상태·정렬을 서버가 정했다 | B |
| 273 | 통보 | counted 물리 칸과 블라인드 실사 마스킹을 추가한다 | B |
| 274 | 통보 | 위치별 실사 라인 전량 치환의 세부 규칙을 정했다 | B |
| 275 | 통보 | 실사 마감 차단 사유와 조정 완료 판정을 라인별로 고정했다 | B |
| 276 | 질의 | 음수 재고를 실사 장부수량에 어떻게 보존할지 결정이 필요하다 | B |

## §2. 생산실행 — 32건

> 작업지시 · 세션 · 실적 · 자재투입 · 공정인계 · LOT 계보

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 35 | 통보 | `:hold`/`:resume` 을 부르는 화면이 0건인데 W/O 층 「작업 중단」이 셋을 함께 잃는다 — ⓐ `WORK_ORDER_HOLD_REASON` 값 0건 ⓑ 중단 구간을 담을 표가 없어 `held` 가 상태 문자열 근사다 ⓒ `RELEASED`→홀드→재개가 세션 없는 `IN_PROGRESS` 를 만든다 | A |
| 36 | 통보 | 선발행 슬롯의 `lot.sourceTypeCode` 값이 `LOT_SOURCE_TYPE` 2값에 없다 — 그런데 `GET /trace/lots?workOrderId=` 은 그 값의 존재를 전제한다 | A |
| 37 | 통보 | `:release` 의 「BOM 소요량 자동 산정」(R29) 규칙이 계약·화면 어디에도 없다 — ⓐ `bom_component` 의 공정 칸 둘 중 어느 축으로 담나 ⓑ `scrap_rate` 를 곱하나 | A |
| 38 | 통보 | `:cancel` 이 이미 발행된 자재 출고요청을 어떻게 하는지 아무 문서도 적지 않았다 — 취소된 W/O 의 요청이 남아 피킹으로 흘러간다. 덤으로 `W-02-06` 에 취소 `reasonCode` 를 고르는 칸이 없는데 계약은 required 다 | A |
| 39 | 통보 | 마감 전 게이트 셋(POP 버퍼 미동기 실적 · 미종료 홀드 · 실적 완결성)을 서버가 판정할 수단이 없다 — 계약 스스로 ⌜서버가 단말 버퍼를 볼 수단이 계약에 없다⌝ 라 적었다 | A |
| 40 | 통보 | 긴급 발행(`W-02-07` · `productionPlanId` 빈 `POST`)이 만들 내부 P/O 의 공장·사업부를 풀 값이 어디에도 없다 — `W-02-07` §8 미결 1 ①안은 그대로는 성립하지 않는다 | A |
| 41 | 통보 | 실적 정정 상신이 등급을 판정할 입력을 받지 않는다 — ⓐ 「B급이라 승인이 필요 없다」 400 은 도달 불가 ⓑ 승인 한 건이 정정 몇 건을 여는지의 축이 데이터에 없다 | A |
| 42 | 통보 | 병합된 I-6 코드가 정정 누계를 「합」으로 확정해 두었는데 I-7 이 「잎만」으로 뒤집는다 — 어느 쪽이 정본인가 | A |
| 43 | 통보 | 생산 LOT 완료의 미달 사유를 담을 칸이 LOT 쪽에 없다 — 슬롯 N 개와 W/O 마감이 `work_order.completion_variance_reason_code` 한 칸을 함께 쓴다 · 서버는 덮어쓴다 | A |
| 44 | 통보 | 정정이 실적↔LOT 배분을 고칠 길이 없다 — W/O 축 누계(042)와 LOT 축 누계가 정정 뒤 갈린다 · 어느 쪽이 「완료 판정」의 정본인가 | A |
| 50 | 통보 | `POST /production/material-returns` 의 소유 화면이 없어 403 게이트에 넣을 근거를 서버가 지어내야 한다 | A |
| 51 | 통보 | 자재 반출이 재고를 옮기는데 원장을 지날 수단이 계약에 하나도 없다 | A |
| 52 | 회신 완료 | ⭐ 「계보가 시작되는 지점」이라 적었는데 서버가 «어느 생산 LOT» 인지 가릴 축이 없다 | A |
| 53 | 통보 | 값 목록 없는 NOT NULL 코드가 여섯인데 계약이 「서버가 정한다」로만 적었다 — 판정 확인 요청 | A |
| 54 | 통보 | ⭐ 단말 토큰을 정의만 하고 어느 오퍼레이션에도 걸지 않아, 인증 축이 화면과 서버에서 갈렸다 — 단말 인증을 언제 세우나 | A |
| 55 | 통보 | 자재 투입에 정정(`:correct`)이 없다 — 읽기 스키마에는 `correctsConsumptionId` 가 있는데 쓰기에 없다 | A |
| 56 | 통보 | `work_session_worker.worker_role_code` 의 기본값 `'OPERATOR'` 가 `WORK_SESSION_WORKER_ROLE` 값 목록에 없다 | A |
| 57 | 통보 | ⭐ 세션의 작업자 귀속이 오늘 0건으로 선다 — REQ-PR-0023 이 화면·계약 어디에도 착지하지 못했다 | A |
| 58 | 통보 | 세션 `:end` 가 참여 중인 작업자를 어떻게 하는지 아무 문서도 적지 않았다 | A |
| 79 | ⚠ 미분류 | lotholdlotstatuscode 두 조회가 다른 뜻으로 채우고 있었다 | A |
| 104 | 통보 | 제품 개체를 처음 만들 때 필수 상태값을 무엇으로 채우는가 | B |
| 105 | 회신 완료 | LOT 배분 수량과 제품 개체 수의 대응 기준 — 회신 반영 완료 | B |
| 106 | 통보 | 시리얼 발번의 번호 연속성과 선택 If-Match 조건 | B |
| 150 | ⚠ 미분류 | `:request-iqc-skip` 의 「자격 축」을 서버가 **두 칸**으로 정했고, 거부 코드가 **둘로 갈린다** | A2 |
| 151 | ⚠ 미분류 | `X-Worker-No` 를 **담을 칸이 0** 인데 부재를 400 으로 거부한다 — 그리고 상신 주체는 그 사번이 «아니다» | A2 |
| 152 | ⚠ 미분류 | 외부 식별자 치환이 **0행 요청**을 받아 전건을 지운다 — 그리고 `lotExternalIdentifierId` 가 매 치환마다 갈린다 | A2 |
| 157 | ⚠ 미분류 | 수리 반출 뒤 «재투입» 등록처가 없어 `RepairExecution.reintroducedLotId` 가 영구 NULL 이다 | A2 |
| 158 | ⚠ 미분류 | 인계 라인의 출발·도착 위치를 W/O 기본 WIP 위치로 채운다 — 못 풀면 400 이고, 그 값은 나중에 **복원되지 않는다** | A2 |
| 159 | ⚠ 미분류 | 인계 확정이 **계약 밖 네 칸**을 서버 값으로 정했다 — 상태 상수·번호 규칙·라인 번호·**수령량** | A2 |
| 160 | ⚠ 미분류 | 공정 인계·수리 두 목록의 **정렬 축**과 **`open=false` 의 뜻**, **`size` 상한**을 서버가 정했다 — 「투입 대기 목록」의 첫 줄은 «가장 최근» 건이다 | A2 |
| 161 | ⚠ 미분류 | 수리 왕복의 데이터 축 **셋**이 비어 있다 — 결과 코드 그룹 0행 · 동시 투입을 막는 제약 0 · 멱등의 둘째 그물 0 | A2 |
| 270 | 통보 | 감사 이력 조회의 기간 경계와 정렬을 서버가 정했다 | B |

## §3. 품질 — 23건

> 검사 · 보류 · 부적합 · 처분 · 특채

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 70 | ⚠ 미분류 | lot_hold 도착 상태가 두 칸이 필요하다 | A |
| 71 | 통보 | **C9(클레임·리콜 재Hold)로 걸린 보류를 풀 길이 0 이다** — 그리고 해제가 LOT 을 «안 옮기는» 구멍이 셋 더 있다 | A |
| 72 | 통보 | `LotHoldEvent.actorId` 가 계약 required 인데 물리(`held_by`/`released_by`)는 nullable 이다 | A |
| 73 | 통보 | `LotStatusSummary.outOfScopeCount` — `user_data_scope` 를 적용하는 조회가 저장소에 0건이다 | A |
| 74 | ⚠ 미분류 | `GET /quality/defect-records`·`…/distribution` — 「기간 필수」 설명인데 `required`·400 이 미선언이다 | A |
| 75 | ⚠ 미분류 | 창고 위치 uomId가 lot 하나에 여럿일 수 있다 | A |
| 76 | 통보 | 부분 보류 한도의 **「보유 수량」**을 무엇으로 재나 — 잔액 행이 0건인 LOT 은 부분 보류가 영영 막힌다 | A |
| 78 | 통보 | `LotStatusTransition.impact` 의 「진행 중인 피킹」·「이미 출고된 수량」 정의 | A |
| 80 | 통보 | **검사 대기(`INSPECTION_PENDING`) LOT 에 클레임·리콜 재Hold(C9)를 못 건다** — C9 의 `from` 이 `NORMAL` 하나다 | A |
| 81 | 통보 | 보류 등록의 `lots[]` 에 **같은 `lotId` 가 두 번** 오면 — 400 `INVALID` 로 막는다 | A |
| 82 | ⚠ 미분류 | 불량코드 분포 — 공정 축(기본값)의 부모 노드를 `DefectDistributionNode` 로 만들 수 없다 (R-14) | A |
| 83 | 통보 | `LotStatusSummary.counts` — `lotTypeCode` 축은 격자 전체(4×3)가 아니다 | A |
| 84 | 통보 | `LotStatusTransition.allowed` — 전이표 판정만 하는가, 실행 가능성까지 보는가 (R-13) | A |
| 85 | 통보 | 검사 결과 DRAFT 판정 칸 — required 인데 물리는 nullable | A |
| 86 | 통보 | 검사 결과 수정의 `measurements: []` — 「전부 지운다」인가 「손대지 않는다」인가 | A |
| 87 | 통보 | 검사 확정이 보류를 푸는데 「1회차 수입검사 합격」의 해제 사유 값이 목록에 없다 | A |
| 88 | 통보 | 불량(`DEFECTIVE`) LOT 의 **재검 합격 확정**이 막힌다 — 「불량은 발신 전이 0」의 필연 | A |
| 181 | 통보 | `W-03-10`(처분 판정 처리)이 **권한 도출 원천에 없다** — 판정 저장이 그 화면 담당자에게 403 이다 | A |
| 182 | 통보 | `DispositionCandidate.receivedAt` 이 **PRODUCT 갈래에서 무엇인가** — 기간 한 번에 원천 하나가 사라진다 | A |
| 183 | 통보 | 처분 판정 대상 목록의 **「상태」가 화면에선 네 갈래인데 서버 축은 하나**다 — 계약 질의 칸이 모자란다 | A |
| 185 | 통보 | 부적합 대상 LOT 의 **「(판정 후)」 칸을 그릴 값이 없다** — `qualityStatusBefore/AfterCode` 가 언제나 같다 | A |
| 186 | 통보 | 조회 **다섯 건이 400 을 선언하지 않았는데** 서버가 400 을 낸다 — 문의 074 와 같은 패턴 | A |
| 188 | 통보 | 계약 안 모순 — `disposition_type_code` 값 목록이 **「확정」과 「미확정」으로 둘 다** 적혀 있다 | A |

## §4. 제품출하 — 28건

> 출하지시 · 피킹 · 배분 · 출하 · 재입고

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 77 | 통보 | 멱등 흡수가 내는 409 의 `code` — 지문 불일치 `DUPLICATE_KEY` · 처리 중 `INVALID_STATE` | A |
| 89 | 통보 | LOT 상태 전이 코드가 없는 자리 **넷**(처분 3 + 재등록 1) — 그리고 계약이 값을 안 준 코드 자리들 | A |
| 154 | ⚠ 미분류 | `GET /trace/lot-status-events` 가 **계약 enum 밖 `transitionCode`** 를 그대로 싣는다 — 재등록(I-23)이 병합되는 순간 열리는 구멍 | A2 |
| 180 | 통보 | 폐기(`SCRAPPED`)로 간 LOT 을 되돌릴 길이 **0**이다 — 오폐기 정정 경로가 없다 | A |
| 184 | 통보 | **「차단이 풀리는 자리」가 둘이다** — 처분 「정상」(I-21)이 재고 재등록(I-23)을 선점한다 | A |
| 190 | 통보 | 재고 예약 목록에 **원천 문서 «유형» 질의 축이 없다** — 제품 피킹이 예약을 걸면 자재 예약과 섞이고 계약 enum 을 런타임에 벗어난다 | A |
| 191 | 통보 | 출하작업지시 번호 접두어 **`SR` 가 현장 입고 번호와 겹친다** — 두 계약 example 이 둘 다 `SR-` 다 | A |
| 192 | 통보 | `ShipmentRequestLine.pickedQty` 의 **`x-source-column` 앵커가 «잔액» 칸을 가리키는데 응답은 «라인» 축**이다 | A |
| 193 | 통보 | **납품라벨 번호를 담을 칸이 저장소 전체에 0개**라 `GET /logistics/shipment-lot-allocations?q=` 가 겨냥할 축이 없다 | A |
| 194 | 통보 | `shipment_lot_allocation` 이 **부분 포장을 담지 못해** `packedQty` 가 **0 또는 배정 전량** 둘뿐이다 | A |
| 195 | 통보 | 출하검사 롤업을 **라인에 매다는 칸이 0개**이고, 그대로 두면 **「합격인데 납품라벨을 영원히 못 뽑는」** 상태가 생긴다 | A |
| 196 | 통보 | **제품 피킹에 창고 축이 0개**이고 **예약이 잔액 차원 11칸 중 4칸만** 담는다 — 「그 LOT 은 영영 피킹 불가」가 생긴다 | A |
| 197 | 통보 | 제품 피킹 `:pick` 이 **`businessDate`·`occurredAt` 을 받지 않는다** — 「오늘」과 채번 기간 축을 서버가 정했다 | A |
| 198 | 통보 | 「칸 불필요」로 닫힌 `ShipmentRequest.statusCode` 가 **물리에서 NOT NULL** 이라 상수를 넣는다 | A |
| 199 | 통보 | 「배정 수량 **≥ 1**」을 **「0 을 거부한다」**로 읽었다 — 계약도 물리도 안 막는 «순수 서버 규칙»이다 | A |
| 200 | 통보 | 제품 피킹을 **되돌릴 길이 계약에 0건**이고, ⭐ **화면이 가리킨 곳에도 0건**이다 | A |
| 201 | 통보 | 출하 조회 넷이 **400 을 선언하지 않았는데** 서버가 **세 계열**의 400 을 낸다 — 문의 074·186 과 같은 패턴 | A |
| 202 | 통보 | ⛔ **계약 자기 모순** — `match.reasonCode` 가 「참이면 `null`」이라 적혔는데 **`enum` 에 `null` 이 없다** | A |
| 203 | 통보 | ⛔ **「판정 순서는 뒤가 이긴다」가 `shipmentProgressCode` 의 `NOT_ALLOCATED` 를 «도달 불가»로 만든다** | A |
| 204 | 통보 | 제품 피킹의 **409 가 네 거부 사유를 `INVALID_STATE` 하나로 접는다** — 화면이 문구를 가를 축이 0이다 | A |
| 205 | 통보 | **부분 배정 작업지시가 「피킹중 ⛔」 배지를 못 단다** — 상시 구조인데 차단 사유가 목록에서 사라진다 | A |
| 206 | 통보 | `ShipmentRequest` 에 **라인 합계 칸이 0개**라 목록이 **라인을 통째로 실어야** 한다 | A |
| 207 | 통보 | 출하지시서 목록의 **「미편성 / 부분편성 / 편성완료」 3상태 중 서버가 세우는 축은 하나뿐**이다 | A |
| 208 | 통보 | 피킹을 되돌릴 때 **`released_qty` 를 어디에 반영할지** 계약이 안 정했다 — 서버는 `Σ(reserved − released)` 로 낸다 | A |
| 209 | 통보 | `W-04-02` 의 **목업과 미결 표가 «확정된» 시간대 필터를 아직 「없다」고 적고 있다** — 같은 문서 안에서 갈렸다 | A |
| 212 | 통보 | 예약 되돌림은 **`consumed` 를 내리고 `released` 를 올린다** — 막는 것은 CHECK 가 아니라 «순서»였다 | A2 |
| 240 | 통보 | 피킹 수량이 배정 수량을 **넘는 것**(과피킹)을 막는 제약이 저장소에 없다 | A |
| 241 | 통보 | `released_qty` 를 **쓰는 코드가 0개**라, 예약을 되돌려도 피킹 롤업이 안 줄어든다 | A |

## §5. 설비·툴 — 16건

> 고장 · 가동중지 · 보전 · 툴 · 교정

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 90 | 통보 | 설비 고장 원인 마스터 원천과 완료 본길 | B |
| 92 | 통보 | 고장 보고 성공과 알림·사진 미지원 | B |
| 93 | 통보 | 고장 start의400 문언과 ETag 재조회 | B |
| 94 | 통보 | 점검·고장 기간의 공장 로컬 날짜 | B |
| 95 | 통보 | 고장 복수 지시·소수 분의 null 의미 | B |
| 97 | 통보 | 오프라인 점검 판정 책임과 측정 정밀도 | B |
| 98 | 통보 | EQI·MLF 채번 날짜와 트랜잭션 경계 | B |
| 109 | 통보 | 비가동 ‘지금 종료’는 서버 최초 처리시각을 사용한다 | B |
| 111 | 통보 | 비가동 요약의 정상 계획시간과 완료보전 건수 원천 | B |
| 113 | 통보 | 보전 결과코드와 지시 마감의 의미 | B |
| 114 | 통보 | 툴 PM 시행일과 누계 리셋의 원자성 | B |
| 115 | 통보 | 보전 지시 원천·유효 부여·스냅샷 검증 | B |
| 116 | 통보 | 보전 예비품 사용수량의 단위와 확인·정정 | B |
| 117 | 통보 | 검교정 결과 의미와 계측기 반영 | B |
| 118 | 통보 | 최근 수집 관측과 미매핑의 의미 | B |
| 119 | 통보 | 툴 증분 환산과 누계 귀속 | B |

## §6. 공통·기준정보 — 10건

> 결재 · 알림 · 발행 · 권한 · 마스터

| # | 구분 | 제목 | 레인 |
|:-:|:-:|---|:-:|
| 18 | 통보 | 결재선 `inProgressCount` 를 셀 연결 칸이 없다 — 유형 축 근사로 내도 되나, I-2 에서 칸을 더할까 | A |
| 19 | 통보 | `IQC_SKIP`(한도승인)의 승인 화면이 `W-01-02` 인가 `W-03-09` 인가 — 「특채」는 9값에 없고, `screenId` 규칙이 걸리는 유형이 없다 | A |
| 21 | 통보 | 승인 «유형»·«대상 유형»의 사람이 읽는 이름을 서버가 어디서 얻나 — `displayName`(required)·끄기 경고·`?q` | A |
| 99 | 통보 | 알림 사건명은 있지만 eventCode 정본이 없다 | B |
| 100 | 통보 | 이벤트별 구독 헤더와 미설정 ETag | B |
| 101 | 통보 | 알림 규칙 수·표시 인원·실제 수신 인원은 다르다 | B |
| 102 | 통보 | 알림 대상의 화면·위치 원천과 과거 유형 표시 | B |
| 155 | ⚠ 미분류 | 첨부 목록이 다형 쌍을 **독립 필터 둘**로 걸고, 상한과 정렬을 서버가 정했다 — 계약이 「**한 대상**」이라 «단수»로 적고도 위반 응답을 선언하지 않았다 | A2 |
| 189 | 통보 | 결재함 목록(`GET /app/approval-requests`)의 **호출 셸 둘이 권한 도출 원천에 없다** — 통보 181 의 형제 | A |
| 271 | 통보 | 예비품 엑셀의 열과 공장 판정을 금형 업로드와 맞췄다 | B |

## §9. 손질 필요 — 28건

⛔ **숨기지 않는다.** 「구분」 줄이 없거나 「걸리는 오퍼레이션」에서 도메인을 못 읽은 것들이다 —
전달 전에 사람이 채운다(요청서에 `**구분: …**` 한 줄을 더하면 다음 실행부터 저절로 잡힌다).

| # | 없는 것 | 제목 | 레인 |
|:-:|---|---|:-:|
| 69 | 도메인 | `businessDate` 의 야간조 경계 도출 규칙이 없다 — 도출 함수를 만들지 않는다 (문의 15 후속) | A |
| 70 | 구분 | lot_hold 도착 상태가 두 칸이 필요하다 | A |
| 74 | 구분 | `GET /quality/defect-records`·`…/distribution` — 「기간 필수」 설명인데 `required`·400 이 미선언이다 | A |
| 75 | 구분 | 창고 위치 uomId가 lot 하나에 여럿일 수 있다 | A |
| 79 | 구분 | lotholdlotstatuscode 두 조회가 다른 뜻으로 채우고 있었다 | A |
| 82 | 구분 | 불량코드 분포 — 공정 축(기본값)의 부모 노드를 `DefectDistributionNode` 로 만들 수 없다 (R-14) | A |
| 91 | 도메인 | 점검·고장 저장 결손과 과거 필수값 | B |
| 96 | 도메인 | 설비 멱등 재전송의 주체와 업무 트랜잭션 | B |
| 103 | 도메인 | 알림 재전송의 주체·이벤트 격리와 공통 오류 | B |
| 107 | 도메인 | 발번 귀속의 저장과 발행 단계별 멱등 보존 | B |
| 108 | 도메인 | 비가동의 필수 귀속·메모·버전과 기존 행의 저장 결손 | B |
| 110 | 도메인 | 비가동 구간의 편집·열린 구간·목록 경계 판정 | B |
| 112 | 도메인 | 비가동 요약의 기간·정밀도·열린 세션·경미 정지 경계 | B |
| 150 | 구분 | `:request-iqc-skip` 의 「자격 축」을 서버가 **두 칸**으로 정했고, 거부 코드가 **둘로 갈린다** | A2 |
| 151 | 구분 | `X-Worker-No` 를 **담을 칸이 0** 인데 부재를 400 으로 거부한다 — 그리고 상신 주체는 그 사번이 «아니다» | A2 |
| 152 | 구분 | 외부 식별자 치환이 **0행 요청**을 받아 전건을 지운다 — 그리고 `lotExternalIdentifierId` 가 매 치환마다 갈린다 | A2 |
| 154 | 구분 | `GET /trace/lot-status-events` 가 **계약 enum 밖 `transitionCode`** 를 그대로 싣는다 — 재등록(I-23)이 병합되는 순간 열리는 구멍 | A2 |
| 155 | 구분 | 첨부 목록이 다형 쌍을 **독립 필터 둘**로 걸고, 상한과 정렬을 서버가 정했다 — 계약이 「**한 대상**」이라 «단수»로 적고도 위반 응답을 선언하지 않았다 | A2 |
| 156 | 구분 | 첨부 3건을 건너뛴다 — 보관용 바이너리 저장소가 DB 밖이고, `CD-ATTACHMENT-TARGET-TYPE` 에 담을 값이 **둘** 없다 | A2 |
| 157 | 구분 | 수리 반출 뒤 «재투입» 등록처가 없어 `RepairExecution.reintroducedLotId` 가 영구 NULL 이다 | A2 |
| 158 | 구분 | 인계 라인의 출발·도착 위치를 W/O 기본 WIP 위치로 채운다 — 못 풀면 400 이고, 그 값은 나중에 **복원되지 않는다** | A2 |
| 159 | 구분 | 인계 확정이 **계약 밖 네 칸**을 서버 값으로 정했다 — 상태 상수·번호 규칙·라인 번호·**수령량** | A2 |
| 160 | 구분 | 공정 인계·수리 두 목록의 **정렬 축**과 **`open=false` 의 뜻**, **`size` 상한**을 서버가 정했다 — 「투입 대기 목록」의 첫 줄은 «가장 최근» 건이다 | A2 |
| 161 | 구분 | 수리 왕복의 데이터 축 **셋**이 비어 있다 — 결과 코드 그룹 0행 · 동시 투입을 막는 제약 0 · 멱등의 둘째 그물 0 | A2 |
| 162 | 구분 | 재고 이동이 진행 조회에서 「전기 완료」로 **먼저** 그려지고, 적치와 겹친 판별자는 **전표 번호로만** 갈린다 | A2 |
| 163 | 구분 | 발행(I-27)과 포장(I-16)이 **같은 두 행**을 잠그는데 계약이 순서도 상호작용도 안 정했고, 빈 배열 치환이 라벨을 **사후에** 무효로 만든다 | A2 |
| 164 | 구분 | `handling_unit.status_code` 의 **세 번째 값**이 이미 저장소에 있고, 「라벨 발행 대기」의 원천이 통보 141 이후 **바뀌었다** | A2 |
| 165 | 구분 | 구성 치환이 **단위를 바꿀 수 있는데** 재포장 이력 라인에 단위 칸이 **0** 이라, 그 변경이 이력에 「변화 없음」으로 남는다 | A2 |
