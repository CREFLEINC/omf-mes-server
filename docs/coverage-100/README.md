# 커버리지 100 루틴 — 운영 규칙 (2026-09-06 확정)

> 계약 커버리지 238/487 → 487/487 을 목표로 자율 반복한다. 이 문서는 사용자와 합의한 규칙의 정본이다.
> 컨텍스트가 정리되더라도 이 문서를 먼저 읽고 이어간다.
> 같은 폴더: `uncovered.tsv`(시작 시점 미커버 249건) · `plan-api.md`·`plan-uiux.md`·`plan-integration.md`(3관점) · `plan.md`(통합 정본, 진행 표 포함) · `assignment.tsv`(249건 → 슬라이스 배정, 기계 대조 249/249).
> 검토 요청서: `../design-inquiries/NNN-제목.md` — 단건, 발생 시점마다 작성, 번호는 이미 보낸 1~15번(`~/omf-design-requests/설계-문의-2026-09-0*.md`)에 이어 **16번부터**. 루틴 끝에 일괄 전달.

## 0. 범위

- 계약 사본 고정: `contracts/COMMIT.txt` = `a6a87e1`. **루틴이 끝날 때까지 `contracts:update`·`contracts:check` 금지.**
  설계 변동 공지 이슈가 올라와도 읽기만 하고 사용자 지시 전에는 손대지 않는다.
- 설계팀 검토 요청을 보내고 회신 대기 중인 것은 제외한다(추적 이슈 #177, 1~3차 문의 1~15번). 대기 중인 물음:
  1 AppUser.statusCode 기본값 · 2 certifiedBy 참조 · 4 pmDueAxisCode · 5 `type` null 16자리 · 6 임박 임계 90 · 7 molds:import 공장 ·
  8 같은 비밀번호 · 9 공지 종료일 · 10 notice_no 채번 · 11 보류 해제 사유(철회 예정) · 12 한도승인 재고 상태 값 ·
  13 LOT_HOLD_STATUS 시드 · 14 채번 규칙 없는 표(GR-/PT-/NTC-) · 15 businessDate/occurredAt 의 C-8-1 어긋남.
  이 물음들이 걸리는 오퍼레이션도 「값 정의」나 「권고안」이면 그대로 구현한다 — 12·13·14 가 그렇다.
  단 「값 정의」(코드 목록·채번 형식·기본값)나 「확실한 권고안」이 있는 것은 권고안대로 구현하고 요청서에 적는다.
- **DB 안에서 끝나는 것만** 구현한다. 바이너리 저장소·실제 외부 전송·프린터/PDF 가 필요한 오퍼레이션은 건너뛰고
  루틴 끝에 목록으로 보고한다. ERP 적재는 아웃박스 테이블에 쌓는 데까지(전송기 없음), 발행 이력은 기록만 — 이건 범위 안.

## 1. 작업 방식

1. **계획**: 시작 시 1회, 남은 전부에 대해 3관점(API 설계·UI/UX 설계·통합) 계획서 → 충돌은 브레인스토밍 → 통합 계획서 1개.
2. **슬라이스마다**: 개별 계획안 1개를 세워 통합 계획서 해당 절과 대조. 차이가 작으면 구현.
   **차이가 크면**(아래 셋 중 하나) 그 슬라이스만 3관점 재수립 + 통합 계획서 해당 절 수정:
   - 통합 계획서에 없던 테이블/마이그레이션이 필요
   - 다른 슬라이스의 순서에 영향
   - 설계 문의가 새로 생김
3. **순서**: `development-strategy.md` 순서 우선. 같은 도메인의 조회 GET 은 함께 묶어 커버리지를 올린다.
4. **PR**: 「전표 하나 + posting 연결 + e2e」 단위. 마이그레이션은 별도 선행 커밋. merge commit(`--merge --delete-branch`).
5. **리뷰·병합**: 크레플 `pr-review` 규칙. Blocker 0 · Major 0 · CI green · 충돌 없음이면 **내가 바로 병합**.
   마이그레이션이 든 PR 은 병합 전에 한 줄 보고만 하고 넘어간다(멈추지 않는다).
6. **보고 주기**: PR 병합마다 한 줄(번호·오퍼레이션 수·커버리지 n/487), 슬라이스 끝에 짧은 절,
   루틴 끝에 전체 보고 + 검토 요청서 목록 + 건너뛴 오퍼레이션 목록.

## 2. 설계 미정 갈림길 판정 절차 (「보수적」 대신 이 절차)

**0단계 — 선례.** 계약 본문 · `docs/기존-구현-도메인-규칙.md` · 이미 구현된 다른 전표. 있으면 인용(판단 아님).

**1단계 — 가장자리 / 본길.**
- 가장자리(특정 조건의 입력에서만 갈림) → 2단계.
- 본길(모든 호출의 결과가 달라짐) → 임의로 못 고른다. 계약 문자 그대로 + 문의. 계약도 침묵하면 오퍼레이션 건너뛰고 사유 기록.

**2단계 — 나중에 뒤집는 비용이 싼 쪽. 같으면 개발 난이도가 낮은 쪽.** 위에서부터 먼저 걸리는 기준으로 결정.
1. 재고·원장·상태를 쓰지 않는 쪽
2. 거부하는 쪽(명시적 에러 코드) — 거부→허용은 호환 완화, 허용→거부는 깨는 변경
3. 스키마를 안 늘리는 쪽, 늘려야 하면 nullable
4. 값을 조용히 도출하지 않는 쪽
5. 개발 난이도가 낮은 쪽 — 위 넷으로 안 갈리거나 서로 충돌할 때. 난이도는 코드 양이 아니라
   **새로 만드는 개념의 수**(테이블·상태값·코드 그룹·상태 전이)로 잰다.

**3단계 — 흔적.** 에러 코드/고정값에 이름, 단언 테스트에 `// 설계 미정 — 문의 NNN`, 요청서에 「몇 단계 몇 번 기준으로 골랐음」.

## 3. 멈춤 조건 (이 셋 외에는 멈추지 않는다)

- 물리 모델 수정이 두 릴리스 규칙(컬럼·테이블 삭제)에 걸릴 때
- 게이트가 원인 불명으로 3회 실패할 때 — 단 간헐 실패는 1회 재실행, 두 번 연속 실패만 1회로 셈. 증거는 작업 #15 에 붙인다
- 계약끼리 모순돼 어느 쪽도 맞출 수 없을 때

## 4. 서브에이전트 모델 배분

판단 밀도가 높은 일에 큰 모델, 패턴을 베끼는 일에 작은 모델.

| 일 | 모델 |
|---|---|
| 3관점 계획 | opus ×3, 새 컨텍스트(서로 다른 관점을 위해 대화를 물려주지 않음) |
| 계획 통합·브레인스토밍·머지 판정·게이트 실행 | 나(fable) 직접 |
| 슬라이스 개별 계획안 | opus |
| 구현 — 코어(원장 쓰기·상태기계·posting·마이그레이션) | opus |
| 구현 — 조회 GET·뷰·컨트롤러·기존 패턴 복제 | sonnet |
| e2e 작성 | sonnet, 코어 슬라이스는 opus |
| 탐색·조사 | haiku(좁음) · sonnet(넓음) |
| PR 리뷰 | opus, 구현자와 다른 새 컨텍스트 |

fork(fable) 는 이 대화를 다 안고 가서 비싸므로 통합 단계 외에는 쓰지 않는다. 대신 각 에이전트 프롬프트에
이 문서 경로와 §2 절차 전문, `contracts/*.json` 읽기 전용, 코딩 규칙 경로를 명시해 넘긴다.

## 5. 변하지 않는 제약 (CLAUDE.md 요약 + 이 저장소 관행)

- `contracts/*.json` 읽기 전용. 물리 모델과 계약이 다르면 물리를 고친다.
- `CREFLEINC/omf-mes`·`omf-mes-client` 에 이슈·코멘트 금지. 설계 문의는 문서로.
- auto 모드에서 `gh pr comment` 차단 → 리뷰는 파일(`$CLAUDE_JOB_DIR/tmp/PR-NNN-리뷰.md`)로 남긴다.
- 권한 게이트는 계약이 403 을 선언한 오퍼레이션만 `OPERATION_PERMISSIONS` 에 등록.
- schema.prisma `@default` 변경 시 `npx prisma generate`. FK 제약 이름은 Prisma 기본형.

## 6. 속도 (2026-09-06 · #213 뒤 사용자 지시 「품질 영향 최소로 시간 단축」)

- **게이트를 두 번 돌리지 않는다** — 구현자가 PR 본문에 게이트 실측(명령·숫자)을 적는다. 리뷰어는 diff 를 읽고 **단위 전체 + 바뀐 e2e 파일만** 돌린다. 회귀 e2e(다른 파일)는 통합자가 병합 직전에 한 번 돌린다.
- **비테스트 diff 예산은 구현 브리프에 350** — 리뷰 수정분(+20~30)이 들어갈 자리를 남겨 400 맞추기 왕복을 없앤다. 한도 400 자체는 그대로다.
- **리뷰 Major 가 계획서 쪽 구멍이면** 코드를 고치기 전에 §0-재수립 한 줄로 판정을 먼저 적는다 — 「일어날 수 없다」 류 추론은 리뷰어가 실측으로 뒤집을 수 있으므로 계획 단계에서 단위 테스트 이름으로 못 박는다.
- 다음 PR 을 리뷰 중인 PR 위에 스택으로 먼저 스폰한다(I-4 ③→④→⑤ 선례). 바꾸지 않는 것: 리뷰어 ≠ 구현자 · 병합 전 e2e 파일 단위 실행.
