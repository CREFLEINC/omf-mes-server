# I-7 개별 계획안 3관점 재검토 브리프 (README §1-2 「차이가 크다」 — 조건 ①(마이그 D2)·③(문의 041~044) 발동)

대상: `docs/coverage-100/slices/I-7.md`(897줄). 결과는 **자기 관점 파일 하나**에만 쓴다 —
`docs/coverage-100/slices/I-7-review-{api|uiux|integration}.md` (150줄 이내). 다른 파일 수정 금지, 구현 금지,
체크아웃 변경 금지(현재 브랜치 `docs/coverage-100-i7-plan` 그대로 · main 병합 완료 d8e799d), `git` 쓰기 금지. 계약 `contracts/*.json` 은 읽기 전용(`jq`).
로컬 DB 는 읽기만(`psql` SELECT · `migrate reset`·`migrate diff --shadow-database-url`·`db seed` 절대 금지). Bash cwd 는 호출마다 리셋되므로 절대경로로 `cd`.

⚠ **계획자가 몰랐던 사실(작성 중 병합됨)**: I-6 은 **전건 병합**됐다(#227~#235 · main 76733fc · 293/487). `I-7.md` §0 ②·§2-4·§8-6·§11 #15 의 「I-6 PR ⑥ 병합 전」 문장은 낡았다. 실물: `src/production/work-order/work-order-close.service.ts:141` `goodQty(tx, workOrderId)` 가 `production_result.aggregate({_sum:{good_qty}})` 를 **필터 없이** 더한다 · `close-rules.ts` 가 3분류 순수 함수 · `work-order-query.service.ts` 집계 세 자리. ⇒ I-7 PR ② 의 「`ACTIVE_RESULT_WHERE` 를 넣는 자리」는 **넷**(query 3 + close 1)이고 전부 I-6 파일 수정이다 — 예산·리뷰 범위에 넣어 판정하라. 또 `moveWithin()` 은 `version_no` 를 올린다(I-6 R-29).

계획자 결론 요지:
ⓐ **마이그 2**(§2-2 · 한 파일 한 선행 커밋): D1 `shift_id DROP NOT NULL` + **D2 `correct_reason_code app.code_t` 추가**(`ProductionResultCorrect.reasonCode` required 인데 담을 칸 없음 — README §5 「물리를 고친다」) · `status_code` 는 상수 `'CONFIRMED'`(I-6 e2e 픽스처 값 · 계약 응답 required 라 nullable 불성립 · §2-3).
ⓑ **상태기계**(§3): L1 호출만(`moveWithin` · `VOIDED` 슬롯은 400 · 두 번째 실적은 `skipped` 정상) · W/O 축 **안 옮김**(`RELEASED`/`IN_PROGRESS`/`SUSPENDED`?/`COMPLETED`/`CLOSED` 허용·`PLANNED`·`CONFIRMED`·`CANCELLED` 400 — §3-2 실측) · `:complete` 는 **어느 상태 칸도 안 옮기고 `lot.completed_at` + 미달 시 `work_order.completion_variance_reason_code`**(`P-02-06` §5-5 선례 · §3-3).
ⓒ **`POST` 코어**(§4): 채번 tx 밖(`plantId null` · `periodDate = occurredAt` UTC 날짜) → `lockWorkOrder` → 상태 검사 → `result_sequence MAX+1` → 다섯 수량 손검사 → FK 존재 400 → **`worker_id` = `X-Worker-No` 유일 원천**(없으면 400 `REQUIRED` · 마스터에 없으면 400 `INVALID` · `app_user_id` 도출 금지 — `plan.md` §5 규칙 9 의 «예외» §4-3) · `idempotency_key` 헤더 그대로 · If-Match 는 오면 `work_order.version_no` 와 대조(§4-6) · `resultSourceCode` 는 받되 검증(§1-5) · `businessDate` 없음.
ⓓ **`:correct`**(§5): 새 행(대체값 · 생략 칸 원본 승계 · `corrects_production_result_id`) · **누계 = 정정된 원본을 뺀 합**(`NOT EXISTS corrects…` · 잎만 · `app.qty_t CHECK ≥0` 라 상쇄 행 불가 → 042) · 정정의 정정 허용 · **A급 = 다섯 수량 중 하나라도 원본과 다름** · 승인 게이트는 **도메인 안 판정**(`assertApproved` 는 0건 통과라 못 씀 — `APPROVED` 있으면 통과 · `PENDING` 400 `APPROVAL_IN_PROGRESS` · 0건/반려 400 `APPROVAL_REQUIRED`) · 승인 1건이 정정 N건을 연다(041ⓑ) · 배분 정정 없음(044).
ⓔ **`:request-approval`**(§6): `ApprovalRequestCreate` 가 `reason` 한 칸이라 등급 판정 입력 없음 → 계약 문자 그대로 요청만 만든다(041ⓐ) · `assertNoOpenRequest` 400 · `ROUTE_NOT_FOUND` 400 · 다형 축(FK 없음).
ⓕ **조회 3**(§7): 목록 where 8 · 기본 `occurred_at desc` + PK · 단건 ETag 없음 · `lot-lifecycle-events` 는 `src/trace/lot-lifecycle/` 신설 · 기간 없으면 400 `REQUIRED`.
ⓖ **횡단**(§8): 403 추가 0 · 멱등 4 · If-Match 선택 2 · `src/core/**` 무변경(코어 PR 0) · 모듈 `src/production/production-result/` + `src/trace/`.
ⓗ **문의 4**(041 상신 등급 입력 없음+승인 소진 축 · 042 정정 누계 해석 · 043 LOT 완료 미달 사유 칸이 W/O 한 칸 · 044 정정이 배분 못 고침) · 기존 인용 5 · 알려둘 것 19(§9-3).
ⓘ **PR 3 → 4**(§10 · 전부 opus · 직렬): ① 마이그+골격+조회 3 ~250 · ② `POST`+배분+L1+누계 정정 ~300 · ③ `:correct`+`:request-approval` ~250 · ④ `:complete`+M1 마디 e2e ~200. M1 «체인» e2e 는 별도.

## 공통으로 읽을 것
- `docs/coverage-100/README.md` §1-2·§2·§5·§6 · `docs/coverage-100/plan.md` §0 151·153 · §1 41·42 · §3 104 · §4 122 · §5 규칙 9·#12 · §7 209 · §8
- `docs/coverage-100/slices/I-7.md` 전문 · `brief-I-7.md`(읽을 것 12·담을 것 13) · `I-6.md` §0 R-12·R-24·R-28·R-29 · §3-2 · §4-2 · §5-3~5-4 · §8-6 · **§11-2**(구현이 달리 한 것)
- 계약 `contracts/production-02생산실행.json` 경로 1606·1785·1828·1915 · 스키마 `ProductionResult` 5661 · `ProductionResultCreate` 5846 · `ProductionResultCorrect` 5803 · `ResultLotAllocation` 5940 · `components.parameters.WorkerNo`·`IfMatchVersionOptional` · `contracts/logistics-01자재창고.json` 6414(`lot-lifecycle-events`)·6572(`:complete`)·`LotComplete` 11480·`LotLifecycleHistoryEvent` 12069 · `ApprovalRequestCreate`
- 실재 코드(main 76733fc 병합본): `src/core/document-state/transitions.ts:112-120·155-165` · `src/core/lot/lot-lifecycle.service.ts` · `src/core/lot/lot-registry.service.ts` · `src/core/approval/approval.service.ts:141-`(`assertApproved`)·`assertNoOpenRequest` · `src/core/numbering/numbering.service.ts` · `src/production/work-order/`(`work-order-progress.ts` · `work-order-query.service.ts:111·123·193` · `work-order-close.service.ts:141` · `work-order-write.service.ts:81-85` `assertVersion` · `lot-source.ts`) · `src/trace/lot/` · `src/notice/notice.controller.ts:140-165`(`actorOf`) · `src/common/permissions/derived-permissions.ts:111·232-234·258` · `src/common/http/master-write.ts:48-51` · `src/common/errors/error-codes.ts`
- 물리 `prisma/schema.prisma`: `production_result` 2807~ · `production_result_lot_allocation` 2858~ · `material_usage_allocation` 2739~ · `lot` 3509~(`completed_at`?·`initial_qty`) · `lot_lifecycle_history` 4630~ · `work_order` 2876~ · `mdm.worker` 2332 근처 · `approval_request` · baseline 마이그의 `app.qty_t`(59-60)·`app.code_t`(50-51)
- 시드 `prisma/seed.ts`: `RESULT_SOURCE` 1459 · `PRODUCTION_RESULT_STATUS` 1467(폐기) · `LATE_ENTRY_REASON` 1063 · `PRODUCTION_RESULT_CORRECT_REASON` 1197 · `LOT_LIFECYCLE_TRANSITION`/`_STATUS` · `WORK_ORDER_COMPLETION_VARIANCE_REASON` 735 · `APPROVAL_TYPE`·결재선 · `NUMBERING_RULES`
- 기존 문의 `docs/design-inquiries/README.md` + 030·035·039·040 · 설계 사본 `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/02/`(`P-02-04` §4·§5 · `P-02-06` §5-3·§5-5·§8 · `W-02-05` §4-D·§5-4·§5-8·§8-8 · `W-02-08`) · `06/W-06-15`
- 자기 관점: api → `plan-api.md` S16 412~441 · S08 220~238 · §5.5 1084 · §7 에러표 / uiux → `plan-uiux.md` 58 · 359~376(U24·U25) · 631 · 1096 · §9-3 · 화면 `P-02-04`·`P-02-06`·`W-02-05` / integration → `plan-integration.md` 101~110 · 152 · 171 · 264~272 · 358 · 498~502 · 583 · 629 · §4-1 M1 · §9 반박 1·3

## 판정할 것 (각 항목에 ✅ 동의 / ✏ 수정(대안+근거) / ⛔ 반대(근거))
1. **문의 041~044 가 정말 새 문의인가**(§9-2) — 041 이 ⓐ(상신 입력 없음)·ⓑ(승인 소진 축)를 한 장에 묶는 게 맞나 · 030(폐기 출고 승인 축)과 겹치나 · 042 가 문의인가 「선례로 닫힌 판정 + 알려둘 것」인가(x-no-code-key 전문이 이미 `corrects…Id` 로 판정하라 했다) · 043 이 `plan-api.md` S08 초안의 문의와 같은 것인가 · 044 가 042 와 한 장이어야 하나 · 「기존으로 미룬 것」 5 중 새 번호가 필요한 것 · 「알려둘 것」 19 중 문의로 올릴 것 · `resultSourceCode` required vs 「서버가 안다」 가 계약끼리 모순(멈춤 조건 ③)인지 「받되 검증」으로 닫히는지.
2. **마이그 D1·D2**(§2-2·§2-3) — D2 칸 이름(`correct_reason_code` vs 형제 `material_consumption.change_reason_code` 축의 `change_reason_code` — 계약 `reasonCode` 와 data-model 명명 규칙) · nullable 이 맞나(원본 행은 빈다) · **`status_code` 상수 `'CONFIRMED'`** — 값을 «짓지 않는다» 는 규칙에 e2e 픽스처 값이 선례가 되나(대안: nullable 로 풀고 응답에서 상수를 채운다 = `plan.md` §0 #10 문자 그대로 · 또는 D1 에 얹어 NOT NULL 해제) · `prisma generate` 필요 표기 · 사전 대조 SELECT · 두 릴리스 규칙 미해당 확인 · `plan.md` §4 정정.
3. **상태기계**(§3) — W/O 상태별 허용/400 표(§3-2)가 계약 문장과 맞나(특히 `RELEASED`(세션 없이 실적) · `SUSPENDED` · `CLOSED` 뒤 실적 = `W-02-05` §5-4 규칙 3 「지연 실적」 — 039 와의 관계) · `VOIDED` 슬롯 400 vs skip · `changedAt` 을 `occurredAt` 으로 · `sourceDocumentTypeCode='PRODUCTION_RESULT'` 상수 위치 · `:complete` 가 어느 상태 칸도 안 옮기는 판정(`P-02-06` §5-5 선례 인용이 정확한가 — `lot.completed_at` 칸이 물리에 실재하는지 **실측**) · `:complete` 대상 = `initial_qty` vs `Σ allocated_qty`(§3-3) · 두 번 완료 = ?
4. **`POST` 코어**(§4) — 순서 · 채번 `plantId null`·`periodDate=occurredAt` UTC(하노이 하루 어긋남 — 알려둘 것) · **`X-Worker-No` 400 `REQUIRED`** 가 `plan.md` §5 규칙 9 의 예외로 성립하나(대안: 세션 `app_user.worker_id`/`mdm.worker.app_user_id` 도출 — 기준 4 vs 계약 `required:true`) · `worker.is_active` 무시 · If-Match 를 `work_order.version_no` 와 대조(§4-6 — 대안: 무시 · `assertVersion` 재사용) · `resultSourceCode` 검증 방식(`RESULT_SOURCE` 시드 값 실측) · `lotAllocations` 합 ≠ `goodQty` 통과 · `work_session_id` 존재 검사 · `late_entry_reason_code` 0값 처리 · 응답 201 ETag 없음.
5. **`:correct`**(§5) — **대체값 + 잎만 세기** 판정(§5-3 · 대안: 차이값 불가 확정 / 원본 `status_code` 를 바꿔 거르기 — x-no-code-key 위반) · `ACTIVE_RESULT_WHERE` **네 자리**(query 3 + `work-order-close.service.ts:141`) 가 PR ② 예산 안인가·I-6 파일 수정이 리뷰 범위에 드나 · A급 판정식(본문이 «준» 칸만 vs 승계 뒤 전체 비교) · 승인 게이트를 도메인 안에 두는 것(코어 `assertApproved` 수정 대안 — I-4 문의 030 과 같은 뿌리인지) · 에러 코드 `APPROVAL_REQUIRED`·`APPROVAL_IN_PROGRESS` 신설 vs 기존 코드 재사용(`error-codes.ts` 실측) · 정정본 `worker_id`·`shift_id`·`work_session_id`·`lot_allocation` 승계 규칙 · 정정본 `result_sequence` · `note` 저장 칸 · ETag 없음.
6. **`:request-approval`**(§6) — `ApprovalRequestInput` 그대로 · `businessUnitId` null(022) · 응답 모양(202?) · 반려 뒤 재상신 · 원본이 정정본이어도 상신 가능? · 상신 대상이 이미 정정된 원본이면?
7. **조회 3 · `lot-lifecycle-events`**(§7) — where 8 매핑 · 정렬 · 단건 404 · 뷰 24칸(`statusCode` 상수 · `shiftId` 생략) · `lot-lifecycle-events` 모듈 위치(`src/trace/lot-lifecycle/` 신설 vs 기존 `src/trace/lot/`) · 기간 강제 400 `REQUIRED`(document-progress 선례 실측) · `transitionCode` 3값 · `fromLifecycleStatusCode` NULL 생략 · 403 없음.
8. **횡단·PR 분할**(§8·§10) — 403 추가 0 실측(5건 등록 위치) · 코어 무변경이 맞나(L1 재사용만 · 승인 판정 도메인 안 · 누계 상수는 `work-order-progress.ts`) · 인계 6행(I-9·I-10·I-11·I-19·I-25·I-26) · **PR 4 순서·모델·예산**(① ~250 마이그 든 PR opus · ② ~300 + I-6 파일 4자리 · ③ ~250 · ④ ~200 · 조회 ①을 sonnet 으로 내릴 수 있나 — 마이그가 들어 opus 라면 마이그 선행 커밋만 떼서 ①a/①b?) · 각 PR 단위 테스트 이름이 e2e 로 못 가는 가드를 덮나 · §10-1 픽스처(worker·shift·uom·`approval_route` 시드 유무)·cleanup 순서 · M1 마디 e2e 를 ④ 가 지는 것 · 신규 e2e 파일 이름.
9. 자기 관점 계획서와 I-7.md 가 어긋나는 자리 중 **구현에 영향 주는 것**만. §11 대조표 19행의 자기 관점 행을 실측으로 확인(#15 는 낡음 — I-6 전건 병합). uiux 는 화면 명세(`P-02-04` §4 작업자 서버 파생·§5 등록 순서 · `P-02-06` §5-3 미달 사유·§5-5 트랜잭션 그림 · `W-02-05` §4-D 정정 5칸·§5-8 등급표·흐름도 · `W-02-08` 달성률)와의 정합을 실측으로.

마지막에 5줄 요약: 「재수립 결과 — I-7.md 에 반영할 수정 N건(목록), plan.md 에 반영할 것, 문의 최종 건수, PR 분할 최종안」.
