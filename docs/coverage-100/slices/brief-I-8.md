# I-8 자재 출고요청·피킹·예약 코어 — 8건 — 개별 계획안 브리프 (계획만, 구현 금지)

산출물: `docs/coverage-100/slices/I-8.md` 하나. 저장소 파일을 고치거나 만들지 않는다(이 파일 하나만 쓴다). 작업 위치는 worktree `/Users/rangkim/projects/crefle/omf/apps/omf-mes-server/.claude/worktrees/i8-plan`(브랜치 `docs/coverage-100-i8-plan` · main e7fa784 · 체크아웃 변경 금지 · `git` 쓰기 금지 — 커밋은 통합자가 한다). `contracts/*.json` 읽기 전용(`jq`). `prisma migrate reset`·`migrate diff --shadow-database-url`·`prisma db seed`·`contracts:update/check` 금지. 로컬 DB 는 `psql` SELECT 만(`.env` DATABASE_URL 의 `?schema=` 는 잘라서). Bash cwd 는 호출마다 리셋되므로 절대경로로 `cd`. 선례 형식: `docs/coverage-100/slices/I-7.md`(§0-재수립은 통합자가 나중에 붙인다 — §1~§11 만 쓴다) · 브리프 선례 `docs/coverage-100/slices/brief-I-7.md`.

**대상 8건(실측 — `@Contract` 미바인딩 목록과 계약 대조 완료)**: `GET /logistics/material-issue-requests` · `GET …/shortage` · `GET …/{materialIssueRequestId}` · `POST /logistics/material-issue-requests` · `GET /logistics/picking-orders` · `GET …/{pickingOrderId}` · `POST /logistics/picking-orders/{pickingOrderId}/lines/{pickingLineId}:pick` · `GET /inventory/reservations`. 현재 299/487 → 목표 307(④ #242 병합 뒤 300 기준).

## 읽을 것 (순서대로)
1. `docs/coverage-100/README.md` — §1-2 3조건 · §2 판정 절차 · §5 불변 제약 · §6 속도(구현 브리프 비테스트 예산 350 · 한도 400 · **코어 전용 ≤200**).
2. `docs/coverage-100/plan.md` — §1 43행(I-8 · 8건 · 의존 I-4 · 마이그 「—」 · **코어 `reserved_qty`/`picked_qty`** · opus · PR 3 · ⛔ I-5 와 직렬 — I-5 는 완료) · 44행(I-9 가 I-8 뒤) · 55행(I-22 제품 피킹이 I-8 코어를 잇는다) · 83행(**M2 체인** — `:release`(출고요청 자동) → 피킹 → 출고 → 생산창고 입고) · 89행 · **106행(예약/피킹 = 코어 PR ≤200 · `posting` 이 `reserved_qty`·`picked_qty` 를 올리고 내린다 · 도메인의 `inventory_balance` 직접 UPDATE 금지 — e2e 감지)** · 211행(M-01-08 결정 10 ↔ C-1 충돌 — 열린 것 인용) · §0 원장 판별자 4값 고정 · §8 진행표.
3. `docs/coverage-100/plan-integration.md` — 12·15행(posting 이 두 칸을 «일부러» 안 건드린다) · 36~42행(체인 ⑥⑦⑧ — `picking_order.source_document_type_code='MATERIAL_ISSUE_REQUEST'` · `picking_line.inventory_reservation_id` 예약 소진 · `goods_issue.source_document_type_code='PICKING_ORDER'`) · 107행(투입은 원장을 안 지난다 — 재고는 출고에서 이미 움직였다) · 132행(`:pick` 원장 없음 · `reserved_qty → picked_qty` 이동) · **150행(4자리: 출고요청이 예약 건다 · `:pick` 예약→피킹 · `goods_issue:post` 피킹 소진 · 제품 피킹 — 「계약과 구현이 어긋난 유일한 자리」)** · 240행(`blocks_picking` 은 I-8) · **276~283행(I-8 절 — `reserve()/pick()/consume()` 코어 · shortage 의 BOM 스냅샷 = `lot.bom_id/bom_version` · 소요식 스크랩률 미적용 · `picking_line.status_code` 값 없음)** · 660행(반박 2 — 도메인 직접 UPDATE 를 코어 전용 PR + 감지로 막는다) · 772~776행.
4. `docs/coverage-100/plan-api.md` — **111~142행 S04**(17건 중 I-4 7건 제외 10건 → 8건은 I-8 · 2건(`shopfloor-receipts`)은 I-9 · 「피킹 라인 상태는 칸 불필요 — `plannedQty ↔ pickedQty`」 · `:pick` 이 `inventory_reservation` 을 걸고 푼다 · 오퍼레이션 표: `POST material-issue-requests` 멱등·If-Match 선택·403 · `:pick` 멱등·If-Match 선택·403 · 나머지 GET 은 전부 —) · §7 에러표.
5. `docs/coverage-100/plan-uiux.md` — 45행(U11 · 예약 ○ · 피킹 ○) · **212~223행(U11 화면 매핑 — `POST` 「멱등, ETag」 · `:pick` 「멱등, ETag, 사번」 ⚠ I-7 교훈: 화면 매핑의 ETag 는 계약 `headers` 실측이 이긴다 — `:complete` 200 에 ETag 없었다)** · **579행(U11 「피킹 지시를 누가 만드는가」 — 계약에 생성 오퍼레이션이 없다 · 파생을 지어내지 않고 요청서로)** · 619·625·626행(순서 근거) · 695행(M-01-08 이 반쯤 열리는 문제) · **1020~1025행(⑪ shortage — 열 「품목/BOM 소요/기출고/부족/요청수량/비고」 · 부족 음수면 0 · 기출고를 화면이 더하지 않는다 · BOM 조회를 화면이 안 한다)** · 1114행 · §9-3 1197행~(M-01-08 결정 10 ↔ C-1).
6. **선행 결과물** — `docs/coverage-100/slices/I-6.md` **§4-3(548행~ 자재 출고요청 자동 발행 — 표에 직접 쓴다)** · §4-4 · R-18(소요식) · R-23(실패 안내 — 「요청 0건 = 실패」 규약을 I-8 이 정한다) · **§8-6 인계 I-8 행(853·857행 — ⓐ `status_code='REQUESTED'` 는 판정에 안 쓰인다 · ⓑ 자동 발행 `reason_code` NULL · ⓒ `bom_component_id` · ⓓ 소요식 동일 · ⓔ `:cancel` 은 요청을 안 건드린다(문의 038 — I-8 이 취소 전파를 정한다) · ⓕ 채번 `MIR`)** · 708행 · 895행(038) · §4-2(선발행 슬롯 `bom_id/bom_version` 스냅샷 — shortage 의 BOM 축) · `I-4.md` **§7-2(707~714행 — I-8 은 새 출고 등록 경로를 만들지 않는다 · `M-01-08` 은 `POST /goods-issues`(`postImmediately:true` · `source=PICKING_ORDER` · `pickingLineId`) 를 그대로 · I-8 이 더하는 것 셋 ⓐ picking 생성 경로 ⓑ 코어 확장 ⓒ `blocks_picking` · I-4 는 `picking_line_id` FK 손검사만 · 예약된 재고는 `available_qty` 손검사 때문에 I-4 경로로 안 나간다 — `picked_qty` 로 옮긴 뒤에야 나간다)** · R-7(`judgment_type_control.blocks_issue` 판정문 — `blocks_picking` 은 같은 방식) · R-1(잠금 7칸 오름차순 한 문장) · §9-3 ⓜ(Hold 강제출고 미허용) · `I-5.md` R-5(`post()` 선잠금) · `I-7.md` §4-3(`X-Worker-No` 처리 — `:pick` 「사번」이 같은 규약인지) · `I-2.md` R-2(채번 `$transaction` 밖).
7. 실재 코어: `src/core/inventory-posting/inventory-posting.service.ts`(머리 주석 30~41행 「⛔ `reserved_qty`·`picked_qty` 는 건드리지 않는다 … 별도 코어다」 · `post()` 45~ · `reverse()` 105~) · `balance-lock.ts`(`lockBalancesInOrder` 39~ · `BalanceLockKey` 7칸) · `posting.types.ts` · `reversal.ts` · `src/logistics/goods-issue/issue-posting.ts`(87·149행 — `posting.post` 호출 자리 · **`consume()` 이 여기 앉으면 I-4 파일 수정 — 예산에 넣는다**) · `goods-issue-rules.ts:61·181`(`PICKING_ORDER` 원천 존재 · `picking_line_id` FK) · `src/production/work-order/material-issue.ts`(60줄 — `ISSUE_REQUESTED` · 소요식 · `bom_component_id`) · `src/production/work-order/work-order-release*.ts`(자동 발행이 요청 헤더·라인을 어떻게 INSERT 하는지 · 예약을 거는지 실측 — 안 걸면 I-8 이 그 자리를 «고쳐야» 하는지 §2) · `src/inventory/balance/`(잔액 조회 모듈 — `GET /inventory/reservations` 가 앉을 자리) · `src/core/document-state/transitions.ts`(`material_issue_request`·`picking_order` 키가 있는가 실측) · `src/core/numbering/numbering.service.ts`(`MATERIAL_ISSUE_REQUEST`·`PICKING_ORDER`·`RESERVATION` 규칙 등재 여부 — 시드 1393·1398) · `src/common/errors/error-codes.ts`(새 코드 금지) · `src/common/permissions/derived-permissions.ts`(403 둘 등록 위치).
8. 물리 `prisma/schema.prisma`: `inventory_balance` 484~(`reserved_qty`·`picked_qty`·`blocked_qty` · **`available_qty` 는 생성 칸 = on_hand − reserved − picked − blocked** ⚠ 피킹분도 가용에서 빠지므로 `goods_issue:post` 의 손검사(I-4 §3-3)와 `consume()` 순서를 같은 잠금 안에서 정해야 한다) · `inventory_reservation` 575~(`reservation_no` UNIQUE · `reservation_type_code` · `source_document_*` · `reserved_qty`/`released_qty`/`consumed_qty` · `status_code` · `ix_reservation_source`) · `material_issue_request` 957~ · `_line` 982~ · `picking_line` 1003~(`inventory_reservation_id` nullable · `status_code` NOT NULL · `uq_picking_line`) · `picking_order` 1034~ · `bom` 2389~ · `bom_component` 2417~ · `lot` 3510~(`bom_id`·`bom_version`) · `judgment_type_control` 4700~(`blocks_picking`) · 트리거 실측(`prisma/migrations/*baseline*` 에서 `inventory_balance` UPDATE 를 막는 트리거가 있는가 — `plan.md` 106 「e2e 감지」의 수단).
9. 시드 `prisma/seed.ts`: `MATERIAL_ISSUE_REQUEST_REASON` 449 · `PICKING_TYPE` 678 · `RESERVATION_TYPE` 686 · 561행(`blocks_picking` 주석) · 채번 1393·1398 · `MATERIAL_ISSUE_REQUEST_STATUS`/`PICKING_ORDER_STATUS`/`RESERVATION_STATUS` 그룹이 **있는지**(없으면 값을 짓지 않는다).
10. 계약 `contracts/logistics-01자재창고.json`: 경로 1662(`/inventory/reservations`) · 3756 · 3921(shortage) · 3964 · 4006 · 4094 · 4136(`:pick`) · 스키마 `InventoryReservation` 10817 · `MaterialIssueRequest` 12229 · `MaterialIssueRequestCreate` 12312 · `MaterialIssueRequestCreateLine` 12388 · `MaterialIssueRequestDetailResponse` 12430 · `MaterialIssueRequestLine` 12448 · `MaterialIssueShortageLine` 12518 · `MaterialIssueShortageList` 12564 · `PickingLine` 12601 · `PickingLinePick` 12769 · `PickingOrder` 12809 · `PickingOrderDetailResponse` 12890 · `InventoryBalance` 의 `reservedQty`/`pickedQty` description(「서버 전기가 올리고 소진되면 내린다」 원문) · 각 오퍼레이션 `parameters`(where 칸 전수) · `responses.*.headers`(ETag 실측) · `x-*` 노트 전건.
11. 화면 사본(읽기 전용): `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/01/M-01-08-자재출고피킹.md`(294줄 — §5-1 「누가 만드는지 미정」 · §6 보류 LOT 차단 · §8 #7 Hold 강제출고) · `screens/02/W-02-10-추가자재출고요청.md`(232줄 — §3 ② 소요표 · 요청 상태 값 목록 미결 §8-3).
12. 기존 문의 `docs/design-inquiries/README.md` + 030·037·038·039 · tmp 초안 `/Users/rangkim/.claude/jobs/a3d5599b/tmp/041~044-*.md`(I-7 · 번호 044 까지 사용 — **I-8 은 045 부터**).

## 담을 것 (I-7.md 의 절 짜임을 따른다)
1. **README §1-2 3조건 판정**을 맨 위에.
2. **마이그** — 초안 「—」. 실측으로 확정: `picking_line.status_code` NOT NULL 인데 값 목록이 없다(G-2) · `material_issue_request.status_code` 상수 · `inventory_reservation.status_code` 값 · 부족한 칸이 있으면 README §5 「물리를 고친다」 · 두 릴리스 규칙 미해당 확인.
3. **코어 — 예약·피킹 수량**(⭐ 이 슬라이스의 심장 · 코어 전용 PR ≤200): `InventoryPostingService` 에 `reserve()/pick()/consume()`(이름·시그니처는 계획자가 계약 문장에서 도출) — 각각 `inventory_balance` 어느 칸을 얼마나 · `inventory_reservation` 행 생성/`released_qty`/`consumed_qty`/`status_code` 전이 · **잠금**(I-4 R-1 7칸 오름차순 · `lockBalancesInOrder` 재사용 · 예약 행 잠금 순서) · `available_qty` 부족 시 400 `NEGATIVE_BALANCE`(기존 코드 재사용) · **`consume()` 이 `post()` «안»에서 도는가 `issue-posting.ts` 가 따로 부르는가**(`available_qty` 가 피킹분을 빼므로 순서가 틀리면 정상 출고가 400 이다 — 판정 근거를 쓴다) · `reverse()`(I-5 역트랜잭션)가 소진된 피킹을 되돌리는가(안 되돌리면 알려둘 것) · 머리 주석 ⛔ 문장을 어떻게 고치나 · 「도메인이 `inventory_balance` 를 직접 UPDATE 하지 않는다」 감지 수단(트리거 실측 결과 · 없으면 단위 grep 가드 또는 안 만든다 — §2).
4. **`POST /material-issue-requests`**(W-02-10 추가 요청) — 본문 스키마 칸 매핑 · `:release` 자동 발행(`material-issue.ts`)과 **같은 INSERT 모양**을 재사용하는가(도메인 간 service 호출 금지 — 공유는 타입·상수뿐 · I-6 §8-6 I-24 행 선례) · **예약을 거는가**(plan-integration 150 「출고요청이 예약 건다」 vs 자동 발행은 예약 0 — 둘이 갈리면 §2 · 자동 발행도 예약을 걸도록 I-6 코드를 고치는지 = 예산) · 채번 `MIR` tx 밖 · `reason_code`(수동은 required?) · 상태 `'REQUESTED'` · 403 · 멱등 · If-Match 선택(무엇의 버전?) · ETag(계약 `headers` 실측) · 취소된 W/O 의 요청(038 — 등록 400? 허용?) · 「요청 0건 = 실패」 규약 확정(R-23).
5. **피킹 지시 생성 주체**(plan-uiux 579 · M-01-08 §5-1) — 계약에 `POST /picking-orders` 없음. §2 절차로: `POST /material-issue-requests` 또는 `:release` 가 파생 생성하나(지어내지 않는다) / 안 만들면 `:pick` 은 e2e 픽스처 직접 INSERT 로만 도달 가능 — 그러면 M2 체인이 서지 않는다는 것을 명시 → 문의 **045** 후보. `picking_order.source_document_type_code` 2값 · `picking_type_code`.
6. **`:pick`** — `PickingLinePick` 본문 칸 · 라인 잠금(`FOR UPDATE`) · `picked_qty` 누적 vs 대체 · `planned_qty` 초과 400 · 부분 피킹 허용? · **코어 `pick()` 호출**(예약 → 피킹 · `inventory_reservation_id` NULL 이면?) · `blocks_picking` 차단(I-4 R-7 판정문 그대로 · `judgment_type_control` 0행이라 오늘 안 막는다) · `lot_hold`/Hold 강제출고(§9-3 ⓜ) · `X-Worker-No` 사번(계약 헤더 실측 · I-7 §4-3 규약) · `picking_line.status_code` 값(G-2 — 어디서 얻나) · `picking_order` 완료 판정(전 라인 피킹 시 헤더 상태 옮기나 — 「칸 불필요」 판정과 정합) · 멱등 `runIdempotent` · If-Match 선택 · ETag(실측) · 403 · 404.
7. **`shortage`** — 질의 칸(`workOrderId` 필수?) · BOM 축 = `lot.bom_id/bom_version` 스냅샷(선발행 슬롯이 없는 W/O 는? — `production_plan.bom_id` 폴백인지 400 인지 §2) · 소요식 = `material-issue.ts` 와 **같은 함수**(도메인 간 import 는 어디에 — `src/common`? 상수·순수 함수 공유 규약) · 기출고 = `goods_issue_line`(어느 축으로 W/O 에 묶나 — `source_document_type_code='WORK_ORDER'`? `PICKING_ORDER`→`material_issue_request`→W/O? 실측) · 요청수량 = `material_issue_request_line` 합 · 부족 음수면 0 · 열 6 매핑.
8. **조회 5**(목록 3 · 단건 2 · `reservations`) — where 전수 매핑 · 정렬 기본 · `PageMeta` · 단건 404 · 뷰 칸 매핑 · ETag(실측) · 403 없음 · `GET /inventory/reservations` 모듈 위치(`src/inventory/reservation/` 신설 vs `balance/` 곁).
9. **횡단** — 403 2건 등록 · 멱등 2 · If-Match 선택 2 · 도메인 배치(`src/logistics/material-issue-request/` · `src/logistics/picking/` · `src/inventory/…`) · 코어 변경 = PR 하나 ≤200 · 다른 도메인 service 호출 0.
10. **e2e** — M2 마디 ⑥⑦⑧ e2e(W/O `:release` → 요청 → (피킹 지시 생성 판정에 따라) `:pick` → `POST /goods-issues source=PICKING_ORDER` → `balance` reserved/picked 0 · on_hand 감소) · 픽스처 · cleanup 순서 · I-4 goods-issue e2e 가 깨지지 않는가(`consume()` 이 `post()` 안이면 `picking_line_id` 없는 라인은 무변경).
11. **설계 미정 자리** — 각각 README §2. 문의는 제목만(**045** 부터 · 기존과 겹치면 「기존」 — 030·037·038·039 · 결정 10↔C-1 은 열린 것 인용). 후보: 피킹 지시 생성 주체 · 요청 상태 축(W-02-10 §8-3) · `picking_line.status_code` 값 · 예약 시점(요청 vs 피킹) · 취소된 W/O 요청 취소 전파(038) · Hold 강제출고 · shortage 의 BOM 폴백.
12. **PR 분할** — 초안 3(plan.md) · 전부 opus · 코어 PR ≤200 선행. 초안: ① 코어 `reserve/pick/consume` + 단위(≤200) ② 요청 `POST` + 조회 3 + shortage ③ `:pick` + 피킹 조회 2 + `reservations` + M2 마디 e2e. 각 PR 파일 목록·예상 diff·**단위/e2e 테스트 이름 목록**·스택 순서.
13. **인계 모양** — I-9(생산창고 입고가 `goods_issue`/`picking` 어느 축을 참조하나) · I-10(투입이 `picking`/`goods_issue_line` 을 읽나) · I-22(제품 피킹이 같은 코어 `reserve/pick` 을 `SHIPMENT_REQUEST` 축으로 재사용 — 시그니처 고정) · I-24 · 통합 계획 대조표(plan.md 43·106 · plan-api S04 · plan-integration 276~283 · plan-uiux U11 · I-6 §8-6 · I-4 §7-2 와 다른 점).

금지: 구현 코드 작성 · 계약 파일 수정 · 다른 문서 수정 · 값 지어내기(값 목록이 없으면 「없다」고 적고 §2 로 판정) · `omf-mes`·`omf-mes-client` 저장소 쓰기 · `git` 쓰기 · 새 error code.

최종 응답(5줄 이내): README §1-2 3조건 판정 · 마이그 건수 · 문의 신설 건수(제목) · PR 분할 최종안(건수·예산) · 멈춤 조건 해당 여부.
