# I-18 · LOT 부가 — 외부식별자·보류 조회·상태 이력·IQC 생략(5건) — 개별 계획안

> 레인: **A2**(`lanes.md` §0-1 · 2026-09-08 신설). 브랜치 접두어 `feat/coverage-100-a2-…` · PR 제목 접두어 `[A2] ` · 문의 대역 **150~179** · 이슈 **#333** · 점유 라벨 `status:in-progress` 는 구현 착수 때 붙인다.
> 통합 계획서 해당 절: `plan.md` §1 **18행**(선행 **I-1** · 마이그 「—」 · 코어 「—」 · **① sonnet · ② opus** · **PR 2** · ∥ I-19) · §5 규칙 **1·2·3·4·5·6·7·9·12** · §7(문의 **019** — `IQC_SKIP` 승인 화면 · 「구현함(I-1 · `openable=false`)」) ·
> `plan-api.md` **§S08**(220~240행) — ⚠ `:complete`·`GET /trace/lot-lifecycle-events` **2건은 I-7 로 옮겨졌다**(둘 다 이미 구현·병합됨) ·
> `plan-uiux.md` **§U6**(166~171행 · 3건) · **§U9**(199~204행 · 1건 · 「멱등, 사번」) · **852행**(물류 자식 치환 PUT 7건의 화면 = 부모의 화면) · 190행(`GET /trace/lot-status-events` 는 §U8 — 화면 `M-01-08`·`W-01-02`·`W-03-01`) ·
> `plan-integration.md` **§I-18**(359~367행) — 「가지. 조회 4 + IQC 생략 요청 1. **원장·상태기계 없음** → 커버리지를 싸게 올리는 슬라이스(병렬 후보)」.
> 선행 결과물: **`I-7`**(`src/trace/lot/lot-lifecycle-event.{controller,service}.ts`·`-view.ts` — 이 슬라이스 조회의 **복제 원본** · `lot-complete.service.ts` — `X-Worker-No`·자격 게이트 선례) · **`I-20`**(`src/quality/lot-hold/` · 마이그 `20260908110558_lot_hold_target_status_and_version` · R-9 「같은 계약 칸을 두 자리가 다르게 채우면 안 된다」 · ⚠ **PR ③ 미병합** — §0 자리 4·§8-2) · **`I-1`**(승인 코어 `src/core/approval/`) · **`I-2`**(채번을 `$transaction` «밖»에서) · **`I-4`**(ETag 없는 쓰기가 부모 `version_no` 를 올리는 규약).
> 실측 기준: 워크트리 `.claude/worktrees/designer-worktree-workspace-ff88ca` · 계약 사본 `contracts/COMMIT.txt` = **a6a87e144116ebaa32c01df5a12a0fd2924427e7** · 실측일 **2026-09-08**.
> ⚠ **초안·3리뷰의 실측은 `main` f931561 기준이었고, 이 커밋본의 base 는 `main` `dd4032e` 다.** 계약 사본은 같고(a6a87e1) 물리·DB 실측 결과도 변화 0 — **움직인 것은 커버리지 기준선 하나**다(아래).
> DB 관측: **2026-09-08 13:57 KST** · `omf-mes-lane-a2-postgres`(`omf_mes_lane_a2`) · `psql -X` **SELECT 만**. 마이그 **60건** 적용 · `trace.lot`·`lot_external_identifier`·`lot_hold`·`lot_status_event`·`app.approval_request`·`approval_route` **전부 0행**(업무 데이터가 비어 있는 것이 정상 — 결손이 아니다) · 코드값은 `mdm.code_value` 실측(§1-5).
> 커버리지: **389 → 394/487**(+5 · 이 슬라이스 전건). base `dd4032e` 에서 `node_modules/.bin/jest contract-coverage` 실측 **389/487**.
> 리뷰 3건: `I-18-review-api.md`(294행) · `I-18-review-uiux.md`(381행) · `I-18-review-integration.md`(258행) — 셋은 서로를 못 읽고 독립으로 판정했다.
> ⭐ **아래 본문은 3관점 재수립(R-1~R-10) 이 «이미 반영된» 최종 판정이다.** §0-재수립 표는 「무엇이 왜 바뀌었나」의 **이력**으로 남긴다 — 판정의 근거이기 때문이다.

---

## ⭐ 0. 리뷰가 반드시 볼 자리 5 (README §1-2 · 2026-09-07 규칙)

> 아래 다섯이 이 슬라이스의 갈림길이고, **뒤집히면 PR 분할·심장의 갈래·파일 배치가 바뀌는 자리**다.

| # | 자리 | 최종 판정 | 뒤집히면 |
|:-:|---|---|---|
| **1** ⭐⭐ | **`:request-iqc-skip` 의 「자격 축」** — 계약이 「**입하돼 수입검사 대기인** LOT 에 대해」(`:6508`)라 적었는데 그 조건을 어기는 요청의 응답을 **한 줄도 안 적었다**. 축 후보 넷: ⓐ `lot.source_type_code = 'INBOUND_RECEIPT_LINE'` ⓑ `lot.status_code = 'INSPECTION_PENDING'` ⓒ 열린 `lot_hold.reason_code = 'INCOMING_INSPECTION_WAIT'` ⓓ 아무것도 안 본다 | **ⓐ+ⓑ 둘 다 본다. ⛔ 두 축의 «코드가 다르다» — ⓐ 위반 400 `INVALID` · ⓑ 위반 400 `STATE_LOCKED`.** 0단계 선례가 **같은 자원의 형제**이고 그 형제가 두 축을 이미 갈라 놓았다 — `lot-complete.service.ts:66-69`(원천유형 위반 = `ERROR_CODE.INVALID`) · `:71-73`(상태 위반 = `STATE_LOCKED`). `:request-iqc-skip` 의 「입하돼」는 그 원천유형 게이트의 거울(ⓐ)이고, 「수입검사 대기」는 `LOT_STATUS` 4값 중 `INSPECTION_PENDING` 으로 등록 코어가 LOT 을 세우는 값이다(`lot-registry.service.ts:24`·`:87`)(ⓑ). §2 **2단계 기준 2**(거부하는 쪽 — 거부→허용은 호환 완화) + 0단계 선례 `goods-issue-update.service.ts:82-83`. ⛔ **ⓒ 는 쓰지 않는다** — 보류는 I-19 `:confirm` 이 닫으므로(`inspection-confirm.service.ts`) 검사 «중»인 LOT 에 이미 열린 보류가 없을 수 있고, 그러면 정당한 요청이 막힌다. ⇒ **통보 150** | PR ② 의 갈래·e2e 3건(202/400 ⓐ/400 ⓑ)이 바뀐다. ⓓ(아무것도 안 본다)로 가면 e2e 2건이 사라지고 예산 −25. ⓒ 를 더하면 픽스처가 보류 축까지 세워야 해 e2e 예산 +40 |
| **2** ⭐⭐ | **`:request-iqc-skip` 의 `X-Worker-No` — 없으면 400 인가, 읽고 지나가는가** | **400 `REQUIRED`(field `X-Worker-No`).** `plan.md` §5 **규칙 9** 는 「헤더가 없어도 400 을 내지 않는다」를 기본으로 두고 **열 자리**를 예외로 열거했는데 이 오퍼레이션이 그 목록에 **없다**. 그러나 열거된 예외의 판정 기준이 「담을 칸 0 이어도 계약 `WorkerNo.required=true` 면 거부」로 이미 굳었고(`:pick` I-8 R-16 · `material-returns` I-10 §4-8 · `precheck-decisions` I-11 §3-7 — 셋 다 담을 칸 0), 그중 **`POST /trace/lots/{lotId}:complete` 가 같은 자원의 형제**다(`lot-complete.service.ts:160-168`). 화면도 POP 축이다(`plan-uiux.md` §U9 — `M-01-13` · 「멱등, **사번**」). ⇒ **열한째 자리**로 규칙 9 목록에 추가한다(⚠ `plan.md` 는 공용 문서 — **레인 A 조율 항목 1** · §11-2). ⛔ 헤더는 계약 검증 가드가 **보지 않는다**(`contract-validator.ts:96` — `$ref` 파라미터는 `in` 만 보고 지나간다) ⇒ 판정은 서비스 몫 | 뒤집히면 e2e 1건이 사라지고 `assertWorkerNo` 재사용이 통째로 없어진다(예산 −15). 그리고 규칙 9 목록을 안 건드리므로 **공용 문서 조율 항목이 하나 준다** |
| **3** ⭐⭐ | **`PUT …/external-identifiers` — ⓐ 잠금 토큰의 원천 ⓑ 부모 `version_no` 를 올리나 ⓒ 치환 알고리즘 ⓓ 요청 안 중복** | ⓐ **`trace.lot.version_no`.** 계약이 직접 적었다(`:6214` — 「이 경로의 조회는 ETag 를 내리지 않는다 — If-Match 에 담을 토큰은 **부모 자원 `GET /trace/lots/{lotId}` 200 의 ETag** 다. 잠그는 단위가 부모이기 때문이다」). I-20 이 같은 문장을 `lot_hold` 에서 실측으로 재확인했다(`lot-hold.controller.ts:17-32` · R-24). ⓑ **올린다.** 응답에 ETag 선언이 **없어**(계약 200 에 `headers` 0) `runVersioned` 를 못 쓰고 → 0단계 선례가 정확히 같은 모양이다: `goods-issue-update.service.ts:46-47` 「응답에 ETag 가 없다(계약 미선언). 그래도 부모 `version_no` 는 **올린다** — 라인이 바뀌면 부모 상세의 내용이 바뀐다. 다음 If-Match 는 상세 GET 이 준다」. `GET /trace/lots/{lotId}` 가 `externalIdentifiers` 를 **본문에 싣기 때문에**(`lot.service.ts:146`) 이 규칙이 그대로 걸린다. ⓒ **전삭제 + 전삽입**(`deleteMany` → `createMany`) — 계약 `LotExternalIdentifierUpsert` 에 **id 칸이 없어**(required 2 · 프로퍼티 4) 자연키 diff 를 할 근거가 없고, 이 표를 참조하는 자식이 **0개**다(`schema.prisma:3601-3614` — 역관계 0). §2 2단계 **기준 5**(새로 만드는 개념 0). ⚠ `lotExternalIdentifierId` 는 매 치환마다 갈린다 — 계약이 「요청에서 빠진 기존 행은 삭제한다」라 적어 그것을 허용한다. ⓓ **요청 안 중복은 400 `UNIQUE_VIOLATION` 으로 앞당겨 막는다** — 물리 유일 인덱스가 **5칸 표현식**이다: `(lot_id, identifier_type_code, COALESCE(partner_id,0), COALESCE(external_system_code,''), external_identifier)`(baseline `migration.sql:1082-1089`). 손검사 없이 두면 P2002 가 500 으로 새거나(표현식 인덱스라 `meta.target` 이 비어 공용 그물에 안 걸린다) 조용히 N→N−1 이 된다(`purchase-order.service.ts:341-350` 이 같은 사고를 이미 한 번 고쳤다) | ⓑ 를 「안 올린다」로 뒤집으면 e2e 「연속 두 번 치환」이 200/200 이 되고 낙관적 잠금이 사실상 죽는다(대신 `:request-approval` 형 — `goods-issue-update.service.ts:161-166` — 이 그 반대 선례라 뒤집힐 여지가 있다). ⓒ 를 diff 로 뒤집으면 PR ② 예산 +60 · e2e +3. ⓓ 를 빼면 500 회귀 위험 |
| **4** ⭐ | **`GET /trace/lots/{lotId}/holds` 가 어느 뷰를 쓰는가** — 같은 계약 스키마 `LotHold` 를 저장소가 **두 벌**로 갖고 있다: `src/trace/lot/lot-view.ts:135 holdView()`(널을 «싣는다») ↔ `src/quality/lot-hold/lot-hold-view.ts:47 lotHoldView()`(`omitEmpty` 로 «키를 생략») | **`trace` 의 `holdView` 를 쓴다 — 새 뷰를 만들지 않는다.** 근거: ⓐ **같은 LOT 의 같은 보류가 두 경로로 나가면 모양이 같아야 한다** — `GET /trace/lots/{lotId}` 의 `LotDetailResponse.holds` 가 이미 `LotHold[]` 이고 `holdView` 로 그린다(`lot.service.ts:148`). 하위 자원 GET 이 다른 뷰를 쓰면 **한 화면(`W-01-07`)이 같은 행을 두 모양으로 본다** — I-20 R-9 가 잡은 것과 **정확히 같은 모양의 사고**다 ⓑ 도메인 경계 — `quality` 의 뷰를 `trace` 가 import 하면 `server-architecture.md:67` 의 정신에 걸린다 ⓒ 둘 다 계약을 통과한다(§1-4) ⇒ **선택 기준은 「같은 파일의 형제와 같은가」**. ⭐ **덤으로 정렬까지 맞춘다** — 상세 GET 의 `include: { lot_hold: true }` 두 자리(`lot.service.ts:126`·`:284`)에 **`orderBy` 가 없다**. 새 `GET …/holds` 는 `held_at desc` 로 고정하므로, 같은 화면이 두 경로에서 **다른 순서**를 본다 ⇒ `lot.service.ts` 두 자리에 같은 `orderBy` 를 **+4줄** 더한다. ⚠ `activeOnly` 기본 `true` 의 «뜻»은 `quality` 선례를 그대로 쓴다 — `activeOnly=false` 는 「해제된 것만」이 아니라 **«전체»**(필터를 «걸지 않는다») · 0단계 선례 `lot-hold-query.service.ts:68-72`(I-20 R-1) | 뒤집혀 `lotHoldView` 로 가면 `src/trace/lot/lot-view.ts` 의 `HoldView` 도 함께 고쳐야 하고, 그 파일은 **레인 A 가 I-20 에서 방금 고친 파일**이라 조율 창이 열린다(자리 5). PR ① 예산 +40 · 회귀 e2e(`trace-lot.e2e-spec.ts` R-9 단언) 재작성 |
| **5** ⭐ | **PR 분할 2 · 파일 배치 · 레인 A 와 겹치는 파일** | **PR 2 유지**(`plan-api.md` §S08 기대값과 같다 — §10-1). 배치 판정 셋: ⓐ `GET /trace/lot-status-events` 는 **경로가 `trace/lots` 와 달라** 전용 컨트롤러다 — 형제가 그 자리에 주석으로 **이 슬라이스를 예고해 두었다**(`lot-lifecycle-event.controller.ts:5-6` — 「오퍼레이션 하나에 디렉터리를 새로 만들지도 않는다(R-13 · **I-18 이 같은 축을 더한다**)」) ⇒ `src/trace/lot/` 안에 파일 셋을 **복제**한다(디렉터리 신설 0) ⓑ 나머지 넷은 경로가 `trace/lots` 아래라 **`LotController` 에 얹는다**. ⛔ `lot.service.ts` 에는 **로직을 안 넣는다** — 이미 **293줄**이고 `+2 메서드`면 ~310 으로 CLAUDE.md 의 ~300 신호에 걸린다(같은 판정을 I-7 이 이미 했다 — `lot-complete.service.ts:44`) ⇒ service 를 **셋 신설**(`lot-external-identifier` · `lot-hold-list` · `lot-iqc-skip`) ⇒ `LotController` 생성자 의존이 **3 → 6**. ⚠ 그 파일에 손대는 것은 **자리 4 의 `orderBy` +4줄 하나뿐**이다 ⓒ **레인 A 와 겹치는 파일 넷**: `src/trace/trace.module.ts`(**공용(A·B)**) · `src/trace/lot/lot-view.ts`(**0줄 — 안 만진다**) · `src/trace/lot/lot.service.ts`(**+4줄 · `orderBy` 만**) · `test/trace-lot.e2e-spec.ts`(A 가 I-20 R-9 회귀를 넣은 파일 · **추가만**) ⇒ **읽기만 하고 «등록/추가 줄»만 더한다**(`lanes.md` §1-4). ⛔ `transitions.ts` **0줄**(전이 0건 · A 가 I-20 에서 쓰는 중) | ⓑ 를 「의존 6은 많다」로 뒤집으면 컨트롤러가 둘로 갈리고(`LotController` + `LotPartsController`) PR ①②가 같은 파일을 안 만져 **병렬 스폰이 가능**해진다(지금은 `LotController` 를 둘 다 만져 직렬). ⓐ 를 「`LotController` 에 합친다」로 뒤집으면 파일 셋이 사라져 PR ① 예산 −60 이지만 형제 주석의 예고와 어긋난다 |

**오퍼레이션 5/5 대조** — `awk -F'\t' '$1=="I-18"' docs/coverage-100/assignment.tsv` = **5행** · 브리프 §1 표 5건 · `plan-integration.md` §I-18 5건 · 계약 실물 5건(`contracts/logistics-01자재창고.json` 총 **90** 오퍼레이션 · 67 path) 이 **전부 같다**. 누락 0 · 여분 0. ⚠ `plan-api.md` §S08 표는 **7행**이다 — `:complete`·`GET /trace/lot-lifecycle-events` 둘은 I-7 로 이관돼 **이미 구현·병합됐다**.

### 0-재수립. 3관점 리뷰 통합 — R-1 ~ R-10 (이력 · 2026-09-08 · 통합자 직접)

> README §1-2 「통합자가 §0-재수립 R-n 표로 통합」. 세 관점은 서로를 못 읽고 독립으로 판정했다.
> 집계 **Blocker 1 · Major 10 · Minor 19 · Nit 11**. §0 다섯 자리에서 **뒤집힌 것은 자리 1 의 에러코드 하나**다.
> 통합자는 R-1~R-10 의 사실 근거를 **전건 직접 실측**했다(인용을 믿지 않았다). 아래 결정은 **위 본문·아래 본문에 이미 녹아 있다** — 이 표는 흔적이다.

| R | 대상 | 리뷰 지적 | 통합자 실측 | 결정 |
|:-:|---|---|---|---|
| **R-1** | §0 자리 1 · 통보 150 | API: 초안이 근거로 든 `:complete` 는 **두 축에 다른 코드**를 쓴다 | ✅ `lot-complete.service.ts:67-69` = `ERROR_CODE.INVALID`(원천유형) · `:71-73` = `STATE_LOCKED`(상태) | **뒤집는다** — `source_type_code` 위반 → 400 **`INVALID`** · `status_code` 위반 → 400 **`STATE_LOCKED`**. 통보 150 문구·e2e 2건 라벨 수정 |
| **R-2** | §7-2 권한 「0줄」 | UIUX **Blocker**: `:request-iqc-skip` 이 `['W-01-13']` 인데 계약·§U9 는 `M-01-13` | ✅ `derived-permissions.ts:259` = `['W-01-13']`. ⭐ 그리고 `operation-permissions.spec.ts` 가 두 표를 **합집합**으로 못박았다(「덮어쓰기면 도출된 화면이 조용히 빠진다」 · 같은 «키»는 허용 · 같은 키에 같은 «권한»만 금지) | **뒤집는다** — `manual-permissions.ts` 에 `'POST /trace/lots/{lotId}:request-iqc-skip': ['M-01-13']` **한 줄 추가**(합집합이라 중복 아님 · spec 통과). ⚠ **W-01-13 과다부여는 못 지운다**(합집합) ⇒ 「알려둘 것」 ⓕ. §10 PR ② 권한 커밋 +1줄 |
| **R-3** | §5-1 · §8-1 e2e #8 | API: 「직역 복제」와 e2e #8 이 **서로를 부정한다** | ✅ 계약 `sourceDocumentTypeCode` = `["string","null"]` **+ enum 3값**(널이 enum 에 없다) · 형제 `lot-lifecycle-event-view.ts:19·26` = `omitEmpty` + `?? undefined` | **e2e #8 을 고친다** — 널이 아니라 **키 생략**. 형제와 같게 간다(계약의 널/enum 모순은 「알려둘 것」 ⓖ). 코드 변경 0 |
| **R-4** | §9-1 #10 | API: `stock-reinstate` 가 **transitionCode 없이** 예약돼 있다 | ✅ `transitions.ts:215-219` 주석 「지어내지 않고 호출자가 넘기게 둔다 · 문의 089」 · 계약 `transitionCode` **required + 닫힌 enum 9값** · 물리 `transition_code` **NOT NULL · enum 없음** | **판정을 바꾼다** — 형제대로 **안 떨어뜨린다**(2단계 기준 5 · 새 개념 0)를 유지하되, **레인 C I-23 병합 뒤 enum 밖 값이 이 조회로 새어 나온다**를 **통보 154** 로 새로 낸다. ⛔ 문의 089 는 **레인 C 소유**라 값을 우리가 고르지 않는다 |
| **R-5** | §0 자리 4 · §7-1 「`lot.service.ts` 0줄」 | UIUX: 상세 GET 의 `include` 에 `orderBy` 가 없다 / 통합: 그 뷰 출력이 A 의 미병합 PR ③ 로 바뀐다 | ✅ `lot.service.ts:126`·`:284` 둘 다 `include: { lot_hold: true }` — **정렬 없음**. 새 `GET …/holds` 는 `held_at desc` | **조건부 PASS 를 해소한다** — 뷰 선택(`holdView`)은 유지. **`lot.service.ts` 두 자리에 `orderBy` 를 더한다**(+4줄 · 같은 정렬). ⚠ 화면은 `M-01-08` 이 아니라 **`W-01-07`** 이 두 경로를 함께 본다(UIUX 정정 채택) |
| **R-6** | §8-2 e2e #11·#12 | 통합 **M-1**: I-20 은 선행 완료가 **아니다**(#331 진행중) | ✅ `I-20.md` — 「PR ③(코어)이 `lot-registry.service.ts:96-101` 에 한 줄 더해 앞으로 태어나는 입하 보류는 `lotStatusCode` 를 **채운다**」 | **바꾼다** — #11·#12 의 **배열 통째 단언을 금지**하고 우리가 소유한 칸만 단언한다. 근거: 깨지는 파일은 A2 가 손 뗀 e2e 인데 **빨개지는 건 A 의 PR** 이다 — 남의 레인에 지뢰를 놓지 않는다. ⇒ 「알려둘 것」 ⓗ |
| **R-7** | §11 인계 | 통합 **M-2**: ②가 ① 위 스택인데 병합자는 A | ✅ `lanes.md` §2-5 의 실제 사고(#153 병합 → #154 가 재지정 없이 CLOSED) | **§11 에 절차를 넣는다** — PR ② 본문 머리에 「**base 가 PR ① 브랜치다. ① 병합 뒤 `gh pr edit <②> --base main` 을 «먼저» 하고 브랜치를 지운다**」를 적고, A 에게 넘기는 인계 코멘트에 같은 문장을 싣는다 |
| **R-8** | §9-4 ⓐ | UIUX **Major**: 계약이 이미 그 문을 열어 뒀다 | ✅ 계약 `GET /app/approval-requests` 에 `targetTypeCode`·`targetId` 실재 + description 이 **「M-01-13 의 「내가 올린 요청」 구획」**을 명시 | **ⓐ 를 걷어낸다** — 그대로 두면 「계약이 일부러 연 문을 열어달라」는 통보가 나간다. 대신 「**계약이 지정한 조회 경로가 `?targetTypeCode=INBOUND_LOT&targetId=` 다**」를 §11-1 인계에 사실로 적는다 |
| **R-9** | 통보 153 | UIUX **Major**: `plan-uiux.md:852` 가 이미 답했다 | ✅ `:852` — 물류 자식 치환 PUT 7건에 `lots/external-identifiers` 포함, 화면 목록에 **`M-01-02`**, 「**부모의 화면이 곧 이 오퍼레이션의 화면**」 | **통보 153 을 내리고 값을 좁힌다** — `['M-01-02','P-01-01']` → **`['M-01-02']`**. 정본이 답을 갖고 있으므로 「통보」가 아니라 **0단계 선례로 닫힌다**. 신규 통보는 **150·151·152·154 넷** |
| **R-10** | §9-3 | 통합: 경계는 맞으나 초안이 **기존 재배정 문서를 못 봤다** | ✅ `docs/design-inquiries/재정리-2026-09-08-레인A.md:30` 이 회신 12 를 이미 **I-21·통보**로 재배정 · 낡은 포인터가 `plan-integration.md` **:365·:646 둘** | **§9-3 결론 유지 · 근거 교체** — 「우리가 새로 판정했다」가 아니라 「**이미 A 가 소유하도록 재배정돼 있다**」. 낡은 포인터 2곳은 **공용 문서**라 우리가 안 고치고 A 조율 항목으로 넘긴다(§11-2) |

**재수립 뒤 달라진 수치**

| | 초안 | 재수립(= 이 문서) |
|---|---:|---:|
| 신규 통보 | 4 (150~153) | **4 (150·151·152·154)** — 153 은 선례로 닫힘, 154 신설 |
| 신규 질의 | 0 | **0**(변화 없음) |
| 「알려둘 것」 | 6 | **8** |
| PR ① 비테스트 | ~245 | **~250** (`lot.service.ts` orderBy +4줄) |
| PR ② 비테스트 | ~240 | **~245** (권한 +1줄 · 에러코드 분기 +2줄) |
| `lot.service.ts` | 0줄 | **+4줄** (R-5) |
| `manual-permissions.ts` | +3 | **+4** (R-2 항목 1줄) |
| 마이그레이션 · `schema.prisma` · `transitions.ts` · 새 ERROR_CODE | 0 · 0 · 0 · 0 | **변화 없음 — 0 · 0 · 0 · 0** |
| 커버리지 | 382 → 387 (`main` f931561) | 재수립으로는 **변화 없음** · ⚠ base 가 `dd4032e` 로 움직여 **389 → 394/487** 로 다시 적었다 |

---

## 1. 계약 읽기 표 — 5 오퍼레이션 전건

### 1-1. 횡단 4축 — 실측(python · `contracts/logistics-01자재창고.json` · `COMMIT.txt` a6a87e1)

| 오퍼레이션 | 성공 | 멱등 | If-Match | 응답 ETag | 사번 | 403 | 404 | 409 |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| `GET /trace/lot-status-events` (`:6331`) | 200 | — | — | — | — | — | — | — |
| `GET /trace/lots/{lotId}/external-identifiers` (`:6190`) | 200 | — | — | — | — | — | — | — |
| `GET /trace/lots/{lotId}/holds` (`:6300`) | 200 | — | — | — | — | — | — | — |
| `POST /trace/lots/{lotId}:request-iqc-skip` (`:6507`) | **202** | ✓ | — | — | **✓** | ✓ | — | ✓ |
| `PUT /trace/lots/{lotId}/external-identifiers` (`:6209`) | 200 | ✓ | **필수** | **✕** | — | ✓ | — | ✓ |

- ⭐ **조회 3건은 400·403·404 를 하나도 선언하지 않았다** ⇒ 권한 게이트 등록 **0줄**(`permission.guard.ts:37-41` — 계약이 403 을 «선언한» 자리에서만 본다) · 없는 `lotId` 의 응답도 계약이 안 적었다(§9-1 #5).
- ⭐ **쓰기 둘 다 응답 ETag 가 없다** ⇒ `runVersioned` 를 못 쓴다. `ifMatchVersion(request)` 를 직접 꺼내 서비스가 «비교만» 한다(0단계 선례 `goods-issue.controller.ts:146-155`). `:request-iqc-skip` 은 **If-Match 자체가 없어** 비교도 없다.
- **멱등 2건**은 `runIdempotent`(`master-write.ts:20-38`). 조회엔 안 붙인다(`plan.md` §5 규칙 2).

### 1-2. 조회 3건 — 질의 칸 전수

| 오퍼레이션 | 질의 | 필수 | 비고 |
|---|---|:-:|---|
| `/trace/lot-status-events` | `occurredFrom`·`occurredTo`(date-time) | **둘 다 required** | 감사 조회는 기간을 강제한다(공유계약 L-3) — 형제와 같다 |
| 〃 | `lotId`(int64) · `transitionCode` | 선택 | `transitionCode` **enum 9값** `C4·C5·C6·C7·C8·C9·C10·C14·C15` · `x-code-key: CD-LOT-STATUS-HISTORY-EVENT-TRANSITION` |
| `/trace/lots/{lotId}/external-identifiers` | (없음) | — | path `lotId` 하나 |
| `/trace/lots/{lotId}/holds` | `activeOnly`(boolean, **default true**) | 선택 | 「해제되지 않은 것만」(`:6314`) |

⛔ **셋 다 `PageMeta` 가 없다** — 응답은 `{ items }` 하나다. 형제 `GET /trace/lot-lifecycle-events` 와 같은 형태이고 그 구현이 「계약에 `PageMeta` 가 없고 기간이 유일한 상한이다」로 이미 못 박았다(`lot-lifecycle-event.service.ts:45-47`).

### 1-3. 쓰기 2건 — 본문 스키마 실측

**`ApprovalRequestCreate`** — required **1** · 프로퍼티 **1**: `reason`(`minLength: 1`). ⭐ 승인 «유형»도 «대상»도 본문에 없다 — `x-internal-note`(`:6569`)가 서버 몫으로 못 박았다: **`approval_type_code=IQC_SKIP` · `target_type_code=INBOUND_LOT` · `target_id=lot_id`**. ⚠ 「한도」를 받는 칸이 **0개**다(계약 본문: 「요청을 만들기만 하고 **한도는 승인자가 정한다**」) — §9-3 참조.

**`LotExternalIdentifierUpsert`**(`:11761`) — required **2**(`identifierTypeCode`·`externalIdentifier`) · 프로퍼티 **4**. `x-source-table: trace.lot_external_identifier` · 칸마다 `x-source-column` 실재 ⇒ 매핑이 계약에 이미 적혀 있다.

| 계약 칸 | 물리 칸 | 제약 |
|---|---|---|
| `identifierTypeCode` | `identifier_type_code` | `maxLength 50` · `x-code-key CD-LOT-EXTERNAL-IDENTIFIER-TYPE` · **enum 없음**(「고객이 늘린다」 G-31) ⇒ `assertCodeValues(… 'LOT_EXTERNAL_IDENTIFIER_TYPE')` |
| `externalIdentifier` | `external_identifier` | `maxLength 150` |
| `partnerId` | `partner_id` | `[integer, null]` · FK `mdm.partner` |
| `externalSystemCode` | `external_system_code` | `[string, null]` + **enum 3값**(`EQUIPMENT_STANDARD_IF`·`TRACKING_SYSTEM`·`UNIERP`) — ⛔ 계약이 enum 으로 «닫았다»(「공통코드 그룹이 아니다」) ⇒ `assertCodeValues` 대상이 **아니다** · 검증 가드가 이미 막는다 |

⛔ **`null` 을 응답에 실을 수 없는 칸이 하나**: `externalSystemCode` 는 `type` 에 `null` 이 있는데 `enum` 에는 없어 널이 계약 스스로를 통과 못 한다(전 계약 16곳 · 되돌림 §Y-1 · 문의 5번). 기존 `identifierView`(`lot-view.ts:103-118`)가 **이미 키 생략으로 처리**했다 ⇒ 그대로 재사용한다. ⚠ 같은 모순이 `LotStatusHistoryEvent.sourceDocumentTypeCode` 에도 있다(R-3 · 「알려둘 것」 ⓖ) — 거기도 **키 생략**으로 간다.

### 1-4. 응답 스키마 — **채울 수 없는 칸 0 · 담을 데 없는 칸 0**

**`LotStatusHistoryEvent`**(`:11970`) required **6** · 프로퍼티 **11** ↔ `trace.lot_status_event`(`schema.prisma:4634-4657`) **17칸**:

| 계약 | 물리 | 판정 |
|---|---|---|
| `lotStatusHistoryId` | `lot_status_event_id` | 이름만 다르다(마이그 주석이 이미 그렇게 적었다) |
| `lotId`·`transitionCode`·`reason`·`sourceDocumentTypeCode`·`sourceDocumentId`·`changedAt`·`changedBy` | 동명 | 그대로 |
| `fromStatusCode` | **`previous_status_code`** | `20260901030000/migration.sql:77-80` — 「이름이 다르나 뜻이 같다 · 컬럼을 갈면 두 릴리스로 나눠야 해 두고 계약에 대응 표기를 요청했다」 |
| `toStatusCode` | **`new_status_code`** | 〃 |
| `lotNo` | (조인) | `lot.lot_no` — 형제 `lot-lifecycle-event-view.ts:6` 과 같은 한 칸 조인 |
| — | `location_id`·`quality_status_code`·`inventory_status_code`·`reason_code`·`created_at` | 계약에 **없다** ⇒ 안 싣는다(코어가 앞의 셋을 «비운다» — `lot-quality-status.service.ts:89-91`) |

⇒ ⭐ **채울 수 없는 칸이 0이다.** 이 표는 `20260901030000_align_lot_status_event` 가 **`GET /trace/lot-status-events` 를 위해 이미 맞춰 놓은 표**다 — `transition_code` NOT NULL 신설 · `reason` 신설 · `changed_by` NOT NULL 승격 · 원천 두 칸 완화 + 짝 CHECK · **그리고 이 오퍼레이션 이름을 대고 `ix_lot_status_event_changed_at` 을 세웠다**(`:66-79`).

⭐ **널 가능 칸의 표기는 형제와 같다 — 키 생략**(R-3). 뷰는 `omitEmpty` + `?? undefined` 로 그린다(`lot-lifecycle-event-view.ts:19`·`:26`). `fromStatusCode`·`sourceDocumentTypeCode`·`sourceDocumentId`·`reason` 넷이 그 대상이다.

**`LotHold`**(`:11814`) required 5 · 프로퍼티 16 — 전건이 `holdView`(`lot-view.ts:135-158`)에 이미 있다. `lotStatusCode` ← `lot_hold.target_lot_status_code`(I-20 M-f · R-9 판정). **`LotExternalIdentifier`** required 4 · 프로퍼티 5 — 전건이 `identifierView` 에 있다.

### 1-5. 값 목록 — 계약이 준 것 / DB 에 있는 것 (2026-09-08 13:57 KST 실측)

| 그룹 | DB(`mdm.code_value`, `is_active`) | 계약 |
|---|---|---|
| `LOT_EXTERNAL_IDENTIFIER_TYPE` | **4** — `SUPPLIER_LOT`·`ERP_LOT`·`CUSTOMER_LOT`·`SUBCONTRACTOR_LOT` | 산문으로 같은 넷(enum 아님) ✅ |
| `LOT_STATUS` | **4** — `NORMAL`·`DEFECTIVE`·`INSPECTION_PENDING`·`SCRAPPED` | 산문으로 같은 넷 ✅ |
| `LOT_STATUS_TRANSITION` | **9** — `C4`~`C10`·`C14`·`C15` | `transitionCode` enum 9값과 **전건 일치** ✅ ⚠ 레인 C I-23(`stock-reinstate`)이 병합되면 이 축 밖의 값이 표에 실린다 — **통보 154** |
| `LOT_HOLD_REASON` | **6** — `INCOMING_INSPECTION_WAIT` 포함 | enum 없음(G-31) ✅ |
| **`APPROVAL_TYPE`** | ⭐ **0행** | 시드가 **일부러 뺐다**(`prisma/seed.ts:1454` — 「시스템 소유」 목록) ⇒ `approval_type_code` 는 코드값 대조 대상이 **아니다**. `'IQC_SKIP'` 은 상수로 선다(선례: `goods-issue.service.ts:40` `GOODS_ISSUE_DISPOSAL` · `production-result-approval.service.ts:9` `PRODUCTION_RESULT_CORRECT`) |

⚠ **`IQC_SKIP` 결재선이 DB 에 0행이다**(`app.approval_route` 0행) ⇒ 운영자가 `POST /app/approval-routes` 로 한 벌 세우기 «전»에는 이 오퍼레이션이 **언제나 400 `ROUTE_NOT_FOUND`** 다. 그것이 계약이 요구한 동작이다(`:6508` — 「결재선이 없으면 400(code=`ROUTE_NOT_FOUND`)이다 — 상신할 곳이 없는 요청을 만들지 않는다」) ⇒ **배포 노트 한 줄**(§12-1).

### 1-6. 에러 코드 — **새 코드 0건**

| 자리 | 코드 | 출처 |
|---|---|---|
| 결재선 0벌/단계 0 | `ROUTE_NOT_FOUND` | `error-codes.ts` 실재 · `approval.service.ts:192·214` 가 이미 던진다 |
| 활성 결재선 2벌 이상 | `ROUTE_AMBIGUOUS` | 〃 `:189` |
| 진행 중 요청 있음 | `APPROVAL_IN_PROGRESS` | 〃 `:127` (`assertNoOpenRequest`) |
| 결재자가 USER 가 아님 | `APPROVER_TYPE_NOT_SUPPORTED` | 〃 `:227` |
| ⭐ **입하 LOT 이 아님**(`source_type_code`) | **`INVALID`** | 실재 · §0 자리 1 · 형제 `lot-complete.service.ts:67-69` 와 같은 코드 |
| ⭐ **검사 대기가 아님**(`status_code`) | **`STATE_LOCKED`** | 실재 · §0 자리 1 · 형제 `lot-complete.service.ts:71-73` 와 같은 코드 |
| 사번 헤더 부재 | `REQUIRED` | 실재 · §0 자리 2 |
| 요청 안 식별자 중복 | `UNIQUE_VIOLATION` | 실재 · §0 자리 3 ⓓ |
| 코드값 밖 `identifierTypeCode` | `INVALID` | `assertCodeValues` 가 낸다 |
| If-Match 불일치 | (409 `ConflictResponse` · `conflictCause=user`) | `assertUpdated()` |

⇒ **`error-codes.ts` 0줄**(레인 공통 금지 · `lanes.md` §1-4).

---

## 2. 물리 대조 + **마이그레이션 결론 — 0건**

| 표 | 위치 | 계약 대비 | 판정 |
|---|---|---|:-:|
| `trace.lot_status_event` | `schema.prisma:4634` | §1-4 표 — 채울 수 없는 칸 0 · 인덱스 2(`ix_lot_status_event_changed_at`·`_lot`) 가 이 오퍼레이션의 두 질의 축을 그대로 받는다 | **마이그 0** |
| `trace.lot_external_identifier` | `:3601` | 계약 5칸 ↔ 물리 8칸(초과분 `created_at`·`created_by` 는 감사 칸) · `uq_lot_external_identifier` 5칸 표현식 유일 인덱스 실재 | **마이그 0** |
| `trace.lot_hold` | `:3617` | I-20 이 `target_lot_status_code`·`release_target_lot_status_code`·`version_no` 를 **이미 더했다**(`20260908110558` · main 병합 확인) | **마이그 0** |
| `trace.lot` | `:3522` | `version_no Int @default(1)` 실재 — If-Match 토큰 원천 | **마이그 0** |
| `app.approval_request`·`_step`·`approval_route`·`_step` | I-1 A6 | 코어가 쓴다 · `ix_approval_request_target` 실재 | **마이그 0** |

⇒ ⭐ **마이그레이션 0건 · `schema.prisma` 0줄.** 통합 계획서 `plan.md` §1 18행(마이그 「—」)·`plan-api.md` §S08(「마이그레이션: 없음」)과 **일치**한다. 인덱스도 더하지 않는다 — 새 질의 축 셋(`lot_id` 단독 2건 · `changed_at` 구간)이 전부 기존 인덱스를 탄다(`ix_lot_status_event_changed_at`·`_lot` · `ix_lot_hold_lot` · `lot_external_identifier` 는 `uq_…` 의 선두가 `lot_id`).

---

## 3. `POST /trace/lots/{lotId}:request-iqc-skip` — 202 · 이 슬라이스의 심장

### 3-1. 한 트랜잭션 순서

컨트롤러(`LotController` · `@Post(':lotId\\:request-iqc-skip')` · `@HttpCode(HttpStatus.ACCEPTED)`):
`x-worker-no` 헤더 원문 + `currentSession(request).userId` 를 뽑아 `runIdempotent(…, HttpStatus.ACCEPTED, …)`. ⛔ `ifMatchVersion` 을 안 읽는다(계약에 If-Match 가 없다) · ⛔ `setEtag` 를 안 부른다.

서비스 `lot-iqc-skip.service.ts` 순서:

1. `assertWorkerNo(context.workerNo)` — **400 `REQUIRED`**(§0 자리 2).
2. LOT 존재·성격 사전 확인 — 없으면 **404**(형제 `lot-complete.service.ts:132` 와 같은 문구 축).
3. ⛔ **채번을 `$transaction` «밖»에서** — `numbering.next('APPROVAL_REQUEST', null, new Date().toISOString().slice(0,10))`. 근거는 I-2 R-2 + `purchase-order.service.ts:288-296`(「열린 트랜잭션 안에서 부르면 커넥션을 둘 쥐고 P2024 로 죽는다」). ⚠ 기간키가 UTC 라 하노이 00:00–07:00 상신은 «전날» 번호를 받는다 — 이 표는 `business_date` 를 안 실어 C-8 자리가 아니다(형제 둘과 같은 문장).
   ⭐ **결번 방지** — 2번에서 이미 400 이 될 LOT 이면 채번을 «건너뛴다»(I-24 R-6 의 같은 처방).
4. `$transaction`:
   ⓐ `SELECT lot_id, source_type_code, status_code FROM trace.lot WHERE lot_id = $1 FOR UPDATE` — ⛔ 잠금이 **필수**다. `assertNoOpenRequest` 는 조회만 하므로 동시 상신을 못 가른다(`approval.service.ts:102-110` 가 「호출자가 트랜잭션 첫 문장에서 대상 행을 잠근다」라 적었다). 없으면 404.
   ⓑ ⭐ **자격 축 두 줄 — 코드가 다르다**(§0 자리 1 · R-1):
      · `source_type_code !== 'INBOUND_RECEIPT_LINE'` → 400 **`INVALID`**(「입하 LOT 이 아닙니다」)
      · `status_code !== 'INSPECTION_PENDING'` → 400 **`STATE_LOCKED`**(「수입검사 대기 상태가 아닙니다」)
      형제 `:complete` 가 같은 두 축에 같은 두 코드를 쓴다(`lot-complete.service.ts:66-73`). 메시지도 갈라 화면이 두 사유를 구분한다.
   ⓒ `approvals.request(tx, { approvalRequestNo, approvalTypeCode: 'IQC_SKIP', targetTypeCode: 'INBOUND_LOT', targetId: BigInt(lotId), businessUnitId: null, requestedBy: BigInt(appUserId), reason })` — 「진행 중 하나」·결재선 선택·단계 전개를 코어가 한 번에 한다.
   ⓓ ⛔ **대상 행에 아무것도 안 쓴다.** `trace.lot` 에 `approval_request_id` 칸이 **없고**(`schema.prisma:3522-3546` 실측), `plan.md` §5 **규칙 12** 가 「`*_CANCEL`·**`IQC_SKIP`**·`PRODUCTION_RESULT_CORRECT` 는 FK 를 쓰지 않는다 — 정본은 다형 축」이라 이미 못 박았다 ⇒ 형제 둘(P/O `:308-321` · 출고 `:200-214`)과 **여기서 갈린다**. `lot.version_no` 도 안 올린다(202 에 ETag 가 없어 화면이 새 토큰을 받을 길이 없다 — `goods-issue-update.service.ts:161-166` 의 그 문장 그대로).
5. `{ approvalRequestId: Number(id) }` 반환.

### 3-2. 주체 — 사번은 읽고 버린다

`requested_by` 는 **계정 세션**이다(`plan.md` §5 규칙 9 · 형제 9 상신 도메인 전부). ⚠ `mdm.worker.app_user_id` 가 실재해 사번으로 푸는 길이 «있지만» 쓰지 않는다: nullable 이라 널이면 상신이 막히고, 「사번을 주체로 승격시키지 않는다」가 규칙 9 의 본문이다. **사번은 읽고 버린다**(`:complete` 와 같다) ⇒ §9-1 #3 · 통보 151.

---

## 4. `PUT /trace/lots/{lotId}/external-identifiers` — 200

### 4-1. 컨트롤러·서비스 순서

컨트롤러: `versionOf(request)`(= `ifMatchVersion` + 가드 어긋남 방어) + `runIdempotent(…, HttpStatus.OK, …)` — 0단계 선례 `goods-issue.controller.ts:112-125`. ⛔ `setEtag` 없음.

서비스 `lot-external-identifier.service.ts::replace()`:

1. **트랜잭션 «밖»** — ⓐ 요청 안 중복 검사(5칸 자연키 · 400 `UNIQUE_VIOLATION`) ⓑ `assertCodeValues(prisma, items.map(… 'LOT_EXTERNAL_IDENTIFIER_TYPE'))` — 등록 경로(`lot.service.ts:277-283`)와 **같은 그룹·같은 필드 이름 규약** ⓒ `partnerId` 존재 확인(없는 id 를 그냥 넘기면 FK 위반이 500 으로 샌다 — `goods-issue-update.service.ts:70-76` 선례).
   ⚠ **0행 요청은 허용한다** — 계약에 `minItems` 가 없고 「요청에서 빠진 기존 행은 삭제한다」가 전체 삭제를 뜻할 수 있다. 출고 라인이 0행을 막은 것은 **등록 쪽 계약이 「최소 1행」이라 적었기 때문**인데(`goods-issue-update.service.ts:56-60`) 여기 등록(`LotCreate.externalIdentifiers`)은 애초에 선택 배열이다 ⇒ §2 2단계 **기준 3·5**. §9-1 #6 · **통보 152**.
2. `$transaction`:
   ⓐ `SELECT version_no FROM trace.lot WHERE lot_id = $1 FOR UPDATE` — 없으면 404 · `version_no !== version` 이면 `assertUpdated(0)`(409 `conflictCause=user`).
   ⓑ `tx.lot_external_identifier.deleteMany({ where: { lot_id } })`
   ⓒ `tx.lot_external_identifier.createMany({ data: items.map(…) })` — `created_by` 는 세션.
   ⓓ `const bumped = await tx.lot.updateMany({ where: { lot_id, version_no: version }, data: { version_no: { increment: 1 }, updated_by: BigInt(appUserId) } }); assertUpdated(bumped.count);` — §0 자리 3 ⓑ. 잠그고 비교했으니 0행일 수 없지만 조건을 걸어 둔다(같은 축의 마지막 그물 · `goods-issue-update.service.ts:139-145`).
   ⓔ `findMany({ orderBy: { lot_external_identifier_id: 'asc' } })` → `identifierView` — **정렬을 명시한다**(계약에 `sort` 가 없다 ⇒ 기본 고정 · I-24 「반환 순서 보장」 선례. 상세 GET 이 이미 같은 정렬이다 — `lot.service.ts:141-144`).

### 4-2. 치환 알고리즘 — 전삭제 + 전삽입

§0 자리 3 ⓒ 그대로. 근거 셋: 계약 `Upsert` 에 **id 칸 0** · 이 표를 참조하는 **자식 0** · §2 2단계 기준 5(새 개념 0). 5칸 자연키 중복은 **손검사로 앞당겨** 400 `UNIQUE_VIOLATION` 을 낸다(표현식 유일 인덱스라 P2002 의 `meta.target` 이 비어 공용 그물에 안 걸린다).

### 4-3. 쓰기 2건의 400 · 404 · 409 갈래 — 순서가 판정이다

| 순 | 조건 | 응답 | 왜 이 순서인가 |
|:-:|---|---|---|
| 1 | `Idempotency-Key` 부재/비-uuid | 400 `REQUIRED`/`INVALID` | 가드가 컨트롤러 앞에서 낸다 |
| 2 | `If-Match` 부재/형식(PUT 만) | 400 `REQUIRED`/`INVALID` | 가드 |
| 3 | 권한 없음 | 403 `PERMISSION_DENIED` | 가드 |
| 4 | 본문 스키마 위반 | 400 | 계약 검증 파이프 |
| 5 | `X-Worker-No` 부재(IQC 만) | 400 `REQUIRED` | ⛔ 가드가 헤더를 안 본다 ⇒ 서비스 첫 줄 |
| 6 | 요청 안 중복·코드값·`partnerId`(PUT 만) | 400 | 트랜잭션 «밖» — 잠글 필요 없는 마스터 조회 |
| 7 | 없는 LOT | **404** | 잠금 문장이 0행 |
| 8 | ⭐ 입하 LOT 아님(IQC 만) | 400 **`INVALID`** | 잠근 «뒤» 재검사(경합 몫) · R-1 |
| 9 | ⭐ 검사 대기 아님(IQC 만) | 400 **`STATE_LOCKED`** | 〃 · **8 과 코드가 다르다** |
| 10 | 진행 중 요청 있음(IQC 만) | 400 `APPROVAL_IN_PROGRESS` | 코어 |
| 11 | 결재선 0/2+ (IQC 만) | 400 `ROUTE_NOT_FOUND`/`ROUTE_AMBIGUOUS` | 코어 |
| 12 | If-Match 불일치(PUT 만) | **409** `ConflictResponse` | 재로드로 풀린다 — 8·9·10·11 과 «반드시» 갈린다(G-1) |
| 13 | 같은 키 다른 본문 / 처리 중 | 409 `user`/`workerLease` | `idempotency.service.ts:110-124` |

---

## 5. 조회 3건

### 5-1. `GET /trace/lot-status-events` — 형제의 **직역 복제**

`lot-lifecycle-event.{controller,service}.ts`·`-view.ts` 를 이름만 바꿔 복제한다. 갈리는 곳 넷:

| | 형제(lifecycle) | 이 슬라이스(status) |
|---|---|---|
| 표 | `lot_lifecycle_history` | `trace.lot_status_event` |
| 전이 enum | `L1·L2·L3` | **`C4·C5·C6·C7·C8·C9·C10·C14·C15`** |
| 정렬 | `changed_at desc, lot_lifecycle_history_id desc` | `changed_at desc, **lot_status_event_id** desc`(인덱스 `ix_lot_status_event_changed_at` 을 탄다 · 동률은 PK 로 닫는다) |
| 뷰 required | 5 | **6**(`changedBy` 가 는다 — NOT NULL) |

- 기간은 **반열림** `gte`/`lt`(형제 `:38` · 공유계약 L-3-1).
- ⭐ **널 가능 칸은 «키 생략»** — 형제 뷰와 같이 `omitEmpty` + `?? undefined`(R-3 · §1-4). ⛔ 널을 싣지 않는다: `sourceDocumentTypeCode` 는 `[string,null]` 인데 enum 3값에 널이 없어 널이 계약을 못 통과한다.
- ⛔ **enum 밖 `transition_code` 행을 «떨어뜨리지 않는다»** — 형제가 이미 그 판정을 적었다(`lot-lifecycle-event.service.ts:33-36`: 「떨어뜨리면 감사 조회가 오히려 «넓어진다». 가드가 이미 400 을 냈다」). 질의 파라미터의 enum 은 검증 가드가 막고, 응답 쪽은 **런타임 검증이 없다**(`contract-validation.guard.ts` 에 response 검증 0 · 실측) ⇒ 0단계 선례로 닫힌다. ⚠ **레인 C I-23 이 병합되면 그 값이 실제로 샌다**(`src/core/document-state/transitions.ts:215-219` 의 `stock-reinstate` 가 `transitionCode` 없이 예약돼 있다) ⇒ **통보 154**. ⛔ 문의 089 는 레인 C 소유라 값을 우리가 고르지 않는다.
- ⚠ **오늘 이 표는 0행이고, 채우는 것은 I-19(검사 확정)·I-20(보류)** 이다 — `lot-quality-status.service.ts:80-93` 이 유일한 writer. 조회를 먼저 세워도 **빈 목록이 나올 뿐 깨지지 않는다**(`plan-integration.md` §I-18 원문).

### 5-2. `GET /trace/lots/{lotId}/external-identifiers`

`lot-external-identifier.service.ts::list(lotId)` — 존재 확인(404) → `findMany({ where: { lot_id }, orderBy: { lot_external_identifier_id: 'asc' } })` → `identifierView`. **PUT 의 응답과 같은 함수**를 쓴다(§4-1 ⓔ).

### 5-3. `GET /trace/lots/{lotId}/holds`

`lot-hold-list.service.ts::list(lotId, activeOnly)` — `prisma.lot.findUnique({ where: { lot_id }, include: { lot_hold: { orderBy: [{ held_at: 'desc' }, { lot_hold_id: 'desc' }] } } })`(404) → `activeOnly ? filter(h => h.released_at === null) : 전체` → **`holdView(h, row)`**(§0 자리 4).

- ⭐ `activeOnly` 기본 **true** · `activeOnly=false` 는 「해제된 것만」이 **아니라 «전체»** ⇒ **필터를 걸지 않는다**(0단계 선례 `lot-hold-query.service.ts:68-72` · I-20 R-1). 참·거짓 갈래가 아니라 **있음/없음 갈래**다.
- 정렬은 계약이 안 적었다 ⇒ `held_at desc, lot_hold_id desc` 로 고정(형제 `lot-hold-query.service.ts:45` 와 같은 축 · `ix_lot_hold_held_at` 실재).
- ⭐ **상세 GET 도 같은 정렬로 맞춘다**(R-5) — `lot.service.ts:126`·`:284` 의 `include: { lot_hold: true }` 두 자리에 같은 `orderBy` 를 더한다(**+4줄**). 안 맞추면 `W-01-07` 이 두 경로에서 같은 행을 다른 순서로 본다.
- ⛔ `bool()` 헬퍼(`lot-rules.ts`)로 `'true'`/`'false'` 문자열을 푼다 — 질의 문자열은 언제나 string 이다(형제 `lot.controller.ts:47` 선례).

---

## 6. 상태기계 · 원장 — **둘 다 0**

- `transitions.ts` **0줄** · `document-state.spec.ts` **0줄**. 전이 0건이다(`plan-integration.md` §I-18 「원장·상태기계 없음」). ⛔ 이 파일은 **레인 A 소유**이고 지금 A 가 I-20 에서 쓰고 있다(`lanes.md` §1-4) — 필요해지면 **고치지 말고 사용자에게 한 줄 보고**한다(§11-2).
- `inventory_transaction`·`inventory_balance` **0줄**. posting 을 부르지 않는다.
- `lot.status_code` 를 **아무도 안 옮긴다** — `:request-iqc-skip` 은 «요청»만 만들고 실행은 승인 뒤 다른 화면 몫이다(공유계약 J-8 「승인은 자물쇠를 풀 뿐 실행하지 않는다」).

---

## 7. 횡단 · 모듈 배치

### 7-1. 파일 — 줄 추정(비슷한 기존 파일의 **실측**으로 잡았다 · I-24 §12-2 교훈)

| 파일 | 상태 | 줄 | 기준이 된 실측 |
|---|:-:|--:|---|
| `src/trace/lot/lot-status-event-view.ts` | 신 | ~35 | 형제 `lot-lifecycle-event-view.ts` **30** |
| `src/trace/lot/lot-status-event.service.ts` | 신 | ~60 | 형제 `lot-lifecycle-event.service.ts` **57** |
| `src/trace/lot/lot-status-event.controller.ts` | 신 | ~25 | 형제 `lot-lifecycle-event.controller.ts` **21** |
| `src/trace/lot/lot-external-identifier.service.ts` | 신 | ~50(PR ①) → ~135(PR ②) | `lot-complete.service.ts` **168** |
| `src/trace/lot/lot-hold-list.service.ts` | 신 | ~45 | 형제 조회 service **57** |
| `src/trace/lot/lot-iqc-skip.service.ts` | 신 | ~95 | `goods-issue-update.service.ts::requestApproval` 본체 ~60 + 자격 축 둘·사번 |
| `src/trace/lot/lot.controller.ts` | 수 | 108 → ~140(①) → ~185(②) | 핸들러 2 + 2 |
| `src/trace/lot/lot.service.ts` | 수 | **+4**(①) | ⭐ R-5 — `include` 두 자리에 `orderBy` 만. 로직 0 · 293 → 297(~300 신호 아래) |
| `src/trace/trace.module.ts` | 수 | +8(①) +6(②) | **공용(A·B)** — 자기 등록 줄만 |
| `src/common/permissions/manual-permissions.ts` | 수 | **+4**(②) | 공용 — **단독 커밋**(`lanes.md` §1-4) · 항목 2 + 근거 주석 2 |
| `src/trace/lot/lot-view.ts` | — | **0줄** | ⭐ 안 만진다(레인 A I-20 PR ③ 와 겹친다 · §0 자리 5 ⓒ) |

### 7-2. 권한 — `manual-permissions.ts` **항목 두 줄**

| 오퍼레이션 | 403 선언 | 등록 상태 | 판정 |
|---|:-:|---|---|
| `POST /trace/lots/{lotId}:request-iqc-skip` | ✓ | 도출표에 **`['W-01-13']`**(`derived-permissions.ts:259`) | ⭐ **+1줄 `['M-01-13']`**(R-2) |
| `PUT /trace/lots/{lotId}/external-identifiers` | ✓ | 두 표 어디에도 **없다** | ⭐ **+1줄 `['M-01-02']`**(R-9) |
| 조회 3건 | ✕ | — | **0줄**(가드가 미선언 자리는 안 본다) |

⭐ **`:request-iqc-skip` 에 `M-01-13` 을 더하는 근거**(R-2) — 계약과 `plan-uiux.md` §U9 가 이 오퍼레이션의 화면을 **`M-01-13`**(POP · 「멱등, 사번」)으로 적었는데 도출표는 `W-01-13` 하나만 갖고 있다. 미등록이면 그 화면이 **403** 을 맞는다. ⛔ 두 표는 **합집합**이다 — `operation-permissions.spec.ts` 가 「덮어쓰기면 도출된 화면이 조용히 빠진다」로 못 박았고, 금지하는 것은 같은 키에 **같은 «권한»** 을 다시 적는 것뿐이라(`:27-36`) `M-01-13` ≠ `W-01-13` 은 통과한다. ⚠ 그래서 **`W-01-13` 과다부여는 우리가 못 지운다** ⇒ 「알려둘 것」 ⓕ.

⭐ **PUT 에 `M-01-02` 하나만 적는 근거**(R-9) — `plan-uiux.md:852` 가 물류 자식 치환 PUT 7건을 묶어 「**부모의 화면이 곧 이 오퍼레이션의 화면**」이라 적고 `lots/external-identifiers` 의 화면으로 **`M-01-02`** 를 들었다. 정본이 답을 갖고 있으므로 이 자리는 **0단계로 닫힌다**(초안이 내려던 통보 153 은 그래서 철회했다). 부모 `PUT /trace/lots/{lotId}` 는 `['M-01-02','P-01-01']` 이지만 **미러링이 아니라 정본의 값**을 쓴다 — `manual-permissions.ts` 머리가 「그 화면이 그 자원의 마스터를 소유한다가 근거일 때만 적는다 — 지어내면 그것이 사실상의 인가 정책이 된다」라 못 박았기 때문이다.

⛔ `operation-permissions.spec.ts:53-64`(403 선언 250 · covered ≥ 152)는 `toBeGreaterThanOrEqual` 이라 **숫자를 안 고쳐도 초록**이다(고칠지는 §9-1 #8).

### 7-3. 멱등 2 · If-Match 필수 1 · ETag 0 · 권한 등록 2줄 · 새 error code 0 · 마이그 0 · `transitions.ts` 0

### 7-4. 다른 도메인 service 호출 **0** · 다른 도메인 import **0**

`core/approval`(코어) · `core/numbering`(코어) · `common/*` 만 import 한다. ⛔ `src/quality/lot-hold/*` 를 import 하지 않는다(§0 자리 4 ⓑ).

---

## 8. e2e — 파일 둘 + 단위 spec 둘

### 8-1. `test/trace-lot-status-event.e2e-spec.ts` — **신 파일 · 8**(PR ①)

⚠ **`test/trace-lot.e2e-spec.ts` 에 얹지 않는다** — 그 파일(597줄)은 레인 A 가 I-20 R-9 회귀로 방금 고쳤고, 이 오퍼레이션은 경로도 픽스처도 다르다(LOT 이 아니라 «사건» 행을 심는다).

픽스처: 형제 `lot-lifecycle-event.service.spec.ts`·`trace-lot.e2e-spec.ts:55-88` 골격 복제(사용자·역할·권한·공장·품목·UoM·입하 사슬) → `prisma.lot_status_event.createMany` 로 사건 행을 **직접 심는다**(I-18 은 이 표에 쓰지 않는다 · 코어 writer 를 통과시키면 `transitions.ts` 에 의존하게 되어 레인 A 와 묶인다). ⛔ `ck_lot_status_event_source`(원천 두 칸은 둘 다 주거나 둘 다 안 준다)와 `changed_by` NOT NULL 을 픽스처가 지킨다.

1. ⛔ `occurredFrom` 이 없으면 **400** — 계약 required 를 가드가 막는다
2. ⛔ `occurredTo` 가 없으면 400
3. ⛔ `transitionCode=C99` 는 **400**(enum 밖 · 가드)
4. ⭐ 기간 안 사건만 온다 — ⭐ **경계 두 행을 심는다**: `occurredFrom` «과 같은» 시각(포함) · `occurredTo` «와 같은» 시각(**제외**). 반열림을 반증 가능하게 못 박는다(README §6-2)
5. ⭐ `lotId` 로 좁힌다 — ⭐ **다른 LOT 의 사건 1행**을 같은 기간에 심어 필터를 반증한다
6. ⭐ `transitionCode=C7` 로 좁힌다 — ⭐ **C8 행 1건**을 같은 기간·같은 LOT 에 심는다
7. ⭐ 정렬이 `changedAt desc` 이고 동률은 id desc — **배열 통째 단언**(3행)
8. ⭐ 응답이 계약 스키마를 만족한다(ajv · `LotStatusHistoryEvent` required 6) + ⭐ **널 가능 칸이 «키 생략»으로 온다**(R-3) — `fromStatusCode` 가 없는 행(C4)과 원천 두 칸이 없는 행에서 `'fromStatusCode' in item === false` 를 단언한다(형제 뷰와 같다 · ⛔ `null` 을 기대하지 않는다)

### 8-2. `test/trace-lot.e2e-spec.ts` — **+13**(① 5 · ② 8)

기존 픽스처(입하 사슬 → LOT 등록 → 서버가 거는 보류)를 그대로 쓴다. ⚠ **레인 A 와 같은 파일**이라 병합 직전 `git merge origin/main` 뒤 게이트를 다시 돈다(`lanes.md` §2-1).

⛔ **배열 통째 단언 금지 규칙**(R-6) — 이 파일의 보류 단언은 **우리가 소유한 칸만** 본다(`lotHoldId`·`reasonCode`·`releasedAt` 유무·건수·순서). `lotStatusCode` 처럼 **레인 A 의 I-20 PR ③ 이 앞으로 채울 칸**은 단언하지 않는다 — 깨지는 파일은 A2 가 손 뗀 e2e 인데 빨개지는 건 A 의 PR 이다.

**PR ① — 조회 5**

9. ⭐ `GET …/external-identifiers` 가 등록 때 넣은 식별자를 준다 · **id 오름차순 배열 통째 단언**
10. ⭐ 식별자 0건인 LOT 은 `{ items: [] }` (⛔ 404 아님)
11. ⭐ `GET …/holds` 기본값이 **해제되지 않은 것만**(서버가 등록 때 건 `INCOMING_INSPECTION_WAIT` 1건) — ⭐ 해제된 보류 1행을 심어 반증 · ⛔ 단언은 **건수 + `reasonCode` + `releasedAt === undefined`** 까지(배열 통째 ✕)
12. ⭐ `activeOnly=false` 는 「해제된 것만」이 아니라 **전체**(2건) — 이 한 줄이 §0 자리 4 의 갈래를 잠근다 · ⛔ 단언은 **건수 2 + `lotHoldId` 두 개의 집합 + `held_at desc` 순서**까지
13. ⭐ `holds[0]` 이 상세 GET(`GET /trace/lots/{lotId}`)의 `holds[0]` **과 완전히 같다**(`toEqual`) — 두 응답을 «서로» 비교하므로 A 의 PR 이 칸을 채워도 함께 움직인다(R-6 에 걸리지 않는다). §0 자리 4(뷰 하나 · 정렬 하나)를 반증 가능하게 만드는 자리

**PR ② — 쓰기 8**

14. ⭐ 치환이 200 이고 **빠진 기존 행이 사라진다**(2 → 1)
15. ⛔ `If-Match` 없으면 400 · 낡은 값이면 **409**
16. ⭐ 치환 뒤 `GET /trace/lots/{lotId}` 의 **ETag 가 +1** — §0 자리 3 ⓑ 를 잠근다. ⛔ 같은 토큰으로 두 번째 치환은 **409**
17. ⛔ 요청 안 5칸 중복은 400 `UNIQUE_VIOLATION` — ⭐ **`partnerId` 만 다른 두 행은 통과**(표현식 인덱스의 `COALESCE(partner_id,0)` 축을 반증)
18. ⛔ 마스터에 없는 `identifierTypeCode` 는 400 `INVALID`
19. ⭐ `:request-iqc-skip` 이 **202** + `approvalRequestId` · `approval_request` 행의 세 칸이 `IQC_SKIP`/`INBOUND_LOT`/`lot_id` 다 · **`lot.version_no` 가 그대로다**
20. ⛔ `X-Worker-No` 없으면 400 `REQUIRED`(§0 자리 2) · ⛔ 두 번째 상신은 400 `APPROVAL_IN_PROGRESS` · ⛔ 결재선 없는 유형은 400 `ROUTE_NOT_FOUND`
21. ⭐ **자격 축 두 개를 «코드까지» 따로 반증한다**(R-1) — ⛔ 생산 LOT(선발행 슬롯)에 상신하면 400 **`INVALID`** · ⛔ `status_code` 를 `NORMAL` 로 바꾼 입하 LOT 은 400 **`STATE_LOCKED`** · ⛔ 무권한 세션은 **403** 2건(PUT · IQC)

결재선 픽스처는 `test/approval-request.fixture.ts::seedRoute(prisma, 'IQC_SKIP', [approverId])` 를 그대로 쓴다(`test/app-approval-request.e2e-spec.ts:65·204` 가 같은 조합 `IQC_SKIP`/`INBOUND_LOT` 를 이미 쓴다).

### 8-3. 단위 spec — **2파일 · 10**

- `lot-status-event.service.spec.ts`(형제 `lot-lifecycle-event.service.spec.ts` 79줄 복제) — `buildLotStatusEventWhere` 6: 기간만 · `+lotId` · `+transitionCode` · 반열림 경계 · enum 밖 값을 안 떨어뜨린다 · `ORDER_BY` 2키.
- `lot-external-identifier.service.spec.ts` — 중복 검사 4: 완전 동일 400 · `partnerId` 만 다름 통과 · `externalSystemCode` null↔`''` 동치 · 0행 허용.

### 8-4. ⭐ 전수 변이 점검 (README §6-2 · 구현자가 PR 을 열기 «전에»)

이 슬라이스가 «새로» 만드는 필터·비교·정렬은 **아홉**이다 — 전부 변이를 넣고 e2e 가 빨개지는지 확인해 PR 본문에 표로 남긴다:
① `changed_at gte`(→`gt`) ② `changed_at lt`(→`lte`) ③ `lot_id` 필터(제거) ④ `transition_code` 필터(제거) ⑤ 정렬 방향(desc→asc) ⑥ 2차 키(제거) ⑦ `released_at === null` 필터(→`!==`) ⑧ `activeOnly=false` 분기(→「해제된 것만」) ⑨ ⭐ **자격 축 둘의 에러코드 맞바꾸기**(`INVALID`↔`STATE_LOCKED` · R-1 이 뒤집은 자리라 반증이 필수다).
⛔ 계획서에 없던 필터를 «추가»했다면 그 자리가 1순위다.

---

## 9. 설계 미정 자리 — README §2 절차로 전건 판정

### 9-1. 판정 요약표

| # | 자리 | §2 판정 | 결정 | 구분 |
|:-:|---|---|---|:-:|
| 1 | ⭐ `:request-iqc-skip` 의 자격 축(계약이 「입하돼 수입검사 대기인」이라 적고 위반 응답을 안 적었다) | 1단계 **가장자리** → 0단계 선례(`:complete` 의 두 게이트) + 2단계 **기준 2**(거부) | `source_type_code`+`status_code` 둘 다 · **코드는 갈린다**(400 `INVALID` / 400 `STATE_LOCKED`) | **통보 150** |
| 2 | ⭐ `X-Worker-No` 부재의 응답 | 0단계 — 같은 자원 형제 `:complete` 가 답을 갖고 있다 | 400 `REQUIRED` · `plan.md` §5 규칙 9 목록에 **열한째** | **통보 151** |
| 3 | 사번을 `requested_by` 로 푸나 | 0단계 — `plan.md` §5 규칙 9(「주체는 계정 세션」) · `:complete` 의 「읽고 버린다」 | **세션 userId** · 사번은 읽고 버린다 | 통보 151 에 함께 |
| 4 | ⭐ PUT 이 부모 `version_no` 를 올리나 | 1단계 **본길** → 0단계 선례 `goods-issue-update.service.ts:46-47` | **올린다** | ✕(선례로 닫힘) |
| 5 | 조회 3건의 없는 `lotId` 응답(404 를 계약이 미선언) | 가장자리 → 0단계(형제 `GET /trace/lots/{lotId}` 가 404) + 2단계 **기준 2** | **404** · ⚠ `/lot-status-events` 의 `lotId` 는 필터라 **빈 목록**(404 아님) | ✕ · 「알려둘 것」 |
| 6 | PUT 이 0행 요청을 받나 | 가장자리 → 2단계 **기준 3·5**(막는 개념을 새로 만들지 않는다) · 등록 쪽이 선택 배열이라 「최소 1행」 선례가 안 걸린다 | **받는다**(전건 삭제) | **통보 152** |
| 7 | ⭐ 화면이 「—」인 PUT 의 권한 코드 | **0단계로 닫힌다**(R-9) — `plan-uiux.md:852` 가 「부모의 화면이 곧 이 오퍼레이션의 화면」 + `M-01-02` 를 이미 적었다 | `manual-permissions.ts` **`['M-01-02']`** | ✕ · ⛔ 초안의 통보 153 **철회** |
| 8 | `operation-permissions.spec.ts` 의 `covered ≥ 152` 를 올리나 | 가장자리 → 2단계 기준 5 | **안 올린다**(공용 파일 충돌 창을 안 연다 · `toBeGreaterThanOrEqual` 이라 초록) | ✕ · 「알려둘 것」 |
| 9 | ⭐ **회신 12**(한도승인 재고 상태 값) | ⛔ **이 슬라이스에 «닿지 않는다»** — §9-3 | **판정 불필요**(이미 A 소유로 재배정됨) | ✕ · §9-3 |
| 10 | `lot_status_event` 의 enum 밖 `transition_code` 를 떨어뜨리나 | 0단계 — 형제 `lot-lifecycle-event.service.ts:33-36` · 2단계 기준 5 | **안 떨어뜨린다** ⚠ 레인 C I-23 병합 뒤 계약 enum 밖 값이 샌다 | **통보 154** |
| 11 | `GET …/holds` 의 정렬(계약 침묵) | 가장자리 → 0단계 형제(`lot-hold-query.service.ts:45`) | `held_at desc, lot_hold_id desc` · ⭐ **상세 GET 두 자리도 같은 정렬로 맞춘다**(R-5 · +4줄) | ✕ |
| 12 | 치환이 `lotExternalIdentifierId` 를 갈아 치우는 것 | 가장자리 → 계약 문자(「요청에서 빠진 기존 행은 삭제한다」) + `Upsert` 에 id 칸 0 | **허용** | 통보 152 에 함께 |
| 13 | `LotStatusHistoryEvent` 널 가능 칸을 널로 싣나, 키를 생략하나 | 0단계 — 형제 뷰 `omitEmpty` + `?? undefined`(R-3) | **키 생략** | ✕ · 「알려둘 것」 ⓖ |

### 9-2. 문의 후보 — **신규 4건 · 전부 「통보」 · 질의 0건**(번호 **150·151·152·154** · A2 대역)

| # | 구분 | 제목 | 걸리는 오퍼레이션 |
|:-:|:-:|---|---|
| **150** | **통보** | **`:request-iqc-skip` 의 자격 축을 서버가 두 칸으로 정했고, 거부 코드가 «둘로 갈린다»** — 계약이 「입하돼 수입검사 대기인 LOT」이라 산문으로만 적고 위반 시의 응답을 한 줄도 안 적었다. 서버는 `lot.source_type_code='INBOUND_RECEIPT_LINE'` 위반을 **400 `INVALID`**, `lot.status_code='INSPECTION_PENDING'` 위반을 **400 `STATE_LOCKED`** 로 가른다(형제 `:complete` 가 같은 두 축에 같은 두 코드를 쓴다). ⚠ 열린 `INCOMING_INSPECTION_WAIT` 보류 유무는 **안 본다** — I-19 `:confirm` 이 그 보류를 닫으므로 검사 중 LOT 이 정당하게 막힌다 | `:request-iqc-skip` |
| **151** | **통보** | **`X-Worker-No` 를 담을 칸이 0인데 부재를 400 으로 거부한다** — `approval_request` 에 사번 칸이 없어 읽고 버리는데, 계약 `WorkerNo.required=true` 와 화면 `M-01-13`(POP)이 이유다. 같은 자원의 `:complete`·`:pick`·`material-returns`·`work-sessions` 가 이미 같은 형태(담을 칸 0 · 400) ⇒ `plan.md` §5 규칙 9 예외가 **열한째**가 된다. ⚠ 그럼에도 **주체는 세션**이다 — `mdm.worker.app_user_id` 로 푸는 길이 있으나 nullable 이라 쓰지 않았다 | `:request-iqc-skip` |
| **152** | **통보** | **외부 식별자 치환이 0행 요청을 받아 전건을 지운다** — 계약이 `minItems` 를 안 걸었고 「요청에서 빠진 기존 행은 삭제한다」가 전건 삭제를 포함한다고 읽었다. 형제 출고 라인은 0행을 400 으로 막았는데(등록 계약이 「최소 1행」) 여기 등록(`LotCreate.externalIdentifiers`)은 **선택 배열**이라 대칭이 반대다. ⚠ 덤 — 치환은 전삭제+전삽입이라 `lotExternalIdentifierId` 가 매번 갈린다(계약이 id 칸을 `Upsert` 에 두지 않아 다른 길이 없다) | `PUT …/external-identifiers` · `GET …/external-identifiers` |
| **154** | **통보** | **`GET /trace/lot-status-events` 가 계약 enum 밖 `transitionCode` 를 그대로 싣는다** — 계약은 `transitionCode` 를 **required + 닫힌 enum 9값**으로 잠갔는데 물리 `transition_code` 는 NOT NULL·enum 없음이고, `src/core/document-state/transitions.ts:215-219` 의 `stock-reinstate` 가 **`transitionCode` 없이** 예약돼 있다(주석: 「지어내지 않고 호출자가 넘기게 둔다 · 문의 089」). 조회는 형제 선례대로 **행을 떨어뜨리지 않으므로**(떨어뜨리면 감사 조회가 오히려 넓어진다), **레인 C I-23 의 재등록이 병합되는 순간 enum 밖 값이 이 응답으로 샌다**. ⛔ 값은 우리가 고르지 않는다 — **문의 089 는 레인 C 소유**(발행 예정 · `lanes.md:47` 「089 는 레인 C 재등록용 예약」 · `plan-integration.md:406` 이 그 1건을 이미 파킹했다) | `GET /trace/lot-status-events` |

**기존으로 미루는 것**: **회신 12**(한도승인 재고 상태 값 — §9-3 · 이미 A·I-21 소유) · **문의 019**(`IQC_SKIP` 승인 화면·`screenId` — I-1 이 「구현함(`openable=false`)」으로 닫았다 · 이 슬라이스가 그 유형의 «첫 상신자»가 된다) · **문의 022**(결재선 `businessUnitId` 파생 매핑 — 9 상신 도메인 중 8자리가 `null` · 여기가 아홉째다) · **문의 089**(`transitionCode` 값 — 레인 C 소유 · 통보 154 가 가리킨다).

⛔ **초안의 통보 153(화면 0건인 쓰기의 권한)은 철회했다** — `plan-uiux.md:852` 가 이미 답을 갖고 있어 0단계 선례로 닫힌다(R-9). 통보로 냈다면 「정본이 이미 답한 것을 다시 묻는」 문서가 됐다.

### 9-3. ⭐ 회신 12 — **이 슬라이스에서는 판정 자체가 필요 없다 · 이미 A(I-21)로 재배정돼 있다**

브리프 §3-2 와 `plan-integration.md` §I-18 이 「회신 12(한도승인 재고 상태 값)가 여기 붙는다」라 적었으나, **그 배정은 이미 옮겨졌다** — `docs/design-inquiries/재정리-2026-09-08-레인A.md:30` 이 회신 12 를 **I-21·통보**로 재배정해 두었다(R-10). 낡은 포인터가 `plan-integration.md` **:365·:646 둘**에 남아 있고, 그 둘은 공용 문서라 **우리가 안 고친다**(§11-2 조율 2).

실측으로도 이 슬라이스에 그 값을 «쓸 자리»가 없다:

- `:request-iqc-skip` 의 본문 `ApprovalRequestCreate` 는 프로퍼티가 **`reason` 하나**다 — 한도를 받는 칸이 0개고, 계약이 스스로 「요청을 만들기만 하고 **한도는 승인자가 정한다**」라 적었다(`:6508`).
- 이 슬라이스는 `trace.lot`·`inventory_balance` 의 **어떤 상태 칸도 안 옮긴다**(§6). 승인 «뒤» 재고 상태를 옮기는 오퍼레이션은 이 계약 파일에도 배정표에도 없다.

⇒ **판정도 흔적 표기도 필요 없다.** 특채(concession) 승인이 LOT/재고 상태를 실제로 옮기는 첫 자리라 **I-21(#332) 단독**이다. ⛔ 이 슬라이스가 값을 먼저 고르면 A 의 판정을 앞질러 잠근다.

### 9-4. 「알려둘 것」 — **8건**(번호 없음 · 다음 전달분 말미)

ⓐ 사번을 읽고 버린다(`approval_request` 에 담을 칸 0) — 「누가 단말에서 눌렀나」가 데이터에 안 남는다.
ⓑ 조회 3건이 **403 을 선언하지 않아** 권한 없는 세션도 남의 LOT 의 보류·외부식별자·상태 이력을 읽는다(계약 그대로).
ⓒ 치환이 `lotExternalIdentifierId` 를 갈아 치운다 — 클라이언트가 그 id 를 캐시하면 깨진다.
ⓓ `GET /trace/lot-status-events` 가 **페이지네이션이 없다** — 기간이 유일한 상한이라 넓은 기간 질의가 전건을 싣는다(형제 `lot-lifecycle-events` 와 같은 자리).
ⓔ `IQC_SKIP` 결재선이 DB 에 0행이라 **오늘 상태로는 이 오퍼레이션이 언제나 400** 이다 — 배포 노트.
ⓕ ⭐ **`:request-iqc-skip` 의 `W-01-13` 과다부여를 우리가 못 지운다**(R-2) — 두 표가 합집합이라 `M-01-13` 을 더해도 도출표의 `W-01-13` 은 남는다. 지우려면 요구서 §3(도출표 원천)이 바뀌어야 한다.
ⓖ ⭐ **계약의 널/enum 모순이 이 응답에도 있다**(R-3) — `LotStatusHistoryEvent.sourceDocumentTypeCode` 가 `[string,null]` 인데 enum 3값에 널이 없어 널이 계약 스스로를 통과 못 한다(`externalSystemCode` 와 같은 모양 · 전 계약 16곳 · 되돌림 §Y-1). 서버는 **키 생략**으로 피한다.
ⓗ ⭐ **오늘 태어나는 입하 보류는 `lotStatusCode` 키가 생략된다**(R-6) — `lot_hold.target_lot_status_code` 를 등록 시점에 채우는 것은 **레인 A I-20 PR ③**(`lot-registry.service.ts:96-101` 한 줄)이고 아직 미병합이다. 그래서 이 슬라이스의 e2e 는 그 칸을 단언하지 않는다(§8-2).

---

## 10. PR 분할 — **2**(통합 계획서와 같다)

| PR | 내용 | 파일 | 비테스트 예산 | 모델 | 스택 |
|:-:|---|---|---:|---|---|
| **①** 조회 3 | `GET /trace/lot-status-events` · `GET …/{lotId}/external-identifiers` · `GET …/{lotId}/holds` | `lot-status-event.{controller,service}.ts`·`-view.ts`(신 3) · `lot-external-identifier.service.ts`(신) · `lot-hold-list.service.ts`(신) · `lot.controller.ts`(+32) · **`lot.service.ts`(+4 · `orderBy`)** · `trace.module.ts`(+8) · e2e 신 1파일(8) + `trace-lot.e2e-spec.ts`(+5) + 단위 6 | **~250** (≤350) | **sonnet** | main 위 |
| **②** 쓰기 2 + 권한 | `:request-iqc-skip` · `PUT …/external-identifiers` · **권한 2줄(단독 커밋)** | `lot-iqc-skip.service.ts`(신) · `lot-external-identifier.service.ts`(+85) · `lot.controller.ts`(+45) · `trace.module.ts`(+6) · `manual-permissions.ts`(+4 · 단독 커밋) · e2e `trace-lot.e2e-spec.ts`(+8) + 단위 4 | **~245** (≤350) | **opus**(승인 코어 재사용 + 낙관적 잠금 — 조회 복제가 아니다) | ① 위 |

- ⭐ **왜 2 인가** — `plan-api.md` §S08 기대값(「① 조회 GET 3건 ② `:request-iqc-skip`·외부식별자 PUT + e2e」)과 같다. 자르는 선은 **읽기/쓰기**다. 합치면 ~495줄로 400 을 넘는다.
- ⛔ **①② 는 직렬**이다 — 둘 다 `lot.controller.ts`·`trace.module.ts`·`lot-external-identifier.service.ts`·`test/trace-lot.e2e-spec.ts` 를 만진다. ⚠ README §6-1 ③(조회 PR 을 리뷰와 나란히 스폰)은 여기서 **① 하나뿐**이라 적용할 자리가 없다.
- ⛔ **PR ② 의 base 재지정**(R-7) — ② 는 ① 브랜치 위에 쌓는다. PR ② 본문 «머리»에 「**base 가 PR ① 브랜치다. ① 병합 뒤 `gh pr edit <②> --base main` 을 «먼저» 하고 ① 브랜치를 지운다**」를 적는다. 안 적으면 `lanes.md` §2-5 의 사고(#153 병합 → #154 가 재지정 없이 CLOSED)가 되풀이된다. 같은 문장을 A 인계 코멘트에도 싣는다(§11-1 ④).
- ⭐ **모델** — 통합 계획서는 슬라이스 전체를 `sonnet` 으로 적었다. **② 만 opus 로 올린다**(`lanes.md` §3-1 의 축: 판단 밀도). 근거: ② 가 승인 코어 호출 + `FOR UPDATE` + 낙관적 잠금 + **코드가 다른 자격 축 둘**을 한 트랜잭션에 넣는다 — 「기존 패턴 복제」가 아니다. ①은 sonnet(형제 직역 복제).
- 게이트: 구현자가 PR 본문에 실측(명령·숫자) + **변이 점검 표**(§8-4 · 아홉)를 적는다. 리뷰어는 **단위 전체 + 그 PR 이 바꾼 e2e 파일만** 돈다(README §6). ⛔ `pnpm exec` 금지 — `node_modules/.bin/jest` 등을 직접 부르고 `prisma generate` 와 `tsc`/`jest` 를 **한 호출로 묶는다**(`lanes.md` §2-2).
- 병합은 **A 에게 넘긴다**(`lanes.md` §0-1 — 통합자는 A 세션 하나뿐). PR 을 열고 리뷰까지 받는 것이 A2 몫이다.

### 10-1. 통합 계획서 대조표

| # | 통합 계획서 | 이 계획안 | 판정 |
|:-:|---|---|:-:|
| 1 | `plan.md` §1 18행 — 마이그 **「—」** | **0건**(§2 · 표 다섯 전부 실재 · `20260901030000` 이 이미 맞춰 놨다) | 같다 |
| 2 | 〃 — 코어 **「—」** | 같다 — `transitions.ts` 0줄 · 새 코어 0. 단 **기존 코어 둘을 쓴다**(`core/approval` · `core/numbering`) | 같다 |
| 3 | 〃 — **PR 2** · `plan-api.md` §S08 도 2 | **2**(§10) | 같다 |
| 4 | 〃 — 모델 **sonnet** | **① sonnet · ② opus** | **다르다**(작다 · 근거 §10) ⇒ ⭐ 이 PR 에서 `plan.md` §1 **18행 모델 칸 한 곳**만 고친다 |
| 5 | 〃 — 선행 **I-1** | 같다(I-1 병합 완료 · `core/approval` 실재) ⭐ 덤 — **I-7 도 사실상 선행**(복제 원본 `lot-lifecycle-event.*`) · ⚠ **I-20 은 «선행 완료가 아니다»** — PR ③(#331) 진행 중이고 그것이 `lotStatusCode` 를 채운다(R-6) | **다르다**(정보 추가·정정) |
| 6 | `plan-api.md` §S08 — 오퍼레이션 **7건** | **5건**(`:complete`·`lot-lifecycle-events` 는 I-7 이 이미 구현) | 같다(§S08 이 그 이관을 각주로 적었다) |
| 7 | 〃 — 「남는 미정 자리는 `:request-iqc-skip` 의 판정 축뿐」 | ⭐ **통보 4건**(150·151·152·154) — 자격 축 외에 **사번 400 · 0행 치환 · enum 밖 전이코드 누출** 셋이 더 있다 | **다르다** |
| 8 | `plan-integration.md` §I-18 — 「회신 12 가 여기 붙는다」(:365·:646) | ⛔ **낡은 포인터다** — `재정리-2026-09-08-레인A.md:30` 이 이미 I-21·통보로 재배정했다(§9-3 · R-10) | **다르다** ⭐ 공용 문서라 A 조율(§11-2) |
| 9 | 〃 — 「원장·상태기계 없음 · 병렬 후보」 | 같다(§6) | 같다 |
| 10 | `plan-uiux.md` §U6 — PUT 의 화면 「—」 | ⚠ **같은 문서 :852 가 답을 갖고 있다** — 「부모의 화면이 곧 이 오퍼레이션의 화면」 + `M-01-02`(R-9) ⇒ 권한 `['M-01-02']` · 통보 철회 | **다르다**(정본 안에서 해소) |
| 11 | `plan.md` §5 규칙 9 — 사번 400 예외 **열 자리** | **열한째**(`:request-iqc-skip`) | **다르다**(§0 자리 2) ⇒ 공용 문서라 A 조율(§11-2) |
| 12 | 커버리지 382(`main` f931561) | **389 → 394/487**(base `dd4032e` 실측) | **다르다**(기준선이 움직였다 · +5 는 같다) |
| 13 | `plan-uiux.md` §U6 — `GET …/holds` 화면 `M-01-08` | ⚠ 두 경로(상세 GET · 하위 GET)를 함께 보는 화면은 **`W-01-07`** 이다(R-5) | **다르다**(정보 정정) |

---

## 11. 인계

### 11-1. 인계 표

| # | 받는 곳 | 내용 |
|:-:|---|---|
| ① ⭐ | **레인 A · I-21**(#332) | **회신 12 는 여기서 판정하지 않는다** — 이미 `재정리-2026-09-08-레인A.md:30` 이 I-21·통보로 재배정했다(§9-3). 이 슬라이스는 `IQC_SKIP` 요청 행만 만들어 두므로, I-21 이 「승인된 IQC 생략 요청을 누가 실행하나」를 열면 `approval_request.(target_type_code='INBOUND_LOT', target_id=lot_id, approval_type_code='IQC_SKIP')` 다형 축으로 읽으면 된다(`assertApproved` 가 그 서명이다) |
| ② | **레인 A · I-20/I-21**(공용·겹치는 파일) | `src/trace/trace.module.ts`(등록 줄만) · `src/trace/lot/lot-view.ts`(**0줄 — 안 만진다**) · `src/trace/lot/lot.service.ts`(**+4줄 · `include` 의 `orderBy` 뿐** · R-5) · `test/trace-lot.e2e-spec.ts`(+13 · **추가만** · 배열 통째 단언 금지 — R-6). ⭐ `holdView` 의 뜻을 **그대로 승계**한다(I-20 R-9 — `lotStatusCode` = 등록 도착 `target_lot_status_code`) — 이 슬라이스가 그 값을 두 번째 경로로 내보내므로 **R-9 를 뒤집으면 여기도 함께 바뀐다** |
| ③ | **화면 · `M-01-13`** | ⭐ 「이 LOT 에 IQC 생략 요청이 걸렸나」의 **조회 경로를 계약이 이미 지정해 두었다**(R-8) — `GET /app/approval-requests?targetTypeCode=INBOUND_LOT&targetId={lotId}` 이고, 그 description 이 「`M-01-13` 의 «내가 올린 요청» 구획」을 명시한다. ⛔ `trace.lot`·`LotDetailResponse` 에 흔적 칸을 더하지 않는 이유가 이것이다(`plan.md` §5 규칙 12 — 정본은 다형 축) |
| ④ | **A2 · I-34 / I-25** | `lot-external-identifier.service.ts` 의 「전삭제+전삽입 + 부모 version 범프」 형태가 A2 의 다음 치환 PUT 들의 본이다. ⚠ `runVersioned` 를 못 쓰는 자리가 이 저장소에 넷째다(P/O `:request-approval` · 출고 라인·상신 · 여기) — **다섯째가 나오면 `common/master` 로 올린다**(I-24 §12-1 ⓒ 와 같은 대기) |
| ⑤ | **PR 병합자(A)** | ⛔ **PR ② 의 base 가 PR ① 브랜치다**(R-7) — ① 을 병합하면 ② 를 **먼저** `gh pr edit <②> --base main` 으로 재지정하고 그 다음에 ① 브랜치를 지운다. 순서를 바꾸면 ② 가 재지정 없이 CLOSED 된다(`lanes.md` §2-5 의 실제 사고 #153/#154) |
| ⑥ | **M3 체인 e2e**(통합자) | 이 슬라이스는 체인의 **가지**다 — 마디를 만들지도 잇지도 않는다(`plan-integration.md` §I-18). M3 픽스처가 `lot_status_event` 를 채우면 §8-1 의 직접 심기를 그 사슬로 갈아도 된다(선택) |
| ⑦ | **레인 C · I-23** | ⭐ **통보 154 의 반대편이다** — `stock-reinstate` 가 `transitionCode` 없이 예약돼 있어(`src/core/document-state/transitions.ts:215-219`) 병합되는 순간 계약 enum 밖 값이 `GET /trace/lot-status-events` 로 샌다. ⛔ 값은 C 가 문의 089 로 고른다 — **A2 는 고르지 않고 조회도 안 좁힌다** |
| ⑧ | 후속 소형 PR | ⓐ `GET /trace/lot-status-events` 의 페이지네이션(계약이 열면 · 「알려둘 것」 ⓓ) ⓑ `lot.service.ts` 가 297줄 — 다음에 그 파일을 만지는 슬라이스가 `get()` 의 세 조회를 이 슬라이스의 service 셋으로 위임하면 ~40줄 준다(**지금은 안 한다** — 레인 A 와 겹친다) |

### 11-2. ⛔ 레인 A 조율 항목 **4건** (A2 가 손대지 않는다 · 사용자 경유 보고)

| # | 자리 | 왜 A2 가 안 고치나 | 무엇을 부탁하나 |
|:-:|---|---|---|
| **1** | `plan.md` §5 **규칙 9** 에 **열한째**(`:request-iqc-skip` 사번 400) 추가 | 공용 문서 — `lanes.md` §1-4 「자기 슬라이스 행만」 | 규칙 9 예외 목록에 한 줄. 통보 151 이 그 근거다 |
| **2** | `plan-integration.md` **:365·:646** 의 회신 12 낡은 포인터 2곳 | 공용 문서 · 재배정의 주인이 A 다(`재정리-2026-09-08-레인A.md:30`) | 「I-18 에 붙는다」를 「I-21 소유(재배정 완료)」로 |
| **3** | `GET /app/approval-requests` 권한에 `M-01-13`·`W-01-02`·`M-CO-01` 누락 | ⛔ **I-18 오퍼레이션이 아니다** — 우리가 안 고친다 | 인계 ③ 의 조회 경로를 `M-01-13` 이 실제로 쓰려면 그 오퍼레이션의 권한이 필요하다는 사실만 전달 |
| **4** | `src/trace/lot/lot-view.ts` · `test/trace-lot.e2e-spec.ts` 가 A 의 I-20 PR ③ 와 겹친다 | 같은 파일을 두 레인이 만진다 | `lot-view.ts` 는 **0줄**(안 만진다) · e2e 는 **추가만** · 병합 직전 `git merge origin/main` 뒤 게이트 재실행(`lanes.md` §2-1) |

### 11-3. 실측 부록 (2026-09-08 · 계약 a6a87e1 · DB `omf_mes_lane_a2` 13:57 KST)

> 리뷰어·구현자는 **이 표의 값을 재측정하지 않는다.** 단 자기 판정이 그 값을 뒤집는다면 반드시 재측정한다.
> 여기 든 것은 전부 **실측**이다 — 추론·판정은 본문에 있고 이 표에는 없다.
> ⚠ 1~54 는 초안 시점(`main` f931561) · **55~62 는 3관점 재수립 때 통합자가 직접 다시 잰 것**이다. 둘 사이에 물리·계약 변화는 0이고, base 가 `dd4032e` 로 움직이며 바뀐 것은 커버리지 기준선(382 → **389**) 하나다.

| # | 확인한 사실 | `파일:줄` |
|:-:|---|---|
| 1 | `awk -F'\t' '$1=="I-18"'` = **5행** — 브리프 §1 · `plan-integration.md` §I-18 과 전건 일치 | `docs/coverage-100/assignment.tsv` |
| 2 | 계약 파일 총 오퍼레이션 **90** · path **67** · 그중 I-18 대상 5 일치 · 누락 0 · 여분 0 | `contracts/logistics-01자재창고.json`(python) |
| 3 | ⭐ 조회 3건은 400·403·404 를 **하나도 선언하지 않는다** — responses 가 `200` 하나뿐 | 같은 파일 `:6190`·`:6300`·`:6331` |
| 4 | ⭐ `PUT …/external-identifiers` 200 에 **`headers` 가 없다**(ETag 미선언) · 응답 4(200·400·403·409 · **404 없음**) | 같은 파일 `:6209-6285` |
| 5 | ⭐ 그 PUT description 원문 — 「이 경로의 조회는 ETag 를 내리지 않는다 — If-Match 에 담을 토큰은 **부모 자원 `GET /trace/lots/{lotId}` 200 의 ETag** 다. 잠그는 단위가 부모이기 때문이다」 | 같은 파일 `:6214` |
| 6 | ⭐ `:request-iqc-skip` 파라미터는 **`IdempotencyKey` + `WorkerNo` 둘** — If-Match 없음 · 성공은 **202** | 같은 파일 `:6509-6535` |
| 7 | ⭐ 그 `x-internal-note` — 「`approval_type_code=IQC_SKIP` · `target_type_code=INBOUND_LOT` · `target_id=lot_id` 로 **서버가 채운다**」 | 같은 파일 `:6569` |
| 8 | ⭐ 그 description — 「입하돼 수입검사 대기인 LOT 에 대해 … **한도는 승인자가 정한다**. 결재선이 없으면 400(code=`ROUTE_NOT_FOUND`)이다」 | 같은 파일 `:6508` |
| 9 | `ApprovalRequestCreate` required **1** · 프로퍼티 **1**(`reason` · `minLength:1`) — 한도 칸 0 | 같은 파일(components.schemas) |
| 10 | `LotExternalIdentifierUpsert` required **2** · 프로퍼티 **4** · **id 칸 없음** · `externalSystemCode` 는 `type:[string,null]` + enum 3값(널이 enum 을 못 통과) | 같은 파일 `:11761` |
| 11 | `LotStatusHistoryEvent` required **6**(`lotStatusHistoryId`·`lotId`·`toStatusCode`·`transitionCode`·`changedBy`·`changedAt`) · 프로퍼티 **11** · `transitionCode` enum **9값** | 같은 파일 `:11970` |
| 12 | `LotHold` required **5** · 프로퍼티 **16** · `lotStatusCode` 는 required **밖**이고 `type:string`(널 불가 ⇒ 키 생략) | 같은 파일 `:11814` |
| 13 | `LotDetailResponse.holds` = **`LotHold[]`** · `externalIdentifiers` = `LotExternalIdentifier[]` — 하위 자원 GET 과 **같은 스키마** | 같은 파일(components.schemas) |
| 14 | `activeOnly` default **true** · 설명 「해제되지 않은 것만」 | 같은 파일 `:6308`·`:6314` |
| 15 | `occurredFrom`·`occurredTo` **required:true** · `lotId`·`transitionCode` 선택 | 같은 파일 `:6337-6395` 부근 |
| 16 | `trace.lot_status_event` **17칸** — `previous_status_code`/`new_status_code` · `transition_code` NOT NULL · `changed_by` NOT NULL · 인덱스 2 | `prisma/schema.prisma:4634-4657` |
| 17 | ⭐ `20260901030000_align_lot_status_event` 가 **이 오퍼레이션 이름을 대고** 표를 계약에 맞췄다 — `transition_code` 신설 · `reason` 신설 · `changed_by` NOT NULL · 원천 두 칸 완화 + `ck_lot_status_event_source` · `ix_lot_status_event_changed_at` | `prisma/migrations/20260901030000_align_lot_status_event/migration.sql:1-80` |
| 18 | `trace.lot_external_identifier` **8칸**(계약 5 + `created_at`·`created_by`) · **자식 관계 0** | `prisma/schema.prisma:3601-3614` |
| 19 | ⭐ `uq_lot_external_identifier` = **5칸 표현식 유일 인덱스** `(lot_id, identifier_type_code, COALESCE(partner_id,0), COALESCE(external_system_code,''), external_identifier)` | `prisma/migrations/20260727000000_baseline_physical_model_v3/migration.sql:1082-1089` |
| 20 | `trace.lot` 에 `approval_request_id` 칸이 **없다** · `version_no Int @default(1)` 있다 | `prisma/schema.prisma:3522-3546` |
| 21 | `trace.lot_hold` 에 `target_lot_status_code`·`release_target_lot_status_code`·`version_no` 가 **이미 있다**(I-20 M-f · main 병합) | `prisma/schema.prisma:3617-3645` · `prisma/migrations/20260908110558_lot_hold_target_status_and_version/migration.sql` |
| 22 | ⭐ `holdView(row, lot)` 가 계약 `LotHold` 16칸 전건을 그린다 · `lotStatusCode` ← `target_lot_status_code` · 널이면 **키 생략** | `src/trace/lot/lot-view.ts:135-158` |
| 23 | `identifierView(row)` 가 계약 `LotExternalIdentifier` 5칸을 그린다 · `externalSystemCode` 는 널이면 키 생략 | `src/trace/lot/lot-view.ts:103-118` |
| 24 | `GET /trace/lots/{lotId}` 가 이미 `identifiers.map(identifierView)` 와 `row.lot_hold.filter(released_at===null).map(h=>holdView(h,row))` 를 낸다 | `src/trace/lot/lot.service.ts:141-151` |
| 25 | 같은 계약 스키마 `LotHold` 의 **둘째 뷰**가 `quality` 에 있다(`omitEmpty` · 선택 칸을 키 생략) | `src/quality/lot-hold/lot-hold-view.ts:47-70` |
| 26 | `open` 기본 true · **`open=false` 는 「해제된 것만」이 아니라 «전체» ⇒ 필터를 안 건다**(I-20 R-1) | `src/quality/lot-hold/lot-hold-query.service.ts:68-84` |
| 27 | 형제 조회 셋의 크기 — controller **21** · service **57** · view **30** · spec **79** | `src/trace/lot/lot-lifecycle-event.*` |
| 28 | ⭐ 형제 컨트롤러 주석이 **이 슬라이스를 예고한다** — 「오퍼레이션 하나에 디렉터리를 새로 만들지도 않는다(R-13 · **I-18 이 같은 축을 더한다**)」 | `src/trace/lot/lot-lifecycle-event.controller.ts:5-6` |
| 29 | 형제가 **enum 밖 값을 안 떨어뜨린다** — 「떨어뜨리면 감사 조회가 오히려 «넓어진다»」 | `src/trace/lot/lot-lifecycle-event.service.ts:33-36` |
| 30 | ⭐ `:complete` 의 **두 게이트가 서로 다른 코드**다 — 원천유형 위반 400 `ERROR_CODE.INVALID`(`:66-69`) · 상태 위반 400 `STATE_LOCKED`(`:71-73`) | `src/trace/lot/lot-complete.service.ts:66-73` |
| 31 | ⭐ `assertWorkerNo` — 「사번을 **읽고 버린다** … 계약이 `required:true` 로 못박았으므로 부재는 거부한다. 헤더는 계약 검증 가드가 보지 않는다」 400 `REQUIRED` | `src/trace/lot/lot-complete.service.ts:160-168` |
| 32 | `lot.service.ts` = **293줄** · `lot.controller.ts` = **108줄** · `lot-view.ts` = **159줄** | `wc -l src/trace/lot/*.ts` |
| 33 | ⭐ 「응답에 ETag 가 없다(계약 미선언). **그래도 부모 `version_no` 는 올린다** … 다음 If-Match 는 상세 GET 이 준다」 + 조건부 UPDATE + `assertUpdated` | `src/logistics/goods-issue/goods-issue-update.service.ts:41-49`·`139-145` |
| 34 | ⭐ 그 반대편 — `:request-approval` 은 「**`version_no` 를 올리지 않는다** — 202 에 ETag 가 없어 … 올리면 다음 쓰기가 영원히 409 다」 | 같은 파일 `:161-166` |
| 35 | 채번은 `$transaction` **밖** — 「열린 트랜잭션 안에서 부르면 커넥션을 둘 쥐고 P2024 로 죽는다」 | `src/logistics/purchase-order/purchase-order.service.ts:288-296` · `goods-issue-update.service.ts:182-190` |
| 36 | `ApprovalService.request()` 가 `assertNoOpenRequest`+`selectRoute`+`expandSteps` 를 한 번에 한다 · ⛔ 「대상 문서의 어떤 행도 건드리지 않는다」 · 「호출자가 트랜잭션 첫 문장에서 대상 행을 잠근다」 | `src/core/approval/approval.service.ts:60-100`·`:102-110` |
| 37 | `ROUTE_NOT_FOUND`(결재선 0 / 단계 0) · `ROUTE_AMBIGUOUS` · `APPROVAL_IN_PROGRESS` · `APPROVER_TYPE_NOT_SUPPORTED` 가 코어에 이미 있다 | 같은 파일 `:127`·`:189`·`:192`·`:214`·`:227` |
| 38 | ⭐ `'POST /trace/lots/{lotId}:request-iqc-skip': ['W-01-13']` 가 **도출표에 이미 있다** | `src/common/permissions/derived-permissions.ts:259` |
| 39 | `'PUT /trace/lots/{lotId}': ['M-01-02','P-01-01']` 는 수동표 · `PUT …/external-identifiers` 는 **두 표 어디에도 없다** | `src/common/permissions/manual-permissions.ts:84` (grep 실측) |
| 40 | 403 선언 **250** · covered `toBeGreaterThanOrEqual(152)` — 한 건 늘어도 초록 | `src/common/permissions/operation-permissions.spec.ts:53-64` |
| 41 | 미등록 403 오퍼레이션은 **통과가 아니라 던짐**(500) — F-6 | `src/common/permissions/permission.guard.ts:47-53` |
| 42 | 계약 검증 파이프가 **헤더를 안 본다**(`$ref` 파라미터는 `in` 만 보고 지나간다) | `src/common/contract/contract-validator.ts:96` · `idempotency.guard.ts:12-16` |
| 43 | 응답 **본문 검증이 없다**(guard 에 response 경로 0 · grep 실측) ⇒ enum 밖 `transitionCode` 가 런타임에 안 걸린다 | `src/common/contract/contract-validation.guard.ts` |
| 44 | `runVersioned` 는 `setEtag` 를 부른다 ⇒ ETag 없는 쓰기는 못 쓴다 · `runIdempotent` 는 세션 userId 를 멱등 기록에 싣는다 | `src/common/master/master-write.ts:20-58` |
| 45 | `assertCodeValues` 가 **비활성 값도 막는다** · 그룹+코드로 대조 | `src/common/master/code-reference.ts:24-52` |
| 46 | 등록 경로가 이미 `LOT_EXTERNAL_IDENTIFIER_TYPE` 으로 대조한다(필드 이름 `externalIdentifiers.identifierTypeCode`) | `src/trace/lot/lot.service.ts:277-283` |
| 47 | `lot_status_event` 의 유일한 writer 는 `LotQualityStatusService.moveWithin` — `location_id`·`quality_status_code`·`inventory_status_code` 를 **비운다** | `src/core/lot/lot-quality-status.service.ts:80-93` |
| 48 | 등록 코어가 LOT 을 `status_code='INSPECTION_PENDING'` 으로 세우고 `INCOMING_INSPECTION_WAIT` 보류를 **서버가 건다** | `src/core/lot/lot-registry.service.ts:16-26`·`:87`·`:96-104` |
| 49 | `test/approval-request.fixture.ts::seedRoute(prisma, typeCode, approverIds)` 실재 · `IQC_SKIP`+`INBOUND_LOT` 조합을 e2e 가 이미 쓴다 | `test/approval-request.fixture.ts:10-33` · `test/app-approval-request.e2e-spec.ts:65`·`:204` |
| 50 | `test/trace-lot.e2e-spec.ts` **597줄** — ajv 검증기 헬퍼(`validator(operation, status)`)·`randomUUID` 멱등키·`cleanup()` 관행 | `test/trace-lot.e2e-spec.ts:32-53` |
| 51 | DB(A2) — 마이그 **60건** · `trace.lot`·`lot_external_identifier`·`lot_hold`·`lot_status_event`·`app.approval_request`·`approval_route` **전부 0행** | `docker exec omf-mes-lane-a2-postgres psql -X`(2026-09-08 13:57 KST) |
| 52 | DB(A2) 코드값 — `LOT_STATUS_TRANSITION` **9행**(C4~C10·C14·C15 · 계약 enum 과 전건 일치) · `LOT_STATUS` 4 · `LOT_EXTERNAL_IDENTIFIER_TYPE` 4 · `LOT_HOLD_REASON` 6 · ⭐ **`APPROVAL_TYPE` 0행** | 같은 명령 |
| 53 | `APPROVAL_TYPE` 이 0행인 이유 — 시드가 「시스템 소유」로 **일부러 뺐다** | `prisma/seed.ts:1454` |
| 54 | 문의 대역 **150~179** 가 전부 미사용(`1[5-7][0-9]` 0건) | `ls docs/design-inquiries/` |
| **55** | ⭐ **재수립** — `operation-permissions.spec.ts` 가 두 표를 **합집합**으로 못 박았다. 같은 «키»는 허용(주석: 「막아야 하는 것은 같은 키에 같은 «권한»을 다시 적는 것」) · 겹치는 키가 0이면 헛통과라며 `shared.length > 0` 까지 단언한다 | `src/common/permissions/operation-permissions.spec.ts:27-45` |
| **56** | ⭐ **재수립** — 계약 `LotStatusHistoryEvent.sourceDocumentTypeCode` = `["string","null"]` **+ enum 3값**(널이 enum 에 없다) | `contracts/logistics-01자재창고.json`(components.schemas) |
| **57** | ⭐ **재수립** — 형제 뷰가 널 가능 칸을 `omitEmpty` + `?? undefined` 로 그린다(키 생략) | `src/trace/lot/lot-lifecycle-event-view.ts:19`·`:26` |
| **58** | ⭐ **재수립** — `stock-reinstate` 가 **`transitionCode` 없이** 예약돼 있다 · 주석 「이력 칸은 NOT NULL 인데 계약 enum 9값(C4~C15)에 재등록을 가리키는 코드가 «없다». 지어내지 않고 호출자가 넘기게 둔다. 설계 미정 — **문의 089(발행 예정)**」 · 물리 `transition_code` 는 NOT NULL·enum 없음 · `plan-integration.md:406` 이 「089 의 답을 기다린다 · I-23 의 재등록 1건만 미룬다」로 이미 파킹 · `lanes.md:47` 「089 는 레인 C 재등록용 예약」 | `src/core/document-state/transitions.ts:215-219` · `prisma/schema.prisma:4634-4657` · `docs/coverage-100/plan-integration.md:406` · `docs/coverage-100/lanes.md:47` |
| **59** | ⭐ **재수립** — 상세 GET 의 `include: { lot_hold: true }` **두 자리 다 정렬이 없다** | `src/trace/lot/lot.service.ts:126`·`:284` |
| **60** | ⭐ **재수립** — 계약 `GET /app/approval-requests` 에 `targetTypeCode`·`targetId` 실재 · description 이 「`M-01-13` 의 «내가 올린 요청» 구획」을 명시 | `contracts/…`(해당 오퍼레이션) |
| **61** | ⭐ **재수립** — `plan-uiux.md:852` 가 물류 자식 치환 PUT **7건**을 묶어 「부모의 화면이 곧 이 오퍼레이션의 화면」이라 적고 `lots/external-identifiers` 의 화면으로 **`M-01-02`** 를 든다 | `docs/coverage-100/plan-uiux.md:852` |
| **62** | ⭐ **재수립** — 회신 12 는 이미 **I-21·통보**로 재배정돼 있다 · 낡은 포인터 2곳 | `docs/design-inquiries/재정리-2026-09-08-레인A.md:30` · `docs/coverage-100/plan-integration.md:365`·`:646` |

---

## 12. 구현 뒤 남은 것 (리뷰 결과 · 통합자가 채운다)

### 12-1. 미완 목록

| # | 자리 | 내용 |
|:-:|---|---|
| ① | 배포 노트 | `IQC_SKIP` 결재선이 **0행**이라 운영자가 `POST /app/approval-routes` 로 한 벌 세우기 전에는 `:request-iqc-skip` 이 언제나 400 `ROUTE_NOT_FOUND` 다(§1-5) |
| ② | 열린 위험(타레인) | 레인 C 의 **I-23** 이 병합되면 `stock-reinstate` 가 계약 enum 9값 밖 `transition_code` 를 쓴다 ⇒ `GET /trace/lot-status-events` 응답이 계약 enum 을 깬다. ⭐ **문의 089 가 실재한다**(레인 A 가 I-21 계획 PR #416 으로 발행 · 처분 3전이에 `C17`·`C18`·`C19` 신설 판정 — 2026-09-08 기준 3관점 재수립 중이라 **미확정**). 통보 154 의 경고가 실제로 발생하는 경로다. 값은 **우리가 고르지 않는다** |
| ③ | 과다부여 | `derived-permissions.ts:259` 의 `:request-iqc-skip` = `['W-01-13']` 는 두 표가 **합집합**이라 지울 수 없다. 요구서 §3 도출 축이라 손대지 않았다(「알려둘 것」 ⓖ) |

### 12-2. 슬라이스 마감표

| | 계획 | 실제(마감 때 채운다) |
|---|---|---|
| PR | **2** — ① 조회 3 · ② 쓰기 2 + 권한 | **3** — 계획 **#387** · ① **#390** · ② **#397** (전건 병합) |
| 오퍼레이션 | ① **3** · ② **2** = **5** | ① **3** · ② **2** = **5** — 계획대로 |
| 커버리지 | **389 → 394/487**(+5 · base `dd4032e`) | ⭐ `main` **401/487**(`3d0443a`) — 이 슬라이스 기여 **+5**. 절대값이 계획보다 큰 것은 레인 B·C 가 그 사이 병합했기 때문이다 |
| 마이그레이션 | **0** | **0** — 계획대로 |
| 코어 | **0** — `core/approval`·`core/numbering` 을 «쓰기만» 한다 | **0** — 계획대로 |
| `transitions.ts` · `error-codes.ts` · `schema.prisma` | **0 · 0 · 0** | **0 · 0 · 0** — 계획대로 |
| e2e | 신 1파일(8) + `trace-lot.e2e-spec.ts` +13 = **21** · 단위 **10** | 신 1파일(8) + `trace-lot` +18 = **26** · 단위 **17**. 리뷰가 빈 단언을 잡아 늘었다 |
| 비테스트 줄 | ① ~250 · ② ~245 (예산 350) | ① **238** · ② **385** — ②가 예산 350 을 35 넘겼다(한도 400 이내). 리뷰 판정 「500 을 막는 가드 둘 + 도메인 주석이라 깎지 말 것」 |
| 신규 문의 | **4건 — 통보 150·151·152·154**(질의 0 · 「알려둘 것」 8) | **4건 · 계획대로**(질의 0). ⚠ 통보 153 은 재수립 R-9 로 «철회»했다 — `plan-uiux.md:852` 가 0단계 선례로 이미 답했다 |
| 레인 A 조율 | **4건**(§11-2) | **3건 남음** — ④(`lot-view.ts`·`trace-lot.e2e-spec.ts` 겹침)는 I-20 병합으로 해소됐다 |
| 리뷰가 잡은 것 | — | 3관점 재수립 **Blocker 1 · Major 10**(R-1~R-10) · PR ① 리뷰 **Major 1 · Minor 2** · PR ② 리뷰 **Minor 3**. ⭐ 세 번 다 같은 종이었다 — **「코드는 맞는데 그것을 지켜보는 단언이 없다」**. 전부 변이를 실제 주입해 닫았다 |
