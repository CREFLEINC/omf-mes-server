# I-19 · 검사 — 의뢰·결과·측정치·판정 확정(11건) — 개별 계획안

> 브리프: `brief-I-19.md`. 산출물은 이 파일 하나다(구현 금지 · 계약·다른 문서·`git` 쓰기 금지).
> 레인: **A**(품질·공정 인계·첨부 · `lanes.md` §0). 브랜치 접두어 `feat/coverage-100-a-…` · PR 제목 접두어 `[A] ` · 문의 대역 **069~089**.
> 통합 계획서 해당 절: `plan.md` §1 **49행**(15번 · 선행 I-7 ✅ · 마이그 **M-e** · 코어 「품질 축 전이표」 · opus · PR 4) · §2 **85행**(M3 체인) · §3 **「LOT 품질 축 전이」 행**(계약이 이름 적은 전이만 등록 · 미등록은 던진다) · §4 **128행**(M-e) · §5 규칙 1·2·3·4·5·6·7·9·10 · §7(문의 후보 3줄 — 처분 전이 · `lot-hold-events` · **LOT 품질 판정 축**) ·
> `plan-api.md` **S18**(462~486) · **§5.1-D 832행**(`inspection_result.status_code` `DRAFT→CONFIRMED`) · **§5.1-E 838~875행**(LOT 품질 축 — 「이 계획 최대의 갈림길」) · **721행**(위험 1) · **1107행**(채번 규칙 ❌) · **1142행**(위험 3) ·
> `plan-uiux.md` **U7**(41행 · 170~181행 · 8건 · 화면 `W-01-01`·`P-02-13`) · **U39**(73행 · 518~526행 · 5건 · 화면 `W-03-05`) · **696행**(§6 위험 8 — 재검 사슬 페이지) · **900·904·920행**(§7-1 목록 세 갈래) · **932~955행**(§7-2 집계 ①③④) · **1094행**(오프라인 큐는 언제나 확정) ·
> `plan-integration.md` **183행**(I-19 행 — 마이그 ⚠#280 · 원장 ✕ · 상태기계 ⭐ · PR 4) · **152행**(§2 LOT 상태 전이 — 「품질 축은 I-19 에서 채우고 I-20·I-21·I-23 이 재사용」) · **366~374행**(§3-1 I-19) · **401행**(§I-23 레인 간 선행) · **519행**(§4-2 M3) · **574행**(공유 코어 5) · **604행**(M-e) · **879~891행**(§10 부록 I-19 전건 11).
> 선행 결과물: **`I-7.md`**(실적 · 검사의 입력원) · `I-24.md` §0·§9·§10·§11·§12(형식의 본보기) · `I-12.md`(전이 키 신설 선례) · `I-6.md` R-12(`LotLifecycleService.moveWithin()` 를 코어 PR 로 뗀 선례) · `I-3.md`(`core/lot` 등록 코어 · 입하 LOT 이 `INSPECTION_PENDING` + `lot_hold` 로 태어난다).
> 실측 기준: `main` **771c541** · 계약 사본 `contracts/COMMIT.txt` = **a6a87e1** · 실측일 **2026-09-07** · 코드는 **I-24 병합본**(#273~#278) · 설계 사본 `.design-reference/omf-mes/design/`(고정).
> 커버리지: 342 → I-19 11건으로 **353**.
> ⛔ **DB 를 읽지 않았다**(브리프 §8 금지). 물리 사실은 전부 `prisma/schema.prisma`·`prisma/migrations/*/migration.sql`·`prisma/seed.ts` 의 정적 실측이다 — 표 행수를 인용하지 않는다.

---

## ⭐ 0-재수립. 3관점 재검토 결정 — R-1 ~ R-21

> 2026-09-07 · 계획안이 나온 뒤 **api · uiux · integration** 세 관점이 각각 새 컨텍스트로 재검토했다.
> **이 표가 아래 본문을 이긴다.** 본문과 어긋나면 여기가 정본이고, 구현 브리프는 이 표를 먼저 싣는다.
> 세 관점의 리뷰 원본은 세션 스크래치패드(`I-19-review-{api,uiux,integration}.md`).
> ⭐ **부록 반증 0건** — 세 관점 누구도 계획자의 「실측 부록」 76행을 뒤집지 못했다(재측정 2건, 값 동일). 뒤집힌 것은 본문 도출값 하나뿐이다(R-20).

### R-1 ~ R-4 · 「리뷰가 반드시 볼 자리 5」 판정

| R | 자리 | 세 관점 | **판정** |
|:-:|---|---|---|
| **R-1** ⭐⭐ | §0 #2 **전이 `from` 집합** | 계획자 3값 상수 · **api 반대** · uiux **조건부(결론 유지·근거 반박)** · integration 찬성 | ⛔ **계획자 안을 뒤집는다 — 액션별 `from` 으로 가른다**(`JUDGED` 상수 폐기). `DEFECTIVE` 는 **`stock-reinstate` 의 `from` 에만** 넣는다. 근거: 계약이 두 자리에서 이름 적었다 — `contracts/quality-03품질.json:4472` 「**불량(Hold)은 발신 전이가 0**」 · `contracts/shipment-04제품출하.json:431` 「⭐ **이 경로에서만** 반영 목적의 **Hold → 정상** 전이가 허용된다(B-13)」. 화면 정본도 같다(`W-03-02` §5-5 도식 = `Hold` 발신 0 — uiux 가 독립 확인). integration 이 댄 `W-03-02:222` 는 **도착값(`to`) 축**이라 `from` 을 반박하지 못한다. §2 2단계 **기준 2**(거부하는 쪽 — 거부→허용은 호환 완화, 반대는 깨는 변경) |
| **R-2** ⭐ | §0 #3 **`ck_inspection_result_qty` 완화** | 셋 다 찬성 · uiux 「**절반이다**」 | ✅ 채택 **+ 항목 하나 더**. 실측: `inspection_result.overall_judgment_code String` = **NOT NULL**(`prisma/schema.prisma`). CHECK 만 풀고 이 칸을 두면 **임시 저장 본길이 여전히 막힌다**(`P-02-13` §5-9 「임시 저장은 항상」). ⇒ **M-e ⓒ 신설** — `overall_judgment_code` NOT NULL 해제 |
| **R-3** | §0 #1 회신 E-3 · §0 #5 의뢰 미생성 | **셋 다 찬성** | ✅ 계획자 안 그대로. 단 낡은 자리가 2곳이 아니라 **4곳**이고(integration) `plan.md` §4-2 **M3 완료 조건**에 「의뢰 마디는 픽스처」를 명시해야 한다 |
| **R-4** ⭐ | §0 #4 **보류 해제와 사유 코드** | api 조건부 · uiux **부분 반대** · integration 조건부 | 🔶 **셋으로 가른다.** ⓐ **해제는 여기서 한다**(계약 `:3621`·`:3749` 두 자리 · 화면이 그렸다) ⓑ ⛔ **불합격은 해제하지 않는다** — `W-01-01` §5-1·§2 「불합격 = Hold 유지 → 반품」(✓확정 2026-07-20). 계획안은 「보류(HELD) 판정만 유지」라 적어 **불합격을 빠뜨렸다**. ⇒ **해제는 합격 확정만** ⓒ ⛔ **`RETEST_PASS`/`RETEST_FAIL` 을 쓰지 않는다** — `W-03-02` §5-4 가 그 둘을 **C7·C8 전용**으로 못 박고 「재판정으로 풀린 건」 집계에 쓴다(uiux). 「1회차 합격」에 붙일 값이 **시드 4값에 없다** ⇒ 값 선택은 **PR ④ 구현 브리프에서 시드 실측 후 확정**하고 e2e 단언에 `// 설계 미정 — 문의 069+3` 을 단다. 문의 069+3 본문을 「**1회차 합격에 붙일 사유 값이 없다**」로 다시 쓴다 |

### R-5 ~ R-11 · 계획자가 보지 않은 자리 (구현 전 반드시 고친다)

| R | 무엇 | 근거 | **판정** |
|:-:|---|---|---|
| **R-5** ⭐⭐ | **`test/document-state.e2e-spec.ts` 가 계획안에 없다** — PR ① 이 세 자리를 깬다 | `:40-49` `STATUS_GROUPS` 축 6개 · **`:68-78` 이 축을 안 거르고 전이코드 전건 일치를 단언**(C4~C14 가 들어오면 반드시 실패) · `:80-91` 「품질 판정 축은 아직 등록할 수 없다」 | §8 에 **넷째 파일**로 넣는다. PR ① 이 같은 커밋에서 고친다. 선례 `I-4.md:773`(STATUS_GROUPS +2) |
| **R-6** ⭐⭐ | **PR ① 만으로는 레인 C 의 I-23 이 안 풀린다** | `lot_status_event.transition_code` NOT NULL · 형제 코어가 코드 없으면 던진다(`lot-lifecycle.service.ts:42`) · 계약 enum 9값에 재등록이 없다 | §11 ① 첫 문장(「병합되면 바로 진행 가능」)을 **삭제**하고 「**전이 코어는 넘어가지만 `transitionCode` 가 없어 C 는 여전히 막힌다 — 문의 069+12 의 답이 있어야 한다**」로 바꾼다. ⛔ **사용자를 통해 레인 C 에 즉시 보고**(`lanes.md` §3) |
| **R-7** ⭐ | **C14 가 LOT 하나 때문에 통째로 400** | 형제 코어는 `from` 밖이면 **건너뛴다**고 주석으로 못 박았다(`lot-lifecycle.service.ts:27-30,56-60`) · `plan.md` §3 이 `{movedLotIds, skippedLotIds}` 반환을 규약으로 적었다 | `assertTransition(…,400)` 폐기. **형제와 같은 모양으로 건너뛰고 `{moved, skipped}` 를 돌린다.** W/O 안에 `SCRAPPED` 가 하나 섞였다고 PQC 확정이 막히면 안 된다 |
| **R-8** | **409 봉투의 `code` 가 required 인데 실을 자리가 없다** | `idempotency.service.ts:115,121` 이 `code` 없이 던진다 · `ConflictExtra`(`conflict.exception.ts:22-27`)에 `currentLotStatusCode` 칸이 없다 | §7-4 「공용 파일 0줄」을 **정정**. 공용 예외 타입을 고쳐야 한다 ⇒ PR ① 비테스트 예산 **≤170 → ≤180** |
| **R-9** | **400 `STATE_LOCKED` vs 409 `INVALID_STATE` 를 계약이 스스로 미결로 적었다** | `contracts/quality-03품질.json:4589` 「400 STATE_LOCKED 가 0건인 것은 결손이 아니다 … 어느 쪽이 정본인지는 **횡단 단일안 소관**」 | §9-1 에 **행을 더한다**. 형제 선례로 400 을 고른 근거를 그 문장이 금지하므로, §2 **1단계 본길 → 계약 문자 그대로 + 문의**(069+15 신설) |
| **R-10** ⭐ | **OQC 확정이 어느 LOT 도 안 옮긴다** | `W-04-03` §5-2 「판정이 Lot Status 를 바꾼다 — 합격→Release, 출하가 풀립니다」 · §4-A 「`lot_id` nullable — 다형과 병존」 | §3-3 의 대상 LOT 규칙을 **「`targetTypeCode` 무관 · `lot_id` 가 있으면 그 1건」**으로 바꾼다 |
| **R-11** ⭐ | **합격 확정이 «다른 열린 보류»를 무시하고 LOT 을 푼다** | 중복 보류가 허용된다(`W-03-02` §4-B) — `INCOMING_INSPECTION_WAIT` 만 닫고 `NORMAL` 로 올리면 **의심자재 보류가 열린 채 출고가 풀린다** | `:confirm` 은 **열린 보류가 더 있으면 `NORMAL` 로 올리지 않는다**. 트랜잭션 순서(§3-1)에 「남은 열린 보류 수 조회」를 넣는다 |

### R-12 ~ R-17 · 값·기본값·근거 정정

| R | 무엇 | **판정** |
|:-:|---|---|
| **R-12** ⭐ | **`finalRoundOnly` 기본값이 없다**(계약에 `default` 키 부재 — uiux 실측) | 도면 수가 증명한다(요약 412 · 목록 412 · **결과 ~450**) ⇒ **집계 3건은 `true` · 목록은 `false`** 를 기본값으로 §4-5 에 못 박고 e2e 1건을 더한다. false 로 구현하면 **불량률이 반토막** |
| **R-13** ⭐ | **교정 만료 판정이 장비 마스터 두 칸을 안 본다** | 실측: `equipment.calibration_required Boolean @default(false)`(`schema.prisma:1680`) · `calibration_due_date DateTime?`(`:1682`). 「이력 0건 = 만료」면 **교정 불요 장비가 전부 만료**로 잡혀 화면 경고가 상시 켜진다 ⇒ §4-4 판정을 **두 칸을 먼저 보는 것**으로 바꾼다 |
| **R-14** | **표시 라벨 칸이 0 · 큐 정렬 축 부재** | `InspectionRequest` 17칸에 `lotNo`·품목명이 없고(대기 큐가 LOT 번호 행이다) `InspectionResult` 에 `itemCode` 가 없다. 큐는 `Table(sort)` 인데 계약에 `sort` 가 없다 ⇒ §1-4 의 「결손 0」을 **정정**하고 문의 **069+2 를 정렬 축까지 넓힌다**(선례 문의 047) |
| **R-15** | **`processId` 판정의 근거가 다른 축의 문장** | 계약이 대는 `W-03-05` §5-2 는 `defect_record.sourceCode` 이야기이고 「2단 조인 유도 방식을 **버렸다**(2026-08-30)」로 끝났다. 게다가 `:3429` 「IQC·OQC 의뢰는 서버가 언제나 채워 내린다」가 전제를 부순다 ⇒ **문의 069+5 본문을 다시 쓴다**(안 고치면 설계팀 왕복 1회 손해) |
| **R-16** | **C15·C16 을 도식 정본이 이미 정해 놨다** | C15 = 「전수 재검 양품」(C14 뒤 합격) · **C16 = 재검 불량분 → 생산불량LOT 경계 위임**(시드에 없다). 「가릴 표식이 없다」는 사실과 다르다 — 표식은 이 슬라이스가 쓰는 `transition_code='C14'` 행이다 ⇒ 결론(짓지 않는다)은 유지하고 **근거·문의 069+8·§11 ③ 을 고친다** |
| **R-17** | 기간 규칙이 셋이 아니라 **둘** | `inspectedFrom`/`To` 는 **한 쌍**(`:830`) · 목록은 조건부(`:753`)인데 `summary`(`:1157`)·`trend`(`:1055`)는 **무조건**이다. 공용 헬퍼 하나로 뭉치면 `summary?inspectionRequestId=…` 가 조용히 200 을 낸다 ⇒ §4-5 에 두 갈래로 적는다 |

### R-18 ~ R-21 · PR 분할과 e2e

| R | 무엇 | **판정** |
|:-:|---|---|
| **R-18** ⭐⭐ | **PR 분할 5 → 7** · M-e 를 **선행 PR** 로 뗀다 | 이유 셋: ⓐ **PR ② 예산이 180~220줄 넘친다**(§7-1 파일 합 ~510 vs 예산 330 — 총량은 맞고 배분이 틀렸다) ⓑ **PR ② 픽스처의 셋째 갈래(`inspection_plan_version_id = null`)가 M-e ⓐ 를 필요로 한다**(실측: 그 칸은 NOT NULL) ⓒ ⑤ 를 별도 컨트롤러로 떼면 `ParseIntPipe`/`'summary'` 함정이 **구조적으로 사라진다**. ⇒ 새 순서: **M-e(마이그 단독) ∥ ②a(의뢰 조회 2 + 골격 + 픽스처) → ①(전이 코어) → ②b(결과 조회 2) → ③(쓰기 2) → ④(`:confirm`) → ⑤(집계 3 + 측정치 · 별도 컨트롤러 · 병렬 가능)**. ⚠ **②a 는 이미 이 결정대로 좁혀 스폰했다**(2026-09-07 · 셋째 픽스처 갈래 제외 · 커버리지 +2) |
| **R-19** ⭐ | **e2e 픽스처가 입하 사슬을 통째로 빠뜨렸다** | `POST /trace/lots` 가 `inbound_receipt_line` 을 UPDATE 하므로(`lot-registry.service.ts:119-121`) **partner → inbound_receipt → inbound_receipt_line** 이 먼저 서야 한다. §8-1·§8-2 에 없다 ⇒ 픽스처와 정리 순서에 넣는다. §8-3 #4(`supplierId`)는 그 전엔 **원리상 통과 불가** |
| **R-20** | 본문 도출값 오류 | `document-state.spec.ts` 의 개수 단언은 **38** 인데 본문 두 곳이 **40** 이라 적었다 ⇒ 정정(부록은 맞았다) |
| **R-21** | 인덱스 0 판정의 **근거**를 바꾼다 | C14 가 매번 도는 `trace.lot(source_type_code, source_id)` 를 §2-5 가 안 봤다. **인덱스 0 은 유지**하되 근거를 「규모」에서 **「선례 5곳과 같게 둔다」**로 바꾼다 |

**요약** — 판정 5 중 **1건 뒤집힘(R-1)** · 2건 조건부(R-2·R-4) · 2건 그대로(R-3). 계획자가 안 본 자리 **17건**. PR 분할 **5 → 7**. 마이그 M-e **2항목 → 3항목**. 새 문의 **14 → 15건**.
⛔ **PR ①(전이 코어)은 R-1·R-5·R-7·R-8 을 반영한 뒤에만 연다.**

---

## ⭐ 0. 리뷰가 반드시 볼 자리 5 (README §1-2 · 2026-09-07 규칙)

> 옛 「차이가 크다」 조건 판정 절은 쓰지 않는다. 아래 다섯이 **3관점 재검토의 판정 항목**이고,
> **뒤집히면 PR 분할이나 심장의 갈래가 바뀌는 자리**다. 통합자의 리뷰 브리프는 이 다섯을 그대로 쓴다.

| # | 자리 | 이 계획안의 판정 | 뒤집히면 |
|:-:|---|---|---|
| **1** ⭐ | **회신 E-3 은 이미 종결됐다 — `transitions.ts` 의 「대기」 주석과 그 단언이 낡았다** | 설계 사본 실측: `LOT상태-확정기록.md:5` 「확정일 **2026-08-07**」 · `P-02-13:435` 「~~회신 E-3 판정 유형 전체 목록~~ ✅ **해소 2026-08-07**」 · `W-06-04:201` 동상 · 계약이 `overallJudgmentCode` 설명에 「근거: **회신 E-3 종결 2026-08-07**」을 직접 적었다(`contracts/quality-03품질.json:3671` 인접). ⇒ `plan-integration.md:369` 가 맞고 `transitions.ts:37-42` 가 낡았다. **전이표를 채운다.** | PR ① 이 통째로 사라지고 **레인 C 의 I-23 재등록이 영구히 막힌다**(`lanes.md` §0) |
| **2** ⭐ | **전이의 `from` 집합을 `INSPECTION_PENDING` 하나로 좁힐 것인가** | ⛔ **좁히지 않는다.** 좁히면 ⓐ **재검 회차 확정이 전부 막힌다**(1회차가 `DEFECTIVE` 로 보낸 LOT 을 2회차가 확정한다 — 계약 `inspectionRound`·`previousResultId` 가 정면으로 설계한 길) ⓑ **OQC 확정도 막힌다**(제품LOT 은 PQC 를 지나 `NORMAL` 이다). ⇒ `from` = `['NORMAL','INSPECTION_PENDING','DEFECTIVE']` · `SCRAPPED` 만 뺀다(계약에 `SCRAPPED` 로 «가는» 전이도 «나오는» 전이도 없다 — `plan-api.md:871`). 0단계 선례: `transitions.ts:256` 이 도달 불가한 `POSTED` 를 「계약 문장대로 두고 좁히지 않는다」로 남겼다 | 심장(§3)의 400/409 갈래와 e2e 이름 6개가 바뀐다. 좁히는 쪽이면 재검·OQC e2e 가 `STATE_LOCKED` 단언으로 뒤집힌다 |
| **3** ⭐ | **`ck_inspection_result_qty` 가 무조건이라 임시 저장이 500 이다** | 실측: `20260727000000_baseline_physical_model_v3/migration.sql:1870-1871` = `CHECK (accepted_qty + rejected_qty + held_qty = inspected_qty)` — **조건 없음**. 계약 `InspectionResultCreate` 설명(`contracts/quality-03품질.json:3789`)은 「⛔ statusCode=확정 이면 … 강제한다 … **작성중은 통과시킨다**」. ⇒ **M-e 에 둘째 항목**을 싣는다 — CHECK 를 `status_code <> 'CONFIRMED' OR (합)` 으로 **완화**(0단계 선례: `20260901010000` 이 `ck_lot_initial_qty` 를 같은 모양으로 갈았다) | M-e 가 1항목이 되고 PR ③ 의 마이그 커밋이 줄어든다. 안 고치면 **`POST` `statusCode=DRAFT` 의 본길이 P2010/500** 이다 |
| **4** ⭐ | **`:confirm` 이 `lot_hold` 를 해제하는가 · 해제 사유 코드에 무엇을 넣는가** | 계약이 「확정이면 Lot Status 전이와 **보류 해제**가 따른다」(`:3621`·`:3749`)라 적었고 `W-01-01:123` 도 `released_at`·`released_by` 를 적었다 ⇒ **1단계 본길 · 계약 문자 그대로 해제한다.** 그런데 `20260901030000/migration.sql` 의 `ck_lot_hold_release_reason` 이 **사유를 강제**하고 시드 `LOT_HOLD_RELEASE_REASON` 4값(`prisma/seed.ts:438-446`)은 전부 「재검사/조사/직권」이라 **1회차 합격의 사유가 없다.** ⇒ 대상을 `reason_code='INCOMING_INSPECTION_WAIT'` + `released_at IS NULL` 로 좁히고 판정에 따라 `RETEST_PASS`/`RETEST_FAIL` 을 넣는다 · **보류(HELD) 판정은 해제하지 않는다** · 문의 **069+3**. **대안(리뷰가 고를 수 있다): 해제를 I-20 `:release` 로 미룬다** | ④ 의 한 트랜잭션 순서와 I-19/I-20 경계가 바뀐다. 미루는 쪽이면 입하 LOT 이 검사 합격 뒤에도 홀드가 열린 채 남는다 |
| **5** ⭐ | **검사 의뢰를 «만드는» 코드가 저장소에 0건이다** | 실측: `grep -rn inspection_request src/` 의 쓰기 사용처 **0**(맞는 것은 `inspection-plan-version.service.ts:23` 의 참조 카운트 표 한 줄뿐). 계약은 `InspectionRequest` 설명에서 「⛔ **등록 경로를 두지 않는다** … 서버가 입고·생산 실적에서 만든다」(`:3521`)라 적었고 I-3(입하)·I-7(실적)은 이미 병합됐는데 만들지 않는다. ⇒ ⛔ **이 슬라이스가 의뢰 생성을 짓지 않는다**(0단계 선례 — `I-24.md` §8-1·§11 ④ 가 ERP 수신기를 같은 이유로 범위 밖에 두고 픽스처를 prisma 로 심었다). e2e 가 `inspection_request` 를 prisma 로 직접 INSERT 한다 · 문의 **069+1** | 뒤집히면 `src/logistics/inbound-receipt/`·`src/production/production-result/` 를 고쳐야 하고(레인 C 소유 파일과 겹친다) PR 이 하나 늘며 §5-9 주체 규칙까지 다시 본다 |

**오퍼레이션 11/11 대조** — `awk -F'\t' '$1=="I-19"' docs/coverage-100/assignment.tsv` = **11행**, 브리프 §2 목록 11건, `plan-integration.md:881-891` 11건, 계약 실물 11건이 **전부 같다**(python 대조 — 총 27 오퍼레이션 중 11 일치 · 누락 0).

---

## 1. 계약 읽기 표 — 11 오퍼레이션 전건

### 1-1. 횡단 4축 — 실측(python · `contracts/quality-03품질.json`)

| 오퍼레이션 | 멱등 | If-Match | ETag | 403 | 성공 | 그 밖 응답 |
|---|:-:|:-:|:-:|:-:|:-:|---|
| `GET /quality/inspection-requests` | — | — | — | — | 200 | — |
| `GET /quality/inspection-requests/{inspectionRequestId}` | — | — | — | — | 200 | 404 |
| `GET /quality/inspection-results` | — | — | — | — | 200 | **400** |
| `GET /quality/inspection-results/defect-rate-trend` | — | — | — | — | 200 | **400** |
| `GET /quality/inspection-results/summary` | — | — | — | — | 200 | **400** |
| `GET /quality/inspection-results/{inspectionResultId}` | — | — | **✓** | — | 200 | 404 |
| `GET …/{inspectionResultId}/measurement-summary` | — | — | — | — | 200 | 404 |
| `GET …/{inspectionResultId}/measurements` | — | — | — | — | 200 | ⭐ **404 미선언** |
| `POST /quality/inspection-results` | ✓ | **선택**(`IfMatchVersionOptional`) | — | ✓ | **201** | 400 · 403 · 409 ⭐ **404 미선언** |
| `PUT …/{inspectionResultId}` | ✓ | **필수** | — | ✓ | 200 | 400 · 403 · 404 · 409 |
| `POST …/{inspectionResultId}:confirm` | ✓ | **필수** | — | ✓ | 200 | 400 · 403 · 404 · 409 |

- **`X-Worker-No`** — 11건 중 **1건만**(`POST /quality/inspection-results` · `WorkerNoOptional` · `required=false`). ⇒ `plan.md` §5-9 의 「없으면 400 `REQUIRED`」 열 자리 목록에 **들어가지 않는다**(그 목록은 전부 `WorkerNo` 필수형이다). §5-5 에서 주체 해석을 따로 판정한다.
- **409 봉투는 전건 `QualityConflictResponse`** — `code` **required** · enum **6값**(`VERSION_CONFLICT`·`DUPLICATE_KEY`·`INVALID_STATE`·`DUPLICATE_HOLD`·`HOLD_QTY_EXCEEDED`·`DISPOSITION_QTY_EXCEEDED`). 이 슬라이스가 쓰는 것은 앞 셋뿐이고 뒤 셋은 I-20·I-21 몫이다.
- ⭐ **`POST` 는 404 를 선언하지 않는다** — 없는 `inspectionRequestId`·`uomId`·`previousResultId` 는 **400 `INVALID`**(FK 그물)로 낸다. `I-24.md` §9-3 ⓕ 와 같은 방향, I-2·I-3 의 「404 미선언인데 404 를 낸다」와는 **반대** 방향이다(그쪽은 「알려둘 것」에 남아 있다) — 여기서는 계약을 따른다.
- ⭐ **`GET …/measurements` 는 404 를 선언하지 않는다** — 없는 결과 id 면 **빈 목록 + `page.total=0`** 이다(자식 컬렉션 GET · `plan.md` §5-4 가 ETag 도 안 붙인다고 적은 그 부류).
- ETag 는 **상세 1건뿐**(`GET …/{inspectionResultId}`). `plan-uiux.md:179-181` 표가 `POST`·`PUT`·`:confirm` 헤더를 「멱등, **ETag**」로 적었으나 **계약 실측은 요청 쪽 `If-Match`** 이고 응답 ETag 는 상세 GET 하나다(브리프 「계약 실측이 이긴다」).

### 1-2. 조회 8건 — 질의 칸 전수 ↔ 가리키는 칸

| 오퍼레이션 | 질의 | 가리키는 물리 칸 |
|---|---|---|
| `GET …/inspection-requests` | `inspectionTypeCode`(`QUALITY_INSPECTION_TYPE`) | `inspection_request.inspection_type_code` |
| | `statusCode`(`INSPECTION_REQUEST_STATUS`) | `status_code` |
| | ⭐ **`pendingOnly`**(bool·default **false**) | 파생 — 계약이 「`pendingOnly=true` ⇔ `statusCode ∈ {REQUESTED, IN_PROGRESS}`」로 **정의를 값으로 못박았다** |
| | `itemId` | `item_id` |
| | ⭐ **`supplierId`** | ⛔ 칸 0 — 계약이 「서버가 푼다 … `lot.source_type_code`/`source_id` → `logistics.inbound_receipt.supplier_id` **2단 조인**」이라 적었다 |
| | `lotId` · `workOrderId` | 같은 이름 FK |
| | `q` | ⛔ **`inspection_request_no` «하나»** — 계약이 범위를 명시로 좁혔다 |
| | `page`(1부터)·`size`(기본 50) | 공용 `pageRequest` |
| `GET …/inspection-results` | `inspectionRequestId` | `inspection_result.inspection_request_id` |
| | `inspectionTypeCode` | 의뢰 조인 — `inspection_request.inspection_type_code` |
| | `overallJudgmentCode`(`ACCEPTED`·`REJECTED`·`HELD`) | `overall_judgment_code` |
| | `statusCode`(`DRAFT`·`CONFIRMED`) | `status_code` |
| | ⭐ **`processId`** | ⛔ 칸 0 — §2-3 |
| | `itemId` | 의뢰 조인 — `inspection_request.item_id` |
| | `inspectedFrom`·`inspectedTo`(date-time) | `inspected_at` gte/lte · **조건부 필수** |
| | ⭐ **`finalRoundOnly`**(bool) | 파생 — §4-2 |
| | ⭐ **`calibrationExpired`**(enum `only`·`exclude`) | 파생 — §4-4 |
| | `sort`(default **`inspectedAt,desc`**) | 허용 키 **3**(`inspectionRequestNo`·`inspectedAt`·`rejectedQty`) · 동률은 `inspection_result_id` |
| | `page`·`size` | 공용 |
| `GET …/summary` | 위 축 중 `page`·`size`·`sort`·`lotId` 를 뺀 **10** | 필터 전체 기준 집계(L-1) |
| `GET …/defect-rate-trend` | `inspectionTypeCode`·`itemId`·`overallJudgmentCode`·`processId`·`finalRoundOnly`·`inspectedFrom`·`inspectedTo`·`calibrationExpired` **8** | 일자 버킷 |
| `GET …/{id}/measurements` | `inspectionItemSpecId` · `calibrationExpired` · `page` · `size` | `inspection_measurement.*` |
| `GET …/{id}` · `…/{id}/measurement-summary` · `…/inspection-requests/{id}` | (질의 0) | ETag = `version_no`(상세 1건만) |

- ⭐ **기간 유계 400 이 셋이다.** `inspection-results` 는 계약이 「⛔ `inspectionRequestId` 도 기간도 없으면 **400**」(`:753`)이라 적었고, `summary`·`defect-rate-trend` 는 `inspectedFrom` 설명이 「**필수** — 공유계약 L-3」이다(스키마 `required` 는 false — 형이 조건부를 못 담는다). ⇒ 셋 다 서버가 손으로 400 `REQUIRED` 를 낸다. `plan-uiux.md:900` 갈래 ①.
- ⭐ **`inspection-requests` 에는 기간 축이 아예 없다.** 계약이 「W-01-01 검사 대기 큐가 부른다」로 «미처리 전건 목록»(L-12) 성격을 적었다 — `pendingOnly` 가 그 자리다. ⇒ ⛔ 기간 400 을 여기 걸지 않는다.
- ⭐ **정렬이 열린 목록은 `inspection-results` 하나다**(`plan-uiux.md:904` 여덟 목록 중 하나). `inspection-requests` 는 `sort` 가 **없다** ⇒ 서버 기본 정렬 하나를 정해야 한다 — §4-1 · 문의 **069+2**.

### 1-3. 쓰기 3건 — 본문 스키마 실측

#### `InspectionResultCreate` — required **8** · 프로퍼티 **13**

| 칸 | required | 물리 |
|---|:-:|---|
| `inspectionRequestId` | ✓ | `inspection_request_id` |
| `inspectedQty` | ✓ | `inspected_qty Decimal(20,6)` · CHECK `> 0` |
| `acceptedQty` · `rejectedQty` · `heldQty` | ✓ ✓ ✓ | 같은 이름 · DEFAULT 0 |
| `uomId` | ✓ | `uom_id` |
| `inspectedAt` | ✓ | `inspected_at` |
| `statusCode` | ✓ | `status_code` — `DRAFT`·`CONFIRMED` |
| `overallJudgmentCode` | — | `overall_judgment_code` **NOT NULL** ⚠ — 계약은 「`statusCode=확정` 이면 필수」 ⇒ §2-2 |
| `previousResultId` | — | `previous_result_id?` — 「회차는 서버가 +1」 |
| `reinspectionReasonCode` | — | `reinspection_reason_code?`(`REINSPECTION_REASON`) |
| `measurements[]`(`InspectionMeasurementInput`) | — | `inspection_measurement` N행 — ⭐ **필수가 아니다**(기준 없는 의뢰 갈래) |
| `remarks` | — | `remarks` |

- ⛔ **`inspectorId`·`terminalId`·`inspectionResultNo`·`inspectionRound` 를 본문이 안 받는다** — 서버가 채운다(§5-5·§5-3).
- `InspectionMeasurementInput` — required **4**(`inspectionItemSpecId`·`sampleNo`·`judgmentCode`·`measuredAt`) · 프로퍼티 **8**. 값 세 칸(`numericValue`·`textValue`·`booleanValue`)은 **≤1** 만 채운다(`ck_measurement_single_value` · 전부 NULL 도 통과 = 「미측정」).

#### `InspectionResultUpdate` — required **0** · 프로퍼티 **8**

`inspectedQty`·`acceptedQty`·`rejectedQty`·`heldQty`·`overallJudgmentCode`·`inspectedAt`·`measurements`·`remarks`.
⛔ `statusCode` 가 **없다** — `PUT` 으로 확정할 수 없다(확정 경로는 둘: `POST statusCode=CONFIRMED` · `:confirm`).
⛔ `previousResultId`·`reinspectionReasonCode` 가 **없다** — 회차 사슬은 못 고친다(B-10 「번복은 재검이지 수정이 아니다」).
⚠ **`[string,null]`·`[integer,null]` 형이 하나도 없다** — 명시 null 로 «해제»하는 칸이 0 이다. ⇒ `optional()` 관행 중 **null 갈래를 쓰지 않는다**.
⭐ `measurements` 는 **치환**이다(부분 병합이 아니다) — 실을 때 그 결과의 측정치 전건을 갈아 끼운다. 근거: `uq_inspection_measurement (result, item_spec, sample_no)` 가 부분 갱신의 키를 주지만 계약이 「부분」을 적지 않았다 ⇒ §2 2단계 기준 4(조용히 도출하지 않는 쪽) → **보낸 배열이 곧 전부**. 배열을 **생략하면 손대지 않는다**(빈 배열과 다르다).

#### `InspectionResultConfirm` — required **0** · 프로퍼티 **2**

`overallJudgmentCode`(「비우면 저장된 값을 쓴다」) · `remarks`.
⭐ **본문이 «선택 2칸»뿐인데 If-Match 는 필수다** — 몸이 비어도 헤더가 잠금을 진다.

### 1-4. 응답 스키마 — 채울 수 없는 칸 / 담을 데가 없는 칸

#### `InspectionRequest` — required **10** · 프로퍼티 **17**

`inspectionRequestId`·`inspectionRequestNo`·`inspectionTypeCode`·`targetTypeCode`(enum **3값** `LOT`·`WORK_ORDER`·`SHIPMENT_REQUEST`)·`targetId`·`itemId`·`targetQty`·`uomId`·`statusCode`·`requestedAt`(required 10) + `inspectionPlanVersionId`(**`[integer,null]`**)·`lotId`·`workOrderId`·`productionResultId`·`coverageFromAt`·`coverageToAt`·`versionNo`.

**채울 수 없는 칸 0 · 담을 데가 없는 칸 0.** 17칸이 물리 `inspection_request` 와 1:1 이다.
⚠ **방향이 반대인 결손이 하나** — 계약 `inspectionPlanVersionId` 는 **nullable**(`:3429`)인데 물리는 **NOT NULL**(`schema.prisma:3343` · baseline `:1819-1820`). ⇒ **M-e ⓐ**(§2-4).

#### `InspectionResult` — required **13** · 프로퍼티 **26**

| 칸 | 원천 | 비고 |
|---|---|---|
| `inspectionResultId`·`inspectionResultNo`·`inspectionRequestId`·`inspectionRound`·`inspectedQty`·`acceptedQty`·`rejectedQty`·`heldQty`·`uomId`·`overallJudgmentCode`·`inspectorId`·`inspectedAt`·`statusCode` | `inspection_result` 같은 칸 | required 13 |
| `confirmedAt`·`terminalId`·`previousResultId`·`reinspectionReasonCode`·`remarks`·`versionNo` | 같은 칸 | — |
| **`inspectionRequestNo`·`inspectionTypeCode`·`itemId`·`lotId`** | **의뢰 조인** | 「목록 한 행을 그리는 데 필요해 함께 내린다」 |
| **`lotNo`** (`[string,null]`) | 파생 — `lot.lot_no` | 「서버가 `lotId` 를 풀어 내린다」 |
| **`processId`·`processName`** (둘 다 nullable) | ⛔ **칸 0** — 파생 | §2-3 · ⭐ 이 슬라이스의 둘째 갈림길 |

**채울 수 없는 칸 2**(`processId`·`processName` — 물리에 공정 축이 없어 조인으로 «판정»해야 한다) · **담을 데가 없는 칸 0**.
⛔ **`confirmedBy` 가 계약에도 물리에도 없다** — `W-04-03:164` 가 「비대칭 자체가 결손의 근거」라 적었지만 **#62 로 「분리하지 않는다」가 확정**됐다(`P-02-13:436`). ⇒ 새 칸을 만들지 않는다.

#### 집계 셋

- `InspectionSummary` — required **6**(`inspectionCount`·`inspectedQty`·`acceptedQty`·`rejectedQty`·`defectRate`·`asOf`) · 프로퍼티 **9**(+`heldQty`·`calibrationExpiredCount`·`finalRoundOnly`). ⭐ `finalRoundOnly` 를 **응답에 되돌려준다**(어느 기준으로 센 수인지).
- `DefectRateTrend` — required **2**(`points`·`asOf`) · `DefectRatePoint` required **4**(`bucket`(일자)·`inspectedQty`·`rejectedQty`·`defectRate`).
- `MeasurementItemSummary` — required **6** · 프로퍼티 **12**. ⭐ `outOfSpecValues` 는 **최대 10건 예시**, `outOfSpecTotalCount` 는 **전체 건수** — 「⛔ `rejectedCount` 로 역산하지 않는다」(계약 명시). `unmeasuredCount` = 값 세 칸이 전부 NULL 인 행.
- `measurement-summary` 200 은 인라인 `{asOf, items[]}` (required 2) · `measurements` 200 은 `{items[], page}` (required 2 · `PageMeta` = `page`·`size`·`total`).

### 1-5. 값 목록 — 계약이 준 것 / 시드에 있는 것

| 축 | 계약 | 시드(`prisma/seed.ts` 정적 실측) | 판정 |
|---|---|---|---|
| `QUALITY_INSPECTION_TYPE` | `IQC`·`PQC`·`OQC` | — | `assertCodeValues` 로 대조. ⚠ 설비 점검의 같은 이름 그룹과 **다른 값**이라 그룹을 가른다 |
| `INSPECTION_REQUEST_STATUS` | 확정 **5값** `REQUESTED`·`IN_PROGRESS`·`COMPLETED`·`SKIPPED`·`CANCELLED` | — | 그대로. ⛔ **우리는 안 옮긴다**(의뢰를 만드는 코드가 0건 — §0 #5) |
| `INSPECTION_RESULT_STATUS` | `DRAFT`·`CONFIRMED` · 「2026-09-02 등재」 | — | 전이표에 싣는다(§6) |
| `INSPECTION_RESULT_OVERALL_JUDGMENT` | **3값** `ACCEPTED`·`REJECTED`·`HELD` · 「근거: 회신 E-3 종결 2026-08-07」 | — | ⭐ 전이 갈래의 축이다 |
| `INSPECTION_MEASUREMENT_JUDGMENT` | **2값** `ACCEPTED`·`REJECTED` — 「종합 판정과 그룹이 **다르다** — 항목 판정에는 「보류」가 없다」 | — | ⛔ 종합 판정 그룹으로 대조하지 않는다 |
| `REINSPECTION_REASON` | 「고객이 늘릴 수 있다 — 계약이 값을 닫지 않는다」 | — | ⛔ 대조를 걸지 않는다(열린 그룹 · I-6 §9-1 #2 선례) |
| **`LOT_STATUS`** | `NORMAL`·`INSPECTION_PENDING`·`DEFECTIVE`·`SCRAPPED` | **4값** · `isSystemOwned` (`seed.ts:543-551`) | ⭐ 전이표의 상태 집합 |
| **`LOT_STATUS_TRANSITION`** | 계약에 없다(우리가 지은 잠정 이름) | **9값** `C4`·`C5`·`C6`·`C7`·`C8`·`C9`·`C10`·`C14`·`C15` (`seed.ts:517-530`) | ⭐ `lot_status_event.transition_code` 의 값 — §6 |
| `LOT_HOLD_RELEASE_REASON` | `LotHoldRelease.releaseReasonCode` required(I-20) | **4값** `RETEST_PASS`·`RETEST_FAIL`·`INVESTIGATION_CLEARED`·`MANAGER_OVERRIDE` (`seed.ts:438-446`) | ⭐ §0 #4 |
| `LOT_HOLD_REASON` | — | `INCOMING_INSPECTION_WAIT` 외 (`seed.ts:772`) | 해제 대상을 이 값으로 좁힌다 |
| `JUDGMENT_TYPE` | — | **값 0건**(`seed.ts` 「⛔ 값을 넣지 않는다 — 회신 E-1」) | ⛔ 이 슬라이스는 `judgment_type_control` 을 **읽지도 쓰지도 않는다**(I-20 몫 · I-4 R-7) |
| `inspection_result_no` 채번 | example **`IRS-2026-0812-0412-1`**(`:3534`) | 규칙 **미등재**(`NUMBERING_RULES` 는 `PRODUCTION_RESULT` 한 줄뿐 · `seed.ts:1426-1432`) | `DEFAULT_PREFIX` 에 **`INSPECTION_RESULT: 'IRS'`** 한 줄 · 기본 패턴 `IRS-{YYYYMMDD}-{SEQ4}` |
| `inspection_request_no` 채번 | example **`IR-2026-0812-0412`**(`:3415`) | — | ⛔ **더하지 않는다** — 만드는 오퍼레이션이 0건이다. ⚠ 그리고 `IR` 은 이미 `INBOUND_RECEIPT` 가 쓴다(`numbering.service.ts:15`) — `I-24.md` §9-1 #10 의 `PO` 충돌과 같은 모양 · 문의 **069+1** 안에 싣는다 |
| `entity_type_registry` | — | `INSPECTION_REQUEST`·`INSPECTION_RESULT` **둘 다 실재**(`seed.ts` ENTITY_TYPES) | `lot_status_event.source_document_type_code` 에 `INSPECTION_RESULT` 를 넣는다 — 지어내지 않는다 |

### 1-6. 에러 코드 — **새 코드 0건**

| 자리 | 코드 | 봉투 |
|---|---|---|
| 기간·의뢰 둘 다 없는 목록 조회 | `REQUIRED` | 400 |
| 수량 합 ≠ 검사 수량(확정일 때) | `INVALID` | 400 |
| 확정 필수인데 `overallJudgmentCode` 없음 | `REQUIRED` | 400 |
| 값 목록 밖 코드(`overallJudgmentCode`·`judgmentCode`·`statusCode`) | `INVALID` | 400 |
| FK 부재(`inspectionRequestId`·`uomId`·`previousResultId`·`inspectionItemSpecId`) | `INVALID` | 400(`POST` 는 404 미선언 · §1-1) |
| `PUT`·`:confirm` 대상 없음 | — | **404**(계약 선언) |
| 확정된 결과를 `PUT` | **`INVALID_STATE`** | **409** ⭐ 계약이 「확정된 것은 **409 INVALID_STATE**」라 문자로 적었다(`:962` 인접 description) — `I-24.md` §1-6 이 400 을 골랐던 자리와 **다르다** |
| 확정된 결과를 다시 `:confirm` | `STATE_LOCKED` | **400**(`assertTransition(..., 400)` — 형제 `planning.production_plan`·`routing` 선례) |
| LOT 이 `SCRAPPED` 인데 확정 | `STATE_LOCKED` | 400(같은 함수가 낸다) |
| If-Match 어긋남 | `VERSION_CONFLICT` · `conflictCause='user'` · ⭐ **`currentLotStatusCode` 를 함께 내린다**(계약이 이 칸을 `QualityConflictResponse` 에 두고 「message 에서 파싱하지 않는다」라 적었다) | 409 |
| `idempotency_key` 중복(다른 본문) | `DUPLICATE_KEY` | 409 |
| 권한 | — | 403(게이트) |

⛔ **`ERROR_CODE` 에 새 값을 더하지 않는다**(`lanes.md` §1-4 · 공용 파일).

---

## 2. 물리 대조 + **마이그레이션 결론**

### 2-1. `quality.inspection_request` 22칸 ↔ 계약 17 프로퍼티

`inspection_request_id`·`inspection_request_no @unique`·`inspection_type_code`·**`inspection_plan_version_id` NOT NULL**·`target_type_code`·`target_id`·`item_id`·`lot_id?`·`work_order_id?`·`production_result_id?`·`target_qty`(CHECK `>0`)·`uom_id`·`coverage_from_at?`·`coverage_to_at?`·`status_code`·`requested_at`·`created_at/by`·`updated_at/by`·`version_no @default(1)`.
⇒ **계약 17칸 전부 1:1** · 결손 0 · 남는 칸은 감사 4 + `version_no`(계약도 갖는다).
⚠ **NOT NULL 하나가 계약 nullable 과 어긋난다** → M-e ⓐ.

### 2-2. `quality.inspection_result` 24칸 ↔ 계약 26 프로퍼티

`inspection_result_id`·`inspection_result_no @unique`·`inspection_request_id`·`inspection_round @default(1)`(CHECK `>0`)·`inspected_qty`(CHECK `>0`)·`accepted_qty/rejected_qty/held_qty @default(0)`·`uom_id`·**`overall_judgment_code` NOT NULL**·`inspector_id` NOT NULL(FK `mdm.worker`)·`inspected_at`·`confirmed_at?`·`terminal_id?`·`status_code`·`previous_result_id?`·`reinspection_reason_code?`·**`idempotency_key @unique`**·`remarks?`·감사 4·`version_no`.
CHECK 셋(실측 · baseline `:1866-1871`): `inspection_round > 0` · `inspected_qty > 0` · **`ck_inspection_result_qty (accepted+rejected+held = inspected)`**.
UNIQUE 둘: `inspection_result_no` · **`uq_inspection_round (inspection_request_id, inspection_round)`**.
⇒ 계약 26칸 중 **20이 1:1** · 파생 6(`inspectionRequestNo`·`inspectionTypeCode`·`itemId`·`lotId`·`lotNo`·`processId`+`processName` 은 §2-3) · **담을 데가 없는 칸 0**.

⭐ **어긋나는 자리 둘**:
1. **`overall_judgment_code` 가 NOT NULL** 인데 계약 Create 는 「`statusCode=확정` 이면 필수」다. ⇒ 작성중 저장에도 값이 있어야 한다. **완화하지 않는다** — 화면(`P-02-13` §5-4 · `W-04-03` §5-1)이 종합 판정을 «고르는 칸»으로 늘 그리고, `HELD`(보류)가 「아직 판정 못 함」의 자리를 이미 갖는다. ⇒ **작성중에 값이 없으면 400 `REQUIRED`**(2단계 기준 3 「스키마를 안 늘리는 쪽」 + 기준 2 「거부하는 쪽」) · 문의 **069+4**.
2. **`ck_inspection_result_qty` 가 무조건** — §0 #3 · M-e ⓑ.

### 2-3. ⭐ `processId`·`processName` — **물리에 공정 축이 없다** (판정)

실측: `inspection_result`·`inspection_request` 어디에도 `process_id` 가 **없다**. 공정으로 잇는 길은 둘뿐이다.

| 후보 | 경로 | 홉 | 빌 때 |
|---|---|:-:|---|
| ⓐ **기준 축** | `inspection_request.inspection_plan_version_id → inspection_plan_version.inspection_plan_id → inspection_plan.process_id`(`schema.prisma:3282` · nullable) | 3 | 기준 없는 의뢰(M-e ⓐ 뒤) · `inspection_plan.process_id` 가 null 인 기준 |
| ⓑ **W/O 축** | `inspection_request.work_order_id → work_order.routing_operation_id → routing_operation.process_id`(NOT NULL) | **2** | IQC·OQC(=`work_order_id` 가 비어 있다) |

계약은 「현장검사만 직접 컬럼이고 **PQC·OQC 는 서버가 2단 조인으로 잇는다**」(`InspectionResult.processId` description)라 적었다. **홉 수가 「2단」과 맞는 것은 ⓑ 하나**다.
⇒ **판정: ⓑ 를 먼저 보고, 비면 ⓐ 로 떨어진다.** 둘 다 비면 **키를 생략한다**(널 금지 · `plan.md` §5-7). `processId` **필터**도 같은 식(`OR` 두 갈래)으로 건다.
⚠ OQC 는 ⓑ 가 늘 비므로 사실상 ⓐ 로만 잡힌다 — 계약 문장(「PQC·**OQC** 는 2단 조인」)과 어긋난다 · 문의 **069+5**.

### 2-4. ⭐ 마이그레이션 결론 — **M-e 한 파일 · 항목 2 · 삭제 0 · 백필 0**

> ⚠ **R-2: 항목이 3개다.** ⓒ `inspection_result.overall_judgment_code` **NOT NULL 해제**가 빠졌다 — 없으면 임시 저장 본길이 막힌다. ⚠ **R-18: M-e 는 PR ③ 이 아니라 «선행 단독 PR» 이다.**


`plan.md` §4 128행은 `| M-e | I-19 | 검사 의뢰 | 기준 완화(#280) |` **한 줄**이고, `docs/server-architecture.md:227` §6 이 `#280`(검사 의뢰 기준 완화)을 「그 도메인 첫 PR 앞에 마이그레이션을 둔다」로 이미 지정했다. 실측으로 **항목이 둘**이 된다.

```sql
-- prisma/migrations/<YYYYMMDDHHMMSS>_inspection_request_plan_optional/migration.sql
--   초까지 넣는다(lanes.md §1-2). 추가·완화만 — 삭제 0 · 백필 0 · 순서 의존 0.

-- ⓐ 검사 기준 없이도 의뢰가 선다 (#280)
--   계약 InspectionRequest.inspectionPlanVersionId 가 [integer,null] 이고
--   「검사 기준이 등록되지 않은 상태에서도 검사를 진행한다는 고객 확정(2026-07-15)」이
--   그 근거다. 물리만 NOT NULL 이라 그 갈래를 «표현할 수 없다».
ALTER TABLE quality.inspection_request
    ALTER COLUMN inspection_plan_version_id DROP NOT NULL;

COMMENT ON COLUMN quality.inspection_request.inspection_plan_version_id IS
    '이 의뢰가 따르는 검사 기준 버전. 비어 있을 수 있다 — 기준 미등록 품목도 검사를 진행한다(✓확정 2026-07-15). 비면 항목별 규격이 없어 결과는 수량 세 칸·종합 판정·자유 입력만으로 성립한다(P-02-13 §5-2 · 계약 #280).';

-- ⓑ 수량 합계 강제를 «확정일 때»로 좁힌다
--   계약: 「statusCode=확정 이면 강제한다 … 작성중은 통과시킨다」.
--   지금은 무조건이라 임시 저장이 DB 에서 튕긴다.
--   ⛔ 완화다 — 통과하던 행은 전부 그대로 통과한다(순서 의존 0).
ALTER TABLE quality.inspection_result DROP CONSTRAINT ck_inspection_result_qty;
ALTER TABLE quality.inspection_result
    ADD CONSTRAINT ck_inspection_result_qty
        CHECK (status_code <> 'CONFIRMED'
               OR accepted_qty + rejected_qty + held_qty = inspected_qty);
```

`schema.prisma` 쪽 변경: `inspection_request.inspection_plan_version_id BigInt?` + 관계 `inspection_plan_version?` (**+2줄**). CHECK 는 Prisma 가 안 담는다(`/// This table contains check constraints …` 주석이 이미 붙어 있다).
⚠ `@default` 를 바꾸지 않으므로 `npx prisma generate` 는 필요 없다 — 단 컬럼 nullability 는 타입에 실리므로 **generate 는 돌린다**(MEMORY 「Prisma 정적 기본값은 generate 가 필요」와 같은 계열).

### 2-5. 인덱스를 **더 걸지 않는 자리** — 근거

| 질의 축 | 받쳐 주는 것 | 판정 |
|---|---|---|
| `/measurements` 를 `inspection_result_id` 로(135,000 자릿수) | `uq_inspection_measurement (inspection_result_id, inspection_item_spec_id, sample_no)` — **선두 칸이 맞다** | ⛔ 더 안 건다 |
| `measurement-summary` 항목별 집계 | 같은 UNIQUE 의 앞 두 칸 | ⛔ |
| `inspection-results` 를 `inspected_at` 구간으로 | 없다 | ⛔ **지금은 안 건다** — 계약이 규모를 「의뢰 412 · 결과 **450**」이라 직접 적었다(`:1378`). 450행에 인덱스는 뜻이 없다. §11 ⑥ 후속 |
| `inspection-requests` 를 `status_code`·`lot_id` 로 | 없다 | ⛔ 같은 이유(412행) |
| `equipment_calibration` 을 (장비, 날짜)로 | `ix_equipment_calibration_history (equipment_id, calibration_date DESC)` | ⛔ 이미 있다 |

⇒ **인덱스 신설 0.** 「규모 주의」(브리프 §5)는 **페이지네이션과 서버 집계**로 받고 인덱스로 받지 않는다 — 큰 표는 측정치 하나이고 그 축은 이미 UNIQUE 가 받는다.

---

## 3. ⭐ 심장 — `POST …/{inspectionResultId}:confirm`

### 3-1. 한 트랜잭션 순서

```
0  (tx 밖) 없음 — 채번도 멱등도 이 경로엔 없다(번호는 POST 가 이미 뽑았다)
   컨트롤러가 runVersioned(…) 로 멱등+If-Match 를 연다
1  결과를 FOR UPDATE 로 잠근다              → 없으면 404
2  version_no 대조                            → 어긋나면 409 VERSION_CONFLICT
                                                 (+ currentVersion · currentLotStatusCode)
3  assertTransition('quality.inspection_result.status_code',
                    'inspection-confirm', row.status_code, 400)
                                               → 이미 CONFIRMED 면 400 STATE_LOCKED
4  판정값 확정 — body.overallJudgmentCode ?? row.overall_judgment_code
   assertCodeValues(INSPECTION_RESULT_OVERALL_JUDGMENT)   → 400 INVALID
5  ⛔ 수량 합 대조(accepted+rejected+held = inspected)      → 400 INVALID
   (DB CHECK 가 M-e ⓑ 뒤에는 «확정일 때만» 잡으므로 서비스가 먼저 문구를 낸다)
6  inspection_result UPDATE — status_code='CONFIRMED' · confirmed_at=now()
   · overall_judgment_code · remarks · version_no+1 · updated_by
7  inspection_request UPDATE — status_code='COMPLETED'
   (계약 statusCode 설명 「:confirm 이 COMPLETED」 — 부수효과 · 전이표에 «안» 넣는다:
    from 이 REQUESTED/IN_PROGRESS 둘이고 계약이 그 축을 코드 그룹으로 열어 두었다)
8  대상 LOT 집합을 정한다 — §3-3
9  LOT 축 전이 — lotQualityStatus.moveWithin(tx, lotIds, action, {
      transitionCode, sourceDocumentTypeCode:'INSPECTION_RESULT',
      sourceDocumentId: inspectionResultId, changedBy: session.userId })
   → lot.status_code UPDATE + lot.version_no+1 + lot_status_event INSERT(N행)
10 C14 판정 — PQC & rejected 수 > acceptance_number 면 같은 W/O 의 생산LOT 전체를
   두 번째 moveWithin(…, 'pqc-acceptance-exceeded', 'C14')                     §3-4
11 보류 해제 — reason_code='INCOMING_INSPECTION_WAIT' AND released_at IS NULL 인
   lot_hold 를 released_at/released_by/release_reason_code 로 닫는다            §0 #4
12 응답 = InspectionResult 뷰 · ETag = 새 version_no
```

⛔ **원장을 지나지 않는다**(`plan-integration.md:369` 「원장: 없음」 · `plan.md` §5-8 판별자 4값에 검사가 없다).
⛔ **`inventory_balance.quality_status_code` 를 건드리지 않는다** — §9-1 #2.
⛔ **알람을 내지 않는다** — 계약 `:confirm` 이 「관리자 알람을 낸다」고 적었으나 알림은 **I-28**(레인 B)이고 `notification` 을 쓰는 코드가 이 슬라이스에 0건이다 · 「알려둘 것」.

### 3-2. 400 · 404 · 409 갈래 — **순서가 판정이다**

| 순 | 조건 | 응답 |
|:-:|---|---|
| 1 | 결과 없음 | **404** |
| 2 | If-Match 어긋남 | **409 `VERSION_CONFLICT`** |
| 3 | 이미 `CONFIRMED` | **400 `STATE_LOCKED`** |
| 4 | 판정값이 값 목록 밖 | 400 `INVALID` |
| 5 | 수량 합 불일치 | 400 `INVALID` |
| 6 | LOT 이 `SCRAPPED` | 400 `STATE_LOCKED`(전이표가 던진다) |

⭐ **버전 대조가 상태 게이트보다 «먼저»다** — I-7 `production-result.service.ts:91-92` 의 순서를 그대로 복제한다(형제 `:release`·`:close` 선례). 이 순서가 뒤집히면 「낡은 화면이 이미 확정된 것을 다시 확정」할 때 문구가 갈린다.

### 3-3. 대상 LOT 집합 — `targetTypeCode` 3값이 가른다

> ⚠ **R-10 이 이 규칙을 바꿨다** — `targetTypeCode` 로 가르지 않는다. **`lot_id` 가 있으면 그 1건**이다(OQC 가 어느 LOT 도 안 옮기는 결함).


| `targetTypeCode` | 대상 LOT | 근거 |
|---|---|---|
| `LOT` | `inspection_request.lot_id` **1건** | IQC 입하 LOT · OQC 제품 LOT |
| `WORK_ORDER` | `inspection_request.lot_id` 가 있으면 **그 1건**, 없으면 **0건** | ⭐ PQC — 계약이 「의뢰가 `lotId`·`workOrderId` 를 «따로» 갖는다 … 둘이 같은 대상을 가리키면 FK 쪽을 쓴다」(A-10 규칙 3)라 적었다. ⛔ **W/O 전체로 넓히지 않는다** — 그것은 C14 만의 규칙이다 |
| `SHIPMENT_REQUEST` | **0건** | OQC 출하지시 대상 — LOT 축이 없다. `shipment_lot_allocation` 으로 넓히는 규칙을 계약이 적지 않았다 ⇒ §2 2단계 기준 4 · 문의 **069+6** |

⇒ **대상이 0건이면 전이 없이 확정만 한다**(400 을 내지 않는다 — 계약이 요구하지 않는다).

### 3-4. ⭐ C14 — 「같은 W/O 의 생산LOT 전체」

계약 원문(`:1465`·`:925` 두 곳에 같은 문장): 「PQC 샘플 검사에서 **불합격 수가 공정별 합격판정개수를 넘으면** 서버가 «같은 W/O 의 생산LOT **전체**»를 `INSPECTION_PENDING`(검사 대기)으로 일괄 전이하고 관리자 알람을 낸다 — 화면이 부르는 추가 액션은 없다. **기준값은 06 계약 `GET /quality/inspection-plan-versions/{id}` 가 갖는다**」.

| 물음 | 실측 | 판정 |
|---|---|---|
| 기준값이 어디 있나 | `inspection_plan_version.acceptance_number Int?`(`schema.prisma:3319`) — `W-06-02:106` 이 「**C14 의 근거 필드**」라 직접 적었다 | 그 칸을 읽는다 |
| 「불합격 수」가 무엇인가 | `rejected_qty`(결과 헤더) vs `inspection_measurement` 중 `judgment_code='REJECTED'` 건수 | ⭐ 계약이 「**개수**」라 적었고 `acceptance_number` 도 정수다. 그런데 `rejected_qty` 는 **수량**이다. ⇒ **`rejected_qty` 를 쓴다** — 샘플 검사에서 「불합격 개수」와 「불합격 수량」이 갈리는 축이 계약·물리 어디에도 없고, 측정치는 «항목×샘플»이라 개수 축이 다르다(항목 5개면 한 샘플이 5행). 2단계 기준 5(새 개념 수가 적은 쪽) · 문의 **069+7** |
| 「같은 W/O」의 범위 | `inspection_request.work_order_id` | 브리프 §3 「좁혀지지 않으면 계약 문자 그대로 「같은 W/O」」 ⇒ `work_order_id` 하나. ⛔ 같은 공정·같은 Routing 으로 넓히지 않는다 |
| 「생산LOT」이 무엇인가 | `lot.work_order_lot_seq` 가 있는 행 = 선발행 슬롯(`lot-registry.service.ts:145-160`) · `lot.source_type_code='WORK_ORDER'`·`source_id=work_order_id` | 그 둘로 좁힌다 |
| 언제 안 도나 | `inspectionTypeCode ≠ 'PQC'` · `acceptance_number` 가 null · `inspection_plan_version_id` 가 null(M-e ⓐ 뒤) · `work_order_id` 가 null | ⛔ **판정을 «건너뛴다»** — 0 으로 접지 않는다(`plan.md` §5-10) |
| 이미 `INSPECTION_PENDING`·`DEFECTIVE` 인 LOT | 전이표의 `from` 에 든다 | 자기 자신으로 가는 전이도 이력 1행을 남긴다(계약이 「전체」라 적었다) |

⭐ **C15 를 쓰지 않는다** — 시드 `C15`(「PQC 검사 필요 → Release(전수 재검 양품)」)는 `C4` 와 **(from, to) 가 똑같다**(`INSPECTION_PENDING → NORMAL`). 어느 LOT 이 C14 로 그 자리에 왔는지 가릴 표식이 데이터에 **없다**(`lot_status_event` 를 거슬러 읽는 것은 도출이다). ⇒ 2단계 기준 4 → **`C4` 하나만 쓰고 `C15` 는 비워 둔다** · 문의 **069+8**.

### 3-5. 전이표 — 계약이 이름 적은 것만

§6 에 코드로 적는다. **이 슬라이스가 «쓰는» 것은 4** (`C4`·`C5`·`C6`·`C14`), **등록만 하고 뒤가 쓰는 것은 5**(`C7`·`C8`·`C9`·`C10` → I-20 · 재등록 → I-23).

---

## 4. 조회 8건

### 4-1. `GET /quality/inspection-requests` · `…/{id}`

- `pendingOnly=true` ⇒ `status_code IN ('REQUESTED','IN_PROGRESS')`. ⛔ 화면이 거르게 두지 않는다.
- `supplierId` ⇒ `lot: { source_type_code: 'INBOUND_RECEIPT_LINE', … }` 2단 조인. ⚠ `lot.source_id` 는 **`inbound_receipt_line_id`** 라 `inbound_receipt_line → inbound_receipt.supplier_id` 로 한 단 더 간다(I-3 `core/lot` 실측) — 계약이 「2단」이라 적었지만 실물은 3단이다 · 「알려둘 것」. Prisma 관계가 `lot.inbound_receipt_line[]` 로 서 있어 raw SQL 없이 선다.
- `q` ⇒ `inspection_request_no contains`.
- ⭐ **기본 정렬** — 계약에 `sort` 가 없다. `W-01-01` §3 이 「검사 대기 큐」라 **`requested_at asc, inspection_request_id asc`**(오래 기다린 것부터 · `plan-uiux.md:901` 갈래 ② 「경과일 긴 순」과 같은 뜻). 문의 **069+2**.
- 상세는 ETag 를 **안 낸다**(계약 미선언).

### 4-2. `GET /quality/inspection-results` — ⭐ 재검 사슬 페이지

계약(`:753`)이 규칙을 통째로 적었다 — 「`finalRoundOnly=false` 면 `page/size/total` 은 **뿌리 결과(1회차) 기준**으로 세고, 사슬 전체가 뿌리와 같은 페이지에 동거한다 — **부모·자식이 페이지로 갈리지 않는다**. 정렬은 뿌리 기준, 사슬 안은 회차 순」.

```
① 뿌리만 세고 페이지를 자른다
   where: 필터 ∧ previous_result_id IS NULL
   orderBy: sort 키(뿌리 기준) → skip/take → total = count(뿌리)
② 그 뿌리들의 사슬 전체를 한 번에 읽는다
   재귀 CTE 대신 «회차 축»으로 푼다 — uq_inspection_round 가 (의뢰, 회차) 라
   같은 inspection_request_id 를 공유하는 형제가 곧 사슬이다.
   where: inspection_request_id IN (뿌리들의 의뢰 id)
③ 뿌리 순서대로 늘어놓고 사슬 안은 inspection_round asc 로 붙인다
   ⇒ items.length ≥ page.total (I-24 includeChildren 과 같은 모양)
```

⭐ **재귀 CTE 를 쓰지 않는다** — `uq_inspection_round (inspection_request_id, inspection_round)` 가 있어 사슬이 **의뢰 하나 = 사슬 하나**로 닫힌다. `previous_result_id` 는 «표시용 들여쓰기 축»이지 조회 축이 아니다(계약이 「`previousResultId` 만으로 들여쓰기가 가능하다」라 적었다). I-24 §12-1 ⓕ 의 「재귀 CTE 순환 방어」 걱정이 여기선 아예 안 생긴다.
- `finalRoundOnly=true` ⇒ 의뢰별 **최대 회차 1건씩**. ⇒ `groupBy(inspection_request_id, _max: inspection_round)` 뒤 `in` — 아니면 「최종 회차」의 정의가 갈린다.
- ⛔ **기간·의뢰 둘 다 없으면 400 `REQUIRED`**.
- `sort` 허용 키 3 밖이면 **400 `INVALID`**(L-4 「정렬 키는 제한한다」).

### 4-3. `GET …/{id}` · `…/{id}/measurements`

- 상세는 **ETag 를 낸다**(`setEtag(response, version_no)`) — 11건 중 유일.
- `/measurements` 는 `page`·`size` 필수 골격(기본 50) · 정렬은 `inspection_item_spec_id asc, sample_no asc`(계약 침묵 · 화면이 「항목 → 샘플」 표라 그 축) · 없는 결과 id 는 **빈 목록**.
- `calibrationExpiredAtMeasurement` 는 §4-4 로 판정해 **행마다** 싣는다.

### 4-4. ⭐ `calibrationExpired` — 파생 판정 하나로 4자리를 쓴다

정의(계약): 「측정 **시점에** 이 장비의 교정이 만료였는가 — **서버가 판정한 값**이다(L-2)」.

```
장비 e, 측정 시각 t 에 대해
  최근 검교정 = equipment_calibration
                where equipment_id = e AND calibration_date <= t::date
                order by calibration_date desc limit 1
  만료 = (최근 검교정이 없다) OR (valid_until IS NULL) OR (valid_until < t::date)
```

⭐ **「검교정 이력이 아예 없는 장비」를 만료로 본다** — 「판정 불가를 0/정상으로 접지 않는다」(`plan.md` §5-10 · L-8). ⚠ `valid_until` 이 nullable 이라 「유효기한 없는 검교정」도 만료다 — 문의 **069+9**.
- 쓰는 자리 넷: `/measurements` 의 행 칸 · `/measurements`·`/inspection-results`·`summary`·`trend` 의 `only`/`exclude` 필터 · `summary.calibrationExpiredCount` · `measurement-summary.equipmentCalibrationExpired`.
- ⛔ **집계에서 자동으로 빼지 않는다**(계약 「무효화 정책이 미정이다(E-9 ①)」). 기본은 섞어서 낸다.
- 구현: 결과 하나 안의 장비는 몇 개뿐이라 **장비 id 집합을 모아 한 번에 읽고 메모리에서 판정**한다(N+1 금지 · L-2).

### 4-5. 집계 셋 — **서버가 센다**

| 오퍼레이션 | 산식 | 함정 |
|---|---|---|
| `summary` | `count(*)` · `sum(inspected/accepted/rejected/held_qty)` · `defectRate = rejected/inspected × 100` · `calibrationExpiredCount = count(distinct result)` where 만료 측정치 존재 · `asOf = now()` | ⭐ **분모는 검사 수량**(생산 수량 아님). `inspectedQty=0` 이면 `defectRate` **키를 생략**(0% 가 아니다 — 계약이 required 로 적었으므로 ⚠ **0 을 낸다**: required 라 생략이 계약 위반이다 · 「알려둘 것」) |
| `defect-rate-trend` | `inspected_at::date` 로 묶어 `bucket`·`inspectedQty`·`rejectedQty`·`defectRate` | ⛔ 관리도 통계(Xbar-R·Cp/Cpk) **만들지 않는다**(결정 11). ⭐ **버킷 축은 UTC 날짜다** — `plant.timezone_code` 로 풀 축이 이 자리에 없다(결과에 공장 칸이 없다) · 「알려둘 것」(I-6 `WO-` 번호 UTC 와 같은 계열) |
| `measurement-summary` | 항목별 `measuredCount`(값 ≥1칸) · `acceptedCount`·`rejectedCount`(judgment) · `unmeasuredCount`(값 3칸 전부 NULL) · `outOfSpecValues`(≤10) · `outOfSpecTotalCount`(전체) · `specText`(서버가 하한·상한·단위로 조립) · 장비 3칸 | ⭐ **`outOfSpecTotalCount` 를 `rejectedCount` 로 역산하지 않는다**(계약 명시 — 규격 밖이어도 자동 불합격이 아니다) · 「규격 밖」 판정은 `numeric_value` 와 `lower_limit`/`upper_limit` 비교이고 **NUMERIC 항목에만** 선다 |

- ⭐ **`asOf` 는 세 곳 다 서버 시각**(L-5).
- ⛔ **`finalRoundOnly` 를 응답에 되돌려준다**(`summary` 만 그 칸을 갖는다).

---

## 5. 쓰기 2건 — `POST` · `PUT`

### 5-1. `POST /quality/inspection-results` — 201

```
0  (tx 밖) 값 검증 → assertCodeValues → 참조 존재 확인(400 INVALID)
   회차 결정: previousResultId 가 있으면 그 행의 inspection_request_id 를 잇고
              회차 = max(그 의뢰의 round) + 1, 없으면 1
   채번: numbering.next('INSPECTION_RESULT', null, inspectedAt 의 UTC 날짜)
         ⛔ $transaction 밖 — I-2 R-2·I-7 선례. 결번 허용
1  runIdempotent(…, 201) 안에서 $transaction
2  inspection_result INSERT (idempotency_key 컬럼에 헤더 값을 그대로 넣는다
                             — material-consumption.service.ts:126 · production-result.service.ts:145 선례)
3  measurements 가 있으면 inspection_measurement createMany
4  statusCode='CONFIRMED' 로 왔으면 §3-1 의 6~11 을 «그대로» 탄다
   ⭐ 확정 경로 둘이 부수효과가 같아야 한다(계약 x-internal-note) ⇒ 같은 함수를 부른다
5  201 + InspectionResult 뷰. ⛔ ETag 를 안 낸다(계약 미선언)
```

- **If-Match 는 선택**(`IfMatchVersionOptional` — 오프라인 큐가 토큰을 안 싣는다 · C-9). 값이 오면 **의뢰의 `version_no`** 와 대조한다(결과는 아직 없다) — ⚠ 계약이 무엇의 버전인지 안 적었다 · 문의 **069+10**. 없으면 검사를 건너뛴다.
- ⭐ **오프라인 큐는 언제나 `CONFIRMED`** (`plan-uiux.md:1094`) — e2e 가 그 갈래를 돈다.
- `uq_inspection_round` 경합은 `DUPLICATE_KEY` → 재시도 없이 409(회차 채기를 `FOR UPDATE` 로 잠그지 않는다 — 한 의뢰에 두 사람이 동시에 재검을 올리는 창이 좁고, 잠그면 의뢰 행을 업무 트랜잭션 내내 쥔다). 「알려둘 것」.

### 5-2. `PUT …/{inspectionResultId}` — 200

- `runVersioned` 그대로(멱등 + If-Match 필수 + 새 ETag). ⚠ 계약이 `PUT` 응답에 ETag 를 **선언하지 않았다** — `runVersioned` 는 늘 낸다. I-1 「알려둘 것」(`PUT …/steps` ETag 내림)과 같은 자리 · 반복 기재.
- `status_code='CONFIRMED'` 면 **409 `INVALID_STATE`**(계약 문자 그대로 · §1-6).
- `measurements` 를 실으면 **그 결과의 측정치를 `deleteMany` → `createMany`**(치환 · §1-3). 생략하면 손대지 않는다.
- 수량 합은 **작성중이므로 강제하지 않는다**(M-e ⓑ 뒤 DB 도 안 잡는다).

### 5-3. 번호·회차·멱등 — 세 칸이 서로 다른 축이다

| 칸 | 축 | 규칙 |
|---|---|---|
| `inspection_result_no` | 전역 UNIQUE | `IRS-{YYYYMMDD}-{SEQ4}` · 기간 키는 **`inspectedAt` 의 UTC 날짜**(서버가 「오늘」로 다시 잡으면 재전송이 하루 뒤 번호를 받는다 — I-7 선례) |
| `inspection_round` | `(의뢰, 회차)` UNIQUE | 서버가 `+1`. ⛔ 본문이 못 준다 |
| `idempotency_key` | 전역 UNIQUE **컬럼** | 헤더 값을 그대로 넣는다. ⭐ 계약이 「전역 UNIQUE 라 `business_date` 경계 문제가 없다」라 적었다 |

⛔ **`business_date` 를 쓰지 않는다** — 이 계약 파일에 `businessDate` 프로퍼티가 **0건**이고 원장을 지나지 않는다(`plan.md` §5-7). 대기 15 누적이 **늘지 않는다**.

### 5-4. 403 게이트 — `manual-permissions.ts` **0줄**

실측: 403 을 선언한 3건이 **전부 `derived-permissions.ts` 에 이미 있다** — `POST /quality/inspection-results`:250 · `POST …:confirm`:251 · `PUT …/{id}`:280.
⛔ GET 8건은 등록하지 않는다 — 계약이 403 을 선언하지 않아 가드가 그 자리를 **보지 않는다**(`permission.guard.ts:37-41`). `GET …/measurement-summary` 가 도출표에 없는 것도 그래서 문제가 아니다.
⇒ **공용 파일 `manual-permissions.ts` 를 이 슬라이스가 건드리지 않는다**(레인 충돌 창 0 · `lanes.md` §1-4).

### 5-5. ⭐ `inspector_id` — 주체를 어디서 푸나 (`plan.md` §5-9 가름)

| 사실 | 실측 |
|---|---|
| `inspection_result.inspector_id` | **NOT NULL** · FK `mdm.worker` |
| 계약 | `X-Worker-No` 가 **`WorkerNoOptional`**(required=false) · Create 설명 「⭐ 검사자·단말은 «보내지 않는다» — 서버가 인증 주체에서 채운다(**관리웹은 계정 토큰**, 현장 단말·모바일은 사번 귀속 헤더)」 |
| 부르는 화면 | `W-01-01`(관리웹) · `W-04-03`(관리웹) · `P-02-13`(POP 단말) — **양쪽이 다 부른다** |
| `worker.app_user_id` | 실재(nullable · `schema.prisma` worker 모델) |

⇒ **판정: 헤더가 있으면 `worker_no` 로 풀고, 없으면 세션의 `app_user_id` 로 `worker` 를 푼다.** 둘 다 못 풀면 **400 `REQUIRED`**(`X-Worker-No` 칸).
⛔ `plan.md` §5-9 의 「헤더 없으면 400」 열 자리 목록에 **더하지 않는다** — 그 목록은 계약이 `WorkerNo`(필수)를 단 자리이고 여기는 `WorkerNoOptional` 이다.
⚠ **관리자 계정에 `worker` 행이 없으면 관리웹이 검사를 저장할 수 없다** — 계약·화면 어디에도 그 연결을 요구하는 문장이 없다 · 문의 **069+11**.
`terminal_id` 는 단말 토큰이 있을 때만 채운다(없으면 **키 생략** — I-11 의 단말 토큰 헬퍼 재사용).

---

## 6. 상태기계 — `transitions.ts` 에 키 **신설 2 · 전이 10**

### 6-1. `quality.inspection_result.status_code` — 전이 1

```ts
/**
 * 검사 성적서 확정. 값은 계약이 `DRAFT`·`CONFIRMED` 로 확정했다(2026-09-02 등재 ·
 * 「⛔ 코드 문자열은 영문이다」 — 그 전에는 이 자리가 계약 7벌의 «유일한» 한국어 enum 이었다).
 *
 * ⚠ `conflictStatus` 는 호출자가 **400** 을 넘긴다 — 재확정은 「재로드해도 안 풀리는 잠금」이다.
 *    ⛔ 그러나 `PUT` 의 확정본 수정은 **409 `INVALID_STATE`** 다 — 계약이 그 자리에만 409 를
 *    문자로 적었다(`InspectionResultUpdate` 설명). 두 자리의 봉투가 다르다.
 * ⛔ 되돌아오는 전이가 없다 — 확정을 푸는 오퍼레이션이 계약에 0건이고, 번복은 «재검 회차»다(B-10).
 * ⛔ 이력 표가 없다 — `transitionCode` 를 쓰지 않는다.
 */
'quality.inspection_result.status_code': {
  'inspection-confirm': {
    from: ['DRAFT'],
    to: 'CONFIRMED',
    sourceOperation: 'POST /quality/inspection-results/{inspectionResultId}:confirm',
  },
},
```

### 6-2. ⭐ `trace.lot.status_code` — 전이 **9** (품질 축을 연다)

> ⛔ **R-1 이 이 절을 뒤집었다.** `JUDGED` 상수(3값)를 쓰지 않고 **액션별 `from`** 으로 가른다. `DEFECTIVE` 는 `stock-reinstate` 의 `from` 에만 넣는다. 아래 표를 그대로 구현하지 말 것 — §0-재수립 R-1 이 정본이다.


```ts
/**
 * LOT 품질 판정 축. ⭐ **회신 E-3 이 종결됐다**(2026-08-07 — 사용자 확정 ·
 * `design/raw/process/uiux/2026-08-07-E3-판정유형-제안안/LOT상태-확정기록.md`).
 * 값은 시드 `LOT_STATUS` 4값(`NORMAL`·`INSPECTION_PENDING`·`DEFECTIVE`·`SCRAPPED` ·
 * `isSystemOwned`)이고, 전이 코드는 시드 `LOT_STATUS_TRANSITION` C4~C15 가 갖는다.
 * 코드는 `trace.lot_status_event.transition_code`(NOT NULL)에 그대로 들어간다.
 *
 * ⛔ 「PQC 검사 필요」는 값이 아니다 — 확정 기록 §1.2 가 「상태 목록이 LOT 종류 공통인데
 *    이름에 검사 종류를 박으면 안 된다」로 `INSPECTION_PENDING` 에 합쳤다. 옛 「보류」도 같다.
 * ⛔ `SCRAPPED` 로 «가는» 전이를 등록하지 않는다 — 계약이 어느 오퍼레이션에도 적지 않았다.
 *    그래서 `from` 에도 넣지 않는다: 폐기된 LOT 은 다시 판정하지 않는다.
 * ⛔ `C15`(전수 재검 양품)를 등록하지 않는다 — `C4` 와 (from, to) 가 같고 어느 LOT 이
 *    `C14` 로 그 자리에 왔는지 가릴 표식이 데이터에 없다(문의 069+8).
 * ⚠ `from` 을 `INSPECTION_PENDING` 하나로 좁히지 않는다 — 좁히면 재검 회차 확정과 OQC
 *    확정이 전부 막힌다(계획안 §0 #2).
 */
'trace.lot.status_code': {
  // ── I-19 이 «쓴다» ─────────────────────────────────────────────
  'inspection-accepted':  { from: JUDGED, to: 'NORMAL',             transitionCode: 'C4',
    sourceOperation: 'POST /quality/inspection-results/{inspectionResultId}:confirm' },
  'inspection-held':      { from: JUDGED, to: 'INSPECTION_PENDING', transitionCode: 'C5',
    sourceOperation: 'POST /quality/inspection-results/{inspectionResultId}:confirm' },
  'inspection-rejected':  { from: JUDGED, to: 'DEFECTIVE',          transitionCode: 'C6',
    sourceOperation: 'POST /quality/inspection-results/{inspectionResultId}:confirm' },
  'pqc-acceptance-exceeded': { from: JUDGED, to: 'INSPECTION_PENDING', transitionCode: 'C14',
    sourceOperation: 'POST /quality/inspection-results/{inspectionResultId}:confirm' },

  // ── I-20 이 쓴다(등록만 · 계약이 이름 적었다) ──────────────────
  'lot-hold-suspect':     { from: JUDGED, to: 'INSPECTION_PENDING', transitionCode: 'C10',
    sourceOperation: 'POST /quality/lot-holds' },
  'lot-hold-claim':       { from: JUDGED, to: 'DEFECTIVE',          transitionCode: 'C9',
    sourceOperation: 'POST /quality/lot-holds' },
  'lot-hold-release-accepted': { from: JUDGED, to: 'NORMAL',        transitionCode: 'C7',
    sourceOperation: 'POST /quality/lot-holds/{lotHoldId}:release' },
  'lot-hold-release-rejected': { from: JUDGED, to: 'DEFECTIVE',     transitionCode: 'C8',
    sourceOperation: 'POST /quality/lot-holds/{lotHoldId}:release' },

  // ── I-23(레인 C)이 쓴다 ───────────────────────────────────────
  // ⛔ `transitionCode` 가 없다 — 재등록을 가리키는 코드가 C4~C15 에 «없다».
  //    `lot_status_event.transition_code` 는 NOT NULL 이라 I-23 이 값을 넣어야 한다.
  //    지어내지 않고 문의 069+12 로 묻는다(레인 C 인계 · §11 ③).
  'stock-reinstate':      { from: JUDGED, to: 'NORMAL',
    sourceOperation: 'POST /logistics/stock-reinstatements' },
},
```
`const JUDGED = ['NORMAL', 'INSPECTION_PENDING', 'DEFECTIVE'] as const;` — `SCRAPPED` 만 뺀 3값.

- `document-state.spec.ts` 가 **두 자리** 바뀐다:
  - **`:227-229` 의 「⛔ 품질 판정 축은 등록돼 있지 않다」 테스트를 «뒤집는다»** — `toBeUndefined()` → 등록 단언 + 「회신 E-3 종결 2026-08-07」 근거 주석. ⭐ 이 단언이 이 슬라이스가 존재하는 이유를 그대로 담고 있다.
  - **`:257-272` 축 목록 12 → 14**(`INSPECTION_RESULT_STATUS`·`LOT_QUALITY` 추가) · **`:276` `toHaveLength(28)` → `40`**(28 + 1 + 9 · `// +10 — 검사 확정 1, 품질 축 신설 9(I-19 PR ①).`).
- ⛔ `quality.inspection_request.status_code` 키를 **만들지 않는다** — 계약이 전이 5개를 「전부 «이미 있는 액션의 부수 효과»다 — 새 경로가 없다」로 적었고, 그중 셋(`REQUESTED` 생성·`SKIPPED`·`CANCELLED`)을 여는 코드가 이 슬라이스에 없다. `:confirm` 이 `COMPLETED` 로 옮기는 것은 서비스가 직접 UPDATE 한다(§3-1 ⑦).

### 6-3. ⭐ `LotQualityStatusService.moveWithin()` — 코어

`src/core/lot/lot-quality-status.service.ts`(신설) — `LotLifecycleService.moveWithin()`(`lot-lifecycle.service.ts` · I-6 R-12)의 **형제**다. 사용처 **4 도메인**: I-19 `:confirm` · I-20 `POST /quality/lot-holds`·`:release` · I-21 처분 · **I-23 재등록(레인 C)**.

```ts
moveWithin(tx, lotIds: bigint[], action: ActionName, ctx: {
  changedBy: bigint;                 // lot_status_event.changed_by 는 NOT NULL
  sourceDocumentTypeCode?: string;   // ck_lot_status_event_source 가 «둘 다 or 둘 다 아님»
  sourceDocumentId?: bigint;
  reasonCode?: string; reason?: string;
}): Promise<{ movedLotIds: bigint[]; skippedLotIds: bigint[] }>
```
- LOT 을 `FOR UPDATE` 로 잠그고 → `assertTransition('trace.lot.status_code', action, lot.status_code, 400)` → `lot.status_code` UPDATE + `version_no+1` → `lot_status_event` INSERT(`previous_status_code`·`new_status_code`·`transition_code`·`changed_at`).
- ⛔ `quality_status_code`·`inventory_status_code`·`location_id` 세 칸은 **비운다** — 재고 차원을 여기서 정할 축이 없다(`plan.md` §5-10 · §9-1 #2).
- ⭐ `moveWithin` 이 **LOT `version_no` 를 올린다** — I-6 R-22 가 같은 자리에서 「선발행 LOT 화면의 If-Match 가 낡는다」를 적었다. 검사 확정 뒤 LOT 상세 화면의 토큰도 낡는다 · 「알려둘 것」.

---

## 7. 횡단 · 모듈 배치

### 7-1. 파일 — `src/quality/inspection/` 아래

| 파일 | 내용 | 예상 |
|---|---|---:|
| `src/core/lot/lot-quality-status.service.ts` | ⭐ 코어 — `moveWithin()` | ~95 |
| `src/core/document-state/transitions.ts` | 키 2 신설 | +60 |
| `src/quality/inspection/inspection-request.controller.ts` | GET 2 | ~45 |
| `src/quality/inspection/inspection-request.service.ts` | 목록(질의 8) + 상세 | ~120 |
| `src/quality/inspection/inspection-request-view.ts` | `InspectionRequest` 매퍼(`omitEmpty`) | ~35 |
| `src/quality/inspection/inspection-result.controller.ts` | GET 4 + POST + PUT + `:confirm` | ~110 |
| `src/quality/inspection/inspection-result-query.service.ts` | 목록(재검 사슬) + 상세 + `/measurements` | ~150 |
| `src/quality/inspection/inspection-result-view.ts` | `InspectionResult`·`InspectionMeasurement` 매퍼 + `processId` 두 갈래 | ~70 |
| `src/quality/inspection/inspection-result-write.service.ts` | `POST`·`PUT` | ~160 |
| `src/quality/inspection/inspection-confirm.service.ts` | ⭐ `:confirm` 한 트랜잭션 + C14 + 보류 해제 | ~150 |
| `src/quality/inspection/inspection-summary.service.ts` | `summary`·`defect-rate-trend`·`measurement-summary` | ~170 |
| `src/quality/inspection/calibration.ts` | 순수 — 교정 만료 판정 + 규격 밖 판정 | ~60 |
| `src/quality/inspection/inspection-rules.ts` | 순수 — 수량 합·판정값·기간 유계·정렬 키 | ~70 |

### 7-2. 모듈 배선 — `quality.module.ts` 만 (+~14줄)

오늘 `imports` 는 `PrismaModule`·`IdempotencyModule` 둘이다(`quality.module.ts:19` 실측). **`NumberingModule`·`DocumentStateModule`·`LotRegistryModule`(코어 서비스 등록처) 셋을 더한다.** `controllers` +2 · `providers` +7.
⛔ **`app-domain.module.ts`·`trace.module.ts` 를 건드리지 않는다** — 레인 B 와 겹치는 두 파일(`lanes.md` §1-4)에 이 슬라이스는 0줄이다.
⛔ **`production.module.ts`·`logistics.module.ts` 0줄.**

### 7-3. 다른 도메인 service 호출 **0** · 다른 도메인 import **0**

부르는 코어는 `NumberingService`·`DocumentStateService`·**`LotQualityStatusService`** 셋. `trace.lot`·`trace.lot_hold`·`trace.lot_status_event` 는 **코어가** 쓴다(도메인이 직접 UPDATE 하지 않는다 — `server-architecture.md:67`).
⚠ **예외 하나** — `inspection_request.status_code` 를 `COMPLETED` 로 옮기는 것은 `quality` 자기 표라 서비스가 직접 UPDATE 한다.

### 7-4. 멱등 3 · If-Match 필수 2 + 선택 1 · ETag 1 · 403 등록 **0(이미 있다)** · 새 error code 0 · `manual-permissions.ts` **0줄**

---

## 8. e2e — 파일 둘 + 단위 spec 넷

`test/quality-inspection-result.e2e-spec.ts` · `test/quality-inspection-summary.e2e-spec.ts`.

### 8-1. 픽스처 — I-7 의 것을 복제한다

> ⚠ **R-19: 입하 사슬(partner → inbound_receipt → inbound_receipt_line)이 빠졌다.** `POST /trace/lots` 가 `inbound_receipt_line` 을 UPDATE 하므로 그것 없이는 §8-3 #4 가 원리상 통과 못 한다. ⚠ **R-18: 셋째 갈래(`inspection_plan_version_id = null`)는 M-e 뒤에만 심을 수 있다.**


법인 → 사업부 → 공장 → 품목 → 공정 → Routing + `routing_operation` → BOM → `production_order`(prisma) → `production_plan`(오퍼레이션) → **W/O**(오퍼레이션) → **선발행 LOT**(`:release`) → **생산 실적**(`POST /production/production-results`) → **`inspection_request` ×N(prisma 직접 INSERT)** → 검사기준(`POST /quality/inspection-plans` + version + `inspection_item_spec` 3) → 검교정 이력 2(만료 1 · 유효 1) → 계정 2(권한 있음/없음) + 작업자 2(계정 연결 1 · 미연결 1) + 단말 1.
⭐ **`inspection_request` 는 prisma 로 직접 INSERT 한다** — 만드는 오퍼레이션이 계약에 0건이다(§0 #5 · `I-24.md` §8-1 의 P/O 와 같은 선례). IQC 갈래(`targetTypeCode='LOT'`) · PQC 갈래(`'WORK_ORDER'`) · **기준 없는 갈래**(`inspection_plan_version_id = null` — M-e ⓐ 없이는 못 심는다) 셋을 심는다.
⭐ 입하 LOT 은 **`POST /trace/lots`** 로 만든다 — `core/lot` 이 `status_code='INSPECTION_PENDING'` + `lot_hold(INCOMING_INSPECTION_WAIT)` 를 함께 세운다(`lot-registry.service.ts:87-101`). 보류 해제 단언의 근거가 된다.
접두어 `PREFIX` 고유(`I19` 등) · ⛔ **`TRUNCATE` 금지**.

### 8-2. 정리 — 자가 치유 `deleteMany`(역순)

`lot_status_event` → `inspection_measurement` → `inspection_result`(**회차 자식 먼저**) → `inspection_request` → `inspection_item_spec` → `inspection_plan_version` → `inspection_plan` → `equipment_calibration` → `lot_hold` → `lot_lifecycle_history` → `production_result_lot_allocation` → `production_result` → `lot` → `work_order` → `production_plan` → `production_order` → `routing_operation` → `routing` → `bom_component`·`bom` → `equipment` → `terminal` → `worker` → `process` → `item` → `plant` → `business_unit` → `legal_entity`. `beforeAll`·`afterAll` 둘 다.

### 8-3. 테스트 이름 — `quality-inspection-result.e2e-spec.ts` **32**

**의뢰 조회(7)**
1. 의뢰 목록이 `page`·`size`·`total` 골격으로 온다
2. `pendingOnly=true` 는 `REQUESTED`·`IN_PROGRESS` **둘 다** 담는다 — 상태 하나로는 못 고른다
3. `pendingOnly` 기본값은 `false` 라 완료·생략·취소도 온다
4. `supplierId` 가 입하 2단 조인으로 걸러진다
5. `q` 는 의뢰번호만 훑는다 — 품목명으로는 안 걸린다
6. ⭐ 기본 정렬이 **의뢰 시각 오름차순**이다(오래 기다린 것부터)
7. 의뢰 상세가 200 이고 없는 id 는 404 다 — ⭐ **ETag 를 안 낸다**

**결과 조회(8)**
8. ⛔ `inspectionRequestId` 도 기간도 없으면 **400 `REQUIRED`**
9. `inspectionRequestId` 만 주면 기간 없이 200 이다(한 의뢰의 회차를 읽는 길)
10. 기간만 주면 200 이다
11. ⭐ `finalRoundOnly=false` 면 `items.length > page.total` 이다 — 사슬이 뿌리와 같은 페이지에 동거한다
12. ⭐ 사슬 안은 **회차 오름차순**이고 뿌리 사이는 `sort` 축이다
13. `finalRoundOnly=true` 는 의뢰마다 **최종 회차 1건**만 낸다
14. `sort` 가 허용 3키 밖이면 400 `INVALID`
15. 결과 상세가 **ETag 를 낸다** — 값이 `versionNo` 와 같다

**측정치(4)**
16. `/measurements` 가 페이지 골격으로 오고 기본 크기가 50 이다
17. 없는 결과 id 는 **빈 목록 + `total=0`** 이다(404 가 아니다)
18. `inspectionItemSpecId` 로 한 항목만 좁혀진다
19. ⭐ `calibrationExpiredAtMeasurement` 를 **서버가 판정해 행마다 싣는다** — 만료 장비 행만 참이다

**저장(8)**
20. `POST` `statusCode=DRAFT` 가 **수량 합이 안 맞아도 201** 이다(M-e ⓑ)
21. `POST` `statusCode=CONFIRMED` 가 수량 합이 안 맞으면 400 `INVALID`
22. `POST` `statusCode=CONFIRMED` 에 `overallJudgmentCode` 가 없으면 400 `REQUIRED`
23. ⭐ `POST` `statusCode=CONFIRMED` 가 `:confirm` 과 **같은 부수효과**를 낸다 — LOT 상태·`confirmed_at`·`lot_status_event` 1행
24. ⭐ **기준 없는 의뢰**(`inspectionPlanVersionId=null`)에 `measurements` 없이 201 이 난다 — `remarks` 자유 입력만으로 선다
25. 같은 `Idempotency-Key` 재전송이 **한 건**이다 — `inspection_result` 행이 늘지 않는다
26. `previousResultId` 를 실으면 회차가 **+1** 이고 같은 의뢰에 매달린다
27. 없는 `inspectionRequestId` 는 **400 `INVALID`**(404 가 아니다 — 계약 미선언)
28. `X-Worker-No` 없이 계정 토큰만으로 201 이다 — 서버가 세션에서 검사자를 푼다

**수정(4)**
29. `PUT` 이 작성중 결과를 고친다 — 새 ETag 가 온다
30. ⭐ 확정된 결과를 `PUT` 하면 **409 `INVALID_STATE`**
31. `If-Match` 가 어긋나면 409 `VERSION_CONFLICT` + `currentVersion`
32. `measurements` 를 실으면 **치환**이고 생략하면 손대지 않는다

**확정(9)** — ⭐ 심장
33. `:confirm` 합격이 LOT 을 `NORMAL` 로 옮기고 `lot_status_event.transition_code='C4'` 를 남긴다
34. `:confirm` 불합격이 `DEFECTIVE` + `C6`
35. `:confirm` 보류가 `INSPECTION_PENDING` + `C5`
36. ⭐ `:confirm` 이 `inspection_request.status_code` 를 `COMPLETED` 로 옮긴다
37. ⭐ 합격 확정이 **수입검사 대기 보류를 닫는다** — `released_at`·`released_by`·`release_reason_code` 셋이 함께 찬다
38. ⭐ 보류 판정은 **보류를 닫지 않는다**
39. ⭐ **재검 회차 확정이 `DEFECTIVE` 에서 출발해도 200** 이다 — `from` 을 안 좁힌 근거
40. 이미 확정된 결과를 다시 `:confirm` 하면 **400 `STATE_LOCKED`**
41. `If-Match` 없이 `:confirm` 하면 428/400(가드) — 계약이 필수로 선언했다

**C14(3)**
42. ⭐ PQC 불합격 수량이 `acceptance_number` 를 넘으면 **같은 W/O 의 생산LOT 전체**가 `INSPECTION_PENDING` 이 되고 각각 `C14` 이력을 남긴다
43. `acceptance_number` 가 null 이면 **판정을 건너뛴다** — 전이가 0건이다
44. IQC 확정은 `acceptance_number` 가 있어도 C14 를 돌지 않는다

**권한(1)**
45. 무권한 계정은 `POST`·`PUT`·`:confirm` 에서 **403**(3건)

### 8-4. 테스트 이름 — `quality-inspection-summary.e2e-spec.ts` **14**

1. ⛔ `summary` 에 기간이 없으면 400 `REQUIRED`
2. `summary` 5칸이 **필터 전체 기준** 합계다 — 페이지와 무관하다
3. ⭐ `defectRate` 의 분모가 **검사 수량**이다(생산 수량이 아니다)
4. `summary` 가 `finalRoundOnly` 를 **응답에 되돌려준다**
5. ⭐ `calibrationExpiredCount` 가 나오되 **합계에서 빠지지 않는다**
6. `calibrationExpired=exclude` 를 주면 그때만 빠진다
7. `asOf` 가 **서버 시각**이다
8. ⛔ `defect-rate-trend` 에 기간이 없으면 400
9. `trend` 가 **일자 버킷**으로 오고 점마다 4칸이 찬다
10. 검사가 0건인 날은 **점이 없다**(0 으로 채우지 않는다)
11. `measurement-summary` 가 **항목 단위**로 접히고 `asOf` 를 낸다
12. ⭐ `outOfSpecValues` 가 **최대 10건**이고 `outOfSpecTotalCount` 는 전체다
13. ⭐ `unmeasuredCount` 가 **값 3칸이 전부 빈 행**을 센다
14. 없는 결과 id 의 `measurement-summary` 는 404 다

### 8-5. 단위 테스트 — **12**

`inspection-rules.ts`: ① 수량 합 판정(확정/작성중 두 갈래) ② 기간 유계 400 셋 ③ 정렬 키 화이트리스트 ④ 회차 +1
`calibration.ts`: ⑤ `valid_until < 측정일` 만료 ⑥ **검교정 이력 0건 = 만료**(0 으로 접지 않는다) ⑦ `valid_until` null = 만료 ⑧ 규격 밖 판정이 NUMERIC 항목에만 선다
`inspection-result-view.ts`: ⑨ `processId` 두 갈래(W/O 축 우선 · 기준 축 fallback · 둘 다 없으면 **키 생략**) ⑩ `lotNo` 파생
`lot-quality-status.service.ts`: ⑪ 등록 안 된 전이는 **던진다**(F-6) ⑫ `sourceDocument*` 를 **둘 다 주거나 둘 다 안 준다**(`ck_lot_status_event_source`)

### 8-6. M3 체인 마디 — 이 슬라이스가 **첫 마디를 만든다**

`plan.md` §2 85행 M3: 「실적 → 검사 의뢰 → 확정(전이) → 불합격 → 보류 → 부적합 → 처분 SCRAP → 폐기 출고 원장 감소」.
이 슬라이스가 만드는 끝점 셋: ⓐ `:confirm` 이 200 ⓑ `lot.status_code` 가 옮겨졌다 ⓒ `lot_status_event` 1행이 `transition_code` 를 갖고 섰다.
⚠ **「검사 의뢰」 마디는 오퍼레이션으로 안 선다** — 의뢰를 만드는 코드가 0건이라 체인 e2e 도 prisma 로 심는다(§0 #5 · 통합자에게 인계).

---

## 9. 설계 미정 자리 — README §2 절차로 전건 판정

### 9-1. 판정 요약표

| # | 자리 | 계약·화면·물리 | §2 판정 | 이 슬라이스의 결정 | 문의 |
|:-:|---|---|---|---|:-:|
| 1 | ⭐ **`transitions.ts` 가 「회신 E-3 대기」라 적었는데 E-3 은 종결됐다** | `transitions.ts:37-42` ↔ `LOT상태-확정기록.md:5`·`P-02-13:435`·`W-06-04:201`·계약 「근거: 회신 E-3 종결 2026-08-07」 | **0단계 — 설계 확정문이 답을 준다** | **전이표를 채운다**(§6-2) · 주석과 spec 단언을 함께 갱신 | ✕ |
| 2 | ⭐ **품질 축의 착지처가 문서마다 다르다** — `trace.lot.status_code` vs `inventory_balance.quality_status_code` | 확정 기록 `:93-94` 는 ①수명주기=`trace.lot.status_code` · ②품질=`inventory_balance.quality_status_code` 로 적었다. **우리 물리는 반대다** — `20260901010000/migration.sql:71` 이 `trace.lot.status_code` 를 「품질 판정 축」으로, `:36` 이 `lifecycle_status_code` 를 신설로 못박았다. `server-architecture.md:146` 은 「`trace.lot.status_code` **+ 재고 행의 `quality_status_code`**」로 둘 다 적고 `:213` 은 「`omf-mes#115` 에서 **재판정 중**」이라 열어 두었다 | 1단계 **본길**(모든 확정) → 0단계 — **이미 선 마이그레이션 주석이 선례다** | ⭐ **`trace.lot.status_code` 만 옮긴다.** `inventory_balance.quality_status_code` 는 **건드리지 않는다** — 재고 행의 차원을 바꾸는 것은 원장을 지나는 일이고(`uq_inventory_balance_dim` 차원) 이 슬라이스는 원장이 없다 | **069+13** |
| 3 | ⭐ **`:confirm` 전이의 `from` 집합** | 계약이 도착 상태만 적고 출발을 안 적었다. `plan-api.md:857-859` 가 `INSPECTION_PENDING →` 로 읽었다 | 1단계 **본길**(재검·OQC 전건) → 계약 문자에 `from` 이 없다 → 0단계 선례 `transitions.ts:256`(도달 불가한 `POSTED` 를 좁히지 않았다) | **`['NORMAL','INSPECTION_PENDING','DEFECTIVE']`** · `SCRAPPED` 만 제외 | **069+14** |
| 4 | ⭐ **`ck_inspection_result_qty` 가 무조건이다** | baseline `:1870-1871` ↔ 계약 `:3789` 「작성중은 통과시킨다」 | 1단계 **본길**(모든 임시 저장) → 계약 문자 그대로 | **M-e ⓑ 로 완화**(§2-4) | **069+4** 에 합침 |
| 5 | ⭐ **`:confirm` 의 보류 해제 사유 코드가 목록에 없다** | 계약 `:3621` 「보류 해제가 따른다」 · `W-01-01:123` · `ck_lot_hold_release_reason` · 시드 4값이 전부 「재검사/조사/직권」 | 물음 A(해제하나) = **본길 · 계약 문자 = 해제한다.** 물음 B(사유) = 값 목록 부재 → README §0 「값 정의는 권고안대로 + 요청서」 | **해제한다** · 대상은 `INCOMING_INSPECTION_WAIT` + 미해제 · 합격 `RETEST_PASS` / 불합격 `RETEST_FAIL` / 보류는 해제 안 함 | **069+3** |
| 6 | ⭐ **검사 의뢰를 만드는 코드가 0건이다** | 계약 `:3521` 「⛔ 등록 경로를 두지 않는다 … 서버가 입고·생산 실적에서 만든다」 · `grep` 쓰기 사용처 0 · I-3·I-7 병합본이 안 만든다 | 1단계 — 이 슬라이스의 11건 밖이다 → 0단계 선례 `I-24.md` §11 ④(ERP 수신기) | ⛔ **짓지 않는다.** e2e 가 prisma 로 심는다 | **069+1** |
| 7 | **`processId` 파생 경로** | `inspection_result`·`inspection_request` 에 공정 칸 0 · 후보 둘(§2-3) · 계약은 「2단 조인」 | 가장자리 → 2단계 기준 4(조용히 도출하지 않는 쪽)·기준 5 | **W/O 축 우선 → 기준 축 fallback → 둘 다 없으면 키 생략** | **069+5** |
| 8 | **C14 의 「불합격 수」가 개수인가 수량인가** | 계약 「불합격 **수**」 · `acceptance_number` 는 정수 · `rejected_qty` 는 Decimal · 측정치는 «항목×샘플»이라 개수 축이 다르다 | 가장자리 → 2단계 기준 5(새 개념 수가 적은 쪽) | **`rejected_qty` 를 쓴다** | **069+7** |
| 9 | **`C15`(전수 재검 양품)를 언제 쓰나** | `C4` 와 (from,to) 가 같다 · 구분 표식 0 | 가장자리 → 기준 4 | ⛔ **쓰지 않는다** — `C4` 하나 | **069+8** |
| 10 | **`SHIPMENT_REQUEST` 대상 OQC 의 LOT 집합** | 계약이 `shipment_lot_allocation` 으로 넓히라고 적지 않았다 | 가장자리 → 기준 4 | **0건 — 전이 없이 확정만 한다** | **069+6** |
| 11 | **작성중인데 `overall_judgment_code` 가 NOT NULL** | 물리 NOT NULL ↔ 계약 「확정이면 필수」 | 가장자리 → 2단계 기준 3(스키마를 안 늘리는 쪽)·기준 2(거부) | ⚠ **정정(PR ②b)** — 이 판정(컬럼을 완화하지 않는다)은 **뒤집혔다**. R-2 재수립이 M-e ⓒ(§2-4)를 신설해 컬럼을 **nullable 로 풀었다**(임시 저장 본길을 열기 위해 — `20260907174412_inspection_request_plan_and_result_draft_relax`). 확정 필수는 여전히 서비스가 400 `REQUIRED`(PR ③④)로 막지만, 그 결과 **읽기 계약**(`InspectionResult.overallJudgmentCode` — required·`type:"string"`, 널 없음)이 DRAFT+NULL 행을 못 그리는 새 자리가 열렸다 → PR ②b 가 **키 생략**으로 판정했다(선례 054 · §2 2단계 기준 4) | **069+4**(작성중 필수는 유지) · **085**(읽기 표현 — 신규) |
| 12 | **`PUT` 의 `measurements` 가 치환인가 병합인가** | 계약 침묵 · `uq_inspection_measurement` 는 병합 키를 준다 | 가장자리 → 기준 4 | **치환**(보낸 배열이 전부) · 생략은 무변경 | ✕ · 「알려둘 것」 |
| 13 | **`POST` 의 선택 `If-Match` 가 무엇의 버전인가** | 계약이 안 적었다 · 결과는 아직 없다 | 가장자리 → 기준 4 | **의뢰의 `version_no`** 로 대조 · 없으면 건너뛴다 | **069+10** |
| 14 | **관리웹 계정에 `worker` 행이 없으면** | `inspector_id` NOT NULL · `worker.app_user_id` nullable · 계약·화면 침묵 | 가장자리 → 기준 2(거부) | **400 `REQUIRED`** | **069+11** |
| 15 | **교정 이력이 0건인 장비** | 계약 「서버가 판정한다」 · `plan.md` §5-10 「판정 불가를 0/정상으로 접지 않는다」 | 0단계 — 횡단 규칙이 답을 준다 | **만료로 본다** · `valid_until` null 도 만료 | **069+9** |
| 16 | **`inspection-requests` 기본 정렬** | 계약에 `sort` 가 없다 · `W-01-01` §3 은 「검사 대기 큐」 | 가장자리 → 0단계(L-12 「경과일 긴 순」) | **`requested_at asc`** | **069+2** |
| 17 | **`defectRate` 의 분모가 0 일 때** | `defectRate` 가 required 라 키 생략이 계약 위반 | 가장자리 → 기준 2 | **`0` 을 낸다** · ⚠ 「판정 불가」와 구분되지 않는다 | ✕ · 「알려둘 것」 |
| 18 | **`trend` 버킷의 시간대** | 결과에 공장 칸이 0 · `plant.timezone_code` 로 풀 축이 없다 | 가장자리 → 기준 4 | **UTC 날짜** | ✕ · 「알려둘 것」(I-6 `WO-` 와 같은 계열) |
| 19 | **재등록 전이의 `transitionCode`** | C4~C15 에 재등록이 없다 · `lot_status_event.transition_code` NOT NULL | 1단계 — **I-23(레인 C) 범위** | ⛔ **`transitionCode` 없이 등록**하고 레인 C 에 인계 | **069+12** |
| 20 | **관리자 알람(C14)** | 계약 「관리자 알람을 낸다」 · 알림은 I-28(레인 B) | 1단계 — 이 슬라이스 밖 | ⛔ **내지 않는다** | ✕ · 「알려둘 것」 |
| 21 | **`judgment_type_control` 을 읽나** | 시드 `JUDGMENT_TYPE` **값 0건** · I-4 R-7 이 「행을 채우는 것이 I-20 의 일」이라 적었다 | 0단계 — 선행 슬라이스가 이미 판정 | ⛔ **읽지도 쓰지도 않는다** | 기존 회신 E-1 |
| 22 | **`x-no-code-key` 16자리 NOT NULL 완화**(`plan.md` §4 N) | 이 슬라이스가 처음 쓰는 표에 해당 칸이 있나 — `inspection_request.status_code`·`inspection_result.status_code` 둘 다 **계약이 `required` 로 적었다** | 0단계 — `plan.md` §4 N 이 「계약이 `required` 로 적은 자리는 제외」 | ⛔ **완화하지 않는다** | ✕ |

**기존으로 미루는 것**: `omf-mes#115`(재고 행 품질 축 재판정 — 069+13 이 그 위에 선다) · `#213`(상태값 코드 문자열 — 이 슬라이스가 쓰는 축은 전부 시드에 실재해 걸리지 않는다) · 회신 **E-1**(`JUDGMENT_TYPE` 값 목록 — I-20) · 회신 **E-9 ①**(교정 만료 무효화 정책 — 계약이 「자동 제외 없음」으로 이미 닫았다).

**문의 아님으로 닫는 것**: `LOT_STATUS`·`LOT_STATUS_TRANSITION`·`LOT_HOLD_RELEASE_REASON`·`INSPECTION_RESULT_OVERALL_JUDGMENT`·`INSPECTION_MEASUREMENT_JUDGMENT` 값 목록(**전부 시드·계약에 실재**) · `INSPECTION_REQUEST_STATUS` 5값(계약이 「확정 5값」으로 닫았다) · `#62` 검사자≠확정자(**해소 2026-08-07** — 분리하지 않는다) · `W-06-13` 검사정책 화면(**폐지 확정 2026-08-03** — `W-06-02` 가 흡수).

**대기 15(`businessDate` 미저장)**: ⭐ **누적이 늘지 않는다** — 이 계약 파일에 `businessDate` 프로퍼티가 **0건**이다(§5-3).

### 9-2. 문의 후보 — **신규 14건**(제목만 · 번호는 「069+n」 · 통합자가 확정 · 레인 A 대역 069~089) **+ PR ②b 구현 중 1건 추가**

| # | 제목 | 걸리는 오퍼레이션 |
|:-:|---|---|
| **069+1** ⭐ | **검사 의뢰를 만드는 주체가 서버 어디에도 없다 — 11건 중 8건이 영원히 빈 목록 위에 선다** | GET 2 · 쓰기 3 |
| **069+2** | **검사 대기 큐의 기본 정렬을 아무도 적지 않았다 — `sort` 도 없고 화면도 순서를 말하지 않는다** | `GET /quality/inspection-requests` |
| **069+3** ⭐ | **검사 확정이 「보류 해제」를 하는데 1회차 합격의 해제 사유 코드가 목록에 없다 — CHECK 는 사유를 강제한다** | `:confirm` · `POST` |
| **069+4** ⭐ | **「작성중은 통과시킨다」를 DB 가 모른다 — CHECK 는 무조건이고 종합 판정 칸은 NOT NULL 이다** | `POST` · `PUT` |
| **069+5** | **응답의 `processId` 를 낼 축이 물리에 없고, 계약이 말한 「2단 조인」은 PQC 에서만 성립한다** | `GET` 목록·상세 · `summary` · `trend` |
| **069+6** | **출하지시 대상 OQC 확정이 어느 LOT 도 옮기지 않는다 — 배분표로 넓히라는 문장이 없다** | `:confirm` |
| **069+7** | **C14 의 「불합격 수」가 개수인지 수량인지 갈린다 — 기준값은 정수인데 우리가 가진 것은 수량이다** | `:confirm` · `POST` |
| **069+8** | **`C15`(전수 재검 양품)가 `C4` 와 구분되지 않는다 — 어느 LOT 이 C14 로 왔는지 가릴 표식이 없다** | `:confirm` |
| **069+9** | **검교정 이력이 0건이거나 유효기한이 빈 장비를 「만료」로 볼 것인가** | `/measurements` · `summary` · `trend` · `measurement-summary` |
| **069+10** | **오프라인 저장의 선택 `If-Match` 가 무엇의 버전인지 계약이 말하지 않는다 — 결과는 아직 없다** | `POST` |
| **069+11** | **관리웹 계정에 작업자 행이 없으면 검사를 저장할 수 없다 — 검사자 칸이 NOT NULL 이고 연결을 요구하는 문장이 없다** | `POST` |
| **069+12** ⭐ | **재고 재등록 전이에 붙일 `transitionCode` 가 C4~C15 에 없다 — 이력 칸은 NOT NULL 이다**(레인 C I-23 이 막힌다) | `POST /logistics/stock-reinstatements`(I-23) |
| **069+13** ⭐ | **품질 판정 축의 착지처를 설계 확정문과 우리 마이그레이션이 반대로 적었다 — `omf-mes#115` 가 아직 재판정 중이다** | `:confirm` · I-20 · I-23 |
| **069+14** | **검사 확정 전이의 «출발 상태»를 계약이 적지 않아, 좁히면 재검과 OQC 가 통째로 막힌다** | `:confirm` |
| **085**(=069+16) ⭐ | **DRAFT 행의 종합 판정 칸을 계약이 못 그린다 — required·`type:"string"`(널 없음)인데 M-e ⓒ 뒤 물리는 nullable 이다.** PR ②b 가 키 생략으로 구현(선례 054 · `docs/design-inquiries/085-*.md`) — 054 와 묶어 답해 달라 | `GET /quality/inspection-results` · `GET .../{id}`(구현됨) · `POST`(PR ③, 같은 갈래 재등장) |

### 9-3. 「알려둘 것」 — **11건**(번호 없음 · 다음 전달분 말미)

ⓐ `PUT` 이 계약 미선언인 ETag 를 내린다(`runVersioned` 관행 · I-1 과 같은 자리) ⓑ `supplierId` 조인이 계약 문장의 「2단」이 아니라 **3단**이다(`lot → inbound_receipt_line → inbound_receipt`) ⓒ `PUT` 의 `measurements` 는 치환이고 생략은 무변경이다 ⓓ `defectRate` 는 분모가 0 이어도 `0` 을 낸다(required 라 생략 불가 — 「판정 불가」와 구분되지 않는다) ⓔ `trend` 의 버킷 축이 **UTC 날짜**다(하노이 자정 부근 검사가 전날 점에 붙는다) ⓕ C14 의 관리자 알람을 내지 않는다(알림은 I-28) ⓖ `moveWithin` 이 LOT `version_no` 를 올려 LOT 화면의 If-Match 가 확정 뒤 낡는다(I-6 R-22 와 같은 자리) ⓗ `uq_inspection_round` 경합을 `FOR UPDATE` 로 막지 않고 `DUPLICATE_KEY` 409 로 낸다 ⓘ `:confirm` 이 `inspection_request.status_code` 를 전이표 밖에서 직접 UPDATE 한다(계약이 「부수 효과」로 적었다) ⓙ `inspection_request_no` 채번 접두어 `IR` 이 이미 입하(`INBOUND_RECEIPT`)의 것이다 — 오늘 부를 자리가 0 이라 더하지 않았다 ⓚ 문의 14 채번 표에 **`inspection_result_no`(`IRS-`)** 한 행을 더한다.

---

## 10. PR 분할 — **5**(⚠ 통합 계획서 셋이 전부 **4** 다)

> ⛔ **R-18 이 이 절을 대체했다 — PR 은 7이다.** 순서: **M-e(마이그 단독) ∥ ②a → ①(전이 코어) → ②b → ③ → ④ → ⑤**. 아래 5행 표는 파일·내용 참고용으로만 읽는다.


| PR | 내용 | 파일 | 비테스트 예산 | 모델 | 스택 |
|:-:|---|---|---:|---|---|
| **①** ⭐ **품질 축 전이 코어**(⛔ **독립·앞쪽 · 레인 C 가 기다린다**) | `transitions.ts` 키 2 신설(전이 10) + `LotQualityStatusService.moveWithin()` + 모듈 등록. **검사 코드 0줄** | `src/core/document-state/transitions.ts`(+60) · `document-state.spec.ts`(단언 3자리 갱신) · `src/core/lot/lot-quality-status.service.ts`(신 · ~95) · `lot-registry.module.ts`(+3) · 단위 spec 2파일 | **≤170** (CLAUDE.md 코어 ≤200 안) | **opus** | **main 위 · 첫 병합** |
| **②** 조회 4 + 골격 + 뷰 | `GET …/inspection-requests` ×2 · `GET …/inspection-results` 목록(재검 사슬)·상세 | `inspection-request.{controller,service}.ts`·`inspection-request-view.ts`(신 3) · `inspection-result.controller.ts`·`inspection-result-query.service.ts`·`inspection-result-view.ts`(신 3) · `inspection-rules.ts`(신) · `quality.module.ts`(+10) · e2e 신 1파일(픽스처 + 조회 15) | **≤330** | **sonnet** | main 위 (① 과 파일 0겹침 · **∥ 병렬**) |
| **③** 마이그 M-e + 쓰기 2 | **커밋 1** 마이그(ⓐⓑ) + `schema.prisma`(+2) · **커밋 2** `POST`·`PUT` + 측정치 + 회차 + 채번 1줄 | `prisma/migrations/…_inspection_request_plan_optional/`(신) · `schema.prisma` · `numbering.service.ts`(+2) · `inspection-result-write.service.ts`(신) · 컨트롤러(+35) · e2e +12 | **≤300** | **opus**(마이그가 들어 병합 전 한 줄 보고 — README §1-5 · `lanes.md` §3) | ② 위 |
| **④** ⭐ **`:confirm` 심장** | 한 트랜잭션 + 전이 배선 + C14 + 보류 해제 + M3 마디 | `inspection-confirm.service.ts`(신) · 컨트롤러(+25) · e2e +12 · 단위 spec | **≤260** | **opus** | ③ 위 |
| **⑤** 집계 3 + 측정치 목록 | `summary` · `defect-rate-trend` · `measurement-summary` · `/measurements` | `inspection-summary.service.ts`·`calibration.ts`(신 2) · 컨트롤러(+30) · e2e 신 1파일(14) | **≤290** | **sonnet** | ④ 위 |

- ⭐ **왜 5 인가 (통합 계획서는 4)** — ⓐ **①(전이 코어)을 앞으로 뗀 것이 레인 간 선행 요구**다(`lanes.md` §0 · 브리프 §3). ⓑ **집계 3건을 목록과 같은 PR 에 넣지 않는다** — `plan-uiux.md:1067` 이 「목록과 같은 PR 에 넣으면 리뷰가 「이 수가 맞나」를 판정할 수 없다」로 이미 근거를 적었고, 실측한 규칙 수(교정 만료 파생 + 3원 분리 + 10건 잘림 + `outOfSpecTotalCount` 역산 금지)가 그 칸을 넘긴다. ⓒ ②③④⑤ 를 합치면 비테스트가 **~1,180** 이라 400×4 로도 안 들어간다.
- ⭐ **①은 «나란히» 스폰한다** — README §6-1 ③ 은 조회 PR 을 리뷰 라운드와 나란히 두라 했지만, ①은 코어라 그 규칙 밖이다(⛔ 「마이그·코어·심장 PR 에는 적용하지 않는다」). 대신 **②(순수 조회 · 마이그 0 · 코어 0)** 를 재검토 라운드와 나란히 띄운다.
- ⛔ **②③④⑤ 는 직렬**이다 — 넷이 `inspection-result.controller.ts` 를 함께 만진다. ⚠ `GET …/summary`·`…/defect-rate-trend` 는 `GET …/:inspectionResultId` **보다 먼저** 선언해야 한다(`ParseIntPipe` 가 `'summary'` 를 400 으로 낸다) — ⑤ 구현자에게 못 박는다.
- 게이트: 구현자가 PR 본문에 실측(명령·숫자)을 적는다. 리뷰어는 **단위 전체 + 그 PR 이 바꾼 e2e 파일만** 돈다. 회귀(`document-state.e2e-spec.ts`·`production-work-order`·`trace-lot`)는 통합자가 병합 직전 한 번(README §6).
- ⛔ **①의 병합 직후 사용자에게 한 줄 보고** — 코어 파일(`transitions.ts`)을 건드렸고(`lanes.md` §3) 레인 C 가 그 신호를 기다린다.

### 10-1. 통합 계획서 대조표

| # | 통합 계획서 | 이 계획안 | 판정 |
|:-:|---|---|:-:|
| 1 | `plan.md` §4 128행 · `plan-integration.md:604` — M-e = 「검사 의뢰 기준 완화」 **1항목** | **2항목**(+ `ck_inspection_result_qty` 조건부 완화) · 한 파일 | **다르다** ⭐ |
| 2 | `plan-api.md` S18 「마이그레이션 **없음**」(467행 인접) | **필요하다** — 계약 nullable ↔ 물리 NOT NULL · CHECK 무조건 | **다르다** ⭐ |
| 3 | `plan.md` §1 49행 · `plan-api.md` S18(PR 3) · `plan-integration.md:183`(PR 4) | **PR 5** | **다르다** |
| 4 | `plan-api.md` §5.1-E 857-859 — 전이 `from` = `INSPECTION_PENDING` | **3값**(`SCRAPPED` 만 제외) | **다르다** ⭐ |
| 5 | `plan-api.md` §5.1-E 「위 **9줄**만 등록」 | **9 등록**(수는 같다) — 단 `C15` 를 빼고 `:confirm` 의 보류 전이(`C5`)를 넣어 **구성이 다르다** | **다르다**(작다) |
| 6 | `plan-uiux.md` 179-181 — 쓰기 3건 헤더가 「멱등, **ETag**」 | 계약 실측 — **If-Match**(요청)이고 응답 ETag 는 **GET 상세 1건뿐** | **다르다**(계약 실측이 이긴다) |
| 7 | `plan-api.md` S18 「쓰는 표」 = 4표 | **7표** — `trace.lot`·`trace.lot_status_event`·`trace.lot_hold` 가 빠졌다(⭐ 심장이 그 셋을 쓴다) | **다르다** |
| 8 | `plan-api.md` S18 「설계 미정 자리」 = **전이표 하나** | **신규 문의 14건** | **다르다** ⭐ |
| 9 | `plan-integration.md:369` — 「회신 E-3 으로 확정됐다 → 여기서 채운다」 | 같다(실측으로 재확인 — `transitions.ts` 쪽이 낡았다) | 같다 |
| 10 | `plan-integration.md:370` — 매핑 4(합격→`NORMAL`·불합격→`DEFECTIVE`·보류→`INSPECTION_PENDING`·C14) | 같다 | 같다 |
| 11 | `plan-integration.md:369` · `plan.md` §5-8 — **원장 없음** | 같다(원장 0 · `inventory_balance` 무변경) | 같다 |
| 12 | `plan.md` §3 「계약이 이름 적은 전이만 등록. 미등록은 던진다(F-6)」 | 같다(§6-2 · `C15`·`SCRAPPED` 를 안 짓는다) | 같다 |
| 13 | `plan.md` §1 49행 — 선행 **I-7** | 같다(#238~#242 병합 · 픽스처를 복제한다) | 같다 |
| 14 | `plan.md` §5 규칙 1 — 403 게이트 등록 | 같다 — **이미 3건 다 등재**돼 있어 추가 0줄(실측) | 같다 |
| 15 | 브리프 §5 「규모 주의 — 인덱스가 필요하면 마이그에 싣되 근거를 적는다」 | **인덱스 0** · 근거 §2-5(UNIQUE 선두 칸이 받는다 · 나머지는 412·450행) | 같다(판정을 적었다) |

---

## 11. 인계

| # | 받는 곳 | 내용 |
|:-:|---|---|
| ① ⭐ | **레인 C · I-23 재등록** | ⛔ **R-6 정정 — PR ① 이 병합돼도 C 는 아직 못 푼다.** 전이 코어(`LotQualityStatusService.moveWithin(tx, lotIds, 'stock-reinstate', …)`)는 넘어가지만 **`lot_status_event.transition_code` 가 NOT NULL 인데 계약 enum 9값에 재등록을 가리키는 코드가 없다** — 문의 **069+12** 의 답이 있어야 한다. ⛔ **전이표를 복제하지 말 것**(`lanes.md` §1-4). ⚠ **`transitionCode` 가 없다** — `lot_status_event.transition_code` 는 NOT NULL 인데 C4~C15 에 재등록을 가리키는 코드가 없다(문의 **069+12**). C 는 값을 지어내지 말고 사용자에게 알린다 |
| ② | **I-20** LOT 상태·보류 | ⓐ 전이 4(`C7`·`C8`·`C9`·`C10`)가 **이미 등록돼 있다** — 배선만 한다 ⓑ ⭐ **`:confirm` 이 이미 보류를 닫는다**(§0 #4) — `:release` 는 「아직 열려 있는 보류」만 본다 ⓒ `judgment_type_control` 에 행을 채우는 것은 여전히 I-20 몫이다(I-4 R-7) ⓓ `GET /quality/lot-hold-events` 의 원천은 `lot_status_event` 가 아니라 `lot_hold` 다 — 이 슬라이스가 그 표에 쓴 `released_*` 3칸이 첫 데이터가 된다 |
| ③ | **I-21** 부적합·처분 | 처분 도착 상태(재작업→`INSPECTION_PENDING` · 폐기→`SCRAPPED` · 정상→`NORMAL`)의 전이는 **아직 등록하지 않았다** — ⭐ **`SCRAPPED` 로 «가는» 전이를 계약이 어느 오퍼레이션에도 적지 않았다**(`plan-api.md:871`). I-21 이 그 자리를 다시 판정하고, 등록이 필요하면 `transitions.ts` 는 레인 A 소유라 우리가 고친다 |
| ④ | **I-18** LOT 부가(∥ 병렬) | `GET /trace/lot-status-events` 가 읽는 표에 **이 슬라이스가 처음 행을 쓴다**. 그 조회가 `transitionCode` 9종 전건을 담는다고 적혔지만 실제로 서는 것은 **`C4`·`C5`·`C6`·`C14` 넷**이다(I-20 뒤 8) |
| ⑤ | **I-29** 통합 대시보드 | `summary` 의 5칸·`defectRate` 산식(§4-5)이 대시보드의 「불량률」과 **같은 축**이어야 한다. ⚠ `W-02-08` 수율(생산 수량 기준·손실 분리)과 **다른 수가 나오는 것이 정상**이다 — 배포 노트 |
| ⑥ | 후속 소형 PR·마이그 | ⓐ `inspection_result(inspected_at DESC)` 인덱스 — 결과가 450행을 크게 넘을 때(§2-5) ⓑ `inspection_request(status_code, requested_at)` — 대기 큐가 느려질 때 ⓒ `lot_status_event` 를 쓰는 코어에 **배치 INSERT** — C14 가 W/O 하나에 LOT 수십을 옮긴다 ⓓ 검사 의뢰 생성 훅(입하·실적) — **069+1 이 답하면** |
| ⑦ | **M3 체인 e2e**(통합자) | 마디 「실적 → 검사 의뢰 → 확정(전이)」. 끝점 셋만 단언 — `:confirm` 200 · `lot.status_code` 이동 · `lot_status_event` 1행. ⚠ **「검사 의뢰」 마디는 오퍼레이션이 없다** — prisma 로 심는다(§8-6) |
| ⑧ | 설계팀(문서로) | 069+13(품질 축 착지처)은 `omf-mes#115` 재판정과 **같은 자리**다 — 새 물음이 아니라 「우리 마이그레이션이 이미 한쪽을 골랐다」는 통지에 가깝다 |

---

## 12. 구현 뒤 남은 것 (리뷰 결과 · 통합자가 채운다)

### 12-1. 미완 목록

| # | 자리 | 내용 |
|:-:|---|---|
| ⓐ | | |
| ⓑ | | |
| ⓒ | | |

### 12-2. 슬라이스 마감

| | |
|---|---|
| PR | |
| 커버리지 | 342 → **353/487**(+11 · 이 슬라이스 전건) |
| 마이그레이션 | **1** — `<YYYYMMDDHHMMSS>_inspection_request_plan_optional`(M-e ⓐⓑ · 완화 2 · 삭제 0 · 백필 0) |
| 코어 | `transitions.ts` 키 신설 2(축 14 · 전이 40) · **`src/core/lot/lot-quality-status.service.ts` 신설** |
| e2e | 결과 · 집계 |
| 신규 문의 | **14건 — 069~082**(§9-2) |
| 리뷰가 잡은 것 | |

---

## ⭐ 실측 부록 (2026-09-07 · `main` 771c541 · 계약 a6a87e1)

> 리뷰어는 **이 표의 값을 재측정하지 않는다.** 단 자기 판정이 그 값을 뒤집는다면 반드시 재측정한다.
> 여기 든 것은 전부 **실측**이다 — 추론·판정은 본문에 있고 이 표에는 없다.

| # | 확인한 사실 | `파일:줄` |
|:-:|---|---|
| 1 | `awk -F'\t' '$1=="I-19"'` 가 **11행** — 브리프 §2 · `plan-integration.md` §10 과 전건 일치 | `docs/coverage-100/assignment.tsv` |
| 2 | 계약 파일의 오퍼레이션 총수 **27** · 그중 I-19 대상 **11** 일치 · 누락 0 | `contracts/quality-03품질.json`(python) |
| 3 | `transitions.ts` 에 `trace.lot.status_code` 키가 **없다** · 「회신 E-3 대기」 주석이 살아 있다 | `src/core/document-state/transitions.ts:37-42` |
| 4 | 등록된 상태 축은 **12**(`mdm.equipment`·`mdm.mold`·`planning.routing`·`planning.production_plan`·`trace.lot.lifecycle_status_code`·`production.work_order`·`production.work_session`·`app.approval_request`·`logistics.goods_issue`·`inbound_receipt`·`goods_receipt`·`putaway_task`) | `transitions.ts:57,74,95,117,129,167,198,215,240,256,257,266` |
| 5 | spec 이 **`TRANSITIONS['trace.lot.status_code']` 가 undefined 임을 단언**한다 · 테스트 이름에 「회신 E-3 대기」가 박혀 있다 | `src/core/document-state/document-state.spec.ts:227-229` |
| 6 | 등록 전이 총수 단언 = **28** | `document-state.spec.ts:276` |
| 7 | 축 목록 단언(12개 상수) | `document-state.spec.ts:253-272` |
| 8 | 등록 안 된 (칸, 액션) 은 **던진다**(F-6) | `src/core/document-state/document-state.service.ts:38-46` |
| 9 | `Transition.transitionCode` 는 **선택**이다 | `src/core/document-state/document-state.types.ts:16-20` |
| 10 | ⭐ **회신 E-3 은 2026-08-07 종결** — 「고객사 회신을 기다리지 않는다」 | `.design-reference/omf-mes/design/raw/process/uiux/2026-08-07-E3-판정유형-제안안/LOT상태-확정기록.md:5` |
| 11 | 확정값 = `정상`·`불량`·`검사 대기`·`폐기` **4** · LOT 종류 공통 | 같은 파일 `:19-26` |
| 12 | ⭐ 확정 기록 §3 이 품질 판정 축의 착지처를 **`inventory.inventory_balance.quality_status_code`** 로 적었다(수명주기는 `trace.lot.status_code`) | 같은 파일 `:93-94` |
| 13 | 「PQC 검사 필요」와 「보류」를 **`검사 대기` 하나로 합쳤다** | 같은 파일 `:44-52` · `:139-146` |
| 14 | `P-02-13` §8 #1 이 회신 E-3 을 **해소(2026-08-07)**로 옮겼다 — 판정 3값 · LOT 상태 4값 | `.design-reference/…/screens/02/P-02-13-PQC제품검사.md:435` |
| 15 | `W-06-04` §8 #5 도 같은 날 종결 · 특채·한도승인은 **상태가 아니라 조건** | `.design-reference/…/screens/06/W-06-04-판정유형코드마스터.md:201` |
| 16 | `W-01-01` §5-2 — 판정 확정이 `confirmed_at` + **Lot Status 전이** + **`lot_hold.released_at`·`released_by`** 를 함께 기록 | `.design-reference/…/screens/01/W-01-01-IQC수입검사판정.md:123` |
| 17 | `#62`(검사자≠확정자) **해소 2026-08-07 — 분리하지 않는다** | `W-01-01…:222` · `P-02-13…:436` |
| 18 | ⭐ `trace.lot.status_code` 컬럼 주석 = **「품질 판정 축(정상·불량·검사 대기·폐기 — LOT_STATUS)」** · DEFAULT `'ACTIVE'` 를 제거했다 | `prisma/migrations/20260901010000_add_lot_lifecycle_axis/migration.sql:71`(주석) · `:66`(DROP DEFAULT) |
| 19 | `lifecycle_status_code`·`completed_at` 이 같은 마이그에서 **신설**됐다 | 같은 파일 `:30-32` |
| 20 | `ck_lot_initial_qty` 를 **DROP 후 완화 재생성**한 선례 | 같은 파일 `:55-57` |
| 21 | ⭐ `lot_status_event.transition_code` **NOT NULL** 신설 · 「C4~C15 · 값 없이 들어온 이력은 영영 알 수 없다」 | `prisma/migrations/20260901030000_align_lot_status_event/migration.sql:20-24` |
| 22 | `lot_status_event.changed_by` 를 **NOT NULL 로 올렸다** | 같은 파일 `:41-42` |
| 23 | `ck_lot_status_event_source` — `source_document_type_code` 와 `source_document_id` 는 **둘 다 or 둘 다 아님** | 같은 파일 `:55-57` |
| 24 | ⭐ `ck_lot_hold_release_reason` — `released_at IS NULL OR release_reason_code IS NOT NULL` | 같은 파일 `:76-78` |
| 25 | ⭐ `ck_inspection_result_qty` 가 **조건 없는** `accepted+rejected+held = inspected` | `prisma/migrations/20260727000000_baseline_physical_model_v3/migration.sql:1870-1871` |
| 26 | `uq_inspection_round (inspection_request_id, inspection_round)` | 같은 파일 `:1868-1869` |
| 27 | `inspection_result.idempotency_key varchar(150) NOT NULL UNIQUE` — **컬럼**이다 | 같은 파일 `:1860` |
| 28 | ⭐ `inspection_request.inspection_plan_version_id` **NOT NULL** | 같은 파일 `:1819-1820` · `prisma/schema.prisma:3342` |
| 29 | `inspection_result.overall_judgment_code` **NOT NULL** · `inspector_id` **NOT NULL**(FK `mdm.worker`) | 같은 파일 `:1851-1852` |
| 30 | `ck_measurement_single_value` = `num_nonnulls(...) <= 1` — **전부 NULL 도 통과** | 같은 파일 `:1891-1893` |
| 31 | `uq_inspection_measurement (inspection_result_id, inspection_item_spec_id, sample_no)` | 같은 파일 `:1889-1890` |
| 32 | ⭐ 계약 `InspectionRequest.inspectionPlanVersionId` 는 **`[integer,null]`** · 「기준 미등록 상태에서도 검사를 진행한다는 고객 확정(2026-07-15)」 | `contracts/quality-03품질.json:3429` |
| 33 | ⭐ 계약 `InspectionRequest` 설명 = 「⛔ **등록 경로를 두지 않는다** … 서버가 입고·생산 실적에서 만든다」 | 같은 파일 `:3521` |
| 34 | ⭐ 계약 `InspectionResultCreate` 설명 = 「statusCode=확정 이면 … 강제한다 … **작성중은 통과시킨다**」 + 「검사자·단말은 «보내지 않는다»」 | 같은 파일 `:3789` |
| 35 | ⭐ `:confirm` 설명 = 「합격이면 정상, 불합격이면 불량, 보류면 검사 대기」 + **C14 일괄 전이** + 「기준값은 `GET /quality/inspection-plan-versions/{id}` 가 갖는다」 | 같은 파일 `:1465` |
| 36 | `POST /quality/inspection-results` 설명에 **같은 C14 문장**이 반복된다(확정 경로 둘의 부수효과 동일) | 같은 파일 `:925` |
| 37 | `InspectionResult.statusCode` 설명 = 「확정이면 Lot Status 전이와 **보류 해제**가 따른다」 | 같은 파일 `:3621`(응답) · `:3749`(Create) |
| 38 | ⭐ 목록 400 규칙 = 「⛔ `inspectionRequestId` 도 기간도 없으면 **400**」 + 재검 사슬 페이지 규칙 전문 | 같은 파일 `:753` |
| 39 | `sort` default = **`inspectedAt,desc`** · 허용 3키 | 같은 파일 `:859-861` |
| 40 | 측정치 규모 = 「의뢰 **412** · 결과 **450** … 측정치는 **135,000 자릿수**」 | 같은 파일 `:1378` |
| 41 | `inspectionRequestNo` example `IR-2026-0812-0412` · `inspectionResultNo` example **`IRS-2026-0812-0412-1`** | 같은 파일 `:3415` · `:3534` |
| 42 | 횡단 4축(python 실측) — 멱등 3 · If-Match 필수 2/선택 1 · **ETag 1**(상세만) · 403 3 · `WorkerNoOptional` 1 | `contracts/quality-03품질.json` |
| 43 | `QualityConflictResponse.code` enum **6값** · `currentLotStatusCode`·`conflictCause` 칸 실재 | 같은 파일(components.schemas) |
| 44 | 403 3건이 **`derived-permissions.ts` 에 이미 있다** | `src/common/permissions/derived-permissions.ts:250,251,280` |
| 45 | GET 7건도 도출표에 있다(`measurement-summary` 만 없다 — 403 미선언이라 무관) | 같은 파일 `:124-130` |
| 46 | 가드는 **계약이 403 을 선언한 자리에서만** 본다 | `src/common/permissions/permission.guard.ts:37-41` |
| 47 | ⭐ `inspection_request` 를 **쓰는 코드가 저장소에 0건**(맞는 것은 참조 카운트 표 한 줄뿐) | `src/quality/inspection-plan/inspection-plan-version.service.ts:23`(유일 히트) |
| 48 | `src/quality/` 는 오늘 `code/`·`inspection-plan/` 둘뿐 · 총 **2,160줄** · `quality.module.ts` imports 2 | `src/quality/quality.module.ts:19` |
| 49 | 입하 LOT 은 `status_code='INSPECTION_PENDING'` + `lot_hold(reason='INCOMING_INSPECTION_WAIT', status='HELD')` 로 태어난다 | `src/core/lot/lot-registry.service.ts:16,21,23,87,96-101` |
| 50 | 선발행 슬롯도 `INITIAL_LOT_STATUS`(=`INSPECTION_PENDING`) + `lifecycle='WAITING'` | 같은 파일 `:152-153` |
| 51 | `LotLifecycleService.moveWithin()` 의 형태(잠금 → 전이 → UPDATE → 이력 INSERT) | `src/core/lot/lot-lifecycle.service.ts:49-75` |
| 52 | 시드 `LOT_STATUS` **4값**(`NORMAL`·`INSPECTION_PENDING`·`DEFECTIVE`·`SCRAPPED` · `isSystemOwned`) | `prisma/seed.ts:543-551` |
| 53 | 시드 `LOT_STATUS_TRANSITION` **9값** `C4`·`C5`·`C6`·`C7`·`C8`·`C9`·`C10`·`C14`·`C15` | `prisma/seed.ts:517-530` |
| 54 | 시드 `LOT_HOLD_RELEASE_REASON` **4값** `RETEST_PASS`·`RETEST_FAIL`·`INVESTIGATION_CLEARED`·`MANAGER_OVERRIDE` | `prisma/seed.ts:438-446` |
| 55 | 시드 `JUDGMENT_TYPE` 은 **값 0건**(회신 E-1 대기) | `prisma/seed.ts:563-566` |
| 56 | `NUMBERING_RULES` 시드는 **`PRODUCTION_RESULT` 한 줄뿐** | `prisma/seed.ts:1425-1431` |
| 57 | `DEFAULT_PREFIX` 에 `INSPECTION_RESULT` 가 **없다** · `INBOUND_RECEIPT: 'IR'` 은 있다 | `src/core/numbering/numbering.service.ts:9-29`(특히 `:15`) |
| 58 | `entity_type_registry` 시드에 `INSPECTION_REQUEST`·`INSPECTION_RESULT` **둘 다 있다** | `prisma/seed.ts:1305-`(ENTITY_TYPES) |
| 59 | ⭐ `inspection_plan_version.acceptance_number Int?` 가 **합격판정개수**(C14 근거 필드) | `prisma/schema.prisma:3317` · `W-06-02-검사기준등록.md:106` |
| 60 | `inspection_plan.process_id BigInt?` 실재(공정 축 후보 ⓐ) | `prisma/schema.prisma:3282` |
| 61 | `work_order.routing_operation_id` NOT NULL → `routing_operation.process_id` NOT NULL(공정 축 후보 ⓑ · **2단**) | `prisma/schema.prisma:2886`(work_order) · `:2569`(routing_operation.process_id) |
| 62 | `equipment_calibration.valid_until DateTime?` **nullable** · `ix_equipment_calibration_history (equipment_id, calibration_date DESC)` 실재 | `prisma/schema.prisma:3210`(valid_until) · `:3220`(인덱스) |
| 63 | `worker.app_user_id BigInt?` 실재(계정↔작업자 연결 축) · `worker_no @unique` | `prisma/schema.prisma:2332`(app_user_id) · `:2327`(worker_no @unique) |
| 64 | I-7 의 주체 해석 = `X-Worker-No` 없으면 400 `REQUIRED` · `worker_no` 로 조회 | `src/production/production-result/production-result.service.ts:166-176` |
| 65 | `idempotency_key` 컬럼에 헤더 값을 그대로 넣는 선례 둘 | `production-result.service.ts:145` · `src/production/material-consumption/material-consumption.service.ts:126` |
| 66 | 채번은 `$transaction` **밖**에서 부르고 번호 문자열만 넘기는 선례 | `production-result.service.ts:77-84` |
| 67 | `runIdempotent`·`runVersioned` 시그니처(후자는 ETag 를 늘 낸다) | `src/common/master/master-write.ts:20,40-57` |
| 68 | `docs/server-architecture.md` §6 이 **`#280`(검사 의뢰 기준 완화)**를 「그 도메인 첫 PR 앞에 마이그레이션을 둔다」로 지정 | `docs/server-architecture.md:222-227` |
| 69 | 같은 문서 §5 #1 — 「재고 행의 품질 상태 축」이 `omf-mes#115` 에서 **재판정 중** | `docs/server-architecture.md:213` |
| 70 | 같은 문서 C-4 — 품질 판정 축 = 「`trace.lot.status_code` **+ 재고 행의 `quality_status_code`**」 · 이력은 `trace.lot_status_event` | `docs/server-architecture.md:146` |
| 71 | `plan-integration.md` §3-1 I-19 — 「원장: 없음」 · 「회신 E-3 으로 4값이 확정됐고 … **여기서 채운다**」 | `docs/coverage-100/plan-integration.md:368-369` |
| 72 | `lanes.md` §0 — C 의 I-23 재등록이 **A 의 I-19 품질 전이 코어 PR 병합**을 기다린다 | `docs/coverage-100/lanes.md`(§0 둘째 불릿) |
| 73 | `plan-api.md` §5.1-E — 「`C4`·`C5`·`C6`·`C15` 는 여는 오퍼레이션을 못 찾았다 … `SCRAPPED` 로 가는 전이도 계약에서 못 찾았다」 | `docs/coverage-100/plan-api.md:869-874` |
| 74 | `plan-uiux.md` §8 — 집계를 목록과 같은 PR 에 넣지 말라는 근거 | `docs/coverage-100/plan-uiux.md:1067` |
| 75 | main = **342/487**(2026-09-07) · 계약 사본 `a6a87e1` | `docs/coverage-100/lanes.md`(§1-5) · `contracts/COMMIT.txt` |
| 76 | 문의 번호 — `062` 까지 파일 실재 · `063~068` 은 I-24 예약 · 레인 A 대역 **069~089** | `docs/design-inquiries/` · `lanes.md` §1-1 |
