# I-6 개별 계획안 3관점 재검토 브리프 (README §1-2 「설계 문의가 새로 생김」 조건 발동 — 035~039)

대상: `docs/coverage-100/slices/I-6.md`(1,017줄). 결과는 **자기 관점 파일 하나**에만 쓴다 —
`docs/coverage-100/slices/I-6-review-{api|uiux|integration}.md` (150줄 이내). 다른 파일 수정 금지, 구현 금지,
체크아웃 변경 금지(현재 브랜치 `docs/coverage-100-i6-plan` 그대로), `git` 쓰기 금지. 계약 `contracts/*.json` 은 읽기 전용(`jq`).
로컬 DB 는 읽기만(`psql` SELECT · `migrate reset`·`migrate diff --shadow-database-url`·`db seed` 절대 금지).

계획자 결론 요지: **「차이가 크다」 ⭕ — 조건 ③(문의 035~039 신설)**. ① ✕(마이그는 M-d 의 «절반» — 4M 식 유일 인덱스 1건만 · `remainder_disposition_code` 는 `close_disposition_code` 로 이미 있다) · ② ✕(순서 그대로 · 인계 «자리» 셋). 통합 계획서와 크게 어긋난 자리 —
ⓐ 마이그 **1**(`uq_work_order_resource_plan` — `WHERE` 없는 **COALESCE 4칸 식** 유일 인덱스 · `schema.prisma` 손대지 않음 · `shift_id` 도 접음 · §2-6) · `plan-api.md` S14 「마이그 없음」·`plan.md` M-d 「둘」 모두 정정
ⓑ `transitions.ts` 에 `production.work_order.status_code` **키를 연다**(5 액션 · `-wo` 접미로 LOT 축과 이름 충돌 회피 · `:close` `from` 에 `IN_PROGRESS` 확정 · `conflictStatus` 400 · §3-1) · `lot_lifecycle_history` 를 쓰는 코어가 없어 **`LotLifecycleService.moveWithin()` 신설**(§3-2 · `from` 밖 슬롯은 «건너뜀» · I-7 이 L1 로 재사용)
ⓒ `:release`(§4): 코어 `preIssueWithin()` 자매 함수(기존 `createWithin` 은 `lot_hold` 를 강제해 못 씀) · `lot_no` 는 `mesLotNo()` 재사용 + `lot-number.ts` 를 `src/core/lot/` 로 **이관** · `plant_id` 는 라인→WIP 위치 순으로 도출(못 풀면 400) · `source_type_code='WORK_ORDER'` **시드 1행 추가**(036) · 자재 출고요청은 **`material_issue_request(_line)` 에 직접 INSERT**(I-8 대기 기각 · `status_code='REQUESTED'` 상수 판정 불사용 · `reason_code` NULL · 소요 = `required_qty × order_qty ÷ bom.base_qty` 스크랩률 미적용 · **`routing_operation_id` 가 W/O 의 것인 라인만** · 도착 위치 못 풀면 요청 안 만들고 배포는 성공 → 037) · `EMERGENCY` 만 제외 · `handoverNote` 는 저장 안 함
ⓓ `:close`(§5): `OPEN_SESSION_EXISTS` 409 를 **전이보다 먼저** · 「열린 세션」= `ended_at IS NULL`(`STOPPED` 도 열림) · 3분류 입력은 `SUM(production_result.good_qty)`(상태 필터 없음) vs `order_qty` · **허용 오차 상수 0**(`operation_policy` 14값에 미달 경계 키 없음·0행이라 읽지 않음) · 본문 4규칙 → 에러 상수 신설 3 · `close_disposition_code`·`completion_variance_reason_code` 에 저장 · **한 번의 UPDATE**(`trg_work_order_closed_immutable`) · L2 = `WAITING` ∧ `NOT EXISTS production_result_lot_allocation` · 아웃박스 `enqueue()` 신설(`message_key` = `{INTERFACE_CODE}:{문서번호}` · 버전 없음 · UNIQUE 충돌은 `alreadyQueued`) · `erpSendItems` 는 해석 없이 payload 에 그대로 · 개발품 제외 없음(전건) · **승인 게이트 없음** · 마감 전 게이트 셋 **아무것도 안 만듦**(039)
ⓔ `:cancel`(§6-1): 5 `from` · `cancellation_reason_code` 에 저장 · `document_cancellation` 다형 표 **안 씀** · `note` 버림 · L3 전건 · 기발행 출고요청 **건드리지 않음**(038)
ⓕ `:hold`/`:resume`(§6-2): 저장 칸·구간 표 없음 → **셋(`reasonCode`·`occurredAt`·`note`) 저장 안 함** · `WORK_ORDER_HOLD_REASON` 0값이라 **코드값 대조 안 걺** · `held=` 는 `status_code='SUSPENDED'` 근사 · `RELEASED`→hold→resume 이 **세션 없는 `IN_PROGRESS`** 를 만드는데 계약 문자 그대로 둠(035) · `X-Worker-No` 안 읽음
ⓖ `POST`(§7-1): `productionPlanId` 비면 내부 P/O+계획 생성(①안) — `bom_id` 는 품목 기본 BOM 후보 정확히 1 아니면 400 · `plan_no`/`production_order_no` 접두어를 `DEFAULT_PREFIX` 에 더해 I-24 승계 · `work_order_dependency` 안 만듦 · `PUT` 자물쇠 = `released_at IS NULL`(§7-2)
ⓗ 횡단(§8): 403 추가 **0**(8건 이미 등록) · `runIdempotent` 9 + `runVersioned` **불사용**(선택 If-Match 둘이 `undefined` 면 500 경로) · If-Match 필수 **4** · 409 는 셋뿐(VERSION_CONFLICT·멱등·`OPEN_SESSION_EXISTS`)+4M 중복 · `src/production/` 신설 · 인계 표(I-7·I-8·I-11·I-24·I-23)
ⓘ PR **4 → 7**(§10 ① 마이그+골격+상세 ~250 sonnet · ② 목록 ~300 sonnet · ③ validation+resource-plans 쓰기 ~240 · ④ POST/PUT+전이 키+hold/resume ~320 · ⑤a 코어 ~185 · ⑤b :release ~210 · ⑥ :close/:cancel+아웃박스 ~330) · 합 ~1,835.

## 공통으로 읽을 것
- `docs/coverage-100/README.md` §1-2·§2·§5·§6 · `docs/coverage-100/plan.md` §1 40행·§3 103~104·§4 M-d·§5 규칙 1·3·9·§7·§8
- `docs/coverage-100/slices/I-6.md` 전문 · `brief-I-6.md`(읽을 것 15·담을 것 13) · 선행 `I-5.md` §0-재수립 R-3·R-6·R-9·R-11 · `I-4.md` R-2·§3-3 · `I-2.md` R-2(채번 트랜잭션 밖)
- 계약 원문 `contracts/production-02생산실행.json` — 경로 10(2238·2558·2715·2802·2851·2905·2995·3085·3178·3267) · 스키마 `WorkOrderResourcePlan` 6086·`WorkOrderResourcePlanCreate` 6126·`WorkOrder` 6193·`WorkOrderCancel` 6456·`WorkOrderClose` 6475·`WorkOrderCreate` 6510·`WorkOrderHold` 6581·`WorkOrderListSummary` 6606·`WorkOrderRelease` 6674·`WorkOrderResume` 6694·`WorkOrderUpdate` 6712·`WorkOrderProgress` 7168 · `ProductionConflictResponse` · x-internal-note 6건 · `contracts/trace-*.json` 의 `Lot.sourceTypeCode`·`GET /trace/lots?workOrderId` · `contracts/integration-*.json` 의 `IntegrationMessage`
- 실재 코드: `src/core/document-state/transitions.ts`(192줄 · LOT 축 112~140) · `src/core/lot/lot-registry.service.ts`(`createWithin` 58~ · `lot_hold` 강제 78~86 · `LOT_HOLD_STATUS` 주석 19~21) · `src/trace/lot/lot-number.ts` · `src/core/numbering/numbering.service.ts`(`DEFAULT_PREFIX`·`prefixOf`·47~58 「tx 밖」·109~117) · `src/integration/message/integration-message.service.ts`(262줄 · 적재 없음) · `src/common/errors/error-codes.ts` · `src/common/permissions/derived-permissions.ts:115·237~242·279` · `src/common/http/master-write.ts:20·48~54` · `src/logistics/document-progress/document-cancel.service.ts:75-78`(If-Match 대조 선례) · `src/logistics/goods-issue/goods-issue-rules.ts:78`(plant 도출 선례)
- 물리: `prisma/schema.prisma` `work_order` 2876~2963(39칸 · `close_disposition_code`·`cancellation_reason_code`·`released_at`·`closed_at` · `bom_id` **없음**) · `work_order_resource_assignment` 4534~4558(`ck_work_order_resource_target`) · `lot` 3509~3585 · `lot_lifecycle_history` 4630~4645(`ck_lot_lifecycle_history_transition`) · `material_issue_request(_line)` 957~1006 · `integration_message` 389~410 · `production_plan` 2504~(`bom_id`·`routing_id` NOT NULL) · `production_order` · `operation_policy` 265 · `work_session` 2980~ · 마이그 `20260826000000_data_model_v4:72-73` · 트리거 `trg_work_order_closed_immutable`
- 시드 `prisma/seed.ts`: `WORK_ORDER_STATUS` 331~ · `LOT_LIFECYCLE_STATUS` 482~ · `LOT_LIFECYCLE_TRANSITION` · `LOT_SOURCE_TYPE`(2값) · `WORK_ORDER_HOLD_REASON` 1259(`values: []`) · `WORK_SESSION_EVENT_REASON`(7값) · `WORK_ORDER_CANCEL_REASON` 723 · `WORK_ORDER_COMPLETION_VARIANCE_REASON` 735 · `OPERATION_POLICY`(14값) · `MESSAGE_STATUS` · `NUMBERING_RULES`(`PRODUCTION_RESULT` 만)
- 기존 문의 `docs/design-inquiries/016~034`(특히 014 채번 표 · 026 · 032~034) · 설계 저장소 사본 `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/02/`(`W-02-02`~`W-02-08` · `W-02-10` · `P-02-10` · `P-02-12`)
- 자기 관점 계획서: api → `plan-api.md` S14(360~385)·§5.1-C(800~822)·§5.4·1043·1067·1103 / uiux → `plan-uiux.md` 54~55·U20·U21(309~330)·625·633·700·⑨(998~1010)·1202 · 화면 명세 `W-02-03` §4-C(validation 6규칙) · `W-02-04` §4-B · `W-02-05` §5-1·§8-1 · `W-02-06` · `W-02-07` §5-2 · `W-02-10` §5-6·§6·§8-3 · `P-02-10` §5-4 / integration → `plan-integration.md` 151~152·170·253~263·282·407~409·497~514·583·599·665·667·§4-1 M1·§6-2·§6-3·§9 #8

## 판정할 것 (각 항목에 ✅ 동의 / ✏ 수정(대안+근거) / ⛔ 반대(근거))
1. **문의 035~039 가 정말 새 문의인가**(§9-2) — 035 가 셋(사유 0값·구간 표 부재·`RELEASED` 재개 비대칭)을 한 장에 묶는 게 맞나, 갈라야 하나 · 036 이 시드 1행으로 닫히는 「권고안 구현」인가 문의감인가 · 037 ⓐⓑⓒ 중 ⓒ(도착 위치 못 풀면 요청 안 만듦)가 문의인가 판정인가 · 038 이 I-8 문의와 겹치지 않나 · 039 가 「계약 자인」(이월 자동 전개처럼 물을 자리 아님)이 아닌가. §9-1 17자리의 §2 절차 적용이 맞나 · 「기존으로 미룬 것」(허용 오차·#145·#66·이월·`achievementRate`·문의 14 표 4행) 중 새 번호가 필요한 것이 있나 · 「알려둘 것」 17건(§9-3) 중 문의로 올려야 할 것.
2. **마이그레이션 1**(§2-6) — `close_disposition_code` = `remainderDispositionCode` 칸이라는 실측(`data_model_v4:72`)에 동의하나 · COALESCE **4칸**(`shift_id` 포함) 식 유일 인덱스 SQL 이 맞나(`resource_id` 0 이 실재 id 와 겹칠 가능성 · `ck_work_order_resource_target` 과의 관계) · `schema.prisma` 안 건드림 + `migrate diff --from-schema-datasource` 드리프트를 「I-1 선례」로 넘기는 것이 게이트 원인불명 3회와 안 얽히나 · 사전 대조 SQL 주석 · `plan.md`·`plan-api.md`·`plan-integration.md`·`plan-uiux.md` 정정 18항(§11)이 빠짐없나(특히 #5 `work_order.bom_id` 없음 → I-8 문장).
3. **상태기계 두 축**(§3) — 새 키 5 액션 `from`/`to`(특히 `:close` `from` 에 `IN_PROGRESS` · `:hold` `from` 에 `RELEASED` 유지 · `:resume` → `IN_PROGRESS` 고정) · `-wo` 접미 이름 규약(대안: 접미 없이 두 축을 같은 이름으로 · 또는 `wo-close`) · `conflictStatus` 400 · `transitionCode` 안 씀 · **`moveWithin()` 코어 시그니처**(§3-2 · I-7 L1·I-19~21 이 그대로 쓴다 · `from` 밖 슬롯 «건너뜀» 이 옳은가 던져야 하나 · 반환 `movedLotIds`) · 생성 직후 이력 안 쓰는 것(`LOT_LIFECYCLE_TRANSITION` 3값에 「생성」 없음).
4. **`:release` 코어**(§4) — `preIssueWithin()` 자매 함수 vs `createWithin` 옵션 확장(사용처 둘 근거) · `lot-number.ts` 코어 이관 · `plant_id` 라인→WIP 도출 순서 · `status_code='INSPECTION_PENDING'` + `lot_hold` 미적용 · `bom_id` 를 `production_plan.bom_id` 에서(계획 없으면 NULL 짝) · 경계 5(N 계산 · `lotSize=0` 400 · Decimal) · **출고요청 직접 INSERT**(§4-3 — `server-architecture.md:67` 도메인 간 금지 하에서 «표 직접 쓰기» 가 선례 §9 #8 과 같은 자리인가 · I-8 이 승계할 규약 6이 충분한가 · `routing_operation_id` 일치 라인만 담는 판정 · 0건이면 헤더 없음) · 채번 N+1 을 tx 밖에서 · `handoverNote` 버림.
5. **`:close` 코어**(§5) — 순서(`OPEN_SESSION_EXISTS` 가 전이 검사보다 먼저) · `ended_at IS NULL` 정의(`STOPPED` 포함) · 3분류 입력(`good_qty` 합 · 상태 필터 없음 · 슬롯 합 불사용) · **허용 오차 상수 0**(`operation_policy` 안 읽음 — 기준 4 와의 정합) · 4규칙 → 에러 상수 3 신설 · **한 번의 UPDATE**(트리거) · L2 집합 SQL = `PreIssuedLotSummary.withResultCount` 와 같은 `EXISTS` · `enqueue()` 시그니처·`message_key` 규약·버전 없음·`alreadyQueued` · `IF-WO-CLOSE-SEND` 상수(정의 행 0) · payload 형태 · 승인 게이트 없음 · 게이트 셋 미구현.
6. **`:cancel`·`:hold`·`:resume`**(§6) — `document_cancellation` 안 쓰는 판정(I-5 축과 다름) · `note` 버림 · L3 집합 · 기발행 출고요청 방치 · **`:hold` 셋 저장 안 함 + 코드값 대조 안 걺**(계약 required 를 형식만 보는 것이 「계약 문자 그대로」인가) · `held=` 근사 · 세션 불변 e2e.
7. **생성·수정·4M·조회**(§7) — 내부 P/O+계획 생성 경로(`business_unit_id`·`plant_id` 도출 · 기본 BOM 정확히 1 · `plan_no`/`production_order_no` 접두어를 I-6 이 더하는 것 vs I-24 대기) · `PUT` 자물쇠 `released_at IS NULL` · null/undefined 7칸 · `resource-plans` 409 봉투 `ErrorResponse` · `validation` 규칙 6 · 목록 질의 23 where 매핑·`withSummary` required 둘 · **sonnet 배분**(①②).
8. **횡단·PR 분할**(§8·§10) — 403 추가 0 실측 · `runVersioned` 불사용 근거 · 409 셋 · 404 미선언 3건에서도 냄 · `src/production/` 배치·파일 9 · 인계 표 6행 · **PR 7 스택 순서·모델·예산**(각 ≤350 · ⑤a ≤200 · ④ ~320 이 `POST` 내부 계획 생성까지 지고도 예산 안인가 · ②③ 를 ① 위에 병렬 스택 가능한가) · 각 PR 단위 테스트 이름 목록이 e2e 로 못 가는 가드(경계 5 · 한 번의 UPDATE · L2/L3 집합 · `alreadyQueued` · `ended_at` 정의)를 덮는가 · §10-1 픽스처(routing·bom 은 planning e2e 에서 베낌 · `work_session`·`production_result` 직접 INSERT)·cleanup 순서 · M1 마디 e2e 를 ⑥ 이 지는 것.
9. 자기 관점 계획서와 I-6.md 가 어긋나는 자리 중 **구현에 영향 주는 것**만(취향 차이는 제외). §11 대조표의 자기 관점 행을 실측으로 확인. uiux 는 화면 명세(`W-02-03` validation 6규칙 표 · `W-02-04` 선발행 미리보기 칸 · `W-02-05` 3분류·`erpSendItems`·잔량 처리 UI · `W-02-06` 취소 사유·`note` · `W-02-07` 발행 직후 `:release` 동선(201 ETag) · `P-02-10` 홀드 사유 7값 · `W-02-10` 자동 발행 요청의 표시)와의 정합을 실측으로.

마지막에 5줄 요약: 「재수립 결과 — I-6.md 에 반영할 수정 N건(목록), plan.md 에 반영할 것, 문의 최종 건수, PR 분할 최종안」.
