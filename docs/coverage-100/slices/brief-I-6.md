# I-6 W/O — 발행·확정배포·중단·재개·취소·마감 + 4M 배정(13건) — 개별 계획안 브리프 (계획만, 구현 금지)

산출물: `docs/coverage-100/slices/I-6.md` 하나. 저장소 파일을 고치거나 만들지 않는다(이 파일 하나만 쓴다). 체크아웃 변경 금지(브랜치 `docs/coverage-100-i6-plan` 그대로). `git` 쓰기 금지. `contracts/*.json` 읽기 전용(`jq`). `prisma migrate reset`·`migrate diff --shadow-database-url`·`prisma db seed` 금지. 로컬 DB 는 `psql` SELECT 만.

## 읽을 것 (순서대로)
1. `docs/coverage-100/README.md` — §1-2 「차이가 크다」·§2 판정 절차·§5 불변 제약·§6 속도(구현 브리프 비테스트 예산 350 — PR 분할이 이 예산을 지켜야 한다)
2. `docs/coverage-100/plan.md` — §1 40행(I-6 · 13건 · 의존 I-2 · 마이그 **M-d** · 코어 「ERP 아웃박스(첫 사용처) · 생명주기 전이」 · opus · PR 4) · 41·46·47행(I-7·I-11·I-24 가 I-6 에 의존) · 82행(M1 완료 조건 한 줄 — 「W/O 발행·확정배포(선발행)」 마디) · §3 103행(ERP 아웃박스 적재 함수 — `src/integration/message` 에 **하나** · `message_key` 규약 한 곳) · 104행(LOT 생명주기 전이 — `transitions.ts` 에 이미 등록 · 호출만) · 121행(M-d: `production.work_order(_resource_assignment)` 부분 유일 인덱스 + `remainder_disposition_code?`) · §8 진행표(I-5 ✅ 280)
3. `docs/coverage-100/plan-integration.md` — 151행(⭐ ERP 아웃박스 5자리 · I-6 `:close` 가 첫 사용처 · 「적재 함수 하나만 붙인다」 · `message_key` 전역 UNIQUE) · 152행(LOT 생명주기 3전이 — I-6·I-7) · 170행(I-6 행 · ⚠ `work_order_resource_assignment` 축이 다르다) · **253~263행(I-6 상세 — 체인 마디 ⑬ · `:release` 가 ⑭ 선발행 + 자재 출고요청 자동 발행(R29)까지 한 트랜잭션 · 상태기계 두 축 · 4M 네 칸 매핑 판정 · 부분 유일 인덱스 SQL 초안 · 설계 미정 4)** · 282행(BOM 버전 — `work_order.bom_id`·`bom_version` 실측) · 407~409행(I-24 `:confirm` 이 W/O 를 «만드는» 쪽 — I-6 이 만드는 `POST /production/work-orders` 와의 경계) · 497~514행(순서·M2 체인) · 583행(I-6 ∥ I-19) · 599행(M-d) · 665·667행(반박 7·9 — 상태값 지어내기 금지)
4. `docs/coverage-100/plan-api.md` — **360~385행(S14 — 마이그 「없음」⚠ vs plan.md M-d 「있음」: 어느 쪽이 맞는지 실측으로 결론 · `mdm.item.development_item` 칸 없음 → 전건 적재 · 상태기계 두 축 · PR 4 초안 · 횡단 표 13행: 멱등 ✓ 9 · If-Match 필수 5/선택 2 · ETag 2 · 403 8)** · **800~822행(§5.1-C `WORK_ORDER_STATUS` 8값 전이표 5 · `CONFIRMED` 를 지나지 않는다 · `IN_PROGRESS` 는 세션 열기의 부수효과 `work-session-start` — I-11 몫)** · 1043행(`OPEN_SESSION_EXISTS` 409) · 1067행(`REMAINDER_DISPOSITION_REQUIRED`/`_NOT_ALLOWED` 400 — 계약이 규칙 넷을 적고 코드를 안 줬다) · 1103행(`work_order_no` 채번 — 「가운데는 항상 MES 가 만든다」 CORE-1)
5. `docs/coverage-100/plan-uiux.md` — 54~55행(U20 9건 · U21 4건) · 309~330행(화면 매핑 · `:hold`/`:resume` 「사번」 = `X-Worker-No`) · 625·633행(순서 근거) · 700행(⚠ `GET /production/work-orders` 질의 파라미터 **23개**) · 998~1010행(⑨ `withSummary` — `WorkOrderListSummary` · 기간 축은 계획 시작 시각) · 1092행 · 1202행(`achievementRate` 분모가 LOT/W/O 에서 다르다)
6. **선행 결과물** — `docs/coverage-100/slices/I-5.md` §0-재수립(R-1~R-14 · 특히 상태기계 등록 방식 R-2/R-6 · `runIdempotent`/If-Match 어댑터 R-9 · 「등록부는 검증·매핑은 코드」 R-5) · `I-4.md` R-1(잠금 오름차순) · R-2(`transitions.ts` 키 «안에» 액션 추가) · §4(`assertApproved`) · `I-3.md` R-12 ⓘ · `I-2.md`(I-6 의 의존 — mdm 무엇을 쓰는가: 품목·라우팅·설비·금형·작업자·근무조 표) · `I-1.md` 승인 코어 절
7. 실재 코어: `src/core/document-state/transitions.ts`(**`'trace.lot.lifecycle_status_code'` 키 112~140행 — `work-order-close`(L2)·`work-order-cancel`(L3) 이미 등록 · `sourceOperation` 칸** · `production.work_order.status_code` 키가 **있는지** 실측 — 없으면 이 슬라이스가 연다 · 키 이름 규약 `'<schema>.<table>.<column>'`) · `src/core/numbering/numbering.service.ts:60`(`next(documentTypeCode, plantId, periodDate)` — **`$transaction` 열기 «전»에** 부른다 · 47~53행 주석) · `prisma/seed.ts` 채번 규칙(`PRODUCTION_RESULT` 만 있고 **`WORK_ORDER`·`MATERIAL_ISSUE_REQUEST` 규칙이 없다** — 실측 · 없으면 시드 추가가 마이그인지 시드인지 판정) · `src/core/lot/lot-registry.service.ts:58`(`createWithin(tx, LotRegisterInput, appUserId)` — 선발행 슬롯이 이 함수로 만들어지는가 · `lifecycle_status_code`·`work_order_lot_seq`·`source_type_code`/`source_id` 를 어떻게 채우나 · `initial_qty > 0` CHECK) · `src/core/approval/approval.service.ts`(`:cancel`·`:close` 에 승인 게이트가 있는지 — 계약 문장으로) · `src/integration/message/integration-message.service.ts`(76~ `list`·`get`·`retry` — **적재 함수가 없다: 이 슬라이스가 만든다** · `MessageView` 모양 · `direction_code`·`message_type_code`·`status_code` 값 셋을 시드에서 실측)
8. 기존 도메인 선례: `src/logistics/purchase-order/`(헤더+라인 CRUD · `requestApproval` · `runVersioned`) · `src/logistics/goods-issue/goods-issue.service.ts`(전이 + 원장을 한 트랜잭션에 · `X-Worker-No`) · `src/logistics/document-progress/document-cancel.service.ts`(직전 슬라이스의 전이·If-Match·멱등 모양 — **가장 최근 선례**) · `src/common/master`(`runIdempotent`·`runVersioned`·`ifMatchVersion`·ETag 헬퍼) · `src/common/http/derived-permissions.ts`·`manual-permissions.ts`(13건 중 403 8건이 도출표에 이미 있는지) · `src/common/errors/error-codes.ts`(`OPEN_SESSION_EXISTS`·`REMAINDER_DISPOSITION_*`·`WORK_ORDER_*` 유무) · `src/mdm/`(설비·금형·작업자·근무조·라우팅 조회 — 4M 유효성 점검이 읽을 표) · `src/logistics/` 에 **`material-issue-request` 모듈이 없다**(I-8 몫) — `:release` 의 「자재 출고요청 자동 발행」이 표 `logistics.material_issue_request(_line)` 에 직접 쓰는가, I-8 이 만들 함수를 기다리는가 → §2 로 판정(⛔ 도메인이 다른 도메인의 service 를 부르지 않는다 · 하지만 표에 직접 INSERT 하면 I-8 이 규약을 두 번 짠다)
9. `prisma/schema.prisma` — `work_order`(2876~ · `work_order_no @unique` · `production_plan_id?` · `routing_operation_id` · `item_id` · `order_qty` · `work_order_type_code @default("NORMAL")` · `status_code` · `version_no` · `planned_start_at` · `default_*_location_id` 3 · `bom_id`? · **`remainder_disposition_code` 없음** — 전 칸 실측) · `work_order_dependency`(2964~ · `predecessor/successor` · `uq_work_order_dependency`) · `work_order_resource_assignment`(4534~ · `resource_type_code` + `equipment_id?`·`mold_id?`·`worker_id?`·`shift_id?` · `planned_start_at/end_at` · `ix_work_order_resource_assignment` 만 · **유일 제약 없음**) · `work_session`(2980~ · `status_code` · `uq_work_session`) · `lot`(3509~ · `lifecycle_status_code?` · `work_order_lot_seq?` · `source_type_code/source_id` · `uq_lot(plant_id, lot_no)`) · `lot_lifecycle_history`(4630~) · `material_issue_request`(957~ · `issue_request_no @unique` · `work_order_id` · `status_code`)·`_line`(982~) · `integration_message`(389~ · `message_key @unique VarChar(150)` · `direction_code` · `status_code` · `payload`) · `production_plan`(2504~) · `routing`·`routing_operation`(2535~·2560~) · `production_result`(마감 3분류 판정의 누적 양품 원천 — 표·칸 실측). check 제약을 `prisma/migrations/` 에서 grep(`ck_work_order*`·`ck_lot*`).
10. `prisma/seed.ts` — `WORK_ORDER_STATUS`(331~ · 8값) · `WORK_ORDER_TYPE`(1264~) · `WORK_ORDER_CANCEL_REASON`(723~) · `WORK_ORDER_HOLD_REASON`(1259~) · `WORK_ORDER_COMPLETION_VARIANCE_REASON`(735~) · `LOT_LIFECYCLE_STATUS`(482~ · 3값) · `LOT_LIFECYCLE_TRANSITION`(다음 블록) · **`RESOURCE_TYPE`·`REMAINDER_DISPOSITION` 코드그룹 유무**(계약 `x-code-key` `CD-RESOURCE-TYPE`·`CD-REMAINDER-DISPOSITION` — 없으면 값 목록이 없다: 지어내지 말고 §2) · `ENTITY_TYPES` 1306행(`WORK_ORDER` 있음) · `INTEGRATION_*` 코드그룹(방향·유형·상태) · 채번 규칙 블록(1395~1440)
11. `test/` — `logistics-goods-issue.e2e-spec.ts`(`makeLot()` · 전이 e2e 모양) · `logistics-document-progress.e2e-spec.ts`(직전 슬라이스 · If-Match·멱등·403 단언 모양) · `mdm-*.e2e-spec.ts` 중 라우팅·설비·금형·작업자 픽스처(`routing_operation` 을 만드는 픽스처가 있는지 — W/O 생성의 FK) · `integration-message.e2e-spec.ts`(있으면 — 아웃박스 조회 단언 재사용)
12. 계약 원문: `contracts/production-02생산실행.json` 의 10 경로(2238·2558·2715·2802·2851·2905·2995·3085·3178·3267행) — parameters·requestBody·responses·`x-*`·description **전부**. 스키마 `WorkOrder`(6193) · `WorkOrderCreate`(6510) · `WorkOrderUpdate`(6712) · `WorkOrderAdjustment`(6418 — 누가 쓰나) · `WorkOrderRelease`(6674 · `lotSize` required · 슬롯 수 N = 올림(orderQty ÷ lotSize) · 마지막 슬롯 나머지 · description 전문) · `WorkOrderHold`(6581) · `WorkOrderResume`(6694) · `WorkOrderCancel`(6456) · `WorkOrderClose`(6475 · `erpSendItems`·`reasonCode`·`remainderDispositionCode`·`remarks`) · `WorkOrderResourcePlan`(6086)·`WorkOrderResourcePlanCreate`(6126) · `WorkOrderListSummary`(6606) · `WorkOrderProgress`(7168) · `validation` 응답 스키마(2851~ 에서 `$ref` 실측) · `x-code-key` 7종(`CD-WORK-ORDER-STATUS`·`-TYPE`·`-CANCEL-REASON`·`-HOLD-REASON`·`-COMPLETION-VARIANCE-REASON`·`CD-RESOURCE-TYPE`·`CD-REMAINDER-DISPOSITION`). `GET /production/work-orders` 질의 23개 전건. 공유계약 `decisions-policy/공유계약.md` 의 R26·R27·R29·CORE-1·G-14·DR-005 · C-8(`business_date` 실린 표는 3개뿐 — W/O 는 아니다).
13. 설계 저장소 사본(읽기 전용): `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/02/` 의 `W-02-02-WO전개편성.md` · `W-02-03-4M자원배정유효성점검.md`(§ 유효성 규칙 — 서버가 무엇을 점검하나: 설비 가동·금형 수명·작업자 자격·근무조 겹침?) · `W-02-04-WO확정배포생산LOT선발행.md`(§4-B·§5-3·§6·§8-2) · `W-02-05-WO마감ERP실적송신.md`(3분류 판정 · 허용 오차 · 이월) · `W-02-06`(취소) · `W-02-07`(발행) · `W-02-08`(목록 요약) · `P-02-10-작업중단홀드등록.md` · `P-02-12-긴급WO현장투입실적.md`(긴급 W/O — 출고요청 자동 발행 제외 축) · `progress/미결-대장.md` 에서 「작업지시」「이월」「선발행」 grep
14. `docs/design-inquiries/README.md` + 최근 `032`·`033`·`034` — 요청서 형식 · 다음 번호 **035**
15. 이미 보낸 문의(`~/omf-design-requests/설계-문의-2026-09-0*.md`) 중 W/O·선발행·이월·마감 관련 · `docs/계약-되돌림-mdm.md`

## 계획서에 반드시 담을 것
1. **계약 읽기 표** — 13건 각각: 파라미터·본문 필드·응답·에러코드(계약이 이름 적은 것)·멱등/If-Match/ETag/403/`X-Worker-No`/`x-*` 노트. 추측 금지, 계약 문장 인용. 목록 GET 질의 23개 **전건**과 각각이 가리키는 칸(없는 칸은 「없다」) · `withSummary` 가 켜질 때 `WorkOrderListSummary` 의 각 칸 정의 · `WorkOrderProgress`(`achievementRate` 분모 = `orderQty`) · `validation` 응답의 규칙 목록(계약이 적은 것만) · 코드그룹 7종의 값 목록(시드 실측 · 없는 것은 「없다」).
2. **물리 대조 + 마이그 결론** — `plan-api.md` S14 「마이그 없음」 vs `plan.md`·`plan-integration.md` M-d 「부분 유일 인덱스 + `remainder_disposition_code?`」 를 실측으로 가른다. 계약 `WorkOrderClose.remainderDispositionCode` 가 저장돼야 하는가(응답 `WorkOrder` 에 다시 실리는가 — 실리면 칸이 필요) · 4M `uq(work_order_id, resource_type_code, resource_id)` 를 부분 유일 인덱스로 만들 SQL 초안(`COALESCE` 식 인덱스 — Prisma 가 `@@unique` 로 못 담는다: `migrate diff` 드리프트 회피 방법 — `prisma/migrations/` 의 기존 식 인덱스 선례 grep) · **`resource_type_code` 값 ↔ 네 칸 매핑**(`EQUIPMENT→equipment_id` · `MOLD→mold_id` · `WORKER→worker_id` · `SHIFT→shift_id`? — `CD-RESOURCE-TYPE` 시드 실측 · 없으면 §2) · `WorkOrderResourcePlan.resourceId` 를 응답에서 네 칸 중 어느 것으로 되돌리나 · 마이그 SQL 초안(forward-only · 별도 선행 커밋 · 하노이 적용 전 SQL 목록에 추가) · 시드 추가(채번 규칙 `WORK_ORDER` 등)가 마이그인지 시드인지.
3. **상태기계 두 축** — `production.work_order.status_code` 키를 `transitions.ts` 에 여는 모양(5액션 · `from` 집합은 plan-api §5.1-C 그대로 · `CONFIRMED` 를 지나지 않는다 · `IN_PROGRESS` 진입은 I-11 몫이라 **이 슬라이스가 등록하지 않는다**(등록하면 I-11 이 두 번 짠다) — 그러나 `:hold` 의 from 에 `IN_PROGRESS` 를 넣어야 하므로 값은 전이표에 나타난다 · `conflictStatus` 400) · LOT 축 L2·L3 을 **부르는** 자리(`:close` — `WAITING`→`VOIDED` 실적 없는 슬롯만 · `:cancel` — `WAITING`·`ACTIVE`→`VOIDED` 전건 · `lot_lifecycle_history` INSERT 를 누가 하나 — 코어 함수가 있는지 `src/core/lot/` 실측 · 없으면 이 슬라이스가 만드는 코어 함수 시그니처) · `:hold`/`:resume` 가 세션을 닫지/열지 않는다 · `:hold` `RELEASED`→`SUSPENDED` 와 `:resume` `SUSPENDED`→`IN_PROGRESS` 의 비대칭(`RELEASED` 에서 홀드하고 재개하면 `IN_PROGRESS` 가 된다 — 세션 없이 — 계약 문장 인용 후 §2) · `:close` from 에 `IN_PROGRESS` 를 넣는가(괄호 표기 — 계약 문장으로).
4. **`:release` 코어** — 한 트랜잭션 순서: 전이 → 선발행 슬롯 N 생성(`lotSize` 규칙 · `lot_no` 채번 — CORE-1 「가운데는 MES」 규칙과 채번 규칙 유무 · `lot_type_code` 값 · `lifecycle_status_code='WAITING'` · `status_code` 품질 축 초기값 · `work_order_lot_seq` 1..N · `source_type_code='WORK_ORDER'`) → 자재 출고요청 자동 발행(⛔ 긴급 W/O 제외 — `work_order_type_code` 값 실측 · BOM 소요 = `work_order.bom_id`·`bom_version` 의 라인 × `order_qty` — BOM 표·칸 실측 · **표에 직접 쓰나 I-8 을 기다리나** §2 판정 · 채번 `issue_request_no`) · 실패 시 전부 롤백 · 멱등키 · If-Match. ⭐ 이 코어 PR 을 ≤200 으로 가를 수 있는가(선발행만 코어 · 출고요청 발행은 별 PR?).
5. **`:close` 코어** — 3분류(미달/정상/초과) 판정 입력(누적 양품 = `production_result` 어느 칸의 합 · 선발행 슬롯 `initial_qty` 합 vs `order_qty`) · 「정상」 허용 오차 = **상수**(값·근거 · `app.operation_policy` 실측 — 표가 있으면 읽는 쪽이 §2 4번 「조용히 도출하지 않는 쪽」과 어떻게 정합) · `REMAINDER_DISPOSITION_REQUIRED`/`_NOT_ALLOWED` 4규칙을 계약 문장으로 표 · `OPEN_SESSION_EXISTS` 409(`work_session.status_code` 열림 값 실측) · L2 폐번 · **ERP 아웃박스 적재 함수** 시그니처(`src/integration/message/` 에 `enqueue(tx, { messageType, messageKey, payload })` 식 하나 · `message_key` 규약 — `{TYPE}:{id}:{version}`? 재마감 불가라 `{TYPE}:{id}` 로 충분한가 · `direction_code`·`status_code` 초기값 시드 실측 · `erpSendItems` 가 payload 를 어떻게 가르나 · `development_item` 칸 없음 → 전건) · 이월 잔량 W/O 자동 생성 **안 만든다**(계약 자인) · 승인 게이트 유무.
6. **`:cancel`** — from 5값 · L3 전건 폐번 · 출고요청이 이미 발행됐으면?(취소 전파 — 계약 문장 · 없으면 §2 · I-8 인계) · `reasonCode`(`CD-WORK-ORDER-CANCEL-REASON` 시드) · 승인 게이트 유무 · 취소 흔적 칸(`work_order` 에 `cancelled_*` 칸이 있는지 · `document_cancellation` 다형 표를 쓰는지 — I-5 R-… 선례와 정합).
7. **W/O 생성·수정 + 4M 배정 5건** — `POST` 가 만드는 것(`production_plan_id` null 허용 — 계획 없는 「수동 발행」 · `routing_operation_id` 필수 · `work_order_dependency` 는 누가 만드나 — I-24 `:confirm` 몫이면 여기서 안 만든다) · `PUT` 은 `statusCode` 없음 · 어느 상태까지 수정 허용(계약 문장 · `PLANNED` 만?) · `resource-plans` POST/DELETE 가 `work_order_resource_assignment` 에 어떻게 얹히나 · `validation` 이 읽는 표와 규칙(W-02-03 실측 · 계약이 적은 규칙만 · 없는 규칙은 「없다」) · `GET` 목록 23 질의의 where 매핑 + `withSummary` 집계 쿼리 모양 · ETag 2(`WorkOrder` 상세·`POST` 응답).
8. **횡단** — 403 8건(`derived-permissions.ts` 에 이미 있는 것 실측 · 화면 `W-02-*`·`P-02-10`) · 멱등 9(`runIdempotent`) · If-Match 필수 5/선택 2(`ifMatchVersion` · 선택은 헤더 없으면 통과) · `X-Worker-No`(`:hold`·`:resume` — goods-issue 선례) · 404 · 400 vs 409(`OPEN_SESSION_EXISTS` 만 409 — I-5 R-… 「`CANCEL_IN_PROGRESS` 400」 판정과의 정합).
9. **e2e 정리 순서** — ⭐ **M1 마디**(`POST` → `:release` → 슬롯 N 개 `WAITING` → 출고요청 1건(긴급이면 0건)) · `:close` 3분류 × 이월 4규칙 · `:cancel` 뒤 슬롯 전건 `VOIDED` + `lot_lifecycle_history` · `:hold`→`:resume` · `OPEN_SESSION_EXISTS`(세션 표 직접 INSERT 픽스처 — I-11 전) · 4M 유일 위반 400 · 아웃박스 1행(`integration_message` 조회 GET 으로 단언) · 픽스처(라우팅·품목·BOM·설비·금형·작업자·근무조 — mdm e2e 헬퍼 재사용 여부) · cleanup 순서(`lot_lifecycle_history` → `lot` → `material_issue_request_line` → `_request` → `work_order_resource_assignment` → `work_order` → `integration_message`).
10. **설계 미정 자리** — 각각 README §2 절차로 판정. 문의가 필요한 것은 제목만(요청서는 구현 시 작성, **035** 부터). 기존 문의와 겹치면 「기존」. 후보: 허용 오차 폭 · 이월 자동 전개 · POP 버퍼 미동기 게이트 · `RELEASED` 홀드→재개가 `IN_PROGRESS` 로 가는 비대칭 · `:cancel` 시 이미 발행된 출고요청 · `resource_type_code` 값 목록 부재 · `remainderDispositionCode` 값 목록 부재 · `lot_no` 채번 규칙 부재 · `achievementRate` 분모 라벨.
11. **PR 분할** — 초안 4(plan.md·S14). 초안: ① 마이그(M-d 선행 커밋) + 조회 GET 4 + `resource-plans` POST/DELETE(sonnet 가능? — `validation` 규칙은 opus) ② `POST`/`PUT` + `transitions.ts` 키 + `:hold`/`:resume`(opus) ③ `:release` 코어(선발행 + 출고요청 · ≤200 · opus) ④ `:close`/`:cancel` + 아웃박스 적재 함수 + L2·L3 호출 + M1 e2e(opus). 각 PR **비테스트 예산 350** 안인지 — 넘치면 5로. 각 PR 의 파일 목록·예상 diff·**테스트 이름 목록을 unit 과 e2e 로 나눠 각각** — e2e 로 도달 못 하는 가드(동시 `:release` 두 번 · 슬롯 수 계산 경계 `lotSize ≥ orderQty` · 3분류 경계값 · 아웃박스 `message_key` 충돌)는 **단위 테스트 이름을 여기서 확정**. 스택 PR 로 낼 순서.
12. **I-7·I-8·I-11·I-24 인계 모양** — 선발행 슬롯을 I-7 실적이 `WAITING→ACTIVE`(L1) 로 옮기는 자리 · I-8 이 `material_issue_request` 를 넘겨받는 규약(`:release` 가 쓴 칸 · 상태 초기값) · I-11 이 `work-session-start` 로 `IN_PROGRESS` 를 여는 자리(`transitions.ts` 같은 키에 액션 «추가» — I-4 R-2 방식) · I-24 `:confirm` 이 `POST /production/work-orders` 의 서비스 함수를 재사용하는가(같은 도메인 `src/production/` 안인가 — 모듈 배치) · I-23 이 아웃박스 적재 함수를 그대로 쓰는가.
13. **통합 계획 대조** — `plan.md` §1 40행·§3 103~104·§4 121 · `plan-api.md` S14·§5.1-C·§7 에러표 · `plan-integration.md` 253~263 · `plan-uiux.md` U20·U21·⑨ 과 다른 점. README §1-2 3조건 중 걸리는 것이 있으면 맨 위에 ⚠ 로.

금지: 구현 코드 작성 · 계약 파일 수정 · 다른 문서 수정 · 값 지어내기(값 목록이 없으면 「없다」고 적고 §2 로 판정) · `omf-mes`·`omf-mes-client` 저장소 쓰기 · `contracts:update`/`contracts:check`.
