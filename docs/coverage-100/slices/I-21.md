# I-21 · 부적합·처분·특채 — 조회 8 + 쓰기 3(11건) — 개별 계획안

> 브리프: `I-21-계획-브리프.md`(세션 스크래치패드). 산출물은 이 파일 + 통보 **089** + 계획 PR 하나다(⛔ 코드 0줄).
> 레인: **A**(품질·공정 인계·첨부 · `lanes.md` §0-1). 브랜치 접두어 `feat/coverage-100-a-…` · PR 제목 접두어 `[A] ` ·
> 문의 대역 — ⭐ **069~089 는 2026-09-08 에 소진**됐고(089 가 이 계획의 통보다) **새 대역 `180 ~ 209`** 를 받았다(`lanes.md` §1-1). 다음 통보는 **180 부터**이고 ⛔ **번호는 통합자가 하나씩 준다**(§0-재수립 R-21).
> 통합 계획서 해당 절: `plan.md` §1 **17행**(17번 · 선행 I-20 · 마이그 **—** · 코어 **—** · opus · PR **3**) ·
> §1 **11행**(LOT 품질 축 전이 — 「처분(I-21)은 계약 문장대로 옮기되 `transitionCode` 는 지어내지 않고 문의」) ·
> §4 마이그 표(**I-21 행이 없다**) · §5 규칙 1·2·3·4·5·6·7·10·11 ·
> `plan-api.md` **§5.1-E 860~885행**(품질 축 전이 9 · ⛔ **871행이 이 슬라이스에서 실측으로 뒤집힌다** — §0 #1) ·
> `plan-uiux.md` **U28**(62행 · 405~417행 · 9건) · **U29**(63행 · 419~424행 · 2건) · **609행**(처분 결재를 걸지 않는다) ·
> **1057행**(부분 처분) · **1168행**(If-Match 토큰 원천이 다른 계약 파일) · **1230행 M**(승인 유형에 특채 없음) ·
> **1231행 N**(⭐ 처분 전이 코드가 9종에 없다 — 이 계획안이 그것을 판정한다) ·
> `plan-integration.md` **185행**(I-21 행 — 마이그 「있음」 · 원장 ✕ · posting ✕ · 상태기계 ⭕ · PR 3) ·
> **387~392행**(§3-1 I-21) · **535행**(§4-2 M3) · **590행**(공유 코어 5 재사용) · **924~934행**(§10 부록 전건 11).
> 선행 결과물: **`I-20.md`**(§0-재수립 R-1·R-5·R-19·R-21·R-22·R-23·**R-24** · §5 전이표 무변경 판정 · **§11 ②**(이 슬라이스로의 인계 3건) · 형식의 본보기) ·
> **`I-19.md` §11 ③**(⭐ 이 슬라이스의 최대 미결) · `I-18.md`(형식 · **통보 154** — enum 밖 `transitionCode` 가 조회로 새는 사고).
> 실측 기준: `main` **03f0166** · 계약 사본 `contracts/COMMIT.txt` = **a6a87e1** · 실측일 **2026-09-08** ·
> 설계 사본 `.design-reference/omf-mes/design/wiki/`(고정) · DB 는 읽지 않았다(물리 사실은 `schema.prisma`·`migrations/*.sql`·`seed.ts` 정적 실측).
> 커버리지: `main` **399/487**(2026-09-08 실측 · `node_modules/.bin/jest contract-coverage`) → I-21 11건으로 **410**.

---

## ⭐ 0-재수립. 3관점 재검토 결정 — R-1 ~ R-21 (2026-09-08 · 통합자 직접 · **본문보다 앞선다**)

> 세 관점이 «서로 모르는 채» 동시에 돌았다 — `I-21-review-api.md`(474줄 · `200c56e`) ·
> `I-21-review-integration.md`(571줄 · `e4c55fc`) · `I-21-review-uiux.md`(`7bf9db5`).
> 합계 **Blocker 5(중복 제거 4) · Major 15 · Minor 24**. 아래 R-n 이 본문과 어긋나면 **R-n 이 이긴다.**
>
> ⭐ **세 관점이 독립적으로 같은 자리에 닿은 것이 둘이다** — 그 둘이 이 재수립의 중심이다.
> ⓐ **`mdm.warehouse.is_defect` 는 실재한다**(api **Blocker** · uiux **Blocker** · integration Major-2)
> ⓑ **후속 롤업(`followUpQty`·`followUpStatusCode`)의 원천이 계획과 다르다**(셋 다)
> ⛔ 통합자가 ⓐ 를 직접 재확인했다 — `prisma/migrations/20260828000000_add_warehouse_is_defect/migration.sql:25`.

### R-1 ~ R-5 · 「리뷰가 반드시 볼 자리 5」 판정

| # | 자리 | API | 통합 | UI/UX | **통합자 판정** |
|:-:|---|:-:|:-:|:-:|---|
| **R-1** | #1 `SCRAPPED` 도착은 계약이 적었다 · 없는 것은 코드뿐 · `C17`·`C18`·`C19` 신설 | 채택(조건 2) | 조건부 채택 | 유지(근거 강화) | ✅ **유지 — 셋이 다 채택했다.** 근거가 오히려 굳었다: `W-03-01` §5-1 「전이 16 중 상태 전이 9」로 C1~C16 사용이 확인됐고, **`W-03-10` §8 #3 이 「우리가 정할 자리」라 직접 적었다**(uiux). ⛔ **조건 셋을 붙인다** — ⓐ 전이표 3행에 **`sourceOperation` 을 문자열로** 적는다(배포된 `GET /quality/lot-status-transitions` 가 그 필드로 거른다 · api) ⓑ `moveWithin` 의 `ctx.transitionCode` 가 죽은 인자인지 확인(api) ⓒ 심는 경로 셋이 깨진다 → **R-6·R-7·R-8** |
| **R-2** | #2 부분 처분 · `lots[]` 단위 일치 강제(400) | ⚠ Major-5 | 유지 | 유지 | ⚠ **절반 반증 — 400 거부를 «재검토»한다.** 계약 지침이 「합을 내리지 말고 **두 값을 보여라**」인데 계획은 반대로 400 으로 막았다(api). ⇒ ⓐ `DispositionRemainingSummary.uomId`(required)를 채울 길을 **단위별 행**으로 다시 보고 ⓑ 그래도 안 되면 400 을 유지하되 **계약 지침을 알고 다른 쪽을 골랐다**는 것을 「알려둘 것」에 적는다. ⛔ 지침을 «모른 채» 400 을 내는 것과 다르다 |
| **R-3** | #3 후속 롤업은 폐기만 셀 수 있다 | Major-3 | Major-6 | Major | ⛔ **셋 다 반증 — 표·칸·필터를 다시 판정한다.** ⓐ 근거가 계약 `x-internal-note`(폐기=`goods_issue` · 정상=**`stock_transfer`** · 재작업=**`production_result`**, 셋 다 「LOT 축까지만」)와 **다르다**(api) ⓑ 수량은 `goods_issue_line.issue_qty` 이고 **`status_code` 4값 처리가 계획에 없다** — **I-20 ①c 의 `POSTED` 필터 Major 와 같은 모양**(integration) ⓒ `followUpPending` 값을 안 정했다: 참이면 `W-04-11` 이 같은 LOT 을 **두 번 재등록**(계약이 경고한 사고), 거짓이면 목록이 **영구히 빈다**(uiux) ⓓ §8-3 #23 단언이 그것을 안 물어 **되돌려도 안 깨진다**(integration·uiux 독립 일치) |
| **R-4** | #4 후보 목록 모집단 — 「불량창고를 가리는 칸이 0개」 | ⛔ **Blocker** | Major-2 | ⛔ **Blocker** | ⛔⛔ **전제가 거짓이다. 판정 #4 를 통째로 다시 쓴다.** `mdm.warehouse.is_defect` 가 **실재한다** — 마이그 `20260828000000` · `schema.prisma:2314` · 계약 `Warehouse.isDefect`(required) · 질의 `GET /mdm/warehouses?isDefect=true` · **`warehouse.service.ts` 에 이미 구현**. 계획자가 **baseline 마이그 한 자리만 봤다** — **I-20 R-24 형 네 번째 사고**. ⇒ 모집단 · 「잔액 창고 둘이면 뺀다」(**계약이 금지한 조용한 탈락**이고 uiux 는 그것이 **부적합 등록의 유일한 진입을 막는다**고 짚었다) · PR ③ 원시 SQL · 픽스처 L4 · e2e #2·#3·8건이 전부 재수립 대상이다. ⚠ 오늘 `is_defect=true` 가 **0행**이라 결론은 살아남을 수 있으나 **이유가 완전히 다르다** — 그때는 「축이 없다」가 아니라 「축은 있는데 데이터가 0행이다」이고, 후자는 **통보 감**이다 |
| **R-5** | #5 If-Match 토큰 원천이 다른 계약 파일 | 유지 | 유지 | 유지 | ✅ **유지 — 셋 다 뒤집지 않았다.** ⛔ 다만 api 가 **더 큰 것을 붙였다** → **R-14** |

### R-6 ~ R-9 · ⛔ Blocker — **이대로 구현하면 `main` 이 빨개진다**

| # | 지적 | 근거 | **처방** |
|:-:|---|---|---|
| **R-6** ⛔ | **`app.code_value` 는 없는 표다** | 정본은 **`mdm.code_value`**(baseline `migration.sql:95`). `app` 스키마에 `%code%` 표 0행 — **통합자가 직접 재확인**. `plan.md` §4 M-h 행에도 같은 오기가 실렸다 | 마이그 M-h 와 `plan.md` §4 행을 **`mdm.code_value`** 로 고친다. 안 고치면 `migrate deploy` 가 **즉시 실패**한다 |
| **R-7** ⛔ | **`test/document-state.e2e-spec.ts` 가 계획에 0회 등장** | PR ④ 가 그 파일의 `STATUS_GROUPS` 지도(축 미등록 → 실패)와 전이코드 대조를 깬다. ⭐ **I-19 R-5 가 똑같은 자리에서 이미 잡았던 사고**이고 그 파일 `:44` 주석이 「I-1 이 같은 실수를 했다」라 적어 두었다 | PR ④ 의 산출물 목록에 그 파일을 넣고 §8 e2e 표에 행을 만든다 |
| **R-8** ⛔ | **M-h ⓐ 와 PR ④ 가 서로를 깬다** | e2e `:99-118` 이 **양방향으로** 단언한다 — ④ 가 먼저면 「표에 있는 코드가 DB 에 없다」, M-h 가 먼저면 「DB 에 있는 코드를 아무도 안 쓴다」. **어느 순서로도 `main` 이 빨갛고 레인 B·C 가 그 빨강을 본다** | **M-h ⓐ(코드값 INSERT)를 PR ④ 안으로 흡수**하고, **ⓑ(`ix_disposition_decision_nonconformance`)만 단독 마이그**로 낸다. ⇒ 마이그 PR 이 코어 PR 과 분리되지 않는 **예외**이고, 그 이유를 커밋에 적는다 |
| **R-9** ⛔ | **판정 저장 권한이 `P-02-13`(PQC) 하나뿐이라 `W-03-10` 화면이 403 이다** | `W-03-10` 이 권한 표 11건 **어디에도 없다**. 원인: DR-008 로 신설된 화면이 derive-map 원천인 요구서 §3 보다 **새것**이다 ⇒ 계획의 「`manual-permissions.ts` **0줄**」이 **반증**된다 | ⑦ 에 **`manual-permissions.ts` 단독 커밋**을 더한다(`lanes.md` §1-4 — 단독 커밋으로 내고 바로 병합해 충돌 창을 줄인다). 요구서 §3 갱신은 **통보 후보 B** |

### R-10 ~ R-12 · 계획자의 주장 중 **틀린 것**

| # | 계획자 주장 | 판정 |
|:-:|---|---|
| **R-10** | 「`plan.md` §4 · `plan-integration.md:185` · `plan-api` 셋이 마이그 유무를 서로 다르게 적었다」 | ⛔ **반증 — api·통합이 «독립적으로» 같은 진단을 냈다: 표의 열을 하나 밀려 읽었다.** `plan-integration.md:162-163` 범례상 5번째 칸은 **「표(물리 존재)」**=있음이고 6번째가 **「마이그」**=**✕** 다. `plan.md` §4·`plan-api.md:520`(「마이그레이션 없음」)과 합쳐 **셋이 다 마이그 0** 이고 어긋난 곳은 **0건**이다. §10-1 #7 의 「posting ✕」는 **아예 없는 칼럼**을 읽은 것이다. ⇒ 「통합 계획서가 틀렸다」가 아니라 **계획자가 틀렸다** |
| **R-11** | 「전이를 안 걸면 폐기 LOT 이 계속 출고·피킹 대상이 된다」(판정 #1 의 「뒤집히면」 칸) | ⚠ **근거 오측(uiux).** 차단 단일 지점은 `mdm.judgment_type_control.blocks_issue` 인데 시드 `JUDGMENT_TYPE` 값이 **0개**라 **오늘은 아무것도 안 막는다**(그 덕에 §8-6 M3 체인의 폐기 출고가 성립한다). ⇒ **결론(전이를 건다)은 유지**하되 근거를 계약 `:2460` 의 「한 트랜잭션(B-8)」과 화면 `W-03-10` §5-2 로 **바꿔 적는다.** 「오늘 안 막힌다」는 사실 자체가 **통보 후보 F** |
| **R-12** | 「`plan-api.md:871` 과 `I-19` §11 ③ 을 **3관점이 다 놓쳤다**」 | ⚠ **과장(api).** **`plan-integration.md:391` 은 이미 옳게 적어 뒀다.** ⇒ 「통합 계획서 셋이 다 틀렸다」가 아니라 **`plan-api` 한 곳이 틀렸고 통합은 맞았다** 로 고친다 |

### R-13 ~ R-19 · 계획자가 안 본 자리

| # | 지적 | 관점 | **처방** |
|:-:|---|:-:|---|
| **R-13** | **응답 널 정책이 통째로 빠졌다** — 계약이 널을 못 받는 «선택» 칸이 **25개**(`Nonconformance` 7·`Concession` 10·`DispositionDecision` 7·`NonconformanceLot` 1)인데, 계획이 널 정책을 적은 유일한 자리가 하필 널을 **허용하는** 유일 스키마(`DispositionCandidate`)다 | api | e2e 가 ajv 로 응답을 검증한다 ⇒ **그대로 구현하면 빨개진다.** §1-4 에 널 정책 표를 세우고 `omitEmpty` 계열 매퍼를 쓴다(I-20 선례) |
| **R-14** ⭐ | **`POST …/disposition-decisions` 는 7계약 통틀어 유일한 「201 + If-Match 필수」다** — 공용 `runVersioned` 는 성공 상태를 `HttpStatus.OK` 로 **고정**하고 전 사용처가 200 PUT 이다 | api · 통합 | **공용 코어(`master-write.ts`)를 건드린다** ⇒ §7-1·§11 에 항목을 만들고 **단독 커밋**. `lanes.md` §1-4 공용 파일 규칙 적용. 이 자리는 **통합자 사전 보고 대상** |
| **R-15** | **죽은 절 셋** — `lots` `minItems:1` · `decisionQty` `exclusiveMinimum:0` · `dispositionTypeCode` enum 을 **계약 가드가 이미** 400 `RANGE`/`INVALID` 로 막는다. §3-4 순서 1 이 스스로 금지한 자리다 | api | 서비스에서 뺀다. ⭐ **되돌려도 안 깨지는 절**이라 §6-2 변이 점검에서 그대로 초록이 된다 |
| **R-16** ⭐ | **`SCRAPPED` 에서 «나가는» 전이가 0** — 축 9전이 전부. **오폐기 되돌림 경로가 0** 이다 | uiux | ⭐ **통보 071(C9 로 걸린 보류를 풀 길이 0)과 «같은 모양»인데 §9-1 판정표에 행이 없다.** 행을 만들고 **통보 후보 A** 로 낸다. ⛔ 전이를 지어내지 않는다 |
| **R-17** ⭐ | **`disposition-normal`(NORMAL 도착)이 I-23 의 `stock-reinstate`(`from:['DEFECTIVE']`)를 선점한다** — 재등록 확정이 **0건 전이**가 되고 `W-04-11` §5-3 의 J-7 약속이 거짓이 된다 | uiux | ⛔ **레인 C 파급이다.** 두 전이의 `from`·`to`·`sourceOperation` 을 나란히 놓고 **겹치지 않게** 설계한 뒤 §11 인계에 적고 **레인 C 에 통지**한다 |
| **R-18** | **`receivedAt` 은 두 갈래 공통 칸**(계약에 갈래 한정어 없음). RETURN 전용으로 읽으면 기간 필터 한 번에 **PRODUCT 가 사라지고** `NULLS LAST` 가 마지막 페이지로 민다 | uiux | 필터·정렬 축을 다시 정하고 e2e 에 **「PRODUCT 갈래가 기간 필터에 살아남는다」** 한 건 |
| **R-19** | **레인 C #403 과 공용 파일 세 자리에서 부딪힌다** — `document-state.spec.ts` 축 배열 · `toHaveLength` · **(양쪽 다 놓친) e2e `STATUS_GROUPS`** | 통합 | ✅ **이미 처리했다** — 2026-09-08 통합자가 #403 에 통지했다(승인 · C 가 먼저 · 숫자 41→42 로 정정 · 먼저 병합한 쪽 기준으로 뒤가 merge). ⛔ **`STATUS_GROUPS` 세 번째 자리는 그 통지에 없었다** — #403 에 덧붙인다 |

### R-20 ~ R-21 · PR 분할 · 통보 번호

| # | | |
|:-:|---|---|
| **R-20** | **PR 9 는 하한이다** | 총량 ≈1,911 은 좋다(I-20 실물 1,909/10건과 밀도가 일치 · 통합 관점 git 실측). ⛔ 그러나 **I-20 은 같은 총량을 12 PR 로 냈다.** ⑥(325/350)·⑦(248/300)이 위험하고 ④는 R-7·R-8 반영으로 ~70 이 된다. ⇒ ⓐ ⑦ 에 `manual-permissions.ts` 단독 커밋(R-9) ⓑ ⑤(특채)를 뒤로 ⓒ **③ 은 R-4 재수립이 끝난 뒤에만 띄운다** ⓓ ⑥·⑦ 은 **분할 계획을 먼저 낸다** |
| **R-21** | **통보 후보 22건 · 번호는 통합자가 하나씩 준다** | 세 관점이 낸 후보 = api 9 · 통합 5 · uiux 8(중복 제거 전). ⛔ 리뷰어들에게 **번호를 주지 않았다** — 2026-09-08 에 마지막 남은 089 를 두 에이전트가 동시에 집는 사고가 났기 때문이다(`lanes.md` §1-1). 레인 A 는 **새 대역 `180 ~ 209`** 를 받았다. ⇒ ⓐ 22건을 **중복 제거·병합**하고 ⓑ **089 본문에 흡수할 것**과 **새로 낼 것**을 가른 뒤 ⓒ 180 부터 **하나씩** 배정한다 |

---

## ⭐ 0. 리뷰가 반드시 볼 자리 5 (README §1-2 · 2026-09-07 규칙)

> 아래 다섯이 **3관점 재수립의 판정 항목**이고, **뒤집히면 PR 분할이나 심장의 갈래가 바뀌는 자리**다.
> 통합자의 리뷰 브리프는 이 다섯을 그대로 쓴다.

| # | 자리 | 이 계획안의 판정 | 뒤집히면 |
|:-:|---|---|---|
| **1** ⭐⭐ | **`SCRAPPED` 로 가는 전이 — 계약은 «도착»을 적었다. 없는 것은 «코드»뿐이다** | ⛔ **`plan-api.md:871`(「계약이 LOT 상태를 옮긴다고 적지 않았다」)과 `I-19.md` §11 ③(「계약이 어느 오퍼레이션에도 안 적었다」)은 실측으로 틀렸다.** `contracts/quality-03품질.json:2460` 이 `POST /quality/nonconformances/{id}/disposition-decisions` 설명에 **직접** 적었다 — 「⭐ 판정 저장과 Lot Status 전이는 **한 트랜잭션**이다(공유계약 B-8) — 처분만 남고 LOT 이 안 바뀌면 다음 화면이 잘못된 대상을 집는다 … ⭐ 도착 상태는 처분에 따라 갈린다 — 재작업 → `INSPECTION_PENDING` · 폐기 → `SCRAPPED` · 정상 → `NORMAL`」. 7개 계약 파일 전수 grep 결과 **`SCRAPPED` 로 LOT 을 보내는 오퍼레이션은 이 하나뿐**이다. `plan-api.md` §5.1-E 의 「계약이 도착 상태를 직접 적은 자리는 넷」 열거가 **이 오퍼레이션을 빠뜨렸다.** ⇒ **전이 3건을 `transitions.ts` 에 등록한다**(§6). ⛔ 없는 것은 `transitionCode` 뿐이고 그것을 §6-2 의 3안 비교표로 판정해 **`C17`·`C18`·`C19` 를 정하고 통보 089** 를 낸다 | 마이그레이션 유무(통합 계획서 §4 에 I-21 행이 없다) · PR **④**(코어 · `transitions.ts` — 레인 A 소유 · **사전 사용자 보고** 대상) · 심장 PR ⑥ · e2e 9건. 「전이를 안 건다」로 뒤집히면 폐기 판정된 LOT 이 계속 출고·피킹 대상이 된다(결정 10 차단 단일 지점) |
| **2** ⭐⭐ | **처분 저장이 «어느 LOT» 을 옮기나 — 그리고 부분 처분이 도착 상태를 충돌시킨다** | `disposition_decision` 에 **`lot_id` 칸이 0개**(`schema.prisma:3200-3216` 실측)이고 `nonconformance_lot` 은 `uq_nonconformance_lot(nonconformance_id, lot_id)` 로 **여러 LOT** 을 담는다. 설계도 답이 없다 — `W-03-10` 은 대상 LOT 을 **단수**로만 그리고(`:71`) §8 #7 이 「대상 LOT 단위가 섞이면 수량 합 불성립」을 **미결로** 남겼다. ⇒ 판정 셋: ⓐ **등록에서 `lots[]` 의 `uomId` 가 전부 같기를 강제한다**(다르면 400 `INVALID`) — 그래야 `affectedQtyTotal`·`remainingQty` 가 성립한다 ⓑ 판정 저장은 **그 부적합의 `nonconformance_lot` 전건**을 옮긴다 ⓒ **매 판정마다 옮기고, 부분 처분이면 마지막 판정의 도착 상태가 남는다**(계약 `:2460` 「판정 저장과 전이는 한 트랜잭션」의 문자 그대로 · 0단계 선례 = I-19 `:confirm`·I-20 `:release` 가 쓰기마다 옮긴다) | §3 심장 B 의 트랜잭션 순서 · e2e 5건(부분 처분 2회 · 마지막 도착) · `NonconformanceLot.qualityStatusAfterCode` 의 뜻. ⓐ 를 뒤집어 「단위가 섞여도 받는다」로 가면 `DispositionRemainingSummary.uomId`(required)를 채울 값이 없어진다 |
| **3** ⭐⭐ | **`followUpStatusCode`·`followUpQty` 는 «폐기»만 셀 수 있다 — 나머지 둘은 오늘 언제나 0 이다** | 계약이 **둘 다 required** 로 적고 「서버가 후속 전표를 롤업해 낸다」(L-2)라 했는데, 처분 3유형의 후속 축이 **서로 다르고 둘이 비어 있다**: ⓐ **SCRAP** → `logistics.goods_issue.source_document_type_code='DISPOSITION_DECISION' + source_document_id`(**NOT NULL 실재** · `ix_goods_issue_source` 실재 · `W-04-10:230` 이 enum 3값에 그 값을 못 박았다) ⇒ **셀 수 있다** ⓑ **NORMAL** → `logistics.stock_reinstatement` 표가 **물리에 없다**(전 마이그레이션 grep 0건 · I-23 · 레인 C 몫) ⇒ **원천 0** ⓒ **REWORK** → `production.work_order.rework_source_nonconformance_id`(nullable · `fk_work_order_rework_nc`)는 **부적합 축**이라 **결정 단위로 못 센다**(`P-04-03:4`) ⇒ **원천 0**. ⇒ **폐기만 세고 나머지 둘은 `followUpQty=0`·`followUpStatusCode='NOT_STARTED'`**, `reinstatable` 은 **`dispositionTypeCode='NORMAL'` 한 축으로 접힌다**(계약이 적은 「재작업이고 그 재작업이 끝난 것」 갈래는 오늘 언제나 false). 통보 089 §4 | PR ②a 의 질의가 통째로 바뀐다(±120줄) · `followUpPending`·`reinstatable` e2e 4건 · 「0 으로 접지 않는다」(L-8)와 충돌하는지의 판정 |
| **4** ⭐ | **`GET /quality/disposition-candidates` 의 모집단 — 창고 축으로는 만들 수 없다** | 계약이 「원천 둘(반품 입고 RETURN + OQC 불합격 PRODUCT)을 **한 목록**으로 합쳐 낸다」·「대상 한 건은 **LOT 단위**다」라 적었다. 물리 실측: **`mdm.warehouse` 에 「불량창고」를 가리는 칸이 0개**(`is_defect` 류 없음 · `warehouse_type_code`·`management_level_code`·`is_external` 뿐)라 `REQ-PR-0025` 「판정은 불량창고 입고 후」를 **창고 축으로 못 만든다**. ⇒ **원천 축으로만 만든다** — RETURN = `goods_receipt.receipt_type_code='RETURN'`(시드 `RECEIPT_TYPE:405` 실재) → `goods_receipt_line.lot_id` · PRODUCT = OQC 확정 불합격 검사 결과 → `inspection_request.lot_id`. `warehouseId`(**required**)·`quantity` 는 **`inventory_balance` 를 LOT 축으로 접어** 낸다. ⛔ **잔액 창고가 둘 이상인 LOT 은 목록에서 뺀다** — required 칸을 조용히 고르지 않는다(§2 2단계 기준 4). 그 사실이 e2e 의 「안 걸리는 행」이다 | PR ③ 의 원시 SQL 이 통째로 바뀌고 예산이 ±80 흔들린다. 「창고를 접지 않고 잔액 행마다 한 행」으로 뒤집히면 계약의 「LOT 단위」와 어긋난다 |
| **5** ⭐ | **If-Match/ETag 의 토큰 원천이 «다른 계약 파일»에 있다 — I-20 R-24 와 같은 유형의 자리** | `POST /quality/nonconformances/{id}/disposition-decisions`(03 품질)의 If-Match 는 **`GET /quality/nonconformances/{id}`(04 제품출하)의 ETag** 다 — 계약이 직접 적었다(`:2460` 「⭐ If-Match 값은 이 경로의 GET 이 아니라 부적합 상세 … 의 ETag 를 담는다 — 잠그는 대상이 처분 결정 한 건이 아니라 **부적합**이기 때문이다. ⛔ 이 경로의 목록 GET 에는 ETag 가 없다」 · `x-internal-note` 「⚠ **토큰 원천 검사기는 한 파일 안에서만 후보를 찾아 이 자리를 못 본다** — 원천이 다른 계약에 있는 첫 사례다」). ⇒ 토큰은 **`quality.nonconformance.version_no`**(실재 · `@default(1)`)이고 **판정 저장이 그 값을 올려야 한다**(계약 x-internal-note 가 명시). `:request-disposition` 의 If-Match 도 같은 토큰이다 | 심장 A·B 의 `runVersioned` 배선 · e2e 6건. 「처분 결정의 판 번호」로 읽으면 **`disposition_decision` 에는 `version_no` 칸이 아예 없어**(기록 전용 표) 죽은 칸 마이그를 하나 더 만든다 — I-20 R-24 가 정확히 그 사고였다 |

**오퍼레이션 11/11 대조** — `awk -F'\t' '$1=="I-21"' docs/coverage-100/assignment.tsv` = **11행** · 브리프 §1 목록 11건 · `plan-integration.md:924-934` 11건 · 계약 실물 11건(python — `shipment-04제품출하.json` 총 **21** 중 5 · `quality-03품질.json` 총 **27** 중 6)이 **전부 같다**(누락 0 · 여분 0).

⭐ **같은 자원이 계약 파일 둘에 흩어져 있다 — 두 파일을 대조했다.**
python 으로 두 파일의 `components.schemas` 를 전수 비교한 결과 **공통 이름은 셋뿐**(`ErrorItem`·`ErrorResponse`·`PageMeta`)이고 그중 `ErrorResponse` 는 **완전 동일**, `ErrorItem`·`PageMeta` 는 **`example` 값만 다르다**(`QTY_EXCEEDS_ALLOCATION`↔`QTY_SUM_MISMATCH` · `total: 84`↔`412`). ⇒ **같은 스키마를 다르게 적은 곳은 0건**이다. 도메인 스키마는 소유가 깨끗이 갈려 있다(부적합·후보·의뢰 = 04 · 처분 결정·특채 = 03). **⛔ 대신 «파일을 가로지르는» 자리가 둘 있다** — 판정 #5(토큰 원천)와 409 봉투 둘(§1-1).

---

## 1. 계약 읽기 표 — 11 오퍼레이션 전건

### 1-1. 횡단 4축 — 실측(python · 두 파일)

| 오퍼레이션 | 계약 파일 | 멱등 | If-Match | ETag | 403 | 성공 | 그 밖 응답 | 409 봉투 |
|---|:-:|:-:|:-:|:-:|:-:|:-:|---|---|
| `GET /quality/nonconformances` | 04 | — | — | — | — | 200 | ⭐ **없다** | — |
| `GET /quality/nonconformances/{nonconformanceId}` | 04 | — | — | **✓** | — | 200 | 404 | — |
| `GET /quality/disposition-candidates` | 04 | — | — | — | — | 200 | ⭐ **없다** | — |
| `GET /quality/nonconformances/{nonconformanceId}/disposition-decisions` | 03 | — | — | ⛔ **없다(명시)** | — | 200 | ⭐ **없다** | — |
| `GET /quality/disposition-decisions` | 03 | — | — | — | — | 200 | ⭐ **없다** | — |
| `GET /quality/disposition-decisions/{dispositionDecisionId}` | 03 | — | — | — | — | 200 | 404 | — |
| `GET /quality/concessions` | 03 | — | — | — | — | 200 | ⭐ **없다** | — |
| `GET /quality/concessions/{concessionId}` | 03 | — | — | — | — | 200 | 404 | — |
| `POST /quality/nonconformances` | 04 | ✓ | ⛔ **없다** | — | ✓ | **201** | 400 · 403 · 409 · ⭐ **404 없다** | **`ShipmentConflictResponse`** |
| `POST …/{nonconformanceId}:request-disposition` | 04 | ✓ | **필수** | ⛔ 200 에 **선언 없다** | ✓ | **200**(`Nonconformance`) | 400 · 403 · 404 · 409 | **`ShipmentConflictResponse`** |
| `POST …/{nonconformanceId}/disposition-decisions` | 03 | ✓ | **필수** | **✓(201)** | ✓ | **201**(`DispositionDecision`) | 400 · 403 · 404 · 409 | **`QualityConflictResponse`** |

- ⭐ **409 봉투가 «둘»이다** — 한 슬라이스가 두 도메인 봉투를 쓰는 첫 자리다. `ShipmentConflictResponse.code` enum **5값**(`VERSION_CONFLICT`·`DUPLICATE_KEY`·`INVALID_STATE`·`ALREADY_CONFIRMED`·`CANCEL_IN_PROGRESS`) · `QualityConflictResponse.code` enum **6값**(앞의 셋 + `DUPLICATE_HOLD`·`HOLD_QTY_EXCEEDED`·**`DISPOSITION_QTY_EXCEEDED`**). ⇒ **부적합 쪽 두 쓰기는 `DISPOSITION_QTY_EXCEEDED` 를 낼 수 없다**(그 값이 출하 봉투에 없다). 잔량 초과는 **판정 저장(03)에서만** 난다 — 자연히 맞는다.
- ⭐ **`POST /quality/nonconformances` 는 404 를 «안» 선언했다** — I-20 `POST /quality/lot-holds`(404 선언)와 **반대**다. ⇒ 없는 `lotId`·`itemId` 는 **400 `INVALID`**(I-19 `POST /quality/inspection-results` 선례와 같은 방향).
- ⭐ **`:request-disposition` 의 200 에는 ETag 헤더가 «없다»** — 그런데 `runVersioned` 는 **늘 `setEtag` 를 낸다**(`master-write.ts:40-56` · I-20 부록 #64). 계약이 «금지»한 것이 아니라 «선언 안 한» 것이라 그대로 둔다(선언 밖 헤더는 계약 위반이 아니다). **알려둘 것**에 남긴다.
- **403 은 쓰기 3건뿐** — 조회 8건은 계약이 403 을 선언하지 않았고 가드는 **선언한 자리에서만** 본다(`permission.guard.ts:37-41` 실측). ⇒ e2e 에 권한 없는 계정을 조회 쪽으로 만들지 않는다.
- ⚠ **`GET /quality/concessions/{concessionId}` 가 `derived-permissions.ts` 에 «없다»**(전수 grep 실측 — 나머지 10건은 `:116`·`:120`·`:121`·`:122`·`:137`·`:138`·`:139`·`:254`·`:255`·`:256` 에 있다). **그래도 500 이 나지 않는다** — 그 오퍼레이션은 403 미선언이라 가드가 `:37-41` 에서 먼저 돌아간다. ⇒ **`manual-permissions.ts` 0줄**(§7-4).

### 1-2. 조회 8건 — 질의 칸 전수 ↔ 가리키는 칸

| 오퍼레이션 | 질의 칸(전수) | 가리키는 칸 · 판정 |
|---|---|---|
| `GET /quality/nonconformances` | **10** — `statusCode`·`sourceCode`·`warehouseId`·`itemId`·`lotId`·`severityCode`·`openedFrom`·`openedTo`·`page`·`size` | `nonconformance.status_code`·`.severity_code`·`.item_id`·`.opened_at`. **`lotId` 는 `EXISTS(nonconformance_lot WHERE lot_id = ?)`**(⛔ nullable 칸 등치 금지 — I-20 R-23 교훈) · **`warehouseId` 는 `EXISTS(inventory_balance b JOIN nonconformance_lot …)`** · **`sourceCode` 는 물리 칸이 «없다» — 파생 축이라 `EXISTS(goods_receipt … receipt_type_code='RETURN')` 로 «질의»한다**(§1-4) |
| `GET /quality/nonconformances/{nonconformanceId}` | 경로 1 | `nonconformance` 1건 + `nonconformance_lot[]` + `lot` 조인. **ETag = `nonconformance.version_no`**(판정 #5) |
| `GET /quality/disposition-candidates` | **10** — `sourceCode`·`withoutNonconformanceOnly`(기본 **false**)·`warehouseId`·`itemId`·`lotId`·`receivedFrom`·`receivedTo`·`q`·`page`·`size` | 원천 둘의 **UNION**(§4-1). `withoutNonconformanceOnly=true` → `NOT EXISTS(nonconformance_lot … WHERE nc.closed_at IS NULL)`. ⚠ **`receivedFrom/To` 는 `date` 이고 「양끝 포함」**이라 계약이 «명시»했다(공유계약 L-3 date 축) — ⛔ **반열림이 아니다.** 이 슬라이스에서 **유일하게 양끝 포함인 기간**이다 |
| `GET /quality/nonconformances/{id}/disposition-decisions` | ⭐ **0** | ⛔ **질의 칸이 하나도 없는데 응답 봉투는 `page`(`PageMeta`)를 required 로 요구한다.** ⇒ **전건을 싣고 `{page:1, size:total, total:total}`** 로 낸다(51번째가 조용히 사라지는 것을 막는다). `summary` 는 `DispositionRemainingSummary`(§1-4). **알려둘 것** |
| `GET /quality/disposition-decisions` | **11** — `dispositionTypeCode`(enum 3)·`nonconformanceId`·`lotId`·`itemId`·`decidedFrom`·`decidedTo`·`warehouseId`·`reinstatable`·`followUpPending`·`page`·`size` | `disposition_decision.*` + `nonconformance` 조인. `lotId`·`warehouseId` 는 위와 같은 `EXISTS`. **`reinstatable`·`followUpPending` 은 판정 #3 이 가른다** |
| `GET /quality/disposition-decisions/{dispositionDecisionId}` | 경로 1 | 1건. ⛔ **ETag 없다**(계약 미선언 · 물리에 `version_no` 도 없다) |
| `GET /quality/concessions` | **8** — `approvalRequestId`·`lotId`·`nonconformanceId`·`statusCode`·`usableOnly`·`validOn`·`page`·`size` | `concession.*` 그대로. **`usableOnly` = 3항 논리곱**(§4-3) |
| `GET /quality/concessions/{concessionId}` | 경로 1 | 1건 |

⭐ **기간 규칙이 «세 갈래»다 — 하나로 뭉치지 않는다**(I-19 R-17 · I-20 §1-2 의 교훈을 그대로 적용).

| 갈래 | 오퍼레이션 | 계약 문자 | 없을 때 | 경계 |
|:-:|---|---|---|---|
| **A 설명은 필수 · 선언은 없음** | `nonconformances`(`openedFrom/To`) | 「기간 필수 — 공유계약 L-3」인데 `required` 도 400 도 **미선언** | ⭐ **400 `REQUIRED`**(§2 2단계 기준 2 — 거부→허용이 호환 완화 · **I-20 §1-2 갈래 C 와 같은 판정**) | **반열림**(`>= from AND < to`) |
| **B 이력 모드에서 필수** | `disposition-decisions`(`decidedFrom/To`) | 「기간 — **이력 모드에서** 필수(L-3)」 | ⭐ **한 쌍으로 본다** — 한쪽만 오면 400 `PAIR`, 둘 다 없으면 **통과**(「이력 모드」의 판정 축이 계약에 없다 · 2단계 기준 3) | **반열림** |
| **C ⛔ 기간을 필수로 두지 «않는다»(명시)** | `disposition-candidates`(`receivedFrom/To`) | 「⚠ 기간을 «필수»로 두지 않는다 — 이 목록은 「지금 판정을 기다리는 것」이라 재고성이고, 기간을 강제하면 **오래된 미처리 건이 화면에서 사라진다**. **양끝 포함**(L-3 — date 축)」 | **통과** | ⭐ **양끝 포함**(`>= from AND <= to`) |

⛔ **갈래 C 의 「양끝 포함」을 갈래 A·B 의 반열림으로 베끼면 하루가 사라진다.** e2e 가 **끝 경계와 같은 값 한 행**을 셋 다 다르게 단언한다(§8).

### 1-3. 쓰기 3건 — 본문 스키마 실측

#### `NonconformanceCreate` — required **4** · 프로퍼티 **7**

| 칸 | 필수 | 담을 곳 | 판정 |
|---|:-:|---|---|
| `itemId` | ✓ | `nonconformance.item_id` | 없는 값 → **400 `INVALID`**(404 미선언) |
| `workOrderId` | — | `.work_order_id` | nullable — 「반품은 W/O 도 검사 결과도 없이 온다」(`W-04-07` §4-A) |
| `inspectionResultId` | — | `.inspection_result_id` | 〃. OQC 갈래는 후보의 `inspectionResultId` 를 그대로 넘긴다(계약 명시) |
| `severityCode` | ✓ | `.severity_code` | 시드 `NONCONFORMANCE_SEVERITY` **3값**(`seed.ts:1156-1163` — `CRITICAL`·`MAJOR`·`MINOR`). `assertCodeValues` 로 대조(**「고객이 늘린다」 G-31 이라 DB 를 본다**) |
| `description` | ✓ | `.description` | `text NOT NULL`. ⭐ **공백만 있는 문자열도 받지 않는다**(A-12 · 계약이 형제 칸 `DispositionDecisionCreate.reason` 에서 명시) → 400 `REQUIRED` |
| `responsibleDepartmentId` | — | `.responsible_department_id` | FK |
| `lots: NonconformanceLotCreate[]` | ✓ | `nonconformance_lot` N행 | **`{lotId, affectedQty, uomId}` 셋 다 required**. 빈 배열 → **400 `RANGE`**(I-8 「`lines: []` 는 `RANGE`」 선례) · 같은 `lotId` 중복 → **400 `INVALID`**(`uq_nonconformance_lot` 이 500 으로 새는 것을 막는다 — **I-20 §12-1 ⓑ 의 교훈**) · ⭐ **`uomId` 가 전부 같아야 한다**(판정 #2 ⓐ) → 다르면 400 `INVALID` · `affectedQty` 는 **`CHECK (> 0)`**(baseline `:1987`) → 400 `RANGE` |

⛔ **받지 않는 칸** — `sourceCode`(서버 파생 · `W-04-07` §5-1-1 「원천 입력 위젯을 그리지 않는다」) · `statusCode`(서버가 `NOT_REQUESTED` 로 연다) · `nonconformanceNo`(채번) · `openedAt`(서버 수신 시각 · B-6) · `action_*` 4칸(판정 «후»에 채워진다 — 이 슬라이스에 그 오퍼레이션이 없다).

#### `DispositionRequest`(`:request-disposition`) — required **2** · 프로퍼티 **3**

| 칸 | 필수 | 담을 곳 | 판정 |
|---|:-:|---|---|
| `requestedQty` | ✓ | ⛔ **담을 데가 0** | §1-4 — **물리에 의뢰 수량 칸이 없다.** 형식만 검증하고 저장하지 않는다(`>= 1` — 계약 「1 이상이어야 한다」 · `W-04-07` §5-7) → 400 `RANGE` |
| `uomId` | ✓ | ⛔ **담을 데가 0** | 〃. **부적합의 단위와 다르면 400 `INVALID`** |
| `remarks` | — | ⛔ **담을 데가 0** | 〃 — `nonconformance` 에 `remarks` 칸이 없다(`schema.prisma:3427-3457` 실측). 읽고 버린다 |

⭐ **이 오퍼레이션이 실제로 바꾸는 것은 `status_code`(`NOT_REQUESTED` → `PENDING_DECISION`)와 `version_no` 둘뿐**이다. 본문 3칸이 전부 갈 곳이 없다 — 통보 089 §5.

#### `DispositionDecisionCreate` — required **4** · 프로퍼티 **4**(전건 필수)

| 칸 | 필수 | 담을 곳 | 판정 |
|---|:-:|---|---|
| `dispositionTypeCode` | ✓ | `disposition_decision.disposition_type_code` | ⭐ **계약 enum 3값**(`REWORK`·`SCRAP`·`NORMAL` · 확정 2026-09-01 `omf-mes#336`). ⛔ **공통코드 그룹으로 받지 않는다**(계약 명시 · `W-03-10` §4-B 「`codeGroupCode=` 포인터를 적지 마라 — 그 호출이 «없다»」) ⇒ **시드 그룹 `DISPOSITION_TYPE` 을 만들지 않는다**(오늘 없다 — seed 전수 grep 0건). 코드 상수로 검증 → 400 `INVALID` |
| `decisionQty` | ✓ | `.decision_qty` | `numeric(20,6)` · **`CHECK (> 0)`**(baseline `:2001`) → 400 `RANGE`. **남은 수량 초과는 409**(§3-3) |
| `uomId` | ✓ | `.uom_id` | **부적합의 단위와 같아야 한다**(계약 `DispositionRemainingSummary.uomId` 「판정 입력의 단위도 이 값으로 고정한다」) → 다르면 400 `INVALID` |
| `reason` | ✓ | `.reason` | `text NOT NULL` · ⭐ **공백만 있는 문자열도 받지 않는다**(계약 명시 · A-12) → 400 `REQUIRED` |

⛔ **`decidedBy`·`decidedAt` 은 받지 않는다** — 서버가 인증 주체와 수신 시각으로 채운다(B-6 · 계약 명시).
⛔ **`approval_request_id` 는 비운 채 둔다** — 「처분이 언제 결재를 타는가」는 `W-04-07` §8 #3 이 **아직 열려 있고**(`W-06-15` 가 소관을 «부인»했다) 확정된 승인 유형 8값에 처분이 없다. `plan-uiux.md:609` 판정 그대로 **결재를 걸지 않는다**(2단계 ② — 없음→있음이 완화).

### 1-4. 응답 스키마 — **채울 수 없는 칸 / 담을 데가 없는 칸**

| 스키마 | required / 프로퍼티 | 자리 | 판정 |
|---|:-:|---|---|
| `Nonconformance` | **12** / 21 | ⭐ **`sourceCode`(required) — 물리에 `source_code` 칸이 0개** | ⛔ **마이그로 세우지 않는다.** 계약이 「⭐ **서버가 대상 LOT 의 입고 유형으로 «파생»**한다」라 직접 적었고 `W-04-07` §4-A 도 같다. ⇒ **`EXISTS(goods_receipt_line l JOIN goods_receipt g … g.receipt_type_code='RETURN' AND l.lot_id = nl.lot_id)` 이면 `RETURN`, 아니면 `PRODUCT`.** `lots` 가 여럿이면 **하나라도 RETURN 이면 RETURN**(놓치지 않는 쪽 · 통보 089 §6) |
| 〃 | | `affectedQtyTotal`·`uomId`(둘 다 required) | **파생** — `sum(nonconformance_lot.affected_qty)` · 단위는 §1-3 이 «같기를 강제»해 하나로 확정된다(판정 #2 ⓐ). ⛔ 화면이 `lots` 를 세지 않는다(L-2) |
| 〃 | | `dispositionProgressCode`(required · enum 3) | **파생** — `NOT_STARTED`(결정 0건) · `PARTIAL`(결정 있고 남은 수량 > 0) · `COMPLETED`(남은 수량 0). ⚠ **`statusCode` 와 축이 다르다**(계약 명시) — 「판정 대기」 한 값이 앞의 둘을 함께 담는다 |
| 〃 | | `lots[]`(required) | `nonconformance_lot` 전건. `NonconformanceLot.qualityStatusBeforeCode`·`AfterCode` **둘 다 required** — ⭐ **등록은 LOT 을 옮기지 않으므로 둘이 «같은 값»(등록 시점 `lot.status_code`)이 된다.** 계약 문자가 「등록 «전»/«후»」라 그대로 따른다(2단계 기준 1 — 상태를 안 쓰는 쪽). ⛔ 처분 판정이 그 칸을 나중에 갱신하지 «않는다»(기록 전용 표). **통보 089 §6** |
| 〃 | | `closedAt` | `.closed_at` — **`DECIDED` 로 갈 때 채운다**(§6-1). `ck_nonconformance_dates(closed_at IS NULL OR closed_at >= opened_at)` 이 이미 강제한다 |
| 〃 | | `versionNo` | required 밖이나 **판정 #5 의 토큰이라 늘 싣는다** |
| `DispositionCandidate` | **7** / 18 | `warehouseId`(required) | ⭐ 판정 #4 — `inventory_balance` 를 LOT 축으로 접는다. **잔액 창고가 둘 이상이면 그 LOT 을 목록에서 뺀다** |
| 〃 | | `quantity`(required) | `sum(inventory_balance.on_hand_qty)` — ⭐ **`on_hand_qty <> 0` 을 «먼저» 건다**(I-20 R-22 교훈 — 창고 이동이 남긴 0 행이 영구히 남는다) |
| 〃 | | `goodsReceiptId`·`receiptNo`·`receivedAt`·`partnerName` | **RETURN 갈래만.** `sourceCode='PRODUCT'` 면 **키 생략**(계약이 「null 이다」라 적었으나 널 금지 규칙이 이긴다 — `plan.md` §5-7). `partnerName` 은 「⭐ 서버가 **원 출하를 거슬러** 풀어 준다」 ⇒ `goods_receipt_line.original_shipment_lot_allocation_id`(실재) → 출하 → 거래처 |
| 〃 | | `inspectionResultId` | **PRODUCT 갈래만** |
| 〃 | | `nonconformanceId`·`nonconformanceNo`·`nonconformanceStatusCode` | 「이 대상에 **이미 만들어진** 부적합」 — **단수**다. §3-1 이 그것을 «열린 부적합 하나»로 강제한다(409 `DUPLICATE_KEY`) |
| `DispositionDecision` | **10** / 18 | ⭐ **`followUpStatusCode`·`followUpQty`(둘 다 required)** | **판정 #3** — SCRAP 만 센다(`goods_issue` 롤업) · REWORK·NORMAL 은 `0`/`NOT_STARTED` 고정. ⛔ **키를 생략하지 않는다**(required) |
| 〃 | | `lotId`·`lotNo`·`itemId`·`itemCode`·`itemName` | required 밖. **`nonconformance_lot` 이 «하나»일 때만 싣는다**(여럿이면 키 생략 — I-20 §0 #5 선례). `itemId` 계열은 `nonconformance.item_id` 라 **언제나** 싣는다 |
| 〃 | | `decidedByName` | `app_user` 조인 |
| 〃 | | `approvalRequestId` | 오늘 **언제나 NULL** ⇒ 키 생략(§1-3) |
| `DispositionRemainingSummary` | **4** / 4 | 전건 | `affectedQtyTotal`(부적합 lots 합) − `decidedQtyTotal`(이 부적합의 `decision_qty` 합) = `remainingQty`. **서버가 계산한다**(L-2) |
| `Concession` | **10** / 20 | `usable` | required 밖 · **3항 논리곱**(§4-3) |
| 〃 | | `unrestrictedAxes` | required 밖 · 「비어 있는 허용 축의 «이름»」 — ⭐ **이름 문자열을 계약이 안 줬다.** **계약 프로퍼티 이름 그대로**(`allowedWorkOrderId`·`allowedProcessId`·`allowedCustomerId`) 싣는다(화면이 매핑할 유일한 안정 키 · 2단계 기준 5 — 새 개념 0). **통보 089 §3** |
| 〃 | | `statusCode`(required) | `concession.status_code` 그대로. ⭐ **값 목록이 없다**(시드에 `CONCESSION_STATUS` 그룹 0건 · `W-03-09` §8 #2 미결) — **통보 089 §2**(⇐ 회신 대기 문의 12) |
| 〃 | | `nonconformanceNo`·`lotNo` | 조인 |

⭐ **담을 데가 «아예» 없는 칸 — 3건**: `DispositionRequest` 의 `requestedQty`·`uomId`·`remarks`(§1-3). 셋 다 required 이거나 값이 있는데 물리에 자리가 0이다. **형식만 검증하고 버린다** — 0단계 선례가 셋 있다(`plan.md` §5-9 의 「읽고 버림형」 `X-Worker-No` 자리들 · `plan.md` §5-7 「원장 안 지나면 형식만 검증하고 저장 안 함」).

### 1-5. 값 목록 — 계약이 준 것 ↔ 시드에 있는 것

| 그룹 | 계약 | 시드(`prisma/seed.ts` 실측) | 판정 |
|---|---|---|---|
| `NONCONFORMANCE_SEVERITY` | 3값 열거(설명) · enum 없음 · 「고객이 늘린다」 | **3값**(`:1156-1163`) — 일치 | `assertCodeValues` 로 대조(G-31 이라 DB 를 본다) |
| `NONCONFORMANCE_STATUS` | 3값 열거 · enum 없음 · 「⛔ 시스템 소유」 | **3값**(`:1166-1174` · `isSystemOwned: true`) — 일치 | 질의 축 대조. **쓰기는 상태기계가 정한다**(§6-1) |
| `DISPOSITION_TYPE` | ⭐ **계약이 `enum` 으로 닫았다**(3값) · 「⛔ 공통코드 그룹으로 받지 않는다」 | ⛔ **그룹 자체가 없다** | ✅ **그대로 둔다** — 그룹을 만들지 않는다. 계약 enum 을 코드 상수로 검증 |
| `LOT_STATUS` | 4값 | **4값**(`:543-552` · `SCRAPPED` = `:550`) | 처분 도착 3값이 전부 이 안에 있다 |
| `LOT_STATUS_TRANSITION` | **enum 9값**(`C4`~`C15` · `LotStatusHistoryEvent.transitionCode` required · `logistics-01자재창고.json:11970`) | **9값**(`:517-531` · `isSystemOwned`) | ⭐ **처분 3전이에 대응하는 코드가 0개다** — §6-2 가 판정한다. **마이그레이션으로 3값을 더한다** |
| `CONCESSION_STATUS` | ⛔ 값 목록을 안 줬다 | ⛔ **그룹 자체가 없다** | **통보 089 §2** — `usable` 의 상태 축을 **`'APPROVED'`** 로 정한다(§4-3). ⛔ 그룹을 세우지 않는다(writer 가 0이라 임의 값이 들어올 길이 없다) |
| `RECEIPT_TYPE` | — | **`RETURN`**(`:400-405`) | `sourceCode` 파생의 축(§1-4) |
| 승인 유형 | ⭐ **9값에 「특채」가 없다**(`plan-uiux.md:1230` M) | — | ⛔ **새 유형을 지어내지 않는다** — 계약이 이미 길을 적었다(「목록 자체는 승인 계약이 낸다 · `GET /quality/concessions?approvalRequestId=` 로 조건을 붙인다」). **문의 019 에 이미 흡수됐다**(`plan.md` §7) |

### 1-6. 에러 코드 — **새 코드 0건**

| 자리 | 봉투 | 코드 | 출처 |
|---|:-:|---|---|
| 빈 `lots` · `affectedQty <= 0` · `decisionQty <= 0` · `requestedQty < 1` | 400 | `RANGE` | 기존(`error-codes.ts:10`) |
| `description`·`reason` 이 공백만 · 기간 갈래 A 부재 | 400 | `REQUIRED` | 기존(`:9`) |
| 없는 `itemId`·`lotId` · `lots` 안 `lotId` 중복 · `lots` 의 `uomId` 불일치 · `dispositionTypeCode` enum 밖 · 코드값 목록 밖 · `uomId` 가 부적합 단위와 다름 | 400 | `INVALID` | 기존(`:13`) |
| 기간 갈래 B 한쪽만 | 400 | `PAIR` | 기존(`:14`) |
| 이미 `DECIDED` 인 부적합에 `:request-disposition` · 처분 도착이 `SCRAPPED` 인 LOT 을 다시 옮기려 함(코어가 0건 옮김) | 400 | `STATE_LOCKED` | 기존(`:17`) · I-19 §3-2 ⑥ · 문의 088 선례 |
| If-Match 불일치(쓰기 2건) | **409** | `VERSION_CONFLICT` | 두 봉투 공통 |
| ⭐ 같은 LOT 에 **열린 부적합**이 이미 있다 | **409** | `DUPLICATE_KEY` | `ShipmentConflictResponse` enum · 근거 = `withoutNonconformanceOnly` 설명(「⭐ 같은 대상을 두 번 등록하는 것을 막는 축이다」)과 `DispositionCandidate.nonconformanceId` 가 **단수**인 것 |
| ⭐ 이미 의뢰된(`PENDING_DECISION`) 부적합에 `:request-disposition` | **409** | `INVALID_STATE` | `W-04-07` §6 「이미 의뢰됨 ⛔ 「판정 대기 중입니다」 — 중복 의뢰 방지」 |
| ⭐ 이미 종결된(`DECIDED`) 부적합에 판정 저장 | **409** | `INVALID_STATE` | 계약 x-internal-note 명시 · `W-03-10:195` |
| ⭐ 처분 결정 수량 합이 대상 수량을 넘는다 | **409** | **`DISPOSITION_QTY_EXCEEDED`** + `remainingQty`·`remainingQtyUomId` | `QualityConflictResponse` enum · `W-03-10:194` |
| 없는 `nonconformanceId`·`dispositionDecisionId`·`concessionId` | 404 | — | 계약이 선언한 자리만(§1-1) |

⛔ **`src/common/errors/error-codes.ts` 는 «안 건드린다»**(`lanes.md` §1-4 — 공용 · 새 코드 금지). 위 전건이 기존 값이다.
⚠ **`ConflictExtra` 에 `remainingQty`·`remainingQtyUomId` 가 «없다»** — 오늘 칸은 `code`·`currentVersion`·`currentLotStatusCode`·`conflictingLotId` 넷(`conflict.exception.ts:19-38` 실측 · 마지막은 I-20 이 더했다). **I-20 §11 ②ⓑ 가 예고한 그대로** 두 칸을 더한다. 공용 파일이므로 **PR ⑥ 안에서 «단독 커밋»**(I-20 선례 · `lanes.md` §1-4 의 `manual-permissions.ts` 규칙과 같은 취급).

---

## 2. 물리 대조 + **마이그레이션 결론**

### 2-1. `quality.nonconformance` 21칸 ↔ 계약 `Nonconformance` 21 프로퍼티

| 계약 | 물리 | 판정 |
|---|---|---|
| `nonconformanceId`·`nonconformanceNo`·`itemId`·`workOrderId`·`inspectionResultId`·`severityCode`·`description`·`responsibleDepartmentId`·`actionDescription`·`actionOwnerId`·`actionDueDate`·`actionCompletedAt`·`statusCode`·`openedAt`·`closedAt`·`versionNo` **16** | 같은 이름의 칸이 전부 있다(`schema.prisma:3427-3457`) | ✅ |
| `sourceCode` **(required)** | ⛔ **없다** | ✅ **파생**(§1-4) — 마이그 0 |
| `affectedQtyTotal`·`uomId`·`dispositionProgressCode` **3** | ⛔ 없다 | ✅ **파생**(L-2) — 마이그 0 |
| `lots[]` | `nonconformance_lot` | ✅ |

**제약·인덱스 실측**(baseline `20260727000000:1957-1980`·`:3142-3144` · `20260807200000:43-44`):
`nonconformance_no UNIQUE` · `ck_nonconformance_dates(closed_at IS NULL OR closed_at >= opened_at)` ·
`ix_nonconformance_open(status_code, severity_code, opened_at) WHERE closed_at IS NULL`(**부분**) · `ix_nonconformance_responsible_department(responsible_department_id)`.

### 2-2. `quality.nonconformance_lot` 9칸 ↔ 계약 `NonconformanceLot` 7 프로퍼티 — **마이그 0**

7 대 7 전건 대응(물리 여분은 `created_at`·`created_by`). `lotNo` 는 `trace.lot` 조인.
제약: `affected_qty CHECK (> 0)`(`:1987`) · **`uq_nonconformance_lot UNIQUE (nonconformance_id, lot_id)`**(`:1993`) ⇒ §1-3 의 「같은 `lotId` 중복 400」이 그 제약이 500 으로 새는 것을 막는다.

### 2-3. `quality.disposition_decision` 10칸 ↔ 계약 `DispositionDecision` 18 프로퍼티

| 계약 | 물리 | 판정 |
|---|---|---|
| `dispositionDecisionId`·`nonconformanceId`·`dispositionTypeCode`·`decisionQty`·`uomId`·`reason`·`decidedBy`·`decidedAt`·`approvalRequestId` **9** | 있다(`schema.prisma:3200-3216`) | ✅ |
| `nonconformanceNo`·`lotId`·`lotNo`·`itemId`·`itemCode`·`itemName`·`decidedByName` **7** | 조인 | ✅ 파생 |
| **`followUpStatusCode`·`followUpQty`**(둘 다 required) | ⛔ 없다 | ⭐ **판정 #3** — 롤업이다. 마이그 0 |
| — | (물리 여분) `created_at` | 「기록 전용 · 감사 칸·`version_no` 제외」(`W-04-07` §4-B 명시) ⇒ ETag 를 안 낸다(§1-1) |

**제약·인덱스 실측**(baseline `:1996-2008`·`:2733-2736`):
`decision_qty CHECK (> 0)` · late FK `fk_disposition_approval` · ⛔ **인덱스가 «0개»다**(PK 뿐).
⭐ **`nonconformance_id` FK 에 인덱스가 없다** — PostgreSQL 은 FK 에 인덱스를 자동 생성하지 않는다(**I-20 R-21 이 같은 자리에서 죽었다**). `GET …/{ncId}/disposition-decisions` 와 `remainingQty` 롤업이 **전건 Seq Scan** 이 된다 ⇒ **M-h 가 인덱스 하나를 세운다**(§2-5).

### 2-4. `quality.concession` 21칸 ↔ 계약 `Concession` 20 프로퍼티 — **마이그 0**

| 계약 | 물리 | 판정 |
|---|---|---|
| `concessionId`·`concessionNo`·`nonconformanceId`·`lotId`·`approvedQty`·`consumedQty`·`uomId`·`validFrom`·`validTo`·`allowedWorkOrderId`·`allowedProcessId`·`allowedCustomerId`·`approvalRequestId`·`statusCode`·`remarks`·`versionNo` **16** | 전부 있다(`schema.prisma:3091-3120`) | ✅ |
| `nonconformanceNo`·`lotNo` | 조인 | ✅ |
| `usable`·`unrestrictedAxes` | ⛔ 없다 | ✅ **파생**(§4-3) |

제약: `concession_no UNIQUE` · `approved_qty CHECK (> 0)` · `ck_concession_dates(valid_to IS NULL OR valid_to >= valid_from)` · **`ck_concession_consumed(consumed_qty <= approved_qty)`** · `ix_concession_lot(lot_id, status_code)` · late FK `fk_concession_approval`.
⭐ **이 표에는 writer 가 «0개»다** — 계약이 생성·승인·수정 경로를 전부 두지 않았다(「⛔ 생성 경로를 두지 않는다 … ⛔ 조건 수정 경로를 두지 않는다 … 승인·반려는 app-공통」). ⇒ **운영에서 이 목록은 빈 채로 뜬다.** e2e 는 prisma 로 직접 심는다(§8-1). **알려둘 것**.

### 2-5. ⭐ 마이그레이션 결론 — **M-h 한 파일 · 항목 2 · 삭제 0 · 백필 0**

> ⚠ **통합 계획서 §4 에 I-21 행이 «없다»**(마이그 0 을 전제했다). `plan-integration.md:185` 는 「마이그 **있음**」이라 적어 **셋이 서로 다르다.** 이 계획안이 **2건**으로 확정한다.

| 항목 | 내용 | 근거 |
|:-:|---|---|
| **ⓐ** | `INSERT INTO app.code_value` — `LOT_STATUS_TRANSITION` 그룹에 **`C17`·`C18`·`C19` 3값**(`ON CONFLICT DO NOTHING`) | §6-2 판정. **선례 실재** — `20260901010000_add_lot_lifecycle_axis` 등 마이그레이션이 `code_value` 를 넣는다. `seed.ts:517-531` 도 같은 커밋에서 3값을 더해 **새 환경과 기존 환경이 같아진다** |
| **ⓑ** | `CREATE INDEX ix_disposition_decision_nonconformance ON quality.disposition_decision (nonconformance_id)` | §2-3 — FK 인덱스 부재(**I-20 R-21**). `GET …/{ncId}/disposition-decisions`·`remainingQty` 롤업·`dispositionProgressCode` 롤업 셋이 전부 이 축이다 |

- **추가만 · 완화 0 · 삭제 0 · 백필 0** ⇒ 두 릴리스 규칙 미해당 · **순서 의존 0**(`lanes.md` §1-2).
- ⛔ **`schema.prisma` 는 인덱스 한 줄만 는다**(`@@index([nonconformance_id], map: "ix_disposition_decision_nonconformance")`). `code_value` 는 데이터라 모델 변경 0.
- ⛔ **FK 제약을 새로 만들지 않는다** — 만들면 이름이 Prisma 기본형이어야 하고 `migrate diff` 가 드리프트를 낸다(MEMORY).
- 파일 이름: `prisma/migrations/<YYYYMMDDHHMMSS>_disposition_transition_codes/migration.sql` — **만드는 시각을 초까지**.

### 2-6. 인덱스를 **더 걸지 않는 자리** — 근거

| 후보 | 왜 안 거나 |
|---|---|
| `nonconformance(opened_at DESC)` | `ix_nonconformance_open` 이 `(status_code, severity_code, opened_at)` 를 이미 갖는다. **부분 인덱스**(`WHERE closed_at IS NULL`)라 종결분은 못 타지만, 목록의 기본 축이 「열린 것」이고 **기간이 필수**(갈래 A)라 범위가 유계다 |
| `nonconformance_lot(lot_id)` | `uq_nonconformance_lot(nonconformance_id, lot_id)` 의 **선두가 `nonconformance_id`** 라 `lotId` 단독 조회는 못 탄다. 다만 이 표는 부적합당 1~3행이고 전체 행수가 부적합 수의 상수배다 ⇒ 규모가 커지면 후속 소형 PR(§11 ⑥ⓐ) |
| `concession(valid_to)`·`(status_code)` | ⭐ **이 표는 writer 가 0개라 운영에서 0행이다**(§2-4). 인덱스를 걸 근거가 성립하지 않는다 |
| `disposition_decision(decided_at DESC)` | 기간 갈래 B 는 **선택**이라 유계가 아니다. 그러나 행수가 부적합 수의 1~3배로 작다 ⇒ §11 ⑥ⓑ |
| `goods_issue(source_document_type_code, source_document_id)` | ⭐ **이미 있다** — `ix_goods_issue_source(source_document_type_code, source_document_id, issued_at DESC)`(`schema.prisma:776`). 판정 #3 의 롤업이 그것을 탄다. **더 걸지 않는다** |
| `inventory_balance(lot_id)` | ⭐ **I-20 R-21 이 M-f 에 이미 실었다**(`ix_inventory_balance_lot`). 판정 #4 의 접기가 그것을 탄다 — **중복으로 만들지 않는다.** ⚠ 구현 착수 시 그 인덱스가 `main` 에 있는지 한 번 확인한다 |

---

## 3. ⭐ 심장 — 쓰기 3건의 한 트랜잭션

### 3-1. `POST /quality/nonconformances` — 201

```
runIdempotent(201):
  1. 코드값 대조   assertCodeValues([{severityCode, 'NONCONFORMANCE_SEVERITY'}])        → 400 INVALID
  2. 본문 형식     lots 비었나 · lotId 중복 · uomId 전부 같나 · affectedQty > 0
                   · description 공백만인가                                             → 400 RANGE/INVALID/REQUIRED
  3. 참조 존재     item(itemId) · lot(lots[].lotId) 전건 · uom(uomId)
                   · workOrderId·inspectionResultId·responsibleDepartmentId(있으면)     → 400 INVALID (⛔ 404 미선언)
  4. ⭐ 중복 판정  같은 lotId 에 열린 부적합(nc.closed_at IS NULL)이 이미 있나          → 409 DUPLICATE_KEY
  ── tx ──────────────────────────────────────────────────────────────────────────
  5. 채번          numbering.next('NONCONFORMANCE', null, <UTC 달력일>)  → 'NC-YYYYMMDD-0001'
  6. INSERT        nonconformance(status_code='NOT_REQUESTED', opened_at=now(), version_no=1)
  7. INSERT        nonconformance_lot N행
                   quality_status_before_code = quality_status_after_code = lot.status_code (§1-4)
  ── /tx ─────────────────────────────────────────────────────────────────────────
  8. 뷰 조립       sourceCode 파생 · affectedQtyTotal · dispositionProgressCode='NOT_STARTED'
```

- ⛔ **LOT 을 옮기지 않는다** — 계약이 등록 전이를 안 적었고, 2단계 기준 1(재고·원장·상태를 안 쓰는 쪽)이다. `transitions.ts` 에 등록 액션을 만들지 않는다.
- ⛔ **`lot_hold` 도 안 건다** — 계약·화면 어디에도 그 문장이 없다.
- ⭐ **채번**: `src/core/numbering/numbering.service.ts` 의 `DEFAULT_PREFIX` 에 `NONCONFORMANCE: 'NC'` 한 줄을 더한다(오늘 없다 — 그대로 부르면 즉시 `Error`). 근거: `W-03-10:56·58` 목업의 `NC-…0042`. 기간 축은 **UTC 달력일**(선례 — `plan.md` §6 I-30 배포 제한 「채번 기간은 기존 UTC 선례」). `plantId` 는 **null**(`nonconformance` 에 공장 축이 0개). 문의 **14**(채번 규칙 없는 표)의 권고안 그대로.

### 3-2. `POST …/{nonconformanceId}:request-disposition` — 200

```
runVersioned(If-Match ← nonconformance.version_no):
  1. 존재           nonconformance(id)                                        → 404
  2. 본문 형식      requestedQty >= 1 · uomId 가 부적합 단위와 같나           → 400 RANGE/INVALID
                    ⛔ 셋 다 저장하지 않는다(§1-3)
  ── tx ────────────────────────────────────────────────────────────────────
  3. 상태 전이      documentState.assertTransition(
                      'quality.nonconformance.status_code',
                      'nonconformance-request-disposition', 현재상태)
                    NOT_REQUESTED 밖 → 409 INVALID_STATE  (⛔ 400 이 아니다 — §3-4)
  4. UPDATE         status_code='PENDING_DECISION', version_no += 1           → 409 VERSION_CONFLICT
  ── /tx ───────────────────────────────────────────────────────────────────
  5. 200 + Nonconformance(갱신본)
```

### 3-3. ⭐ `POST …/{nonconformanceId}/disposition-decisions` — 201 + ETag (이 슬라이스의 심장)

```
runVersioned(If-Match ← nonconformance.version_no · ETag ← 갱신된 version_no):
  1. 존재           nonconformance(id)                                        → 404
  2. 본문 형식      dispositionTypeCode ∈ {REWORK,SCRAP,NORMAL} · decisionQty > 0
                    · reason 공백만 아님 · uomId = 부적합 단위                → 400 INVALID/RANGE/REQUIRED
  ── tx ────────────────────────────────────────────────────────────────────
  3. ⭐ 잠금        SELECT … FROM quality.nonconformance WHERE nonconformance_id = ?
                    FOR UPDATE                       ← ⭐ 잔량 판정이 이 잠금 «안»에 있어야 한다
  4. 종결 판정      status_code = 'DECIDED' 면                                → 409 INVALID_STATE
  5. 잔량 판정      remaining = sum(nonconformance_lot.affected_qty)
                              − sum(disposition_decision.decision_qty)
                    decisionQty > remaining →  409 DISPOSITION_QTY_EXCEEDED
                                               + remainingQty · remainingQtyUomId
  6. INSERT         disposition_decision(decided_by=세션, decided_at=now())
  7. ⭐ LOT 전이    lotQualityStatus.moveWithin(tx, <이 부적합의 nonconformance_lot 전건>,
                      {REWORK:'disposition-rework', SCRAP:'disposition-scrap',
                       NORMAL:'disposition-normal'}[유형],
                      { sourceDocumentTypeCode:'DISPOSITION_DECISION',
                        sourceDocumentId: <새 decision id>,
                        transitionCode: 'C17'|'C18'|'C19',
                        reason: body.reason })
                    movedLotIds 가 0건이면                                    → 400 STATE_LOCKED
  8. 남은 수량 0 이면
       상태 전이     assertTransition('quality.nonconformance.status_code',
                                      'nonconformance-decide', 'PENDING_DECISION')
       UPDATE        status_code='DECIDED', closed_at=now()
  9. UPDATE         nonconformance.version_no += 1     ← ⭐ 계약이 «명시»로 요구한다
  ── /tx ───────────────────────────────────────────────────────────────────
 10. setEtag(새 version_no) · 201 + DispositionDecision
```

⭐ **잠금 순서가 판정이다**(I-20 R-5 가 같은 자리에서 죽었다). 5(잔량 재계수)가 3(부적합 잠금) **안**에 있어야 한다 — 밖이면 두 사람이 동시에 `remaining=120` 을 읽고 각각 120 을 저장해 **합계가 대상 수량을 넘는다.** If-Match 만으로는 못 막는다(둘 다 같은 토큰을 들고 온다). **동시성 e2e 를 넣는다**(§8-5 #34).
⭐ **7 이 8 보다 «먼저»다** — LOT 이 못 움직이면(전부 `SCRAPPED`) 400 이고 부적합을 종결시키지 않는다.
⚠ **부분 처분이면 7 이 매번 돈다** — 160 REWORK 뒤 40 SCRAP 이면 LOT 은 **`SCRAPPED` 로 끝난다**(판정 #2 ⓒ). 계약이 그것을 막는 문장을 안 적었고 `trace.lot.status_code` 는 LOT 하나에 한 값이라 **수량으로 갈 수 없다**. 통보 089 §6.

### 3-4. 400 · 404 · 409 갈래 — **순서가 판정이다**

| 순서 | 자리 | 코드 | 왜 이 순서인가 |
|:-:|---|---|---|
| 1 | 계약 검증 가드(형식·enum·required) | 400 | 서비스보다 «앞»이다 — ⛔ **서비스에서 같은 것을 다시 단언하면 되돌려도 안 깨진다**(I-20 R-19 #3·#9·#24) |
| 2 | 권한(403 선언 3건) | 403 | 가드 |
| 3 | 멱등 키 재생 | 200/201 | `runIdempotent` |
| 4 | 경로 자원 존재 | **404** | 404 를 «선언한» 둘만(`:request-disposition`·판정 저장). 등록은 400 |
| 5 | If-Match 토큰 | **409 `VERSION_CONFLICT`** | `runVersioned` |
| 6 | 본문 형식·참조 존재 | 400 | 여기서 처음 400 이 난다 |
| 7 | **업무 상태**(이미 의뢰됨 · 이미 종결됨 · 중복 부적합) | **409** | ⭐ 계약 enum 이 `INVALID_STATE`·`DUPLICATE_KEY` 를 **가지고 있다** — 400 으로 내리지 않는다. ⚠ I-20 R-3 이 「400/409 를 단정하지 마라」로 남긴 자리와 **다르다**: 저기는 계약이 스스로 미결이라 적었고, 여기는 **두 봉투 enum 이 값을 갖고 화면이 그 값을 쓴다**(`W-03-10:195`·`W-04-07` §6) |
| 8 | **잔량 초과** | **409 `DISPOSITION_QTY_EXCEEDED`** | 잠금 안 |
| 9 | **전이 0건**(코어가 못 옮김) | **400 `STATE_LOCKED`** | 0단계 선례 = `inspection-confirm.service.ts` 의 같은 갈래(문의 088). 재로드해도 안 풀린다 |

---

## 4. 조회 8건

### 4-1. `GET /quality/disposition-candidates` — 원천 둘의 UNION (판정 #4)

```
WITH cand AS (
  -- RETURN: 반품 입고로 들어온 LOT
  SELECT gl.lot_id, 'RETURN' AS source_code, g.goods_receipt_id, g.goods_receipt_no,
         g.receipt_datetime::date AS received_at, <원 출하 거래처>, NULL::bigint AS inspection_result_id
    FROM logistics.goods_receipt_line gl
    JOIN logistics.goods_receipt g ON g.goods_receipt_id = gl.goods_receipt_id
   WHERE g.receipt_type_code = 'RETURN'
  UNION ALL
  -- PRODUCT: OQC 확정 불합격
  SELECT req.lot_id, 'PRODUCT', NULL, NULL, NULL, NULL, r.inspection_result_id
    FROM quality.inspection_result r
    JOIN quality.inspection_request req ON req.inspection_request_id = r.inspection_request_id
   WHERE r.status_code = 'CONFIRMED' AND req.inspection_type_code = 'OQC'
     AND r.overall_judgment_code = <불합격> AND req.lot_id IS NOT NULL
)
-- LOT 축으로 접고 잔액을 붙인다
SELECT … FROM cand c
  JOIN LATERAL (
    SELECT sum(b.on_hand_qty) AS qty,
           CASE WHEN count(DISTINCT b.warehouse_id) = 1 THEN min(b.warehouse_id) END AS warehouse_id,
           min(b.uom_id) AS uom_id
      FROM inventory.inventory_balance b
     WHERE b.lot_id = c.lot_id AND b.on_hand_qty <> 0        -- ⭐ I-20 R-22
  ) bal ON TRUE
 WHERE bal.warehouse_id IS NOT NULL                          -- ⭐ 창고가 둘 이상이면 뺀다(판정 #4)
```

- ⭐ **한 LOT 이 두 원천에 다 걸릴 수 있다**(반품된 제품이 OQC 불합격이었던 경우). ⇒ `UNION ALL` 뒤 **`lot_id` 로 접고 `RETURN` 을 이긴 값으로 둔다**(상세 머리가 입고 전표를 그린다 — `W-04-07` §3). 통보 089 §6.
- `withoutNonconformanceOnly` 는 **`NOT EXISTS`** 로 쓴다 — ⛔ nullable 관계 칸에 `NOT` 을 걸면 `IS NULL` 이 UNKNOWN 이라 **부적합이 아예 없는 LOT 이 통째로 사라진다**(I-20 §0 #5 · I-19 ⑤b 실측).
- `q` 는 LOT 번호·입고번호·품목(코드·명) **네 칸 ILIKE**.
- ⚠ `overall_judgment_code` 의 「불합격」 값 — 시드 `JUDGMENT_TYPE` 이 **값 0건**(회신 E-1 대기 · I-20 부록 #73)이다. I-19 가 쓴 상수를 **그대로 재사용**한다(`src/quality/inspection/inspection-rules.ts`). ⛔ 새로 짓지 않는다.

### 4-2. `GET /quality/nonconformances` · `…/{id}` · `…/{id}/disposition-decisions` · `GET /quality/disposition-decisions` · `…/{id}`

- **`sourceCode` 는 목록 «질의»에도 쓰이고 응답에도 실린다** — 같은 `EXISTS` 식을 **한 곳에 둔다**(`nonconformance-source.ts` 헬퍼 · 사용처 2).
- `dispositionProgressCode`·`affectedQtyTotal`·`remainingQty` 는 **세 오퍼레이션이 같은 롤업**을 쓴다 ⇒ 한 SQL 조각을 공유한다. ⚠ I-19 R-17 의 「공용 헬퍼로 뭉치지 마라」와 **반대 자리다** — 저기는 기간 «규칙»이 달랐고 여기는 **정의가 계약 한 문장**이다.
- 정렬 — 계약이 `sort` 를 **어느 조회에도 안 줬다**(질의 칸 전수 실측). ⇒ 기본 정렬을 서버가 정한다: 부적합 `opened_at DESC, nonconformance_id DESC` · 처분 결정 `decided_at DESC, disposition_decision_id DESC` · 후보 `received_at DESC NULLS LAST, lot_id DESC` · 특채 `valid_from DESC, concession_id DESC`. ⭐ **2차 키를 반드시 둔다**(I-20 R-10 — 동률이면 페이지가 어긋난다) · **NULL 자리를 못 박는다**(후보의 `received_at` 은 PRODUCT 갈래에서 NULL 이다).

### 4-3. `GET /quality/concessions` · `…/{id}` — `usable` 3항 논리곱

```
usable = status_code = 'APPROVED'                                   ← ⭐ 우리가 정한다(통보 089 §2)
     AND valid_from <= 기준일
     AND (valid_to IS NULL OR valid_to >= 기준일)                    ← ck_concession_dates 가 순서만 강제한다
     AND (approved_qty - consumed_qty) > 0                          ← ck_concession_consumed 가 상한만 강제한다
```

- **기준일** = `validOn` 이 오면 그 값, 없으면 **`CURRENT_DATE`(서버 UTC 달력일)**. ⛔ **타임존 캐스팅을 하지 않는다**(`@db.Date` 끼리 비교 · `CLAUDE.md`). ⚠ `concession` 에 공장 축이 0개라 `plant.timezone_code` 로 풀 길이 없다 — 하노이 로컬 자정과 **최대 7시간 어긋난다.** 통보 089 §2.
- `usableOnly=false`(또는 미지정)면 **필터를 안 건다.** ⛔ `false` 를 「쓸 수 없는 것만」으로 읽지 않는다(I-20 §1-2 `open=false` 교훈).
- ⭐ **특채가 LOT 을 어느 상태로 두는가는 이 슬라이스의 물음이 아니다** — 설계가 이미 답했다: 「**최종 상태는 셋 다 `정상`이고 다른 것은 조건이다**」(`W-03-09:31` · LOT 상태 확정 2026-08-07) · 「검사 불합격 → 불량 ↓ 특채 승인 승인 수량만큼 → **LOT 상태 = 정상** + `concession` 이 범위를 건다」(`:164-169`) · 「⚠ 상태만 바뀌는 것이 아니다 — `concession` 행이 살아 있어야 출고 시점에 범위 검사가 걸린다(A-14)」(`:171`). ⇒ **회신 대기 문의 12 는 「값을 새로 정하라」가 아니라 「이미 `NORMAL` 이다」로 닫힌다.** 그 전이를 «일으키는» 오퍼레이션은 계약에 **0건**이라 I-21 범위 밖이다(§9-1 #9).

---

## 5. 원장 · 재고 — **둘 다 0**

- ⛔ **`inventory_transaction` 을 안 쓴다** — `plan-integration.md:388` 「⛔ 직접은 없다 — 전부 다른 슬라이스의 오퍼레이션이 진다. 이 구조가 「원장 판별자 4값으로 닫힌다」의 두 번째 증거다」.
- ⛔ **`inventory_balance` 는 «읽기만»** 한다(후보 목록의 창고·수량 · 부적합 목록의 `warehouseId` 필터). 원시 SQL 로 조인한다 — **다른 도메인 service 호출 0**(0단계 선례: `balance-query.ts` 도 `mdm`·`trace` 를 조인해 읽는다).
- ⛔ **`inventory_balance.quality_status_code` 를 «쓰지» 않는다** — 부분 전이로 재고 행을 가르는 것(`W-03-09:173`)은 **특채 승인 경로**의 일이고 그 오퍼레이션이 계약에 0건이다.
- ⛔ **`blocked_qty` 를 안 본다** — 차단 판정은 Lot Status 단일 지점(결정 10 · `server-architecture.md` §0).

---

## 6. 상태기계 — `transitions.ts` 에 **키 신설 1 · 전이 5**

> ⛔ **`src/core/document-state/transitions.ts` 는 레인 A 소유이고 B·C 가 같은 파일을 읽는다**(`lanes.md` §1-4).
> **행 추가만** 한다 — 기존 행 수정·삭제 ⛔. 착수 «전에» 사용자에게 한 줄 보고한다(`lanes.md` §3).

### 6-1. `quality.nonconformance.status_code` — 키 신설 · 전이 2

| 액션 | from → to | 여는 오퍼레이션 | 근거 |
|---|---|---|---|
| `nonconformance-request-disposition` | `['NOT_REQUESTED']` → `PENDING_DECISION` | `:request-disposition` | `W-04-07` §4-A 「⭐ 이 화면이 올리는 것은 `PENDING_DECISION` 까지이고 `DECIDED` 로 올리는 것은 03(`W-03-10`)이다」 |
| `nonconformance-decide` | `['PENDING_DECISION']` → `DECIDED` | 판정 저장(**남은 수량 0 일 때만**) | 〃 · 계약 `dispositionProgressCode.COMPLETED` = 「남은 수량 0」 |

⛔ **`transitionCode` 를 쓰지 않는다** — 이 축에는 이력 표가 없다(선례: `work_order.status_code` 키의 주석).
⛔ **되돌리는 전이(`DECIDED` → `PENDING_DECISION`)를 만들지 않는다** — 「오판정 정정 경로」는 `W-03-10` §8 #9 가 **미결**로 남겼고 계약에 오퍼레이션이 0건이다(2단계 기준 2).

### 6-2. ⭐⭐ `trace.lot.status_code` — 전이 3 · **`transitionCode` 3안 비교표**(브리프 §2 ⓐ)

**사실 넷**(실측):
1. 계약이 **도착 상태를 직접 적었다** — `quality-03품질.json:2460`(§0 #1). 7파일 전수 grep 결과 `SCRAPPED` 로 LOT 을 보내는 오퍼레이션은 **이 하나뿐**이다.
2. 시드 `LOT_STATUS_TRANSITION` **9값**의 이름(`seed.ts:517-531`) 중 **`to = SCRAPPED` 인 것이 0개**다 — `C4` 판정→Release · `C5` 판정→보류 · `C6` 판정→Hold · `C7` 보류→Release · `C8` 보류→Hold · `C9` Release→Hold · `C10` 의심자재→보류 · `C14` 판정→PQC · `C15` PQC→Release.
3. 도식이 **C1~C16 을 이미 쓰고 있다** — `W-03-02:183-186` 이 `C11`(보류 발신)·`C16`(PQC → 02-S-I 경계 위임)을 적었다. 시드 9값은 그중 **상태 전이인 것만** 골라 담았다(`seed.ts` 주석 · 회신 E-9). ⇒ **비어 있는 첫 번호는 `C17`** 이다.
4. `trace.lot_status_event` 에 **불변 트리거가 없다**(전 마이그레이션 `CREATE TRIGGER` 6개 전수 실측 — 이 표는 그중 하나도 아니다). ⇒ 값이 틀리면 **`UPDATE` 한 문장**으로 갈아 끼운다(0단계 선례 = I-20 §11 ⑥ⓓ 의 087 「되돌릴 때」).

| 안 | 무엇을 하나 | 계약 정합 | 새 개념 | 되돌림 비용 | 판정 |
|:-:|---|---|:-:|---|:-:|
| **A** | **새 코드 3**(`C17`·`C18`·`C19`) + 시드·마이그 3값 + 전이 3행 | ⚠ `LotStatusHistoryEvent.transitionCode` **enum 9값 밖**이라 `GET /trace/lot-status-events` 응답이 계약을 벗어난다 — **통보 154 가 적은 바로 그 구멍**이고, 그 통보의 결론이 「조회는 enum 밖 값을 떨어뜨리지 않는다 · **089 로 값을 정하실 때 enum 도 함께 열어 주십시오**」다 | **3** | 값이 바뀌면 `lot_status_event.transition_code` **UPDATE 한 문장**(사실 4) + 시드 3줄 | ✅ **채택** |
| **B** | **기존 9종으로 접기** — REWORK→`C5`(`to` 일치) · NORMAL→`C4`(`to` 일치) | ⛔ **SCRAP 이 접힐 코드가 0개**(사실 2) ⇒ **셋 중 하나가 구조적으로 성립하지 않는다.** 게다가 `C4`·`C5` 의 「판정」은 **검사 판정 전용**이라 계약·설계가 못 박았다(`W-03-02:103-105` 「검사 판정 화면 — W-01-01(IQC)·P-02-13(PQC)·W-04-03(OQC)」) ⇒ 감사 조회가 **검사 보류와 처분 재작업을 한 통에 담아** 두 사건을 못 가른다 | 0 | — | ⛔ **탈락(성립 불가)** |
| **C** | **전이를 안 건다**(LOT 을 안 옮긴다) | ⛔ 계약 `:2460` 이 「**한 트랜잭션**(B-8) — 처분만 남고 LOT 이 안 바뀌면 **다음 화면이 잘못된 대상을 집는다**」로 이유까지 적었다(§2 1단계 「본길이면 계약 문자를 먼저 따른다」). 그리고 **폐기 판정된 LOT 이 계속 출고·피킹 대상**이 된다(결정 10 — 차단 판정 단일 지점) | 0 | ⭐ **소급 불가에 가깝다** — 나중에 붙여도 이미 판정된 건의 LOT 상태를 되짚을 원본이 없다(§2 1-1 ⓐ) | ⛔ **탈락** |

**§2 절차 흔적** — 0단계 선례 셋(`stock-reinstate` 가 코드 없이 등록돼 있다 · 문의 087 이 시드 밖 값을 짓고 통보했다 · 통보 154 가 enum 밖 값을 조회로 내보내기로 이미 판정했다) → 1단계 **본길**(모든 처분 저장의 결과가 달라진다) · 계약이 «도착»은 줬고 «코드»는 안 줬다 → **1-1단계**: 도착 상태는 MES 본질이지만 **계약이 이미 정했다**. 우리가 정하는 것은 **감사 라벨 문자열**뿐이라 본질이 아니고, 되돌림이 UPDATE 한 문장이라 비용도 낮다 ⇒ **「정하고 통보」** → 3단계 흔적 = 코드·계획서·테스트에 **`// 결정 — 통보 089`**.

| 액션 | from → to | `transitionCode` | 여는 오퍼레이션 |
|---|---|:-:|---|
| `disposition-rework` | `['NORMAL','INSPECTION_PENDING','DEFECTIVE']` → `INSPECTION_PENDING` | **`C17`** | 판정 저장 |
| `disposition-scrap` | `['NORMAL','INSPECTION_PENDING','DEFECTIVE']` → `SCRAPPED` | **`C18`** | 〃 |
| `disposition-normal` | `['NORMAL','INSPECTION_PENDING','DEFECTIVE']` → `NORMAL` | **`C19`** | 〃 |

- ⭐ **`from` 에 `SCRAPPED` 를 «넣지 않는다»** — 여기가 「거부하는 쪽」을 남기는 자리다(2단계 기준 2). 이미 폐기된 LOT 을 다시 처분하면 코어가 0건 옮기고 **400 `STATE_LOCKED`** 다.
- ⭐ **`from` 을 세 값으로 «넓게» 두는 근거** — 대상 LOT 의 출발 상태가 원천 둘로 갈린다: `PRODUCT`(OQC 불합격)는 `DEFECTIVE`(C6), `RETURN`(반품 입고)은 원 LOT 을 그대로 쓰므로(`W-04-07` §5-4) `NORMAL` 이거나 `INSPECTION_PENDING` 이다. **좁히면 RETURN 갈래 본길이 통째로 400 으로 죽는다.**
- ⚠ **`DEFECTIVE` 를 출발로 두는 것이 「불량(Hold)은 발신 전이가 0」(계약 `:4472` · `W-03-02:186`)과 부딪히는가 — 부딪히지 않는다.** ⓐ 그 문장은 **`W-03-02` 화면의 3전이(C7·C8·C9) 범위**에서 쓰였고, `W-03-10`(처분 판정 처리)은 **2026-08-13 에 신설**된 뒤 나온 화면이라 그 도식보다 새것이다. ⓑ **저장소가 이미 `DEFECTIVE` 발신 전이를 하나 갖고 있다** — `stock-reinstate`(`transitions.ts:218-219` · `DEFECTIVE → NORMAL`). ⓒ **I-20 §5 의 「무변경」 판정과도 모순되지 않는다** — 저 판정은 「`:release` 의 C7·C8 이 `DEFECTIVE` 를 안 받는다」였고, 이 슬라이스는 **기존 행을 손대지 않고 새 행만 더한다.**
- **개수 단언** — `document-state.spec.ts:423` `toHaveLength(41)` → **`46`**(+5). ⭐ **B·C 와 매번 충돌하는 자리**(`lanes.md` §1-4) ⇒ **PR ④ 를 단독으로 뜨고 먼저 병합**한다.
- `LotQualityStatusService.moveWithin(tx, lotIds, action, ctx)` 를 그대로 쓴다 — `LotQualityMoveContext` 에 `sourceDocumentTypeCode`·`sourceDocumentId`·`reasonCode`·`reason`·`transitionCode` 칸이 **이미 있다**(I-20 부록 #54). **코어를 새로 만들지 않는다.**

---

## 7. 횡단 · 모듈 배치

### 7-1. 파일 — 줄 추정(⭐ I-19 §12-2 #10 · I-24 §12-2 교훈으로 «후하게»)

| 파일 | 신/수 | 비테스트 |
|---|:-:|--:|
| `src/quality/nonconformance/nonconformance.controller.ts` | 신 | ~90 |
| `src/quality/nonconformance/nonconformance-query.service.ts` | 신 | ~150 |
| `src/quality/nonconformance/nonconformance-view.ts` | 신 | ~90 |
| `src/quality/nonconformance/nonconformance-source.ts`(파생 축 · 사용처 2) | 신 | ~40 |
| `src/quality/nonconformance/nonconformance-rules.ts` | 신 | ~90 |
| `src/quality/nonconformance/nonconformance-write.service.ts` | 신 | **~200** |
| `src/quality/nonconformance/disposition-candidate-query.ts`(원시 SQL) | 신 | **~200** |
| `src/quality/disposition/disposition.controller.ts` | 신 | ~70 |
| `src/quality/disposition/disposition-query.service.ts` | 신 | **~180** |
| `src/quality/disposition/disposition-view.ts` | 신 | ~80 |
| `src/quality/disposition/disposition-rollup.ts`(잔량·진행·후속 롤업 · 사용처 3) | 신 | ~90 |
| `src/quality/disposition/disposition-write.service.ts` | 신 | **~200** |
| `src/quality/concession/concession.controller.ts` | 신 | ~50 |
| `src/quality/concession/concession-query.service.ts` | 신 | ~120 |
| `src/quality/concession/concession-view.ts` | 신 | ~70 |
| `src/core/document-state/transitions.ts` | 수 | +20 |
| `src/core/document-state/document-state.spec.ts` | 수 | +6 |
| `src/core/numbering/numbering.service.ts` | 수 | +2 |
| `src/common/errors/conflict.exception.ts` | 수 | +8 |
| `src/quality/quality.module.ts` | 수 | +26 |
| `prisma/schema.prisma` · 마이그 · `seed.ts` | 수/신 | ~55 |
| **합계(비테스트)** | | **≈1,837** |

⇒ 400×3 으로 **못 들어간다.** PR 은 **8** 이다(§10).

### 7-2. 모듈 배선 — `quality.module.ts` 만

컨트롤러 **3**(`NonconformanceController`·`DispositionController`·`ConcessionController`) · provider **8**.
imports 는 **이미 다 있다** — `PrismaModule`·`AuthModule`·`IdempotencyModule`·`NumberingModule`·`DocumentStateModule`·`LotRegistryModule`(`quality.module.ts` 실측).
⚠ **라우트 순서 함정이 하나 있다** — `GET /quality/disposition-decisions`(리터럴)와 `GET /quality/disposition-decisions/{id}`(파라미터)가 같은 컨트롤러에 서고, `GET /quality/disposition-candidates` 는 **다른 컨트롤러**(부적합 쪽)다. Nest 는 컨트롤러 등록 순서로 매칭하므로 **`disposition-candidates` 를 `disposition-decisions/:id` 보다 «먼저» 등록**하거나 서로 다른 접두 세그먼트라 안전한지 확인한다(`candidates` ≠ `decisions` 라 실제로는 안전하다 — 그래도 e2e 가 잠근다).
⚠ `lanes.md` §1-4 — `quality.module.ts` 는 **레인 A 담당**이다. `src/trace/trace.module.ts`·`src/app/app-domain.module.ts` 는 **안 건드린다.**

### 7-3. 다른 도메인 service 호출 **0** · 다른 도메인 import **0**

- `trace.lot`·`trace.lot_status_event` 는 **코어가 쓴다**(`LotQualityStatusService` · `server-architecture.md:67`·`:145-147`). 도메인이 직접 UPDATE 하지 않는다.
- `inventory.inventory_balance`·`logistics.goods_receipt(_line)`·`logistics.goods_issue`·`quality.inspection_result` 는 **읽기만** 한다(원시 SQL · service 호출 0).
- `production.work_order` 는 **안 읽는다** — 판정 #3 이 REWORK 롤업을 0 으로 접었기 때문이다.
- ⛔ **원장 0 · posting 0**(§5).

### 7-4. 멱등 3 · If-Match 필수 2 · ETag 2 · 403 등록 **0줄** · 새 error code 0 · 코어 신설 0

- **멱등** — 쓰기 3건 전부 `runIdempotent`(조회엔 안 붙인다 · `plan.md` §5-2).
- **If-Match** — `:request-disposition`·판정 저장 **둘 다 필수**. 토큰은 **`nonconformance.version_no` 하나**(판정 #5). 등록에는 없다.
- **ETag** — `GET …/{nonconformanceId}` 200 · 판정 저장 201. ⚠ `:request-disposition` 200 은 계약 미선언인데 `runVersioned` 가 낸다(§1-1).
- **403** — `derived-permissions.ts` 에 쓰기 3건이 이미 있다(`:254`·`:255`·`:256`) ⇒ **`manual-permissions.ts` 0줄**.
- **`@Contract`** — 11건 전건.
- **코어 신설 0** — `LotQualityStatusService`·`NumberingService`·`DocumentStateService` 를 그대로 쓴다. 코어 «수정»은 3파일 28줄(§6·§3-1)뿐이라 **PR ④ 하나**로 묶는다(≤60 · `CLAUDE.md` 코어 ≤200 안).

---

## 8. e2e — 파일 셋 + 단위 spec 넷

> ⭐ 각 이름 옆의 **「죽이는 변이」**가 README §6-2 의 전수 변이 점검을 계획 단계에서 미리 친 그물이다.
> 구현자는 **PR 을 열기 «전에»** 자기가 «새로 만든» 필터·비교·정렬·`count(where)`·분기에도 같은 점검을 돌리고 표를 PR 본문에 싣는다.

### 8-1. 픽스처 — I-20 의 것을 복제하되 **처분 사슬을 처음부터 넣는다**

| 이름 | 내용 | 왜 |
|---|---|---|
| L1 | LOT · `status_code='DEFECTIVE'` · OQC 확정 불합격 검사 결과 있음 | PRODUCT 갈래 본길 |
| L2 | LOT · `status_code='NORMAL'` · 반품 입고(`receipt_type_code='RETURN'`)로 들어옴 · 원 출하 배분 있음 | RETURN 갈래 본길 + `partnerName` 역추적 |
| L3 | LOT · `status_code='INSPECTION_PENDING'` · 반품 입고 | `from` 세 값 중 셋째 |
| L4 | LOT · **잔액 창고가 둘** | ⭐ **판정 #4 「안 걸리는 행」** — 후보 목록에서 빠져야 한다 |
| L5 | LOT · 잔액 행이 있으나 **`on_hand_qty = 0`** | ⭐ **I-20 R-22 「0 행」** — `quantity` 합에도 창고 접기에도 안 들어간다 |
| L6 | LOT · `status_code='SCRAPPED'` | ⭐ **`from` 밖** — 판정 저장이 400 `STATE_LOCKED` |
| L7 | LOT · **두 원천에 다 걸린다**(반품 + OQC 불합격) | ⭐ 후보 목록에 **한 행**으로 · `sourceCode='RETURN'` |
| N1 | 부적합 · `NOT_REQUESTED` · lots = {L1 320 EA} | 의뢰 본길 |
| N2 | 부적합 · `PENDING_DECISION` · lots = {L2 200 EA} · 결정 0건 | 판정 본길 · `dispositionProgressCode='NOT_STARTED'` |
| N3 | 부적합 · `PENDING_DECISION` · lots = {L3 100 EA} · 결정 `REWORK 60` | `PARTIAL` · `remainingQty=40` |
| N4 | 부적합 · `DECIDED` · `closed_at` 있음 · 결정 합 = 대상 합 | `COMPLETED` · 409 `INVALID_STATE` |
| N5 | 부적합 · lots **2건**(L1 · L3) · **같은 uom** | 여러 LOT 전이 · `DispositionDecision.lotId` **키 생략** |
| N6 | 부적합 · lots = {L2} · 열려 있다(`closed_at IS NULL`) | ⭐ 409 `DUPLICATE_KEY` 픽스처 |
| D1 | 처분 결정 `SCRAP 50` + 그것을 원천으로 하는 `goods_issue`(`DISPOSITION_DECISION`) 30 EA | ⭐ **`followUpQty=30`·`PARTIAL`** — 판정 #3 의 유일한 «살아 있는» 롤업 |
| D2 | 처분 결정 `REWORK 60` · 재작업 W/O 완료 | ⭐ **`followUpQty=0`·`NOT_STARTED`·`reinstatable=false`** — 반증 행 |
| D3 | 처분 결정 `NORMAL 40` | ⭐ **`reinstatable=true`** |
| C1 | 특채 · `APPROVED` · `valid_to` = 오늘 · 잔여 > 0 | ⭐ **「한계와 같은 값」** — `usable=true` |
| C2 | 특채 · `APPROVED` · `valid_to` = 어제 | `usable=false` |
| C3 | 특채 · `APPROVED` · `approved_qty = consumed_qty` | ⭐ 잔여 0 — `usable=false` |
| C4 | 특채 · `REJECTED` · 기간·잔여 정상 | ⭐ **상태 축만으로 걸러지는 행** |
| C5 | 특채 · 허용 3축 **전부 NULL** | `unrestrictedAxes` 3개 |
| **Q1** ⭐ | 부적합 · lots = {L3 **120.000000** EA} | ⭐ **스케일 경계** — `decisionQty = 119.9999995`(스케일 7자리)를 받는다. `numeric(20,6)` 반올림이 **120.000000** 이 되어 잔량을 «정확히» 채우는지, 아니면 400 인지를 단언(#394 리뷰 사고) |

**정리** — 자가 치유 `deleteMany`(역순): `goods_issue_line` → `goods_issue` → `disposition_decision` → `concession` → `nonconformance_lot` → `nonconformance` → `lot_status_event` → `inventory_balance` → `goods_receipt_line` → `goods_receipt` → `inspection_result` → `inspection_request` → `lot`.
⚠ **I-20 R-20 의 교훈** — 픽스처가 만든 하류(`goods_issue`)를 정리 순서에 안 넣으면 `lot` 삭제가 FK 로 막힌다.

### 8-2. `test/quality-nonconformance.e2e-spec.ts` — **34**

| # | 이름 | 죽이는 변이 |
|:-:|---|---|
| 1 | 목록 — 기간 둘 다 없으면 **400 `REQUIRED`** | 갈래 A 를 갈래 C(통과)로 베끼면 깨진다 |
| 2 | 목록 — `openedFrom` 만 오면 400 `REQUIRED` | 한쪽만 검사 |
| 3 | 목록 — **끝 경계와 같은 시각의 행은 «안» 나온다**(반열림) | `<` → `<=` |
| 4 | 목록 — 시작 경계와 같은 시각의 행은 나온다 | `>=` → `>` |
| 5 | 목록 — `statusCode='PENDING_DECISION'` 이 N1(NOT_REQUESTED)을 **뺀다** | 필터 삭제 |
| 6 | 목록 — `severityCode` 가 시드 3값 밖이면 400 `INVALID` | `assertCodeValues` 삭제 |
| 7 | 목록 — `lotId=L2` 가 N2 를 «집고» N1 을 뺀다 | `EXISTS` → nullable 등치(I-20 R-23) |
| 8 | 목록 — `warehouseId` 가 L4(창고 둘) 부적합도 **집는다** | 후보 목록의 「창고 둘은 뺀다」를 여기에 잘못 베끼면 깨진다 |
| 9 | 목록 — ⭐ `sourceCode='RETURN'` 이 N2(반품 LOT)를 집고 N1(OQC LOT)을 **뺀다** | 파생 축 삭제 · 반대로 매핑 |
| 10 | 목록 — `itemId` 등치 | 필터 삭제 |
| 11 | 목록 — 기본 정렬 `opened_at DESC` · **같은 시각 두 건은 id DESC** | 2차 키 삭제(I-20 R-10) |
| 12 | 목록 — `page=2&size=1` 이 겹치지 않는다 | 페이지 계산 |
| 13 | 상세 — 200 + **ETag = `nonconformance.version_no`** | ETag 를 `disposition_decision` 쪽에서 내면 깨진다(판정 #5) |
| 14 | 상세 — `lots[]` 에 `qualityStatusBeforeCode` = `AfterCode` = 등록 시점 상태 | 등록이 LOT 을 옮기면 깨진다 |
| 15 | 상세 — `affectedQtyTotal` 이 lots 합이다(N5 = 2행) | 첫 행만 읽기 |
| 16 | 상세 — `dispositionProgressCode` **3값 전건**(N2 NOT_STARTED · N3 PARTIAL · N4 COMPLETED) | 롤업을 `statusCode` 로 접기 |
| 17 | 상세 — 없는 id → **404** | — |
| 18 | 등록 — 201 · `nonconformanceNo` 가 **`NC-` 로 시작**하고 `status_code='NOT_REQUESTED'` | 접두어·초기 상태 |
| 19 | 등록 — `lots: []` → 400 `RANGE` | 빈 배열 검사 |
| 20 | 등록 — 같은 `lotId` 2행 → 400 `INVALID`(⛔ **500 이 아니다**) | `uq_nonconformance_lot` 이 새는 것 |
| 21 | 등록 — ⭐ `lots` 의 `uomId` 가 서로 다르면 400 `INVALID` | 판정 #2 ⓐ 삭제 |
| 22 | 등록 — `affectedQty = 0` → 400 `RANGE` | `>` → `>=` |
| 23 | 등록 — `description` 이 공백만 → 400 `REQUIRED` | trim 삭제 |
| 24 | 등록 — 없는 `itemId` → **400 `INVALID`**(⛔ 404 아니다) | 404 를 선언 안 한 자리 |
| 25 | 등록 — ⭐ **L2 에 열린 부적합(N6)이 있으면 409 `DUPLICATE_KEY`** | 중복 판정 삭제 |
| 26 | 등록 — **종결된(`DECIDED`) 부적합만 있는 LOT 은 다시 등록된다** | 위 판정에서 `closed_at IS NULL` 을 빼면 깨진다 |
| 27 | 등록 — 같은 `Idempotency-Key` 재전송이 **행을 늘리지 않는다** | 멱등 |
| 28 | 등록 — ⛔ **LOT 상태가 안 움직이고 `lot_status_event` 가 0행** | 등록에 전이를 붙이면 깨진다 |
| 29 | 등록 — 권한 없는 계정 → 403 | 게이트 |
| 30 | 의뢰 — 200 · `NOT_REQUESTED` → `PENDING_DECISION` · `version_no` +1 | 전이·판 올림 |
| 31 | 의뢰 — 이미 `PENDING_DECISION` → **409 `INVALID_STATE`** | 400 으로 내리면 깨진다(§3-4 7) |
| 32 | 의뢰 — If-Match 불일치 → 409 `VERSION_CONFLICT` | 토큰 |
| 33 | 의뢰 — `requestedQty = 0` → 400 `RANGE` · **`requestedQty` 는 어디에도 저장되지 않는다** | 담을 데 0 을 확인 |
| 34 | 의뢰 — 없는 id → 404 | 404 선언 자리 |

### 8-3. `test/quality-disposition.e2e-spec.ts` — **38**

| # | 이름 | 죽이는 변이 |
|:-:|---|---|
| 1 | 후보 — 원천 둘이 **한 목록**으로 나온다(L1 PRODUCT · L2 RETURN) | UNION 한쪽 삭제 |
| 2 | 후보 — ⭐ **L4(잔액 창고 둘)는 «안» 나온다** | `count(DISTINCT)=1` 삭제 |
| 3 | 후보 — ⭐ **L5(잔액 0행)의 창고가 `warehouseId` 에 «안» 섞인다** · `quantity` 에도 안 더해진다 | I-20 R-22 |
| 4 | 후보 — ⭐ **L7(두 원천)이 «한 행»이고 `sourceCode='RETURN'`** | 접기 삭제 |
| 5 | 후보 — `sourceCode='PRODUCT'` 가 L2 를 뺀다 | 필터 |
| 6 | 후보 — `withoutNonconformanceOnly=true` 가 **부적합 있는 L2 를 빼고** 없는 L1 을 남긴다 | `NOT EXISTS` → `IS NULL` 등치 |
| 7 | 후보 — ⭐ `withoutNonconformanceOnly=true` 인데 **종결된 부적합만 있는 LOT 은 «남는다»** | `closed_at IS NULL` 조건 |
| 8 | 후보 — ⭐ **`receivedTo` 와 «같은 날»의 행이 나온다**(양끝 포함) | 갈래 C 를 반열림으로 베끼면 깨진다 |
| 9 | 후보 — `receivedFrom` 하루 뒤면 안 나온다 | 경계 |
| 10 | 후보 — 기간을 아예 안 줘도 200 이고 오래된 건이 나온다 | 갈래 C 를 갈래 A 로 베끼면 깨진다 |
| 11 | 후보 — `q` 가 LOT 번호·입고번호·품목코드·품목명 **네 칸**을 본다 | 칸 하나씩 삭제 |
| 12 | 후보 — PRODUCT 갈래는 `goodsReceiptId`·`receiptNo`·`receivedAt`·`partnerName` **키가 없다**(널 아님) | 널로 내리기 |
| 13 | 후보 — RETURN 갈래의 `partnerName` 이 **원 출하 거래처**다 | 역추적 삭제 |
| 14 | 후보 — 이미 부적합이 있는 대상은 `nonconformanceStatusCode` 를 함께 싣는다 | 조인 |
| 15 | 후보 — 정렬 `received_at DESC **NULLS LAST**` · 2차 키 `lot_id DESC` | NULL 자리(I-20 R-10) |
| 16 | 이 부적합의 결정 — ⭐ **질의 칸이 0인데 `page` 가 `{1, total, total}`** 로 전건이다 | `size=50` 고정으로 51번째가 사라짐 |
| 17 | 이 부적합의 결정 — `summary.remainingQty` = 대상 − 결정(N3 = 40) | 서버 계산 삭제 |
| 18 | 이 부적합의 결정 — `summary.uomId` 가 부적합 단위다 | 단위 축 |
| 19 | 이 부적합의 결정 — ⛔ **응답에 ETag 헤더가 없다** | 계약 명시 |
| 20 | 결정 목록 — `dispositionTypeCode='SCRAP'` 이 D2·D3 을 뺀다 | 필터 |
| 21 | 결정 목록 — `lotId` 가 `nonconformance_lot` 을 거쳐 걸린다 | `EXISTS` |
| 22 | 결정 목록 — ⭐ `reinstatable=true` 가 **D3(NORMAL)만** 집고 **D2(REWORK·재작업 완료)는 뺀다** | 판정 #3 — 「재작업 끝난 것」을 흉내 내면 깨진다 |
| 23 | 결정 목록 — ⭐ `followUpPending=true` 가 **D1(부분 30/50)을 집고** 완료분을 뺀다 | 롤업 |
| 24 | 결정 목록 — ⭐ **D1 의 `followUpQty=30`·`followUpStatusCode='PARTIAL'`** | `goods_issue` 롤업 삭제 |
| 25 | 결정 목록 — ⭐ **D2·D3 의 `followUpQty=0`·`NOT_STARTED`**(원천 0) | 0 을 「모른다」로 접거나 키 생략하면 깨진다 |
| 26 | 결정 목록 — 기간 한쪽만 → 400 `PAIR` · 둘 다 없으면 **통과** | 갈래 B |
| 27 | 결정 목록 — 정렬 `decided_at DESC` + 2차 키 | 2차 키 |
| 28 | 결정 상세 — 200 · **ETag 없다** · 없는 id → 404 | — |
| 29 | 결정 상세 — N5(lots 2건)의 결정은 `lotId`·`lotNo` **키를 생략**한다 | 첫 LOT 을 조용히 고르면 깨진다 |
| 30 | 판정 저장 — 201 + **ETag = 부적합의 «새» `version_no`** | 판정 #5 · 판 올림 |
| 31 | 판정 저장 — **`REWORK` 가 LOT 을 `INSPECTION_PENDING` 으로** 옮기고 `lot_status_event.transition_code='C17'` | 전이·코드값 |
| 32 | 판정 저장 — **`SCRAP` 가 `SCRAPPED` · `C18`** | 〃 |
| 33 | 판정 저장 — **`NORMAL` 가 `NORMAL` · `C19`**(NORMAL→NORMAL 도 이력 1행) | 무변화라고 건너뛰면 깨진다 |
| 34 | 판정 저장 — ⭐ **동시 두 요청이 잔량 120 을 나눠 갖는다**(둘째가 409 `DISPOSITION_QTY_EXCEEDED`) | 잠금이 잔량 판정 밖이면 둘 다 통과 |
| 35 | 판정 저장 — `decisionQty` = 남은 수량 **정확히**면 201 이고 `status='DECIDED'`·`closed_at` 채워짐 | ⭐ 「한계와 같은 값」 |
| 36 | 판정 저장 — ⭐ **Q1: `decisionQty=119.9999995` 가 스케일 6으로 접혀 잔량 120 을 채운다**(또는 400) — 단언을 값으로 못 박는다 | 스케일 경계(#394) |
| 37 | 판정 저장 — 부분 처분 2회(`REWORK 60` → `SCRAP 40`) 뒤 **LOT 은 `SCRAPPED`**(마지막이 남는다) | 판정 #2 ⓒ · 「첫 판정만 옮긴다」로 바꾸면 깨진다 |
| 38 | 판정 저장 — **L6(`SCRAPPED`)이 대상이면 400 `STATE_LOCKED`** · `DECIDED` 부적합이면 409 `INVALID_STATE` | `from` 에 `SCRAPPED` 를 넣으면 깨진다 |

### 8-4. `test/quality-concession.e2e-spec.ts` — **13**

| # | 이름 | 죽이는 변이 |
|:-:|---|---|
| 1 | 목록 — 200 · 기본은 전건(C1~C5) | — |
| 2 | ⭐ `usableOnly=true` 가 **C1 만** 남긴다 | 3항 중 하나만 검사 |
| 3 | ⭐ C1 은 `valid_to` 가 **오늘과 같은 날**인데 `usable=true` | `>=` → `>` |
| 4 | C2(어제 만료) 는 `usable=false` | 경계 |
| 5 | ⭐ C3(잔여 0) 는 `usable=false` | `> 0` → `>= 0` |
| 6 | ⭐ C4(`REJECTED`) 는 **상태 축만으로** 빠진다 | 상태 축 삭제 |
| 7 | `usableOnly=false` 는 **필터를 안 건다**(전건) | `false` 를 「쓸 수 없는 것만」으로 읽으면 깨진다 |
| 8 | `validOn=<C2 의 유효기간 안 날짜>` 면 C2 가 `usable=true` | 기준일 축 |
| 9 | `approvalRequestId`·`lotId`·`nonconformanceId`·`statusCode` 네 필터 각각이 **안 걸리는 행**을 뺀다 | 필터 삭제 |
| 10 | ⭐ C5 의 `unrestrictedAxes` 가 **3개**이고 값이 `allowedWorkOrderId`·`allowedProcessId`·`allowedCustomerId` | 이름 규약 |
| 11 | 허용 축이 하나만 찬 특채는 `unrestrictedAxes` 가 2개 | 반증 행 |
| 12 | 상세 — 200 · 없는 id → 404 | — |
| 13 | 정렬 `valid_from DESC` + 2차 키 `concession_id DESC` | 2차 키 |

### 8-5. 단위 spec — **4파일 · 30**

| 파일 | 무엇 | 수 |
|---|---|--:|
| `nonconformance-rules.spec.ts` | `lots` 검증 5(빈·중복·단위 불일치·수량 0·설명 공백) · 중복 부적합 판정 3 · `sourceCode` 파생 4 | 12 |
| `disposition-rollup.spec.ts` | 잔량 산식 4(0·부분·정확·초과) · `dispositionProgressCode` 3 · `followUp*` 유형별 3 · `reinstatable` 2 | 12 |
| `concession-view.spec.ts` | `usable` 3항 진리표 4 · `unrestrictedAxes` 2 | 6 |
| `document-state.spec.ts`(수정) | 전이 5 등록 확인 + **`toHaveLength(46)`** | (기존 파일) |

### 8-6. M3 체인 마디 — 이 슬라이스가 **셋째 마디를 잇는다**

`plan-integration.md:535` M3 = 실적 → 검사 의뢰 → 확정 → 불합격 → 보류 → **부적합 → 처분(SCRAP)** → 폐기 출고(I-4 재사용) 원장 감소.
끝점 셋만 단언 — `POST /quality/nonconformances` 201 · 판정 저장 201 · `lot.status_code='SCRAPPED'`.
⚠ **「폐기 출고」 마디는 I-4 의 `POST /logistics/goods-issues` 를 `source_document_type_code='DISPOSITION_DECISION'` 으로 부른다** — 통합자가 M3 체인에서 확인한다(§11 ⑤).

---

## 9. 설계 미정 자리 — README §2 절차로 전건 판정

### 9-1. 판정 요약표

| # | 자리 | 단계 | 판정 | 흔적 |
|:-:|---|:-:|---|---|
| 1 ⭐⭐ | 처분 3전이의 `transitionCode` | 0→1→**1-1(통보)** | **`C17`·`C18`·`C19` 신설** + 시드·마이그 3값(§6-2) | `// 결정 — 통보 089` |
| 2 ⭐ | `concession.status_code` 의 「유효」 값(⇐ **회신 대기 12**) | 0→1→**1-1(통보)** | **`'APPROVED'`** — `app.approval_request.status_code` 가 이미 쓰는 값을 그대로 쓴다(2단계 기준 5 — 새 개념 0). ⛔ 시드 그룹을 세우지 않는다(writer 0) | 〃 |
| 3 ⭐ | `usableOnly` 의 기준일 | 1-1(통보) | `validOn` ‖ **서버 UTC 달력일**. ⛔ 타임존 캐스팅 금지 · `concession` 에 공장 축 0 | 〃 |
| 4 ⭐ | `unrestrictedAxes` 의 이름 문자열 | 1-1(통보) | **계약 프로퍼티 이름 3값** | 〃 |
| 5 ⭐⭐ | 처분 저장이 옮기는 LOT 집합 · 부분 처분의 도착 충돌 | 0→1(계약 문자)→**1-1(통보)** | `nonconformance_lot` **전건** · **매 판정마다** · 마지막 도착이 남는다(§0 #2) | 〃 |
| 6 ⭐ | `lots[]` 단위 혼합 | 2단계 기준 2 | **400 `INVALID`**(거부하는 쪽) — `W-03-10` §8 #7 미결을 우리가 닫는다 | 〃 |
| 7 ⭐ | `followUpStatusCode`·`followUpQty` 의 원천 결손 | 0→1-1(통보) | 폐기만 센다 · 나머지 `0`/`NOT_STARTED`(§0 #3) | 〃 |
| 8 ⭐ | 후보 목록의 창고가 둘일 때 | 2단계 기준 4 | **목록에서 뺀다**(조용히 고르지 않는다) | 〃 |
| 9 | 특채 승인 시 LOT 재고 상태(⇐ **회신 대기 12 의 원 물음**) | **0단계에서 닫힌다** | 설계가 이미 답했다 — 「최종 상태는 셋 다 **정상**」(`W-03-09:31`·`:164-171`). ⛔ **그 전이를 «일으키는» 오퍼레이션이 계약에 0건**이라 I-21 범위 밖 | 통보 089 §2 |
| 10 | 처분이 결재를 타는가 | 2단계 기준 ② | **걸지 않는다**(`plan-uiux.md:609` 그대로 · `W-04-07` §8 #3 이 아직 열려 있다) | 알려둘 것 |
| 11 | `NC-` 채번 접두어·기간 축 | 0단계(문의 14 권고안) | `NC-{YYYYMMDD}-{SEQ4}` · `plantId=null` · **UTC 달력일** | 〃 |
| 12 | 등록 중복 판정의 축(`closed_at IS NULL`) | 2단계 기준 2 | 409 `DUPLICATE_KEY` | 〃 |
| 13 | `:request-disposition` 본문 3칸이 갈 데가 없다 | 1단계(계약 문자) | 형식만 검증하고 버린다 | 〃 |
| 14 | 자식 컬렉션의 `page` 봉투 | 2단계 기준 4 | 전건 + `size=total` | 〃 |

### 9-2. 통보 — **1건**(번호 **089** · 레인 A 대역의 마지막)

⚠ **089 는 원래 「레인 C 재등록 전이 코드」 예약분**이었다(`lanes.md` §1-1 · `I-20.md` §11 ① · **통보 154** · `transitions.ts:217-218` 주석 「설계 미정 — 문의 089(발행 예정)」).
⇒ **089 를 «그 구멍 전체»를 담는 통보로 쓴다** — 제목이 「LOT 상태 전이 코드가 없는 자리 넷(처분 3 + 재등록 1) + 계약이 값을 안 준 코드 자리」가 되어 **기존 참조가 전부 그대로 유효**하다.
⛔ 레인 C 는 **089 를 다시 만들지 않는다** — 재등록 값 자체는 여전히 **C 의 판정**이고, 089 §7 이 그 자리를 비워 둔다.

089 가 담는 것: §1 처분 3전이 코드(`C17`·`C18`·`C19`) · §2 특채 상태 축과 기준일(⇐ 회신 12) · §3 `unrestrictedAxes` 이름 · §4 후속 롤업 원천 결손 · §5 `DispositionRequest` 3칸이 갈 데가 없다 · §6 부분 처분이 LOT 상태를 갈라 담을 수 없다 · §7 재등록 자리(레인 C).

### 9-3. 「알려둘 것」 — **9건**(번호 없음 · 다음 전달분 말미)

| | 내용 |
|:-:|---|
| ⓐ | ⛔ **`plan-api.md:871` 과 `I-19.md` §11 ③ 이 계약을 잘못 읽었다** — 「계약이 `SCRAPPED` 전이를 안 적었다」는 사실이 아니다(`quality-03품질.json:2460`). `plan-api.md` §5.1-E 의 「도착 상태를 직접 적은 자리 넷」 열거에 **판정 저장이 빠졌다.** I-20 R-24 와 **같은 유형의 세 번째 사고**다 |
| ⓑ | `:request-disposition` 200 에 계약이 ETag 를 선언하지 않았는데 `runVersioned` 가 낸다(선언 밖 헤더 · 위반은 아니다) |
| ⓒ | `GET /quality/concessions/{concessionId}` 가 `derived-permissions.ts` 에 없다 — 403 미선언이라 오늘은 무해하지만 계약이 403 을 더하는 순간 500 이 된다 |
| ⓓ | ⭐ **`quality.concession` 에 writer 가 0개다** — 계약이 생성·승인·수정 경로를 전부 두지 않아 운영에서 이 목록은 빈 채로 뜬다(`W-03-09` §8 #3·#4 가 「요청을 만드는 화면 미확인」으로 열려 있다) |
| ⓔ | `concession.consumed_qty` 를 올리는 주체가 미특정(`W-03-09` §8 #5) — 출고·투입 슬라이스가 그것을 안 올리면 `usable` 이 영원히 참이다 |
| ⓕ | `quality.sorting_result`(선별)는 `disposition_decision_id NOT NULL` 로 물리에 실재하지만 **1차 범위 밖**(`omf-mes#118` 종결) — 우리가 그 표에 한 줄도 안 쓴다 |
| ⓖ | 부적합 목록의 `ix_nonconformance_open` 이 **부분 인덱스**(`WHERE closed_at IS NULL`)라 종결분 조회는 못 탄다 |
| ⓗ | `src/common/contract/contract-coverage.spec.ts:30` 의 제목이 **`(n/490)`** 인데 실제 분모는 **487** 이다(문구 불일치) |
| ⓘ | `W-03-10` §8 #8(「판정 저장이 `nonconformance.version_no` 를 올리는가」)은 계약 x-internal-note 가 이미 「올려야 한다」로 답했다 — 화면 §8 이 낡았다 |

---

## 10. PR 분할 — **9**(⚠ 통합 계획서 셋이 전부 **3** 이다)

> ⭐ 처음 그은 선(8)에서 하나가 늘었다 — `DispositionController` 를 한 PR 로 묶으면 **컨트롤러 70 + query 180 + view 80 + rollup 90 + 모듈 8 = 428** 로 한도 400 을 넘는다(I-20 R-17 이 같은 산술로 죽었다). ⇒ **②를 ②a·②b 로 가른다.**

| PR | 오퍼레이션 | 파일 합산(§7-1) | 예산 | 모델 | base |
|:-:|---|--:|--:|---|---|
| **M-h** | 마이그 단독(§2-5 항목 2 + `schema.prisma` + `seed.ts`) | 55 | ≤80 | **opus** | `main` · 병합 전 **한 줄 보고** |
| **①** | `GET /quality/nonconformances` · `…/{id}` + `NonconformanceController` 골격 | 컨트롤러 60 + query 150 + view 90 + source 40 + 모듈 10 = **350** | ≤350 | sonnet | `main` |
| **②a** | `GET /quality/disposition-decisions` · `…/{dispositionDecisionId}` + `DispositionController` 골격 + 롤업 | 컨트롤러 50 + query 120 + view 80 + rollup 90 + 모듈 8 = **348** | ≤350 | sonnet | ① 위 |
| **②b** | `GET …/{ncId}/disposition-decisions`(+`summary`) | query +60 + 컨트롤러 +20 = **80** | ≤120 | sonnet | ②a 위 |
| **③** | `GET /quality/disposition-candidates`(원시 SQL) | candidate-query 200 + 컨트롤러 +25 + 모듈 4 = **229** | ≤280 | sonnet | ②b 위 |
| **④** ⭐ | **코어** — `transitions.ts` 전이 5 · `document-state.spec.ts` 개수 41→46 · `numbering` `NC` 한 줄 | 20+6+2 = **28** | ≤60 | **opus** | `main` · ⛔ 착수 **전에** 한 줄 보고(A 소유 파일 · B·C 가 읽는다) |
| **⑤** | `GET /quality/concessions` · `…/{concessionId}` + `ConcessionController` | 컨트롤러 50 + query 120 + view 70 + 모듈 8 = **248** | ≤300 | sonnet | ③ 위 |
| **⑥** ⭐ | **심장 A** — `POST /quality/nonconformances` · `:request-disposition` | write 200 + rules 90 + 컨트롤러 +35 = **325** | ≤350 | **opus** | ⑤ + ④ 위 |
| **⑦** ⭐ | **심장 B** — `POST …/disposition-decisions`(잠금·잔량·전이·종결) + `conflict.exception.ts` +8(**단독 커밋**) | write 200 + 컨트롤러 +25 + view +15 + conflict 8 = **248** | ≤300 | **opus** | ⑥ + M-h 위 |

**합계 비테스트 ≈ 1,911**(M-h 55 + ① 350 + ②a 348 + ②b 80 + ③ 229 + ④ 28 + ⑤ 248 + ⑥ 325 + ⑦ 248) — §7-1 합산(1,837)에 컨트롤러 증분 중복 계상분이 얹힌 값이다. **전건 350 이하 · 코어 PR 은 60 이하**.

- ⭐ **왜 9 인가 (통합 계획서는 3)** — ⓐ **마이그가 있다**(통합 계획서 §4 에 I-21 행이 없다 · `plan-integration.md:185` 만 「있음」이라 적었다) ⓑ **코어 PR 이 있다**(`plan.md` §1 17행의 「코어 —」가 낡았다 — `transitions.ts` 는 A 소유 공용 파일이라 단독으로 뜨고 먼저 병합해야 B·C 와의 충돌 창이 짧다) ⓒ **비테스트 총량이 ≈1,900** 이라 400×3 으로 안 들어간다 ⓓ 심장이 **둘**(부적합 쓰기 · 처분 쓰기)이고 트랜잭션 모양이 완전히 다르다.
- ⭐ **①·②a·②b·③·⑤ 는 `quality.module.ts` 를 공유하므로 «직렬»** 이다 — 뒤 PR 이 `merge origin/main` 후 게이트를 다시 탄다(`lanes.md` §2). ⛔ 「파일 0겹침이라 병렬」이라고 적지 않는다(I-20 R-17 이 그 오기로 죽었다).
- ⭐ **M-h 와 ④ 만 `main` 위에서 병렬**이다(서로 파일 0겹침 · 도메인 파일 0겹침).
- ⭐ **① 은 3관점 재수립과 «나란히» 스폰할 수 있다**(README §6-1 ③ — 마이그 0 · 코어 0 · 순수 조회). ⛔ **M-h·④·⑥·⑦ 은 R-n 확정 뒤에만.** ⚠ **②a 는 나란히 스폰하지 않는다** — 판정 #3(후속 롤업)이 그 PR 의 질의를 정면으로 겨눈다.
- 게이트: 구현자가 PR 본문에 실측(명령·숫자)과 **전수 변이 점검 표**(README §6-2)를 적는다. 리뷰어는 **단위 전체 + 그 PR 이 바꾼 e2e 파일만** 돈다. 회귀(`document-state`·`quality-lot-status`·`quality-inspection-result`·`inventory-balance`)는 통합자가 병합 직전 한 번.

### 10-1. 통합 계획서 대조표

| # | 통합 계획서 | 이 계획안 | 판정 |
|:-:|---|---|:-:|
| 1 | `plan.md` §1 17행 · §4 — **마이그 0** | **2항목 1파일**(code_value 3 + 인덱스 1) | **다르다** ⭐ |
| 2 | `plan.md` §1 17행 — **코어 —** | **코어 PR ④**(`transitions.ts` 전이 5 · `numbering` 1줄) | **다르다** ⭐ |
| 3 | `plan.md` §1 11행 · `plan-uiux.md:1231` — 「`transitionCode` 는 지어내지 않고 **문의**」 | ⭐ **정하고 통보**(README §2 1-1 2026-09-08 규칙 변경 — 「문의」는 옛 규칙 문장이다) | **다르다** ⭐ |
| 4 | `plan-api.md:871` — 「계약이 `SCRAPPED` 전이를 적지 않았다」 | ⛔ **틀렸다** — `quality-03품질.json:2460` 이 적었다 | **뒤집는다** ⭐⭐ |
| 5 | `plan.md` §1 17행 — **PR 3** | **9** | **다르다** ⭐ |
| 6 | `plan-integration.md:389` — 「수량 합이 대상을 넘으면 계약이 말하지 않는다 → 2단계 기준 2 → **400**」 | ⛔ **409 `DISPOSITION_QTY_EXCEEDED`** — 계약 enum 이 그 값을 갖고 있고 `remainingQty` 를 함께 내리라 적었다 | **뒤집는다** ⭐ |
| 7 | `plan-integration.md:185` — 원장 ✕ · posting ✕ · 상태기계 ⭕ | 같다 | 같다 |
| 8 | `plan-uiux.md:609` — 처분 결재를 걸지 않는다 | 같다 | 같다 |
| 9 | `plan-uiux.md:1230` M — 승인 유형에 특채 없음 → 새 유형 짓지 않는다 | 같다(문의 019 에 흡수) | 같다 |
| 10 | `plan.md` §6 「목표 커버리지 481/487」 | I-21 은 **건너뜀 0건** — 11건 전건 구현 | 같다 |

---

## 11. 인계

| # | 받는 곳 | 내용 |
|:-:|---|---|
| ① ⭐⭐ | **레인 C · I-23 재등록** | **통보 089 가 재등록 자리를 §7 로 함께 담는다** — `transitions.ts:217-218` 의 「문의 089(발행 예정)」 주석과 **통보 154** 의 참조가 그대로 유효하다. ⛔ **C 는 089 를 다시 만들지 않는다.** 재등록 `transitionCode` 값 자체는 여전히 **C 의 판정**이고, 이 슬라이스가 `C17`·`C18`·`C19` 를 썼으므로 **다음 빈 번호는 `C20`** 이다. ⭐ 그리고 **`logistics.stock_reinstatement` 표가 물리에 없다**(전 마이그 grep 0건) — C 가 세워야 하고, 그 표가 서기 전까지 `DispositionDecision.followUpQty` 의 NORMAL 갈래는 0 이다(판정 #3) |
| ② | **레인 C · I-4 폐기 출고** | `POST /logistics/goods-issues` 를 **`source_document_type_code='DISPOSITION_DECISION'`** 으로 부르는 것이 `followUpQty` 의 **유일한 살아 있는 롤업 원천**이다(`W-04-10:230` 이 enum 3값을 못 박았다). 그 값이 안 실리면 이 슬라이스의 롤업이 통째로 0 이 된다 |
| ③ | **I-6 · 재작업 W/O** | `work_order.rework_source_nonconformance_id` 는 **부적합 축**이라 처분 «결정» 단위 롤업이 불가능하다. 결정 축 칸이 필요하면 그때 마이그를 세운다(§0 #3 · 통보 089 §4) |
| ④ | **I-18**(레인 A2 · `GET /trace/lot-status-events`) | ⭐ **이 슬라이스가 `transitionCode` 에 «계약 enum 밖» 값 3개를 처음 «실제로» 쓴다** — 통보 154 가 예고한 구멍이 I-23 보다 **먼저** 열린다. ⛔ A2 는 조회에 필터를 더하지 않는다(154 의 판정 그대로). I-19 뒤 4 → I-20 뒤 8 → **I-21 뒤 11**(C4·C5·C6·C7·C8·C9·C10·C14 + C17·C18·C19) |
| ⑤ | **M3 체인 e2e**(통합자) | 마디 「부적합 → 처분(SCRAP) → 폐기 출고」. 끝점 셋 — 등록 201 · 판정 저장 201 · `lot.status_code='SCRAPPED'`. ⚠ 폐기 출고는 **I-4 재사용**이라 이 슬라이스가 오퍼레이션을 만들지 않는다 |
| ⑥ | 후속 소형 PR·마이그 | ⓐ `nonconformance_lot(lot_id)` 인덱스 — `lotId` 단독 조회가 느려질 때 ⓑ `disposition_decision(decided_at DESC)` ⓒ `nonconformance(opened_at)` 전체 인덱스(종결분 조회) ⓓ `concession.status_code` 값 목록이 오면 시드 그룹 신설 + `assertCodeValues` 한 줄 |
| ⑦ | 설계팀(문서로) | **089 를 먼저 보낸다** — 레인 C(I-23)가 같은 답을 기다리고 있고, 이 슬라이스가 그 enum 구멍을 **먼저 연다** |

### 11-4. ⛔ 사용자에게 올리는 것 (`lanes.md` §3)

1. ⭐ **`src/core/document-state/transitions.ts` 를 건드린다**(키 신설 1 · 전이 5 · `document-state.spec.ts` 개수 41→46) — **PR ④ 착수 «전에»** 보고한다. B·C 가 같은 파일을 읽는다.
2. ⭐ **마이그레이션이 든 PR(M-h)을 병합하기 직전** 한 줄 보고(멈추지는 않는다).
3. ⭐⭐ **문의 번호 대역이 소진된다** — 이 슬라이스가 **089** 를 쓰면 레인 A 대역 069~089 가 **전부 소진**된다. 남은 슬라이스(I-29 통합 대시보드 · #337 부채)와 3관점 재수립이 낼 통보를 담을 번호가 **0개**다. ⇒ **레인 A 에 새 대역(예: 180~209)을 배정해 주십시오.**
4. ⭐ **`src/core/numbering/numbering.service.ts` 의 `DEFAULT_PREFIX` 에 한 줄**(`NONCONFORMANCE: 'NC'`)을 더한다 — 코어 파일이다(§3-1).
5. ⭐ **`src/common/errors/conflict.exception.ts` 에 두 칸**(`remainingQty`·`remainingQtyUomId`)을 더한다 — 공용 파일이라 **단독 커밋**으로 낸다.

---

## 12. 구현 뒤 남은 것 (리뷰 결과 · 통합자가 채운다)

### 12-1. 미완 목록

| # | 자리 | 내용 |
|:-:|---|---|
| | | |

### 12-2. 슬라이스 마감

| | |
|---|---|
| PR | |
| 오퍼레이션 | **? / 11** |
| 커버리지 | 399 → **?/487** |
| 마이그레이션 | |
| 코어 | |
| e2e | |
| 단위 | |
| 신규 통보 | **089**(목표) |
| 새 `ERROR_CODE` | **0**(목표) · `manual-permissions.ts` **0줄**(목표) · `transitions.ts` **전이 +5**(목표) |

**리뷰가 잡은 것** —

**⭐ 계획서가 틀렸던 자리** —

| # | 자리 | 계획 | 실제 |
|:-:|---|---|---|
| | | | |

---

## ⭐ 실측 부록 (2026-09-08 · `main` 03f0166 · 계약 a6a87e1)

> 리뷰어는 **이 표의 값을 재측정하지 않는다.** 단 자기 판정이 그 값을 뒤집는다면 반드시 재측정한다.
> 여기 든 것은 전부 **실측**이다 — 추론·판정은 본문에 있고 이 표에는 없다.

| # | 확인한 사실 | `파일:줄` |
|:-:|---|---|
| 1 | `awk -F'\t' '$1=="I-21"'` 가 **11행** — 브리프 §1 · `plan-integration.md` §10 과 전건 일치 | `docs/coverage-100/assignment.tsv` |
| 2 | 계약 오퍼레이션 총수 — `shipment-04제품출하.json` **21** · `quality-03품질.json` **27**. I-21 대상 5 + 6 = 11 일치 · 누락 0 · 여분 0 | 두 파일(python) |
| 3 | ⭐ 두 파일의 **공통 스키마 이름은 셋**(`ErrorItem`·`ErrorResponse`·`PageMeta`) · `ErrorResponse` 완전 동일 · 나머지 둘은 **`example` 값만** 다르다 | 두 파일(python 전수 비교) |
| 4 | ⭐⭐ 판정 저장 설명 — 「⭐ 판정 저장과 Lot Status 전이는 **한 트랜잭션**(B-8) … ⭐ 도착 상태는 처분에 따라 갈린다 — 재작업 → `INSPECTION_PENDING` · 폐기 → **`SCRAPPED`** · 정상 → `NORMAL`」 | `contracts/quality-03품질.json:2460` |
| 5 | ⭐⭐ 같은 설명 — 「⭐ **If-Match 값은 이 경로의 GET 이 아니라 부적합 상세**(`GET /quality/nonconformances/{nonconformanceId}` · 제품출하 계약)**의 ETag 를 담는다** … ⛔ 이 경로의 목록 GET 에는 ETag 가 없다」 | 같은 줄 |
| 6 | ⭐ 그 `x-internal-note` — 「409 는 둘이다 … ⚠ 그래서 판정 저장은 **`nonconformance.version_no` 를 올려야 한다** … ⚠ **토큰 원천 검사기는 한 파일 안에서만 후보를 찾아 이 자리를 못 본다** — 원천이 다른 계약에 있는 첫 사례다」 | 같은 오퍼레이션(python) |
| 7 | ⭐ 7개 계약 파일 전수 grep — `SCRAPPED` 로 **LOT 을 보내는** 오퍼레이션은 판정 저장 **하나뿐**(나머지 20 출현은 전부 「값 목록 열거」다) | `contracts/*.json`(grep) |
| 8 | `NonconformanceCreate` required **4**(`itemId`·`severityCode`·`description`·`lots`) · 프로퍼티 7 · `NonconformanceLotCreate` required **3** | `contracts/shipment-04제품출하.json`(python) |
| 9 | `Nonconformance` required **12** · 프로퍼티 21 · `sourceCode` 는 「⭐ 서버가 대상 LOT 의 **입고 유형**으로 파생한다」 | 같은 파일 |
| 10 | `DispositionRequest` required **2**(`requestedQty`·`uomId`) · 프로퍼티 3 | 같은 파일 |
| 11 | `DispositionCandidate` required **7** · 프로퍼티 18 · 「⭐ 단위가 **LOT**」 · 「⭐ 원천 둘을 한 목록으로 합쳐 낸다」 | 같은 파일 |
| 12 | `disposition-candidates.receivedFrom` — 「⚠ **기간을 «필수»로 두지 않는다** … **양끝 포함**(L-3 — date 축)」 | 같은 파일 |
| 13 | `nonconformances.openedFrom` — 「기간 필수 — 공유계약 L-3」인데 `required` 미선언 · 응답 **200 하나** | 같은 파일 |
| 14 | `ShipmentConflictResponse.code` enum **5값**(`VERSION_CONFLICT`·`DUPLICATE_KEY`·`INVALID_STATE`·`ALREADY_CONFIRMED`·`CANCEL_IN_PROGRESS`) | 같은 파일 |
| 15 | `DispositionDecision` required **10** · 프로퍼티 18 · `followUpStatusCode`·`followUpQty` **둘 다 required** | `contracts/quality-03품질.json`(python) |
| 16 | `DispositionDecisionCreate` required **4 = 프로퍼티 4**(전건 필수) · `reason` 「공백만 있는 문자열도 받지 않는다(A-12)」 | 같은 파일 |
| 17 | `DispositionRemainingSummary` required **4 = 프로퍼티 4** | 같은 파일 |
| 18 | `Concession` required **10** · 프로퍼티 20 · `usable`·`unrestrictedAxes` 는 **required 밖** | 같은 파일 |
| 19 | `QualityConflictResponse.code` enum **6값** — `DISPOSITION_QTY_EXCEEDED` 실재 · `remainingQty`·`remainingQtyUomId` 프로퍼티 실재 | 같은 파일 |
| 20 | `GET …/{ncId}/disposition-decisions` — **질의 칸 0** · 응답에 `items`·`page`·`summary` **셋 다 required** | 같은 파일 |
| 21 | `GET /quality/disposition-decisions.reinstatable` — 「처분이 **정상**이거나, 처분이 **재작업이고 그 재작업이 끝난 것**을 서버가 한 축으로 판정 … ⛔ 폐기 결정은 포함하지 않는다」 | 같은 파일 |
| 22 | `dispositionTypeCode` — 「⛔ **공통코드 그룹으로 받지 않는다**」 · enum 3값 | 같은 파일 |
| 23 | `quality.nonconformance` — `version_no Int @default(1)` 실재 · **`source_code` 칸 없음** · `affected_qty_total`·`disposition_progress_code` 없음 | `prisma/schema.prisma:3427-3457` |
| 24 | `quality.nonconformance_lot` — `uq_nonconformance_lot(nonconformance_id, lot_id)` · `affected_qty CHECK (> 0)` | `schema.prisma:3463-3480` · baseline `:1987`·`:1993` |
| 25 | ⭐ `quality.disposition_decision` — `lot_id` 칸 **없음** · `version_no` **없음** · `updated_at` **없음** · **인덱스 0개**(PK 뿐) | `schema.prisma:3200-3216` · baseline `:1996-2008` |
| 26 | `quality.concession` — 21칸 · `ck_concession_dates` · `ck_concession_consumed` · `ix_concession_lot(lot_id, status_code)` | `schema.prisma:3091-3120` · baseline `:2036-2062`·`:3197-3198` |
| 27 | `mdm.warehouse` — 「불량창고」를 가리는 칸 **0개**(`warehouse_type_code`·`management_level_code`·`is_external`·`is_active` 뿐) | baseline `:299-321` |
| 28 | ⭐ `logistics.goods_issue.source_document_type_code` **NOT NULL** 실재 + `ix_goods_issue_source(source_document_type_code, source_document_id, issued_at DESC)` | `schema.prisma:748-749`·`:776` |
| 29 | ⭐ `logistics.stock_reinstatement` 표가 **물리에 없다**(전 마이그레이션 grep 0건) | `prisma/migrations/**`(grep) |
| 30 | `logistics.goods_receipt.receipt_type_code` NOT NULL · `goods_receipt_line.lot_id` **NOT NULL** · `original_shipment_lot_allocation_id` 실재 | `schema.prisma`(두 모델) |
| 31 | `production.work_order.rework_source_nonconformance_id` **nullable** · late FK `fk_work_order_rework_nc` · `disposition_decision_id` 칸은 **없다** | baseline `:793`·`:2759-2761` |
| 32 | `quality.sorting_result.disposition_decision_id` **NOT NULL** 실재(1차 범위 밖 표) | baseline `:2011-2031` |
| 33 | `trace.lot.status_code` NOT NULL · `version_no Int @default(1)` | baseline `:1057`·`:1066` |
| 34 | 시드 `NONCONFORMANCE_SEVERITY` **3값**(`CRITICAL`·`MAJOR`·`MINOR`) | `prisma/seed.ts:1156-1163` |
| 35 | 시드 `NONCONFORMANCE_STATUS` **3값**(`NOT_REQUESTED`·`PENDING_DECISION`·`DECIDED`) · `isSystemOwned` | `prisma/seed.ts:1166-1174` |
| 36 | ⭐ 시드에 **`DISPOSITION_TYPE` 그룹이 없다** · **`CONCESSION_STATUS` 그룹도 없다**(전수 grep 0건) | `prisma/seed.ts` |
| 37 | 시드 `LOT_STATUS` **4값** — `SCRAPPED` = `:550` | `prisma/seed.ts:543-552` |
| 38 | ⭐ 시드 `LOT_STATUS_TRANSITION` **9값**의 이름 — `C4` 판정→Release · `C5` 판정→보류 · `C6` 판정→Hold · `C7` 보류→Release · `C8` 보류→Hold · `C9` Release→Hold · `C10` 의심자재→보류 · `C14` 판정→PQC · `C15` PQC→Release. **`to = SCRAPPED` 인 것이 0개** | `prisma/seed.ts:517-531` |
| 39 | 시드 `RECEIPT_TYPE` 에 **`RETURN`**(반품입고) 실재 | `prisma/seed.ts:400-405` |
| 40 | ⭐ 시드 `NUMBERING_RULES` 등재 유형은 **`PRODUCTION_RESULT` 하나뿐** · `numbering.service.ts` 의 `DEFAULT_PREFIX` 에 **`NONCONFORMANCE` 없음** ⇒ 그대로 부르면 던진다 | `prisma/seed.ts:1425-1431` · `src/core/numbering/numbering.service.ts:9-47`·`:170-179` |
| 41 | `numbering.next(documentTypeCode, plantId, periodDate)` · 규칙 없으면 `{PREFIX}-{YYYYMMDD}-{SEQ4}` | `src/core/numbering/numbering.service.ts:49-51`·`:89` |
| 42 | ⭐ `transitions.ts` 에 `quality.nonconformance.status_code` 축이 **없다** · `trace.lot.status_code` 축에 처분 액션 **0개** | `src/core/document-state/transitions.ts`(전문) |
| 43 | ⭐ `stock-reinstate` = `from:['DEFECTIVE']` → `NORMAL` · `transitionCode` **없음** · 주석 「설계 미정 — 문의 **089**(발행 예정)」 | 같은 파일 `:215-219` |
| 44 | 전이 총수 단언 = **41** | `src/core/document-state/document-state.spec.ts:423` |
| 45 | `LotQualityStatusService.moveWithin(tx, lotIds, action, ctx)` — 미등록 액션이면 던지고 `FOR UPDATE` 로 잠근 뒤 `from` 밖은 건너뛰며 `lot_status_event` 를 남긴다 | `src/core/lot/lot-quality-status.service.ts:46-51` |
| 46 | ⭐ `trace.lot_status_event` 에 **불변 트리거가 없다** — 전 마이그레이션의 `CREATE TRIGGER` 6개는 `set_updated_at`·`inventory_balance_qty`·`lot_relation_cycle`·`inventory_transaction(_line)_immutable`·`work_order_closed_immutable` 이다 | `prisma/migrations/**`(grep) |
| 47 | ⭐ 11건 중 **10건이 `derived-permissions.ts` 에 있다** — 빠진 것은 `GET /quality/concessions/{concessionId}` 하나 | `src/common/permissions/derived-permissions.ts:116,120,121,122,137,138,139,254,255,256`(grep 전수) |
| 48 | 가드는 **계약이 403 을 선언한 자리에서만** 본다 ⇒ 위 하나는 오늘 500 을 내지 않는다 | `src/common/permissions/permission.guard.ts:37-41` |
| 49 | `ConflictExtra` 칸 **4개**(`code`·`currentVersion`·`currentLotStatusCode`·`conflictingLotId`) — `remainingQty` 계열은 **없다** | `src/common/errors/conflict.exception.ts:19-38` |
| 50 | `ERROR_CODE` 에 `STATE_LOCKED`·`RANGE`·`PAIR`·`REQUIRED`·`INVALID` 전부 있다 — **새 코드 0** | `src/common/errors/error-codes.ts:9,10,13,14,17` |
| 51 | `runIdempotent`·`runVersioned` 시그니처(후자는 `setEtag` 를 **늘** 낸다) · `setEtag(response, versionNo)` | `src/common/master/master-write.ts:20`·`:40-56` · `src/common/optimistic-lock/optimistic-lock.ts:20` |
| 52 | `assertCodeValues(prisma, checks)` — `CodeCheck = {field, value, groupCode}` | `src/common/master/code-reference.ts:7-11`·`:24-27` |
| 53 | ⭐ `src/quality/` 아래 **부적합·처분·특채 파일이 0개**(34파일 6,236줄 중 0) · `test/quality-*.e2e-spec.ts` **8파일**에도 0 | `src/quality/`·`test/`(ls) |
| 54 | `QualityModule` imports = `PrismaModule`·`AuthModule`·`IdempotencyModule`·`NumberingModule`·`DocumentStateModule`·`LotRegistryModule` — **더 넣을 것이 없다** | `src/quality/quality.module.ts` |
| 55 | main 커버리지 = **399/487** · 스펙 제목은 `(n/490)` | `node_modules/.bin/jest contract-coverage` · `src/common/contract/contract-coverage.spec.ts:30` |
| 56 | ⭐ `W-03-09:31` — 「✓설계확정 결정 10 … **LOT 상태 확정**(2026-08-07) — 「**최종 상태는 셋 다 `정상`**이고 다른 것은 **조건**이다」」 | `.design-reference/…/screens/03/W-03-09-특채한도승인처리.md:31` |
| 57 | 같은 문서 `:164-171` — 「검사 불합격 → 불량 ↓ 특채 승인 승인 수량만큼 → **LOT 상태 = 정상** + `concession` 이 범위를 건다」 · 「⚠ 상태만 바뀌는 것이 아니다 … A-14 가 여기서 실제로 작동한다」 | 같은 파일 |
| 58 | 같은 문서 `:173` — 「⛔ **부분 전이다** — 500 중 100 만 `정상`이 되므로 **재고 행이 둘로 갈린다**」 | 같은 파일 |
| 59 | 같은 문서 §8 — 미결 **5건 전부 미해소**(승인 유형 값 · **`concession.status_code` 값 목록** · 한도승인 요청 화면 · 특채 요청 화면 · `consumed_qty` 를 올리는 주체) | 같은 파일 `:220-233` |
| 60 | ⭐ `W-03-10:126-131` — 「`disposition_decision` INSERT + `trace.lot.status_code` UPDATE … **공유계약 B-8** … 처분만 남고 LOT 이 안 바뀌면 **다음 화면이 잘못된 대상을 집는다** — `W-04-11`(재등록)이 아직 불량인 LOT 을 정상으로 본다」 | `.design-reference/…/screens/03/W-03-10-처분판정처리.md:126-131` |
| 61 | 같은 문서 `:194-196` — 「남은 수량 초과 ⛔ **409 `DISPOSITION_QTY_EXCEEDED`**」 · 「이미 닫힌 부적합 ⛔ **409 `INVALID_STATE`**」 | 같은 파일 |
| 62 | ⭐ 같은 문서 §8 #7 **미결** — 「대상 LOT 단위가 섞이면 수량 합 불성립」 · #8 「판정 저장이 `version_no` 를 올리는가」 · #9 「오판정 정정 경로가 없다」 | 같은 파일 `:231-233` |
| 63 | 같은 문서가 `disposition_decision` 스키마 8칸을 열거하는데 **`lot_id` 가 없다** · 레이아웃은 대상 LOT 을 **단수**로 그린다 | 같은 파일 `:40-48`·`:71` |
| 64 | ⭐ `W-04-07` §4-A — 「**원천** `source_code` … ⭐ **서버 파생 — 화면이 보내지 않는다.** 대상 LOT 이 반품 입고로 들어왔으면 `RETURN`, 그 밖은 `PRODUCT`」 | `.design-reference/…/screens/04/W-04-07-재작업폐기판정의뢰.md` §4-A |
| 65 | 같은 문서 §4-A — 「⭐ 이 화면이 올리는 것은 **`PENDING_DECISION` 까지**이고 `DECIDED` 로 올리는 것은 03(`W-03-10`)이다」 | 같은 파일 |
| 66 | 같은 문서 §6 — 「이미 의뢰됨 ⛔ 「판정 대기 중입니다」 — **중복 의뢰 방지**」 · 「낙관적 잠금 충돌 B-1 — `nonconformance.version_no`」 | 같은 파일 §6 |
| 67 | 같은 문서 §5-4 — 「`W-04-06`(반품 입고)은 **원 LOT 을 그대로 썼다**」 | 같은 파일 §5-4 |
| 68 | 같은 문서 §8 #3 — 「**처분이 언제 결재를 타는가** … ⚠ 2026-09-02 확인 — **아직 열려 있다.** 소관으로 지목한 화면이 소관을 «부인»한다」 | 같은 파일 §8 |
| 69 | ⭐ `W-03-02` §5-1 — 「C4·C5·C6 = **검사 판정 화면**(W-01-01 IQC · P-02-13 PQC · W-04-03 OQC)」 · 「C16 PQC 검사 필요 → 02-S-I (경계 위임) — 화면 아님」 | `.design-reference/…/screens/03/W-03-02-LotStatus판정전이처리.md:103-107` |
| 70 | 같은 문서 §5-5 — 「보류 수신 C5·C10 / 발신 C7·C8·**C11** · Release 수신 C4·C7·C15 / 발신 C9 · Hold 수신 C6·C8·C9 / **발신 (없음)** · PQC 수신 C14 / 발신 C15·**C16**」 ⇒ 도식이 C1~C16 을 이미 쓴다 | 같은 파일 `:183-186` |
| 71 | `seed.ts` 의 `LOT_STATUS_TRANSITION` 주석 — 「품질 판정 축의 전이 **정본 9종**이다. 판정 유입 경계·데이터 배선·경계 위임은 상태 전이가 아니라 **제외**했다고 설계팀이 밝혔다(2026-09-01 회신 E-9)」 | `prisma/seed.ts:511-516` |
| 72 | ⭐ `W-04-10:104`·`:230` — 폐기 요청은 `goods_issue` 의 **`source_document_type_code`+`source_document_id`**(A-10 다형)로 처분 결정을 가리키고 계약이 enum 3값(`PICKING_ORDER`·`GOODS_RECEIPT`·**`DISPOSITION_DECISION`**)으로 닫았다 | `.design-reference/…/screens/04/W-04-10-제품폐기요청.md` |
| 73 | `W-04-11:121-123`·`:192` — 재등록 대상은 처분이 **정상 또는 재작업 완료**이고, 본문 필수는 **`lotHoldId`** 다(`disposition_decision_id` 가 아니다) | `.design-reference/…/screens/04/W-04-11-재고재등록.md` |
| 74 | `P-04-03:4` — 「⭐ **재작업 W/O 가 부적합을 직접 가리킨다** — `work_order.rework_source_nonconformance_id`」 · `production_result` 에 처분 결정 칸 **없다** | `.design-reference/…/screens/04/P-04-03-재작업실적등록.md` |
| 75 | 통보 **154** — 「조회는 enum 밖 값을 떨어뜨리지 않는다 … **089 로 재등록 전이 코드를 정하실 때 `LotStatusHistoryEvent.transitionCode` 와 질의 파라미터의 enum 도 «함께» 열어 주십시오**」 | `docs/design-inquiries/154-lot-status-events-가-계약-enum-밖-transitionCode-를-그대로-싣는다.md` |
| 76 | `재정리-2026-09-08-레인A.md` §2 — 「**12** 한도승인 재고 상태 값 · **I-21**(특채) · 통보 · ⭐ **I-21 착수 때 §2 절차로 정한다. 코드값이라 `UPDATE` 로 갈아 끼울 수 있다**(비용 낮음)」 | `docs/design-inquiries/재정리-2026-09-08-레인A.md` |
| 77 | `plan-uiux.md:1231` **N** — 「처분 전이 코드가 `transitionCode` 9종에 없다(W-03-10) … 전이 코드를 지어내지 않는다. **요청서 후보**」 | `docs/coverage-100/plan-uiux.md:1231` |
| 78 | `plan-uiux.md:609` — U28 「처분 결재 필요 여부 … 2단계 ② — **결재를 걸지 않는다**(없음 → 있음이 완화)」 | 같은 파일 `:609` |
| 79 | `plan-integration.md:389` — 「수량 합이 대상 수량을 넘으면? 계약이 말하지 않는다 → 2단계 기준 2「거부하는 쪽」 → **400**」 | `docs/coverage-100/plan-integration.md:389` |
| 80 | `plan-api.md:871` — 「**`SCRAPPED` 로 가는 전이도 계약에서 못 찾았다** … 계약이 LOT 상태를 옮긴다고 적지 않았다」 | `docs/coverage-100/plan-api.md:871` |
