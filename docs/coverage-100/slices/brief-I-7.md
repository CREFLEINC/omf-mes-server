# I-7 생산 실적 — 등록·정정·상신 + LOT 생명주기 L1 + 생산 LOT 완료(7건) — 개별 계획안 브리프 (계획만, 구현 금지)

산출물: `docs/coverage-100/slices/I-7.md` 하나. 저장소 파일을 고치거나 만들지 않는다(이 파일 하나만 쓴다). 작업 위치는 worktree `/Users/rangkim/projects/crefle/omf/apps/omf-mes-server/.claude/worktrees/i7-plan`(브랜치 `docs/coverage-100-i7-plan` 그대로 · 체크아웃 변경 금지 · `git` 쓰기 금지 — 커밋은 통합자가 한다). `contracts/*.json` 읽기 전용(`jq`). `prisma migrate reset`·`migrate diff --shadow-database-url`·`prisma db seed` 금지. 로컬 DB 는 `psql` SELECT 만(`.env` DATABASE_URL 의 `?schema=` 는 잘라서). 절대경로로만 `cd`.

## 읽을 것 (순서대로)
1. `docs/coverage-100/README.md` — §1-2 「차이가 크다」 3조건 · §2 판정 절차 · §5 불변 제약 · §6 속도(구현 브리프 비테스트 예산 350 · 한도 400 · 코어 전용 ≤200 — PR 분할이 이 예산을 지켜야 한다).
2. `docs/coverage-100/plan.md` — §1 41행(I-7 · 7건 · 의존 I-6 · 마이그 **D1** · 코어 「—」 · opus · PR 3) · 42행(M1 체인 e2e 가 I-7 뒤) · 45·48·50·63행(I-10·I-25·I-19·I-26 이 I-7 에 의존) · 82행(M1 완료 조건 — 「실적(L1) → 제품 입고(기존) → balance 3행」) · §0 151행(**원장 판별자 4값 고정 — 실적은 원장을 안 지난다**) · 153행(`PRODUCTION_RESULT_CORRECT` 는 승인 FK 를 쓰지 않는다 — 다형 축) · §3 104행(`moveWithin()` 을 I-7 이 L1 로 재사용) · §4 122행(D1 = `production_result.shift_id` NOT NULL 해제) · §7 209행(`achievementRate` 라벨 — I-7 배정) · §8 진행표(I-6 이 곧 ✅ 293).
3. `docs/coverage-100/plan-integration.md` — 101~110행(§1-4 실적은 원장을 «지나지 않는다» · 제품 재고는 기존 입고 `sourceDocumentTypeCode='PRODUCTION_RESULT'`) · 152행(생명주기 3전이 · L1 은 I-7) · 171행 · **264~272행(I-7 상세 — 체인 마디 ⑮~⑱ · `production_result_lot_allocation`·`material_usage_allocation`·`lot_lifecycle_history` 가 실적 저장과 같은 트랜잭션 · 설계 미정 2: `:correct` A급 판정식 잠정 「수량 5칸 중 하나라도 바뀌면 A급」 · 지연 실적의 마감 뒤 편입 R83)** · 358행(`GET /trace/lot-lifecycle-events` 는 표가 이미 있다) · 498~502행(M1 체인 순서) · 583행(I-19 는 I-7 뒤) · 629행(⭐ **`businessDate`/`occurredAt` C-8-1 — 원장을 안 지나면서 `businessDate` 를 싣는 오퍼레이션은 「받아서 형식만 검증하고 저장하지 않는다」 · 건수만 누적**) · 659·661행(반박 1·3 — 원장 판별자 늘리기 금지 · 역트랜잭션 4벌).
4. `docs/coverage-100/plan-api.md` — **412~441행(S16 — ⚠ 「posting 있음 — 실적이 완제품을 잡고」는 `plan.md` §0 151행·`plan-integration.md` §1-4 와 모순 → 통합 정본이 이긴다: 실적은 원장을 안 지난다 · `shift_id` NOT NULL 완화 · 실적 `status_code` 「칸 불필요」 · A급 = 수량 칸이 바뀌면)** · 220~238행(S08 — `GET /trace/lot-lifecycle-events` · `POST /trace/lots/{lotId}:complete` 는 **S08 에 배정돼 있었으나 `plan.md` 가 I-7 로 옮겼다** — 「`:complete` 는 L1·L2·L3 셋 밖의 새 전이 · `LOT_LIFECYCLE_STATUS` 3값에 완료가 없다 → 본길 · 계약 문자 그대로 두고 어느 칸도 안 옮기고 W/O 사유만 기록 + 문의」 초안을 §2 로 다시 판정) · 1084행(채번 `PRODUCTION_RESULT` 규칙 ✅ `PR-{YYMMDD}-{SEQ4}` DAILY — 유일하게 규칙이 등재된 전표) · §7 에러표(`ROUTE_NOT_FOUND` · 진행 중 요청 400).
5. `docs/coverage-100/plan-uiux.md` — 58행(U24 5건) · 359~376행(U24·U25 화면 매핑 — `POST` 실적 「멱등, ETag, 사번」 · `:correct` 「멱등」 · `:complete` 「멱등, ETag, 사번」) · 631행(순서 근거 — M1 tracer bullet 종점) · 1096행 · §9-3(`achievementRate` 분모).
6. **선행 결과물** — `docs/coverage-100/slices/I-6.md` §0-재수립 **R-12**(`moveWithin` 반환) · R-24(`work_session` 픽스처 NOT NULL 칸 · M1 체인 e2e 는 I-7 뒤 별도) · §3-2(`moveWithin` 시그니처) · §4-2(선발행 슬롯 모양 — `source_type_code='WORK_ORDER'`·`source_id`·`work_order_lot_seq`·`lifecycle_status_code='WAITING'`·`initial_qty`·`bom_id`/`bom_version` 스냅샷) · §5-3~§5-4(`:close` 3분류 입력 = `SUM(production_result.good_qty)` 상태 필터 없음 · L2 대상 = `WAITING` ∧ `NOT EXISTS production_result_lot_allocation` — **I-7 이 배분 표를 같은 트랜잭션에 써야 하는 이유**) · 535행(`completed_at` 은 I-7·`P-02-06` 이 채운다) · **846~858행 §8-6 인계 I-7 행**(`moveWithin(tx,{lotIds, action:'production-result-recorded', sourceDocumentTypeCode:'PRODUCTION_RESULT', sourceDocumentId, changedAt})` 그대로 · 슬롯 축 `lot(source_type_code='WORK_ORDER', source_id=workOrderId)` + `work_order_lot_seq` 오름차순) · 916행 ⓟ(`trg_work_order_closed_immutable` — 마감 뒤 `work_order` UPDATE 는 500 — I-7 이 `work_order.completed_at` 을 만질 때 확인) · 1022행(I-6 e2e 가 `production_result` 를 직접 INSERT 한 픽스처 — I-7 이 API 로 바꿀지) · §11 대조표 16·18행 · `I-5.md` R-2/R-6/R-9(상태기계 등록·`runIdempotent`/If-Match 어댑터) · `I-4.md` R-1(잠금 오름차순)·R-2(`transitions.ts` 키 «안에» 액션 추가)·§4(`assertApproved`) · `I-2.md` R-2(채번은 `$transaction` 밖)·승인 상신 코어(`request`·`assertNoOpenRequest`).
7. 실재 코어: `src/core/document-state/transitions.ts` 112~120(**`'production-result-recorded'` L1 `from:['WAITING'] to:'ACTIVE'` 이미 등록 · `sourceOperation: 'POST /production/production-results'`**) · 155~165(`production.work_order.status_code` 키 — I-6 이 열었다 · `IN_PROGRESS`·`COMPLETED` 값이 어디서 진입하는지: `work-order-close` `from:['COMPLETED','IN_PROGRESS']` — **`COMPLETED` 로 옮기는 액션이 등록돼 있는가** 실측 · 없으면 `:complete`/실적이 여는지 §2) · `src/core/lot/lot-lifecycle.service.ts`(`moveWithin` 전문 · `skippedLotIds` 의미) · `src/core/lot/lot-registry.service.ts`(`preIssueWithin` · `PREISSUED_LIFECYCLE` · `lot_hold` 미적용) · `src/core/lot/index.ts` 배럴 · `src/core/numbering/numbering.service.ts`(`next('PRODUCTION_RESULT', plantId, periodDate)` — 규칙 등재 · **plantId 축** — 실적의 공장은 어디서: `work_order` 에 `plant_id` 없음(I-6 R-7 — `production_plan→production_order.plant_id`) · `$transaction` 밖) · `src/core/approval/approval.service.ts` 20~35(`ApprovalRequestInput` — `approvalRequestNo` 호출자 채번 · `businessUnitId` null 공통본 · 문의 022) · 102~(`assertNoOpenRequest(tx, targetTypeCode, targetId, approvalTypeCode)`) · 141~(`assertApproved` — `:correct` 가 A급이면 승인 게이트를 거는가: 계약 문장 「승인이 끝나면 정정은 `:correct` 로 기록한다」 → `:correct` 에서 `assertApproved` 를 부르는지 · B급(수량 안 바뀜)은 승인 없이 되는지) · `src/common/master/master-write.ts`(`runIdempotent`·`runVersioned`) · `src/common/idempotency`(**`production_result.idempotency_key` 칸 NOT NULL UNIQUE 가 별도로 있다** — `IdempotencyService` 의 `app` 표와 이중 · 무엇을 넣나: 헤더 값 그대로? · `work_session`·`goods_issue` 2635·3384 행 선례 실측) · `src/app/notice/notice.controller.ts` 140~165(**`X-Worker-No` 선례 — `actorOf()` · 단말 토큰 없어 세션이 주체 · 헤더는 덧붙임**) · `src/common/permissions/derived-permissions.ts` 111·232~234·258(403 5건 이미 등록 — `GET` 단건·`lot-lifecycle-events` 는 없음 실측).
8. 기존 도메인 선례: `src/production/work-order/`(**가장 최근 선례 — 파일 배치·`lockWorkOrder`·`assertVersion`·`runIdempotent`·`currentSession(request)?.userId`·`today()` UTC 주석**) · `work-order-progress.ts`(`progressOf`/`progressOfMany` — `production_result` 다섯 수량 합 · **`status_code` 로 안 거른다** 187행 주석 · 정정본을 어떻게 세나: `corrects_production_result_id` 가 있는 행을 «대신» 세나 «더해» 세나 — I-6 실측 그대로 인용 · I-7 이 정정을 넣으면 `progressOf`·`:close` 3분류가 이중 계산되지 않게 판정) · `work-order-query.service.ts`(`PreIssuedLotSummary.withResultCount` — 배분 표 `EXISTS`) · `src/trace/lot/`(`lot.controller.ts` GET/POST/PUT 4 · `lot.service.ts` · `lot-view.ts` — `:complete` 와 `lot-lifecycle-events` 가 이 모듈에 붙는가 · `LotView.lifecycleStatusCode`) · `src/logistics/document-progress/`(목록·상세·기간 강제 L-3 선례 — `lot-lifecycle-events` 의 `occurredFrom/To` 「감사 조회는 기간을 강제한다」) · `src/app/approval/`(상신 후 결재함 흐름 · `PRODUCTION_RESULT_CORRECT` 승인 유형 시드 유무 · 결재선 `ROUTE_NOT_FOUND` 400 선례 — I-2 `requestApproval`).
9. `prisma/schema.prisma` — `production_result` 2807~2857(30칸 · `production_result_no @unique` · `work_session_id?` · `result_sequence` + `uq_production_result_seq(work_order_id, result_sequence)` · `corrects_production_result_id?` 자기참조 · 다섯 수량 `Decimal(20,6) @default(0)` · `uom_id` NOT NULL · `result_source_code` NOT NULL · `occurred_at` · `recorded_at` DB 기본 · `late_entry_reason_code?` · **`worker_id` NOT NULL** · `equipment_id?`·`mold_id?` · **`shift_id` NOT NULL → D1** · `terminal_id?` · **`status_code` NOT NULL — 계약 `x-no-code-key` 「코드 그룹을 세우지 않는다 · 기록 전용」 + 시드 `PRODUCTION_RESULT_STATUS` 폐기(1467행) → 무엇을 넣나: 상수? NULL 완화(마이그 D1 에 얹나)? — §2 로 판정 · `work-order-query.service.ts:187` 주석 참고** · `idempotency_key @unique` · `version_no`) · `production_result_lot_allocation` 2858~(`allocated_qty > 0` CHECK · `uq_production_result_lot(production_result_id, lot_id)` · `uom_id`) · `material_usage_allocation` 2739~(`material_consumption_id` NOT NULL — **I-10 몫**: I-7 이 이 표를 쓰는가 · 투입이 없으면 못 쓴다 → `plan-integration.md` 267행 「같은 트랜잭션」 문장을 실측으로 정정) · `lot_lifecycle_history` 4630~ · `lot` 3509~(`lifecycle_status_code?` · `work_order_lot_seq` · `source_type_code`/`source_id` · `initial_qty`·`current_qty`? — **실적 배분이 `lot.current_qty` 를 올리는가**: 원장을 안 지나므로 §2 · `completed_at`? 칸 유무) · `work_session` 2980~(`ended_at`·`status_code` — 실적의 `work_session_id` 가 열린 세션이어야 하나 · I-11 전이라 픽스처 직접 INSERT) · `work_order` 2876~(`completed_at`·`completion_variance_reason_code` — `:complete` 가 채우는 칸 · `trg_work_order_closed_immutable`) · `approval_request`(`target_type_code`·`approval_type_code`) · `mdm.worker`(`worker_no @unique` 2327행 — `X-Worker-No` → `worker_id` 해석) · 마이그 `20260727000000_baseline_physical_model_v3:1640~1680`(**`ck_production_result_nonzero` — 다섯 합 > 0 CHECK** · 500 으로 새지 않게 손검사 — I-6 R-28 방식).
10. 시드 `prisma/seed.ts`: `RESULT_SOURCE`(1459 — 시스템 소유 목록에 있음 · 값 `MANUAL`/`IOT` 가 실제로 시드돼 있는지 실측) · `PRODUCTION_RESULT_STATUS`(1467 — 폐기) · `LATE_ENTRY_REASON`(1063 · `values: []`) · `PRODUCTION_RESULT_CORRECT_REASON`(1197 · `values: []`) · `LOT_LIFECYCLE_TRANSITION`(500~ · L1·L2·L3 · 「생성」 없음 · 「완료」 없음) · `LOT_LIFECYCLE_STATUS`(482~ · 3값 — 완료 없음) · `WORK_ORDER_COMPLETION_VARIANCE_REASON`(735) · `APPROVAL_TYPE`/결재선 시드(`PRODUCTION_RESULT_CORRECT` 유형이 있는지 · 결재선 `approval_route` 가 그 유형에 있는지 — 없으면 e2e 가 `ROUTE_NOT_FOUND` 만 닿는다) · `NUMBERING_RULES`(`PRODUCTION_RESULT` DAILY).
11. 계약 원문 — `contracts/production-02생산실행.json` 경로 4(1606·1785·1828·1915) · 스키마 `ProductionResult` 5661(required 14 · `statusCode` `x-no-code-key` · `correctsProductionResultId` · `workerId` · `recordedAt` · `terminalId`) · `ProductionResultCreate` 5846(required `workOrderId`·`uomId`·`resultSourceCode`·`occurredAt` — ⚠ `resultSourceCode` 설명 「서버가 안다 — 화면이 고르는 값이 아니다」인데 **required** — 계약끼리 모순인가(멈춤 조건 ③)·아니면 「받되 검증」인가 §2 · `lotAllocations[]` `ResultLotAllocation`(5940 · `lotId`·`allocatedQty`) · `lateEntryReasonCode` · `equipmentId`·`moldId` · `workSessionId?` · **`shiftId` 없음** → D1 근거 · **`workerId` 없음** → `X-Worker-No` 헤더 · `businessDate` 없음) · `ProductionResultCorrect` 5803(required `reasonCode` · 다섯 수량 · `note`) · `:request-approval` description 전문(A급 서버 판정 · `ROUTE_NOT_FOUND` 400 · 진행 중 요청 400 · 반려 뒤 재상신 · **「승인이 끝나면 정정은 `:correct` 로 기록한다 — 이 오퍼레이션은 승인 요청만 만든다」** — 요청 본문 스키마가 무엇인지: 정정 내용을 상신에 싣나 · 승인 뒤 `:correct` 가 그 내용과 같은지 대조하나) · GET 목록 질의 8(`workOrderId`·`workSessionId`·`shiftId`·`equipmentId`·`occurredFrom/To`·`page`·`size` — 기간 강제 여부) · 오류 봉투 · x-internal-note. `contracts/logistics-01자재창고.json` `GET /trace/lot-lifecycle-events` 6414(질의 4 · **`occurredFrom/To` 「감사 조회는 기간을 강제한다(L-3)」** · `LotLifecycleHistoryEvent` 12069 required 5 · `lotNo` 조인) · `POST /trace/lots/{lotId}:complete` 6572(`LotComplete` 11480 — required **`businessDate`·`occurredAt`** · `completionVarianceReasonCode` 「계획 수량에 미달하면 필수 · 서버가 400」 · 「LOT 상태와 작업지시 사유를 한 트랜잭션」 · **W/O `:close` 의 `reasonCode` 와 같은 축** · ⛔ 라벨 출력 밖 · 파라미터 헤더 3(멱등·If-Match·X-Worker-No)).
12. 기존 문의 `docs/design-inquiries/README.md` + 016~040(특히 **035**(`:hold` 셋) · **039**(마감 전 게이트 — R83 지연 실적 편입과 같은 뿌리) · **040**(계획 없는 W/O) · 022(`businessUnitId` 공통본) · 014 채번 표) · 설계 저장소 사본 `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/02/`(`P-02-04` §5 실적 등록 · `P-02-06` §5-5 LOT 완료 · `W-02-05` §5-3·§5-4·§8-1 · `W-02-08`) 과 `06/`(`W-06-15` 결재함).

## 담을 것 (I-6.md 와 같은 짜임 — §0 재수립 표는 3관점 뒤에 채운다 · §1 계약 실측 · §2 마이그 · §3 상태기계 · §4~§6 오퍼레이션별 · §7 조회 · §8 횡단·인계 · §9 설계 미정·문의·알려둘 것 · §10 PR 분할·테스트 이름 · §11 통합 계획 대조)
1. **README §1-2 3조건 판정을 맨 위에** — ① 마이그 D1 한 건(+ `status_code` 완화가 붙으면 둘) ② 순서 영향(I-10·I-19·I-25·I-26 이 기다리는 «자리») ③ 문의 신설 여부(**041** 부터).
2. **마이그 D1** — `shift_id` NOT NULL 해제 SQL(forward-only · 두 릴리스 미해당) · Prisma 스키마 `shift_id BigInt?` 동기 · `migrate diff --from-schema-datasource` 드리프트 0 확인 절차 · **`status_code` 처리를 같은 마이그에 얹는가**(NULL 완화 vs 상수 `'RECORDED'` 같은 값 지어내기 — ⛔ 값 지어내기 금지 → 완화가 정합이면 D1 에 함께 · 아니면 근거) · `ck_production_result_nonzero` 는 손검사(400 `INVALID`) · 사전 대조 SELECT.
3. **상태기계** — L1 `production-result-recorded` 이미 등록 → **호출만**(`moveWithin` · 대상 슬롯 = `lotAllocations[].lotId` 의 것 · `WAITING` 아닌 슬롯은 `skipped` — 이미 `ACTIVE` 면 정상(두 번째 실적) · `VOIDED` 면? 400 인가 skip 인가 §2) · `sourceDocumentTypeCode='PRODUCTION_RESULT'` 문자열 상수 위치(`lot-source.ts` 의 `WORK_ORDER_LOT_SOURCE` 선례) · `changedAt = occurredAt`? `recorded_at`? · **W/O 축**: 실적이 `work_order.status_code` 를 옮기는가(`IN_PROGRESS` 진입은 I-11 세션 몫 — 실적은 안 옮긴다 · 그러나 `RELEASED` 상태 W/O 에 실적이 들어오면 허용인가 400 인가 — 계약 문장으로 · `SUSPENDED`·`CLOSED`·`CANCELLED` 에 실적 400) · **`:complete` 축**: `LOT_LIFECYCLE_STATUS` 3값에 완료 없음·품질 축 4값에도 없음 → `plan-api.md` S08 초안(어느 칸도 안 옮기고 `work_order.completed_at`·`completion_variance_reason_code` 만 기록)을 §2 로 판정 · `work_order.status_code` 를 `COMPLETED` 로 옮기는가(전이표에 `COMPLETED` 진입 액션이 있는지 실측 · `:close` `from` 에 `COMPLETED` 가 있으니 누군가 넣어야 한다 — I-6 이 열어둔 자리인지 I-6.md §3-1 표에서 확인) · 「계획 수량에 미달」 = `SUM(good_qty)` vs `lot.initial_qty`? `work_order.order_qty`? — 계약 「LOT 을 완료」이므로 LOT 단위인지 W/O 단위인지 · `completionVarianceReasonCode` 는 `WORK_ORDER_COMPLETION_VARIANCE_REASON` 그룹 · `businessDate` 는 받고 저장 안 함(629행 · 건수 +1).
4. **`POST /production/production-results`(코어)** — 한 트랜잭션 순서: 채번 `next('PRODUCTION_RESULT', plantId, ?)` **tx 밖**(plantId 도출 R-7 · periodDate = `occurredAt` 의 날짜? `today()`? — DAILY 규칙 · UTC 하루 어긋남 알려둘 것) → `lockWorkOrder` → 상태 허용 검사 → `result_sequence` = `MAX+1`(잠긴 W/O 아래라 경합 없음) → 다섯 수량 손검사(음수 400 · 합 0 400 · 기본 0) → `uom_id`·`equipment_id`·`mold_id`·`work_session_id` 존재 400(#13 방식) → `worker_id` = `X-Worker-No` → `mdm.worker.worker_no` 해석(헤더 없으면? 세션 사용자에 worker 가 연결돼 있나 — `app_user.worker_id`? 실측 · 없으면 400 `REQUIRED` field `X-Worker-No` — 계약 헤더 정의 인용) · `shift_id` NULL(D1) 또는 `work_session.shift_id` 승계 · `terminal_id` NULL · `late_entry_reason_code` 코드값 검증(그룹 0값 — I-6 ⓕ 방식 「대조 안 걺」과 정합) · `result_source_code` 검증(`MANUAL`/`IOT`) · `idempotency_key` = 헤더 값 · `status_code` 값 → INSERT 헤더 → `lotAllocations` INSERT(`allocated_qty > 0` · 같은 lot 중복 400 · lot 이 이 W/O 의 슬롯인지 400 · **합 = goodQty 인가**: 계약이 안 적으면 검사 안 함 — 문장으로) → L1 `moveWithin` → `work_order.completed_at`? 안 만짐 · 응답 201 `ProductionResult` + ETag(`plan-uiux` 「ETag」) · 멱등 `runIdempotent` · If-Match 선택(`ifMatchVersion` — 무엇의 버전인가: W/O? — 계약 헤더 설명 인용).
5. **`:correct`** — 원본 잠금(`FOR UPDATE`) · 원본이 정정본이면?(정정의 정정 — 체인 허용? 400?) · 새 행 INSERT(`corrects_production_result_id=원본` · `result_sequence` 새 번호 · 다섯 수량 = 본문 값 있으면 그것, 없으면 원본 값 승계? — **정정 행이 «대체값»인지 «차이값»인지** 계약 문장으로 · `progressOf`/`:close` 3분류가 정정본을 어떻게 세는지 I-6 실측(`work-order-progress.ts`) 과 맞춘다 — 어긋나면 I-6 코드 수정이 필요한지(비테스트 예산에 포함) · `reasonCode` `PRODUCTION_RESULT_CORRECT_REASON`(0값) · `note` 저장 칸(`remarks`?) · **A급이면 승인 게이트**(`assertApproved(tx,'PRODUCTION_RESULT', id, 'PRODUCTION_RESULT_CORRECT')` — 승인 없으면 400 코드?) · B급은 바로 · lot 배분 정정은 없음(계약 스키마에 없음) · 원본 `lot_allocation` 승계? · ETag 없음 · 멱등.
6. **`:request-approval`** — A급 판정식(잠정 「다섯 수량 중 하나라도 원본과 다르면 A급」 — 본문에 정정 내용이 있는가: **스키마 실측** — 없으면 무엇으로 판정하나 → 멈춤 조건 ③ 후보) · `assertNoOpenRequest` 400 · 결재선 없음 `ROUTE_NOT_FOUND` 400 · `approvalRequestNo` 채번 tx 밖 · `businessUnitId` null(022) · `targetTypeCode='PRODUCTION_RESULT'`·`approvalTypeCode='PRODUCTION_RESULT_CORRECT'`(시드 유무) · 응답 모양 · 반려 뒤 재상신.
7. **`GET` 목록·단건 · `GET /trace/lot-lifecycle-events`** — where 8 매핑 · 정렬 기본(`occurred_at desc`?) · `PageMeta` · 단건 404 · `ProductionResult` 뷰 24칸 매핑(`workerId`·`recordedAt`·`terminalId`·`statusCode` 값) · ETag 단건? · `lot-lifecycle-events` 는 `src/trace/` 에 · 기간 강제(L-3 — 없으면 400 `REQUIRED`? 선례 `document-progress` 실측) · `lotNo` 조인 · `transitionCode` 필터 · 403 없음.
8. **횡단** — 403 5건 등록 실측(단건 GET·`lot-lifecycle-events` 는 계약에 403 이 있는지) · 멱등 4 · If-Match 선택 2(`POST`·`:complete`) · ETag(`POST` 201 응답 · `:complete`) · `X-Worker-No` 처리 규약(notice 선례 그대로 — 세션 주체 + 헤더 덧붙임 · 실적은 `worker_id` NOT NULL 이라 다르다) · 404 · 400 vs 409(진행 중 요청은 계약이 400) · 도메인 배치(`src/production/production-result/` 신설 · `:complete` 는 `src/trace/lot/`) · **`src/core/**` 무변경**(코어가 「—」 인지 실측 — 배분·L1 이 코어 함수 재사용만이면 코어 PR 없음).
9. **e2e 정리** — ⭐ **M1 체인 e2e**(R-24 · `plan.md` 42행 별도 PR 1 — 이 슬라이스가 지는가 뒤 별도인가 판정: P/O 승인 → 입하 → 입고 → 기타출고 → W/O 발행·배포 → 실적(L1) → 제품 입고(기존 GR `source=PRODUCTION_RESULT`) → balance 3행 → 입고 취소 → 역트랜잭션 → 원복 — 기존 e2e 헬퍼 재사용 목록) · 픽스처(I-6 e2e 의 W/O·슬롯·세션 직접 INSERT 재사용 · `worker` · `shift` · `uom`) · cleanup 순서(`lot_lifecycle_history` → `production_result_lot_allocation` → `production_result` → `approval_request` …) · I-6 e2e 가 직접 INSERT 한 `production_result` 픽스처는 그대로 둔다(API 로 바꾸지 않는다 — 이유).
10. **설계 미정 자리** — 각각 README §2 절차. 문의는 제목만(**041** 부터 · 기존과 겹치면 「기존」). 후보: A급 판정식 · `resultSourceCode` required vs 「서버가 안다」 · `status_code` 기록 전용 칸 값 · `worker_id` 주체(단말 토큰 전) · `:complete` 축(어느 칸도 안 옮김) · 「계획 수량 미달」 기준(LOT vs W/O) · 정정 행 대체값/차이값 · 정정의 정정 · 지연 실적 마감 뒤 편입(R83 — 039 와 같은 뿌리 → 기존) · `businessDate` 미저장(건수 누적 → 기존) · `achievementRate` 라벨(기존) · `lotAllocations` 합 = `goodQty`?
11. **PR 분할** — 초안 3(plan.md) · 예산 ≤350 · 코어 PR(있다면) ≤200. 초안: ① D1 마이그 선행 커밋 + 조회 GET 3(목록·단건·`lot-lifecycle-events` · sonnet) ② `POST` 실적 + 배분 + L1(opus · 코어 재사용만이면 일반 PR) ③ `:correct` + `:request-approval` + `:complete`(opus) · M1 체인 e2e 는 ④ 또는 별도. 각 PR 파일 목록·예상 diff·**단위/e2e 테스트 이름 목록**(e2e 로 못 가는 가드 — `result_sequence` 경합 · 다섯 합 0 · 슬롯 아닌 lot · `skipped` 처리 · A급 판정 경계 · 정정의 정정) · 스택 순서.
12. **인계 모양** — I-10(`material_usage_allocation.production_result_id`·`output_lot_id` 를 투입이 채운다 — 실적이 뭘 남겨야 하나) · I-19(검사 의뢰가 `production_result_id` 를 참조 — `inspection_request` FK) · I-25(인계가 실적을 읽나) · I-26(개체 발번이 `lot` 을 읽나) · I-11(세션이 열리면 `work_session_id` 필수가 되나) · I-9(제품 입고가 `PRODUCTION_RESULT` source 로 실적 id 를 참조하나 — 기존 GR 코드 실측).
13. **통합 계획 대조표** — `plan.md` §1 41·§3 104·§4 122·§7 209 · `plan-api.md` S16(posting 「있음」 모순 · `status_code` 「칸 불필요」 · PR 5→3) · S08(`:complete`·`lot-lifecycle-events` 배정 이동) · `plan-integration.md` 264~272(`material_usage_allocation` 같은 트랜잭션 — I-10 몫이면 정정) · `plan-uiux.md` U24·U25 와 다른 점. README §1-2 3조건 중 걸리는 것이 있으면 맨 위에 ⚠.

금지: 구현 코드 작성 · 계약 파일 수정 · 다른 문서 수정 · 값 지어내기(값 목록이 없으면 「없다」고 적고 §2 로 판정) · `omf-mes`·`omf-mes-client` 저장소 쓰기 · `contracts:update`/`contracts:check` · `git` 쓰기.

최종 응답(5줄 이내): README §1-2 3조건 판정 · 마이그 건수 · 문의 신설 건수(제목) · PR 분할 최종안(건수·예산) · 멈춤 조건 해당 여부.
