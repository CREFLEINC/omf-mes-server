# I-8 개별 계획안 3관점 재검토 브리프 (README §1-2 「차이가 크다」 — 조건 ③(문의 045·046) 발동)

대상: `docs/coverage-100/slices/I-8.md`(849줄). 결과는 **자기 관점 파일 하나**에만 쓴다 —
`docs/coverage-100/slices/I-8-review-{api|uiux|integration}.md` (150줄 이내). 다른 파일 수정 금지, 구현 금지,
체크아웃 변경 금지(worktree `/Users/rangkim/projects/crefle/omf/apps/omf-mes-server/.claude/worktrees/i8-plan` · 브랜치 `docs/coverage-100-i8-plan` 그대로 — main e7fa784 기준 · I-7 ③④ 병합본(main d72b09e)과의 차이는 `src/trace/lot/`·`src/production/production-result/` 뿐이라 I-8 자리에는 영향 없음), `git` 쓰기 금지. 계약 `contracts/*.json` 은 읽기 전용(`jq`).
로컬 DB 는 읽기만(`psql` SELECT · `migrate reset`·`migrate diff --shadow-database-url`·`db seed` 절대 금지). Bash cwd 는 호출마다 리셋되므로 절대경로로 `cd`.

계획자 결론 요지:
ⓐ **마이그 0**(§2-4): `picking_line.status_code`·`inventory_reservation.status_code` 는 계약 `x-no-code-key` + 응답 required 라 **상수**(서버가 쓰는 자리 0) · `material_issue_request.status_code` 는 계약이 `LOGISTICS_DOCUMENT_STATUS` 를 이름으로 적어(시드 4값) I-6 의 `ISSUE_REQUESTED='REQUESTED'` 를 **`'REGISTERED'` 로 정정**(대상 행 0 · §4-3 · 상수의 집은 `logistics/material-issue-request/…constants.ts` + `material-issue.ts` 는 값만 고침).
ⓑ **코어 함수 둘**(§3): `pick(tx, PickMove[])`·`consume(tx, ConsumeMove[])` 를 `reservation-qty.ts` 신설(원시 UPDATE … RETURNING · 0행이면 400 `NEGATIVE_BALANCE`) · **`reserve()` 는 안 만든다**(걸 잔액 행을 정할 근거 0 · 사용처 0 · 첫 사용처 I-22) · 잠금은 호출자(`lockBalancesInOrder`) · `:pick` 잠금 순서 `picking_line → balance → reservation` · **`consume()` 은 `post()` 안이 아니라 `issue-posting.ts` 가 손검사 «앞»에서 부르고 손검사식에 소진량을 더한다**(§3-5 · I-4 파일 수정 ~25줄) · `reverse()` 는 `picked_qty` 를 안 되돌린다(무변경 · 알려둘 것) · 감지 수단은 트리거가 없어 **정적 가드 spec**(`balance-write-guard.spec.ts`).
ⓒ **`POST` 요청**(§4): 채번 tx 밖 · W/O `CANCELLED`/`CLOSED` 면 400 `STATE_LOCKED`(038 반쪽) · `reasonCode` 선택 · 중복·BOM 밖 허용 · 예약 ✕ · 피킹 ✕ · If-Match 받되 무시 · ETag ✕ · 「요청 0건 = 실패」 판정 안 만듦(R-23 답).
ⓓ **피킹 지시 생성 주체**(§5): 계약에 생성 오퍼레이션 0 · 배정 축 3겹(출발 창고·LOT/위치·부족 시) 부재 → 지어내지 않음 → **문의 045**(예약 시점 01 vs 04 어긋남도 함께). `:pick` 은 e2e 픽스처 INSERT 로만 도달 · `GET /inventory/reservations` 는 오늘 영원히 빈 목록.
ⓔ **`:pick`**(§6): 라인 FOR UPDATE(경로 두 id 불일치도 404) → If-Match 409 먼저 → 이미 출고된 라인 400 `STATE_LOCKED` → `pickedQty ≤ planned`(400 `RANGE`) · `lotId` 불일치 400 → 차단 둘(`lot_hold` 미해제 400 · `blocks_picking` — 오늘 0행) → Δ = pickedQty − picked_qty **대체** → `pick()` → 라인 UPDATE. `status_code` 둘 다 안 옮김(`transitions.ts` 무변경) · `X-Worker-No` 없으면 400 `REQUIRED` · `mdm.worker` 조회 안 함 · 저장 칸 없음.
ⓕ **shortage**(§7): BOM 축 **`production_plan.bom_id` 단일**(plan-integration 282 「lot 스냅샷」을 뒤집음 · 폴백 없음) · 소요식을 `src/core/bom/requirement.ts` 로 이관(`:release` 와 한 함수 · `scrap_rate` ✕) · 같은 품목 두 줄이면 합치고 `bomComponentId` 생략 · 기출고 = `goods_issue_line → picking_line → picking_order(MATERIAL_ISSUE_REQUEST) → material_issue_request.work_order_id` 조인 · `POSTED` 만 · `issued_qty` 칸은 안 씀 → **문의 046**.
ⓖ **조회 5**(§8): 정렬 PK 역순 · 계약이 null 허용한 칸은 널로 냄 · `reservations` 는 `src/inventory/balance/` 안 파일 둘(I-7 R-13) · `openOnly` 원시 조각 · `pickSequenceRank` 는 지시 안 같은 품목 라인 사이 순위(FEFO/FIFO · 키 널이면 생략) · `held` 는 LOT 집합 1회 조회.
ⓗ **횡단**(§9): 403 추가 1(`:pick` → `manual-permissions.ts`) · 멱등 2 · If-Match 선택 2 · ETag 0 · 새 error code 0 · 도메인 간 service 호출 0 · 남의 파일 셋 수정(`issue-posting.ts`·`material-issue.ts`·`release-plan.ts`).
ⓘ **문의 2**(045 피킹 지시·예약 생성 자리 부재 · 046 `issued_qty` 올리는 자리 부재) · 해소 보고 2(#145 · omf-mes#213) · 기존 인용 6 · 알려둘 것 12.
ⓙ **PR 4**(§11 · 전부 opus · 직렬): ① 코어 ~150(≤200) · ② 조회 3 + `core/bom` 이관 ~325 · ③ `POST` + `reservations` ~275 · **④ `:pick` + 피킹 조회 2 + `consume()` 배선 + M2 마디 e2e ~385**(⚠ README §6 예산 350 초과 · 한도 400 근접 — 계획자는 「한 벌로 내고 리뷰가 넘기면 ④a/④b」).

## 공통으로 읽을 것
- `docs/coverage-100/README.md` §1-2·§2·§5·§6 · `docs/coverage-100/plan.md` §0 #10 · §1 43·44·55 · §2 83 · §3 106 · §4 · §5 규칙 7·9·10 · §7 211 · §8
- `docs/coverage-100/slices/I-8.md` 전문 · `brief-I-8.md`(`/Users/rangkim/.claude/jobs/a3d5599b/tmp/brief-I-8.md` · 읽을 것 12·담을 것 13) · `I-4.md` §3-2·§3-3·§5-3·§7-2·R-1·R-7 · `I-6.md` §4-2·§4-3·§8-6·R-7·R-18·R-23 · `I-7.md` §4-3·R-13·**§11-2**(구현이 계획과 달리 한 것 — 계획서 구멍의 선례)
- 계약 `contracts/logistics-01자재창고.json` 경로 1662·3756·3921·3964·4006·4094·4136 · 스키마 `InventoryReservation` 10817 · `MaterialIssueRequest` 12229 · `…Create` 12312 · `…CreateLine` 12388 · `…Line` 12448 · `MaterialIssueShortageLine` 12518 · `PickingLine` 12601 · `PickingLinePick` 12769 · `PickingOrder` 12809 · `InventoryBalance.reservedQty/pickedQty` description · `x-no-code-key` 전건 · 04 계약 `shipment-requests …:pick` 의 예약 문장
- 실재 코드(main): `src/core/inventory-posting/`(`inventory-posting.service.ts` 30~41·45·105·245~294 · `balance-lock.ts:39` · `posting.types.ts`) · `src/logistics/goods-issue/issue-posting.ts`(87·120~135·149·299) · `goods-issue-rules.ts:61·181` · `src/production/work-order/material-issue.ts`·`release-plan.ts:78-86·104`·`work-order-release.service.ts:86-87·105~` · `src/inventory/balance/` · `src/core/document-state/transitions.ts` · `src/common/permissions/derived-permissions.ts:175·181`·`manual-permissions.ts` · `src/common/http/master-write.ts` · `src/common/errors/error-codes.ts` · `src/logistics/document-cancel/document-cancel.service.ts:75-78` · `src/logistics/document-progress/document-progress.controller.ts:74-79`
- 물리 `prisma/schema.prisma`: `inventory_balance` 484~ · `inventory_reservation` 575~ · `material_issue_request` 957~ · `_line` 982~ · `picking_line` 1003~ · `picking_order` 1034~ · `goods_issue_line`(`picking_line_id`) · `bom` 2389 · `bom_component` 2417 · `lot` 3510 · `lot_hold` · `judgment_type_control` 4700 · `production_plan.bom_id` · baseline 마이그의 CHECK(`ck_picking_qty`·`ck_reservation_qty`·`ck_material_issue_line_qty`)·트리거 6(`trg_inventory_balance_qty`)
- 시드 `prisma/seed.ts`: `LOGISTICS_DOCUMENT_STATUS` 1069 · `MATERIAL_ISSUE_REQUEST_REASON` 449 · `PICKING_TYPE` 678 · `RESERVATION_TYPE` 686 · `JUDGMENT_TYPE` · 채번 1393·1398
- 기존 문의 `docs/design-inquiries/README.md` + 030·031·037·038 · 화면 사본 `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/01/M-01-08-자재출고피킹.md`(§5-1·§5-5·§5-7·§6·§8 #1~#7) · `02/W-02-10-추가자재출고요청.md`(§3 ②·§5-3·§6·§8 #3·#4) · `02/W-02-04`(§6) · `02/W-02-01`(§5-4 예약 원천)
- 자기 관점: api → `plan-api.md` S04 111~142 · §5.3 · §5.4 · §7 에러표 / uiux → `plan-uiux.md` 45 · 212~223(U11) · 579 · 619~626 · 695 · 1020~1025 · 1114 · §9-1 #7 · §9-3 1197~ / integration → `plan-integration.md` 12·15 · 36~42 · 107 · 132 · **150** · 240 · **276~283** · 660(§9 반박 2) · 772~776 · §4-1 M2

## 판정할 것 (각 항목에 ✅ 동의 / ✏ 수정(대안+근거) / ⛔ 반대(근거))
1. **문의 045·046 이 정말 새 문의인가**(§10-2) — 045 가 `plan-uiux.md` 579·`plan.md` §7 211(결정 10 ↔ C-1)·038 과 겹치나 · 예약 시점(01 「요청 시」 vs 04 「피킹 시」)을 045 에 묶는 게 맞나 **아니면 「계약끼리 모순」(멈춤 조건 ③)인가** — 계획자는 「01 쪽을 안 하는 것으로 양립」이라 했다 · 046 이 문의인가 「알려둘 것」인가(`issued_qty` required·readOnly 칸을 0 으로 내리는 것이 계약 위반인지) · 해소 보고 2(#145·#213)가 맞나 · 알려둘 것 12 중 문의로 올릴 것 · 기존 인용 6 의 번호가 맞나.
2. **마이그 0 · 상수 정정**(§2·§4-3) — `picking_line.status_code` 상수를 «누가» 넣나(생성 경로가 없으니 픽스처만 — 그러면 서버 코드에 상수가 있을 이유가 있나) · `inventory_reservation.status_code` 도 같은 물음 · **`'REQUESTED'`→`'REGISTERED'` 정정이 I-6 e2e·단위를 어디까지 건드리나 실측**(`grep -rn REQUESTED src test`) · 상수의 집을 둘로 두는 것(`logistics` 정본 + `production` 복사)이 「같은 값 두 줄」 e2e 로 충분한가 vs `src/core/` 상수 한 줄(계획자는 기준 5 로 기각) · 대상 행 0 확인 · 두 릴리스 규칙 미해당.
3. **코어**(§3) — 시그니처(`BalanceDimension` 11칸 vs `BalanceLockKey` 7칸 — 품질·재고·소유 3칸을 코어가 받는 것이 맞나 · `field` 를 코어가 받는 것) · **`consume()` 을 손검사 «앞»에 두고 손검사식에 소진량을 더하는 방식**(대안: 되읽기 · 대안: `post()` 에 `pickingLineId` 축 — 기준 5 기각이 맞나) · `pick()` 예약 있음 갈래의 하한 3개가 CHECK·트리거를 정확히 앞당기나(`ck_reservation_qty` 실측 식) · Δ<0 되돌림에서 `consumed_qty` 감소가 CHECK 를 어기지 않나 · 잠금 순서 `picking_line → balance → reservation` 이 출고 경로(`balance` 만)와 교착이 없나 · `reverse()` 무변경 판정(취소 뒤 재출고 400 — 알려둘 것으로 충분한가 · I-5 인계) · `reserve()` 를 안 만드는 것이 `plan.md` §3 106·plan-integration 279 와 어긋나는데 계획 PR 이 그것을 고치는 것으로 닫히나 · 정적 가드 spec 의 정규식이 오탐/미탐(`$queryRaw` 문자열 · `inventory_balance` 를 읽기만 하는 파일) 없이 서나 · 파일 분리(`reservation-qty.ts` 신설)와 ≤200.
4. **`POST` 요청**(§4) — 순서·검증표 · `STATE_LOCKED` 대상 상태 집합 {CANCELLED, CLOSED} 가 맞나(`COMPLETED`? · I-6 상태 표 실측) · `plantId` 를 W/O→계획→생산오더로 푸는 왕복 · `destinationLocationId` 존재만 보고 창고 정합은 안 보나 · `requestedBy` 칸(계약 null 허용 — 세션 계정을 넣나 널인가) · `line_no` 부여 · If-Match 무시 판정 · `LINE_REQUIRED`·`RANGE` 코드가 `error-codes.ts` 에 실재하나 · 038 흔적 주석.
5. **`:pick`**(§6) — ③ 「이미 출고된 라인 400」 판정 축(`goods_issue_line.picking_line_id` 존재 · 취소된 출고는?) · Δ<0 정정 허용이 맞나(계약 `PickingLinePick` 문장 실측) · 잔액 차원 되읽기 2행+ 400 `INVALID`(031 과 같은 문장) · `blocks_picking` 판정문이 I-4 R-7 `blocks_issue` 와 같은 모양인가 · `lot_hold` 축이 출고와 반대인 것(계약 문장 실측) · `X-Worker-No` 존재만 보고 마스터 조회 안 하는 것(I-7 §4-3 과 다른 규약 — 기준 5 가 맞나 vs 계약 ⌜없으면 거부⌝의 「없음」에 «마스터에 없음»이 드나) · `picking_order.status_code` 안 옮김 · If-Match 대상이 라인 `version_no` 인 것 · 404 두 갈래.
6. **shortage**(§7) — BOM 축 `production_plan.bom_id`(plan-integration 282 를 뒤집는 근거가 서나 · 계획의 BOM 이 바뀔 수 있나 실측 — `PUT /production/production-plans` 유무) · `src/core/bom/requirement.ts` 이관이 「사용처 둘」로 정당한가 · 재수출 3줄 vs `release-plan.ts` import 직접 변경 · 합치기·`bomComponentId` 생략 · 정렬 · **기출고 조인 체인이 물리에 서나**(`goods_issue.source_document_type_code='PICKING_ORDER'` 축과 `picking_line_id` 축 중 무엇으로 W/O 에 묶는지 실측 · `goods_issue.status_code` 값) · `workOrderId` 필수 400 `REQUIRED`·404 미선언.
7. **조회 5**(§8) — where 매핑 전수(계약 `parameters` 대조) · 정렬 PK 역순(대안: `created_at desc` · 계약 L-4 실측) · 널 vs 생략 판정이 `plan.md` §5 규칙 7 과 어긋나지 않나(계약 `type: [..,"null"]` 칸 목록 실측) · `reservations` 모듈 위치 · `openOnly` 원시 조각 · `pickSequenceRank` 모집단·FEFO/FIFO 키(`item.shelf_life_days`·`fifo_policy_code` 실재?) · `held` 다건이면 최신 · `statusCode` 필터 대조 안 걺.
8. **횡단·PR 분할**(§9·§11) — 403 1건 `manual-permissions.ts`(도출표를 손대지 않는 규약 실측) · 인계 6행(I-9 `goods_issue` 축 · I-10 · I-22 시그니처 · I-24 · I-5 · 통합자 픽스처) · **PR 4 순서·예산 — ④ ~385 가 README §6 예산 350 을 넘는다: 미리 ④a(피킹 조회 2 + 뷰)/④b(`:pick` + `consume()` 배선 + M2 마디)로 가르라고 할 것인가**(계획자: 한 벌로 내고 리뷰가 넘기면 가른다 — I-7 R-4 「죽은 코드」 논거가 여기 서나 · 대안: `consume()` 배선을 ①(코어 PR)이나 ③으로 옮겨 ④를 줄인다 — 코어 PR 에 I-4 파일 수정이 들어가는 것이 ≤200·「코어 전용」 규칙에 맞나) · ② ~325 도 350 안이나 `core/bom` 이관을 ①로 옮길지 · 각 PR 단위·e2e 이름이 가드를 덮나 · §11-1 픽스처(`insertPickingOrder()` · `inventory_reservation` 1행 · `judgment_type_control`) · cleanup 순서 · **M2 마디 e2e 의 `POST /goods-issues(source=PICKING_ORDER)` 가 I-4 규칙(`goods-issue-rules.ts:61·181`)을 통과하는 픽스처 모양** · `logistics-goods-issue.e2e-spec.ts` 무변경 회귀.
9. 자기 관점 계획서와 I-8.md 가 어긋나는 자리 중 **구현에 영향 주는 것**만. §12 대조표 13행의 자기 관점 행을 실측으로 확인. uiux 는 화면 명세(`M-01-08` §5-5 트랜잭션 · §5-7 결품 · §6 보류 차단 · `W-02-10` §3 ② 열 · §6 사유 · `W-02-04` §6)와의 정합을 실측으로.

마지막에 5줄 요약: 「재수립 결과 — I-8.md 에 반영할 수정 N건(목록), plan.md 에 반영할 것, 문의 최종 건수, PR 분할 최종안(④ 가름 여부 포함)」.
