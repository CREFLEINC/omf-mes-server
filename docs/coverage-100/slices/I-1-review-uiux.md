# I-1 재검토 — **UI/UX 설계 관점**

> 근거: 화면 명세 `W-06-15`·`W-CO-09`·`W-03-09`·`M-01-13`·`W-01-02` 정독(읽기 전용) ·
> 계약 `contracts/app-공통.json`(`a6a87e1`) `jq` 실측 · `plan-uiux.md` §1-2 U1·§6-2·§8-5·§9-3.
> 실측일 2026-09-06. **화면이 그 갈림길에서 무엇을 보이게 되는가**로만 가른다.

## 1. 문의 후보 — 018 ✅ · 019 ✏ · 020 ⛔ · **021 신설**

### 018 `inProgressCount` 유형 축 근사 — ✅ 동의(새 문의 · §2 절차 판정도 동의)

화면이 이 수를 쓰는 자리가 둘이고 **둘 다 «경고»다** — W-06-15 §5-6 끄기 확인 다이얼로그(「진행 중인 요청
3건은 그대로 진행됩니다」) · §5-7 편집 구획(「진행 중 N건」 + 「옛 결재선으로 끝난다」). 유형 축 근사는
**과대 계상**이라 경고 방향으로 안전하다 — 적게 말하지 않는다. 가장자리 ✅ · 2단계 기준 3 ✅.

✏ 요청서에 한 줄 더 묻자 — A6 인덱스가 서면 한 유형의 활성 결재선은 「사업부 지정본 N + 공통본 1」이고
**그 N+1 개가 전부 같은 수를 보인다**(W-06-15 §3 좌측이 결재선을 나란히 세운다). ⇒ 「§5-7 문구를 「이
**유형**으로 진행 중 N건」으로 바꿔도 되나」를 함께 묻는다. 근사가 «틀린 수»가 아니라 «다른 축의 참값»이 된다.

### 019 `screenId` 「특채·한도승인 → W-03-09」 — ✏ 새 문의는 맞다. **물음을 다시 세워야 한다**

1. **«특채»는 `approvalTypeCode` 9값에 없다**(실측 · W-06-15 §8-2 「실측 후보 9 중 특채만 빠진 그대로다」).
   ⇒ 계약이 「⭐ 첫 확정 규칙」이라 부른 그것이 **실제로 걸리는 유형은 `IQC_SKIP` 하나뿐**이다.
2. **그 `IQC_SKIP` 의 승인 화면이 두 화면에 걸쳐 열려 있다** — W-CO-09 §5-1 「⚠ `W-01-02` 는 **예외**다 …
   판정은 업무 화면, 결재는 결재함」 · W-03-09 §8 #3 「한도승인 요청은 어느 화면이 만드나 — `W-01-02` 로
   보이나 **확인 안 함**」(미결·표시). 계약 `x-internal-note` 가 의심한 자리를 **화면 명세 둘이 이미 열어 뒀다.**
3. `plan.md` §7 에 「승인 유형 9값에 특채 없음 — 결재함으로 찾는 길 확인 | **I-21**」 행이 있다. **겹친다.**

⇒ 제목을 「`IQC_SKIP`(한도승인)의 승인 화면이 `W-01-02` 인가 `W-03-09` 인가 — 화면 명세 둘이 서로 열어 둔 채다」
로 세우고 `plan.md` §7 의 I-21 행에 「019 로 함께 묻는다」를 달아 **중복 발행을 막는다.**
판정(키 생략 · `openable=false`)은 ✅ 그대로.

### 020 `INBOUND_LOT` 대응 표 — ⛔ **문의가 아니다. 철회를 권한다**

- **화면 명세가 이미 답했다** — W-01-02 §0 「**대상은 자재LOT(`trace.lot`)다**」 · M-01-13 §0
  「`target_type_code`=`INBOUND_LOT` 고정 … IQC 대기 중인 LOT」. 상신 경로도
  `POST /trace/lots/{lotId}:request-iqc-skip` 이라 `targetId = trace.lot.lot_id` 다. §2 **0단계에서 끝난다.**
- 계약이 가리킨 `omf-mes-server#74` 는 **우리 저장소 이슈**다. 설계에 물을 것이 아니라 표지를 우리가 고친다.

⛔ 대신 **구현 블로커가 하나 있다(실측)** — `app.entity_type_registry` 에
`@@unique([schema_name, table_name], map: "uq_entity_type_table")` 가 있고 `LOT → (trace, lot)` 행이
**이미 있다**(`prisma/seed.ts:1305`). ⇒ `INBOUND_LOT → (trace, lot)` 을 **넣을 수 없다.**
I-1.md §2-5 의 결론은 맞되 **이유가 다르다** — 「계약이 미정이라」가 아니라 「등록부가 표 하나에 유형 하나만
받는다」다. 같이 넣겠다는 `INVENTORY_ADJUSTMENT`·`PURCHASE_ORDER` 두 행은 **충돌 없다** ✅(등록부 17행 실측).

### ⭐ 021 (신설) `ApprovalTarget.displayName` 이 `required` 인데 **사람이 읽는 이름의 원천이 없다**

계약: 「사람이 읽는 대상 이름. **서버가 만든다**」 · example `"기타 출고 · 문서 #4412"` · **required**.
I-1.md §2-5 은 `entity_type_registry` 에서 낸다고 적었는데 —

- **그 표에 표시명 칸이 0개다**(실측 — `entity_type_code`·`schema_name`·`table_name`·`id_column_name`·`is_active`+감사 2).
- 공통코드도 막혔다 — `APPROVAL_TYPE` 은 `seed.ts:1403` 의 **「enum 으로 승격돼 그룹이 필요 없어진 것」**
  목록에 있어 `code_value` 행이 없다. `CD-APPROVAL-TARGET-TYPE` 도 `enum` 이라 `codeGroupCode=` 호출이 없다.

⇒ 서버가 낼 수 있는 것은 **「`INBOUND_LOT` #31」뿐**이다. W-CO-09 §5-3 이 「목록에 보일 업무 값이 **사유
하나뿐**」이라 적은 화면에서 대상 구획의 마지막 식별 정보까지 코드 문자열이 된다. 하노이는 한/베 두 축이라
서버에 한국어 표를 박는 것도 답이 아니다(`localized_text` 는 «행» 축이지 «유형» 축이 아니다).

**같은 뿌리의 물음이 하나 더 붙는다** — W-06-15 §5-6 은 끄기 다이얼로그에 「`W-01-06` … 상신이 막힙니다」를
요구하며 「⚠ **막히는 화면 목록을 화면이 지어내지 않는다 — 계약이 내려 준다**(§9-2)」로 못박았다. 실측 —
`:deactivate` 는 「화면이 무엇이 막히는지 먼저 보인다」라고만 하고 대응을 **안 준다**. `ApprovalRoute` 에도
칸이 없고 1:1 도 아니다(계약 스스로 「자재 폐기 `W-01-06` 와 제품 폐기 `W-04-10` 는 «같은» `GOODS_ISSUE_DISPOSAL`」).

⇒ **021 을 한 건으로 묶는다**: 「승인 «유형»과 «대상 유형»의 사람이 읽는 이름을 서버가 어디서 얻나 —
`displayName`(required)과 결재선 끄기 경고가 같은 원천을 요구한다」. §2 절차: 0단계 선례 없음 · 1단계
**본길**(8 유형 전건) · 계약이 「서버가 만든다」만 적고 원천을 안 적었다 ⇒ **문의**. 그동안은 I-1.md 안대로
유형 코드 + `#{targetId}` 로 내고 흔적을 남긴다(새 필드·표는 안 만든다 · 기준 3).

⛔ **I-1.md §6-4 「문의로 올리지 않는 것 ②」를 갈라야 한다.** 거기 든 W-CO-09 §8 #8 은 화면 «필터 칸»의
표시명(설계 소관 · `omf-mes#352`)이고 `displayName` 은 **계약이 「서버가 만든다」로 못박은 `required` 값**이다.
둘을 한 덩이로 접은 것이 **021 을 놓치게 했다.**

## 2. `request()`·`assertNoOpenRequest`·`assertApproved` 를 I-2 로 — ✅ 동의(e2e 에 조건 하나)

근거 확인 ✅ — 상신 첫 사용처는 `assignment.tsv:18` I-2(`purchase-orders:request-approval`). 미뤄도 대기 없음.
⚠ **e2e 픽스처가 화면 경로를 한 번도 안 지난다.** I-1 의 결재함·결재 e2e 18건이 승인 요청 행을 만들 방법이
없어 `prisma` 직접 INSERT 로 심게 되는데, W-CO-09 §3·§7-1 의 `Stepper` 전체가 **「결재선 → `approval_step`
전개」의 결과물**이고 그 전개는 I-2 에서 처음 돈다 — 손으로 심으면 전개가 틀려도 통과한다. ⇒ **PR ③·④
픽스처를 `selectRoute()` 결과로 만든다.** I-2 에서 상신이 서면 그 e2e 를 상신 경유로 바꾼다.

## 3. J-8 후속 통지 = 상태 조회 갈래 — ✅ 동의

화면 셋이 **전부 같은 쪽으로 그려져 있다** — 콜백을 넣으면 화면 문구가 거짓이 된다.
W-CO-09 §5-7 「승인하면 요청이 승인 상태가 됩니다. **실제 처리(전기·출고·조정 반영)는 대상 화면에서 별도로
합니다**」 · W-01-02 §5-4 「승인해도 **ERP 입고 전표는 발행되지 않는다**」(「승인했으니 입고됐다」 오해를 화면이
막는다) · W-03-09 §5-5(승인 결과는 상태 전이 + `concession` 범위 유지까지, 실행은 출고 시점).
`assertApproved` 를 I-1 에서 안 만드는 것도 ✅ — 사용처 0.

## 4. §6-2 `screenId` 생략 + `openable=false` / §6-3 `approverIsActive=false`

**§6-2 ✅ 동의 — 다만 0단계에서 끝난다(2단계까지 갈 필요 없다).** 화면이 물러날 자리를 이미 그렸다 —
W-CO-09 §6 「대상 경로가 없는 유형 → ⚠ 「이 대상은 아직 열 수 있는 화면이 없습니다」 · 버튼 **비활성**」(§5-8
동일). `docs/계약-재검토-2026-09-04.md` §3 도 「세 자리를 구현할 때 **그대로 따른다**」로 이미 정했다.
⚠ **결과를 019 본문에 적어야 한다** — 8 유형 전건에 규칙이 없어 `openable` 은 **언제나 거짓**이고, W-CO-09 우측
「대상」 구획의 **유일한 액션**(「대상 화면에서 보기 ↗」)이 1차 내내 죽는다.

**§6-3 ✅ 동의 · 문의 대상 아님.** 화면 근거가 정확히 그쪽 — W-06-15 §6 「승인자가 비활성 사용자 → ⚠ 「이
사용자는 사용 중지 상태입니다 — **결재가 멈춥니다**」 · **저장은 허용**」. 참을 내리면 화면이 「괜찮다」고 말한다.
USER 행에서 조인이 비는 경우(삭제된 사용자)도 답이 같아 갈래가 하나다.

## 5. PR 4개 분할·순서 — ✅ 4분할 동의, ✏ 둘

화면 축으로는 PR ①+② = `W-06-15` 완성 · ②(결재함 조회)+④ = `W-CO-09` 완성. 400줄 제약 때문에 화면 단위로
더 못 자른다(`:activate`/`:deactivate` 를 ① 로 옮기면 ①이 ~460줄) → **현행 유지 ✅**.

✏ **(a) PR ① 단독으로는 `W-06-15` 가 열리지 않는다 — PR 설명에 적자.** 실측 — `ApprovalRouteUpdate` 의
프로퍼티는 `businessUnitId`·`minValue`·`maxValue` **셋뿐**이고 `isActive` 가 **없다**. ⇒ `is_active` 를 바꾸는
길이 `:activate`/`:deactivate`(PR ②)밖에 없어, PR ① 만으로는 W-06-15 §5-9 「사용 / 사용 안 함」과 §3 좌측
「(사용 안 함)」 행이 **만들어질 수 없다**(POST 는 항상 활성 생성). e2e 를 ② 로 몬 판단은 같은 사실을 본 것 ✅.

✏ **(b) 순서를 ③ → ① → ② → ④ 로.** §3-5 이 「`selectRoute` … 두 곳이 한 헬퍼를 나눠 쓴다」라 적었는데
그 헬퍼가 서는 PR ③ 이 **①·② 뒤**다. 지금 순서면 ① 이 지역 구현하고 ③ 이 다시 뜯는다. ③ 의 단위 테스트는
결재선 픽스처만 있으면 되어 선행이 없다(CLAUDE.md 「코어는 전용 PR」과도 맞다).

## 6. 계획서 ↔ I-1.md 어긋남 — 구현에 영향 주는 것 셋

**(1) 「나」의 주체 해석 — I-1.md 가 옳다. 두 계획서를 고쳐야 한다.**
`plan-uiux.md` §1-3 U1 은 「사번 헤더가 없으면 `assignedToMe`·`requestedByMe`·`myTurnOnly` 를 **400 으로 거부**」,
`plan.md` §5 #9 는 「`X-Worker-No` … 감사 칸이 아니라 **주체**」다. ⛔ **계약과 어긋난다** — 계약 `assignedToMe`
설명이 「승인 화면은 관리웹뿐이라(`W-CO-09`·`W-01-02`·`W-03-09`) **지금은 계정 토큰에서만 풀린다**」로 이미
갈랐다. 관리웹이 헤더 없이 부르는 것이 **정상**이라 400 은 W-CO-09 를 통째로 막는다. `docs/계약-되돌림-mdm.md`
**Y-5** 도 「주체는 계정 세션 … 헤더는 «누가 어느 단말에서» 만 덧붙인다」로 이미 정했다. ⇒ §5-5 의 복제 ✅.
⚠ 결과는 적어야 한다 — `actorOf` 는 세션이 없으면 401 이라 **M-01-13 「내가 올린 요청」 구획은 이 슬라이스에서
서지 않는다**(단말 토큰 부재). 다행히 M-01-13 §6 이 걱정한 「빈 목록 → 「요청이 없다」로 읽혀 다시 올린다」는
**일어나지 않는다**(401 은 조용하지 않다) ✅. 이미 보낸 「알려만 두는 것」(2026-09-05·-B)은 **공지 3자리만**
들었으므로 다음 전달분에 `?requestedByMe` 와 M-01-13 구획을 **한 줄 덧붙인다**(새 번호 아님).

**(2) `plan-uiux.md` §6-2 의 U1 e2e 요구가 I-1 에서 성립하지 않는다.** 「U1 의 e2e 는 … 「`W-01-06` 에서
상신 → `W-CO-09` 에서 승인 → `W-01-06` 에서 전기」 한 줄이어야 한다」인데 `goods-issues:request-approval` 은
**I-4**(`assignment.tsv:38`)다. ⇒ 그 한 줄은 **I-4 완료 시점**에 서고 `plan.md` §2 M1 체인에 적는다.
I-1 은 「결재선 정의 → 승인 → 상태만 바뀐다(원장 0건)」까지다.

**(3) `plan.md` §1 I-1 행 PR 3 → 4** — I-1.md §8-1 이 이미 적었다 ✅. 순서·선행·마이그레이션표는 손댈 것 없다.

⛔ 취향 차이라 뺀 것: PR 안의 파일 쪼개기 · 테스트 이름 표현 · `omitEmpty` 를 새로 만들지 여부(실측 후 판단).

## 5줄 요약 — 재수립 결과

1. **I-1.md 수정 6건** — ① 020 **철회**(대응 표는 W-01-02 §0·M-01-13 §0 과 상신 경로가 이미 답했다) ② §2-5 의 「등록부에 안 넣는다」 **이유 교체**(계약 미정 ✗ → `uq_entity_type_table` 에 `LOT → (trace, lot)` 이 이미 있어 넣을 수 없다) ③ §6-4 에 **021 신설** ④ §6-4 「문의로 올리지 않는 것 ②」를 **둘로 가른다**(화면 필터 표시명 = 설계 소관 / `displayName` = 서버가 낼 물음) ⑤ 019 **제목 재정의** + `openable` 이 8 유형 전건 거짓이 되는 결과 명기 ⑥ §7 PR 순서 **③→①→②→④** 및 「PR ① 단독으로는 W-06-15 가 열리지 않는다(`ApprovalRouteUpdate` 에 `isActive` 없음)」 명기.
2. **`plan.md` 반영 4건** — §7 에 I-1 3행(018·019·021) 추가 + 기존 「승인 유형 9값에 특채 없음 | I-21」 행에 「019 로 함께 묻는다」 교차표기 · §1 I-1 PR 3 → 4 · §5 #9 를 Y-5(주체 = 계정 세션)로 되맞춤 · §2 M1 체인에 「상신→승인→전기 한 줄은 I-4 에서 선다」 명기.
3. **`plan-uiux.md` 반영 1건** — §1-3 U1 둘째 행(「사번 없으면 400 거부」) **철회**. 계약이 「지금은 계정 토큰에서만 풀린다」로 갈랐고 400 은 W-CO-09 를 막는다.
4. **문의 최종 3건** — 018(`inProgressCount` 유형 축 근사) · 019(`IQC_SKIP` 승인 화면 `W-01-02`/`W-03-09`) · 021(승인·대상 유형의 표시명 원천). **건수는 3 그대로이고 구성이 바뀐다** — 020 철회, 021 신설.
5. **「차이가 크다」 판정은 ✅ 유지** — 셋 중 셋째에 걸린다. §1 순서표·§3 코어표·§4 마이그레이션표(A6 하나)는 그대로 서고, 재작업은 `plan.md` §7 + 위 두 계획서 정정뿐이다.
