# I-5 다형 취소 — `document-progress` 조회 2 · `:request-cancel` · `:cancel` + 역트랜잭션 코어 `posting.reverse()`(4건) — 개별 계획안 브리프 (계획만, 구현 금지)

산출물: `docs/coverage-100/slices/I-5.md` 하나. 저장소 파일을 고치거나 만들지 않는다(이 파일 하나만 쓴다). 체크아웃 변경 금지(브랜치 `docs/coverage-100-i5-plan` 그대로). `git` 쓰기 금지. `contracts/*.json` 읽기 전용(`jq`). `prisma migrate reset`·`migrate diff --shadow-database-url` 금지.

## 읽을 것 (순서대로)
1. `docs/coverage-100/README.md` — §1-2 「차이가 크다」·§2 판정 절차·§5 불변 제약
2. `docs/coverage-100/plan.md` — §0 #5(16행 · I-5 는 **I-4 직후 6번째** — 코어라 늦을수록 재작업 · 화면 W-01-13 이 늦게 온전해지는 것은 배포 노트) · §1 39행(I-5 · 4건 · 의존 I-3·I-4 · 코어 `posting.reverse()` · opus · PR 3) · 43행(⛔ I-8 과 직렬 — 같은 `inventory-posting.service.ts`) · 56·58~59행(I-23·I-13·I-14 가 I-5 에 의존) · 82행(M1 완료 조건: 「입고 취소 요청·승인·실행 → 역트랜잭션 → balance 원복」) · §3 101~102행(역트랜잭션 코어 PR ≤200 · `reversal_of_transaction_id`·`reversal_of_business_date` · `NEGATIVE_BALANCE` 400 / 어댑터 유형↔표는 `app.entity_type_registry` 에서 읽음 · 후속 판정 두 갈래 · `SUCCESSOR_EXISTS` 요청·실행 둘 다) · §5 152행 #12(승인 FK 는 업무 승인 하나만 · `*_CANCEL` 은 FK 를 쓰지 않는다 — 정본은 `approval_request.(target_type_code, target_id, approval_type_code)`) · §8 진행표(I-4 ✅ 276)
3. `docs/coverage-100/plan-integration.md` 148~149행(⭐ 다형 취소·역트랜잭션 두 줄 — 「J-7·J-8 을 두 번 짜면 반드시 갈린다」) · 155행(`screenId` 생략 헬퍼 — I-1 `omitEmpty` 재사용) · 169행 · 244~250행(I-5 상세 — 체인 마디 · 코어 · `successorCount`·`cancellable` 판정 · ⭐ `:cancel` 승인 후 재판정 e2e 「승인 → 그 사이 후속 생성 → 실행 400 → 승인은 살아 있음」 · 취소 흔적은 `document_cancellation`) · 401행(I-23 출하 취소가 같은 규약의 두 번째 구현) · 496·506행(왜 W/O 보다 앞인가) · 568행 · 588행(⛔ I-5 ∥ I-8 금지) · 661·663행(역트랜잭션 4벌 · 유형↔표를 코드에 박지 않는다) · 731~735행
4. `docs/coverage-100/plan-api.md` 165~182행(S06 — 마이그 「없음」 · posting 「전기된 문서면 역트랜잭션, 전기 전이면 상태만(`CancelResult.reversed`)」 · PR 3 초안 · `DocumentProgress.screenId` 생략 · 횡단 표 4행: `:cancel`·`:request-cancel` 멱등 ✓·If-Match 필수·403 ✓) · 71행(`inbound_receipt_line.lot_id` 가 후속 판정에 필요 — I-3 이 이미 세웠다: 실측) · 722행(⭐ `SUCCESSOR_EXISTS` 재판정 두 번 — J-8) · 744~760행(A. `LOGISTICS_DOCUMENT_STATUS` 4값 · `document-request-cancel` `REGISTERED`·`POSTED` → `CANCEL_REQUESTED` · `document-cancel` `CANCEL_REQUESTED` → `CANCELLED` 조건부 원장) · **766~800행(B. 다형 취소 경로 도식 — 하나의 컨트롤러·서비스 · 유형별 어댑터 3 · ⛔ If-Match 토큰 출처는 대상 문서의 상세 GET · 후속 판정 두 갈래 · `cancelBlockedReasonCode` 5값이 `cancellable` 과 «같은 함수» · 출하는 이 경로가 아니다)** · 936행 · 1038행
5. `docs/coverage-100/plan-uiux.md` 289~292행(4건 × 화면 `W-01-13` · 멱등·ETag) · 1027~1033행(⑫ 파생 판정 3칸 · `TYPE_NOT_CANCELABLE` — 목록은 **9종**을 내는데 취소 실행은 3종뿐 · 나머지 6종은 회색 버튼) · 1151행(U18 — ETag 는 대상 문서 리소스 상세 GET 이 준다)
6. **선행 결과물** — `docs/coverage-100/slices/I-4.md` §0-재수립 **R-2**(`transitions.ts` `'logistics.goods_issue.status_code'` 키 «안에» 액션만 더한다 — 키를 다시 만들지 않는다) · **R-12 ⓐ**(취소가 잔액을 되돌릴 때 R-1 의 7칸 오름차순 한 문장 불변식 · 입고 `postReceipt` 순서를 코어 차원에서 한 번에 · `NEGATIVE_BALANCE` 코드 재사용) · §2 223행(`goods_issue` 에 취소 흔적 3칸이 **이미 있다** — `cancelled_at`·`cancelled_by`·`cancellation_reason_code`) · §3-7 · §4(`assertApproved` 시그니처 — `approvalTypeCode` 필수 · 취소 승인은 `GOODS_ISSUE_CANCEL`) · §5 상태기계 표 · `I-3.md` **R-12 ⓐ**(취소가 `received_qty` 를 되돌릴 때 부모 P/O 오름차순 `FOR UPDATE` + 하한 손검사) · **ⓑ**(`inbound_receipt.status_code` 축을 `transitions.ts` 에 열 때 §7-4 임시 자물쇠를 걷을지 재판정) · **ⓒ**(취소가 `lot_id` 가 가리키는 LOT 을 어떻게 두나) · **ⓕ**(부착 라인 치환과 LOT 어긋남 — 026 답 대기) · **ⓘ**(부모 `FOR UPDATE` 순서 맞추기) · §7-4(계약 밖 자물쇠) · 문의 026 행(655행 · 입하 `POSTED` 도달 경로가 없다 — **취소 전이가 `REGISTERED` 에서도 열리는가**) · `I-1.md` 승인 코어 절 · `approval.mapper.ts:83`(`screenId` 생략 선례)
7. 실재 코어: `src/core/inventory-posting/inventory-posting.service.ts`(187줄 · `post(tx, PostingInput)` — 잔액 갱신 순서·잠금·`inventory_transaction(_line)` 쓰는 모양 · 멱등키 · **`reverse()` 는 없다: 이 슬라이스가 만든다**) · `posting.types.ts`(`PostingEndpoint`·`PostingLine`·`PostingInput`·`PostingResult{inventoryTransactionId, businessDate, alreadyPosted}`) · `src/core/approval/approval.service.ts`(`request`·`assertNoOpenRequest`·`assertApproved:141`·R-4 주석 `:136`) · `src/core/document-state/transitions.ts`(`'logistics.goods_issue.status_code'` 키 152~158행 · `work-order-cancel` 선례 106~ · `inbound_receipt`·`goods_receipt` 키가 있는지 실측) · `src/core/numbering/`(역트랜잭션 번호 `reversalTransactionNo` 를 채번하나 — `inventory_transaction` 번호 칸이 있는지 실측) · `src/core/lot/`
8. 기존 도메인 선례: `src/logistics/goods-issue/`(`issue-posting.ts` `postIssue()` — 라인별 `from` 모양 · 되짚기 `inventory_transaction_line_id` · 상태 잠금 · `goods-issue.service.ts` `post()` · `goods-issue-update.service.ts` `requestApproval` — 취소 상신의 거울상) · `src/logistics/goods-receipt/receipt-posting.ts`(입고 원장 · **역트랜잭션이 되돌릴 `to` 라인**) · `src/logistics/inbound-receipt/`(`received_qty` 가 P/O 라인에 어떻게 얹히는지 — 취소가 되돌릴 자리 · `lot_id`) · `src/logistics/purchase-order/purchase-order.service.ts:276-`(`requestApproval` 선례) · `src/app/approval/`(결재 `:approve` · `approval.mapper.ts` `omitEmpty`) · `src/common/permissions/derived-permissions.ts`·`manual-permissions.ts`(4건 중 도출표에 이미 있는 것) · `src/common/master`(`runIdempotent`·`runVersioned`·`ifMatchVersion`) · `src/common/errors/error-codes.ts`(`SUCCESSOR_EXISTS`·`ALREADY_CANCELLED`·`CANCEL_IN_PROGRESS`·`STATE_LOCKED`·`TYPE_NOT_CANCELABLE`·`NEGATIVE_BALANCE` 유무 실측)
9. `prisma/schema.prisma` — `inventory_transaction`(610~ · `reversal_of_transaction_id`·`reversal_of_business_date` 622~625 · 복합 FK `fk_inventory_transaction_reversal` · 파티션) · `inventory_transaction_line` · `inventory_balance` · `document_cancellation`(3731~ · `previous_status_code`·`reason_code`·`reason_detail`·`cancelled_at`·`cancelled_by` · 인덱스 `ix_document_cancellation_target`) · `entity_type_registry`(3747~ · `schema_name`·`table_name`·`id_column_name`) · `goods_issue`(736~ · 취소 3칸)·`goods_receipt`(803~)·`inbound_receipt`(865~) 의 `status_code`·`version_no`·`source_document_*` · `material_consumption`(LOT 을 가리키는 재고 사용 축 실측) · `picking_order` · `approval_request`(76~). check/유일 제약을 `prisma/migrations/` 에서 grep(`ck_inventory_transaction*`·`reversal`).
10. `prisma/seed.ts` — `ENTITY_TYPES`(1291~ · `INBOUND_RECEIPT`·`GOODS_RECEIPT`·`GOODS_ISSUE`·`INBOUND_RECEIPT_LINE` 있음 — **9종 조회 유형이 전부 등록돼 있는지** · `PICKING_ORDER`·`MATERIAL_CONSUMPTION`·`INVENTORY_TRANSACTION`·`STOCK_TRANSFER` 등 유무) · `CD-CANCELABLE-DOCUMENT-TYPE` 코드그룹 유무 · `LOGISTICS_DOCUMENT_STATUS` 4값 · 취소 `reason_code` 코드그룹(`CANCEL_REASON`?) 유무 · `approval_route` 시드에 `*_CANCEL` 3유형(`INBOUND_RECEIPT_CANCEL`·`GOODS_RECEIPT_CANCEL`·`GOODS_ISSUE_CANCEL` — 이름은 시드에서 실측)이 있는지 — e2e 「요청 → 승인 → 실행」이 돌려면 필요 · `inventory_transaction` 유형 코드(`INVENTORY_TRANSACTION_TYPE`?)에 «역» 유형이 있는지
11. `test/logistics-goods-issue.e2e-spec.ts`(`makeLot()`·`stock()`·`insertRegisteredIssue()`·`stockedIssue()`·`postIssue()` — 잔액 픽스처 재사용) · `test/logistics-goods-receipt.e2e-spec.ts`(입고 원장 픽스처 · cleanup 순서) · `test/logistics-inbound-receipt.e2e-spec.ts` · `test/app-approval-decision.e2e-spec.ts`(결재 `:approve` 를 e2e 로 부르는 선례 · `goods_issue.count()`) · `test/inventory-*.e2e-spec.ts`(원장·잔액 조회 — 역트랜잭션 뒤 잔액 원복을 검증할 GET)
12. 계약 원문: `contracts/logistics-01자재창고.json` 의 `/logistics/document-progress`(2148~ · description 「⭐ successorCount·cancellable 을 서버가 판정」 전문 · 쿼리 파라미터 전건 — `documentTypeCode` 9종 enum · 페이징·정렬)·`/{documentTypeCode}/{documentId}`(2300~)·`:request-cancel`(6666~ · `x-code-key: CD-CANCELABLE-DOCUMENT-TYPE` 6681)·`:cancel`(6771~) — parameters·requestBody·responses·`x-*`·description **전부**. `components.schemas` `CancelResult`(7150 · `reversed`·`reversalTransactionNo`·`reversalBusinessDate`)·`DocumentProgress`(7229 · required 7241~ · `successorCount` 7309 · `cancelBlockedReasonCode` 7319 5값)·`DocumentProgressDetail`(7354)·`DocumentProgressStep`(7380 — **단계 목록을 무엇으로 채우나**: 체인 마디 P/O → 입하 → 입고 → 출고 …)·`DocumentSuccessor`(7426 — 두 갈래 문장). 공유계약 C-8(`businessDate` — **역트랜잭션의 `business_date` 는 누가 보내나**: `:cancel` 본문에 실리는가 · 없으면 §2 판정) · J-7(`CANCEL_IN_PROGRESS`)·J-8(재판정)·A-10(유형↔표 서버 소유).
13. 설계 저장소 사본(읽기 전용): `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/01/W-01-13-물류문서진행현황취소.md`(§5-2 파생 3칸 · 취소 사유 입력 · 9종 목록) · `screens/04/W-04-12-출하확정취소.md`(같은 규약의 둘째 구현 — 무엇이 같고 다른가) · `decisions-policy/공유계약.md` 의 J-7·J-8·A-10·C-8 · `progress/미결-대장.md` 에서 「취소」 grep
14. `docs/design-inquiries/README.md` + `026`·`030`·`031` — 요청서 형식 · 다음 번호 **032**
15. 이미 보낸 문의(`~/omf-design-requests/설계-문의-2026-09-0*.md`) 중 취소·`businessDate`(회신 15) 관련 · `docs/계약-되돌림-mdm.md`

## 계획서에 반드시 담을 것
1. **계약 읽기 표** — 4건 각각: 파라미터·본문 필드·응답·에러코드(계약이 이름 적은 것)·멱등/If-Match/ETag/403/`X-Worker-No`·`x-*` 노트. 추측 금지, 계약 문장 인용. 조회의 `documentTypeCode` **9종 enum 전건**과 각 값이 가리키는 표(`entity_type_registry` 실측과 대조 — 없는 유형은 「없다」) · `CD-CANCELABLE-DOCUMENT-TYPE` 3값 · `:request-cancel` 본문(사유 코드·상세) · `:cancel` 본문(있다면 `businessDate` · 없으면 역트랜잭션 날짜의 출처를 §2 로) · `CancelResult` 3칸 · `DocumentProgressStep` 의 단계 정의.
2. **물리 대조** — 계약 ↔ `document_cancellation`·`entity_type_registry`·3표의 `status_code`·`version_no` · `inventory_transaction` 역 칸 2. **`goods_issue` 취소 3칸 vs `document_cancellation` 한 표** — 둘 다 쓰나 한쪽만 쓰나(§2 2단계 「스키마를 안 늘리는 쪽」·「흔적」으로 판정 — `plan-api.md` 766~ 은 한 표, I-4.md 223 은 3칸 실재). 마이그 필요/불필요 실측 결론(필요하면 SQL 초안 · forward-only · 별도 선행 커밋).
3. **역트랜잭션 코어 `posting.reverse()`** — 시그니처 초안(`reverse(tx, { inventoryTransactionId, businessDate, reversalBusinessDate, ... })`) · 원 트랜잭션 라인을 읽어 **반대 부호**로 새 `inventory_transaction(_line)` 을 쓰고 `reversal_of_*` 2칸을 채우는 모양 · `from`/`to` 뒤집기 vs 수량 부호 뒤집기(원장 불변식 — 기존 `post()` 가 음수 수량을 허용하는지 실측) · 잔액 갱신 순서(I-4 R-1 7칸 오름차순 한 문장 · 입고 `postReceipt` 순서와 통일) · `NEGATIVE_BALANCE` 400(입고 취소 뒤 잔액이 이미 나갔으면) · **이미 역처리된 트랜잭션을 두 번 되돌리지 않는 가드**(`reversal_of_transaction_id` 유일 제약 유무 실측 · 없으면 손검사) · 멱등키 · 파티션 키(`business_date`) 복합 FK · 되짚기 `inventory_transaction_line_id` 가 라인에 남은 것을 어떻게 두나. **코어 전용 PR ≤200** 로 가른다 — 포함 파일과 단위 테스트 이름.
4. **후속 판정 함수 하나** — `cancellable`·`cancelBlockedReasonCode`·`successorCount` 를 조회 GET 과 `:request-cancel`·`:cancel` 이 **같은 함수**로 판정(plan-api B). 두 갈래: ⓐ `source_document_type_code`/`source_document_id` 역조회(`GOODS_RECEIPT`·`GOODS_ISSUE`·`PICKING_ORDER`·`INVENTORY_TRANSACTION` — 각 표의 칸 이름 실측) ⓑ `MATERIAL_CONSUMPTION` 은 LOT 축(`inbound_receipt_line.lot_id`·`goods_receipt_line.lot_id` → `material_consumption.lot_id`? 실측). **`INVENTORY_TRANSACTION` 을 후속으로 셀 때 자기 자신(전기 원장)을 빼는 규칙** · 취소된 후속은 세지 않는가 · 5값의 우선순위(`ALREADY_CANCELLED` > `CANCEL_IN_PROGRESS` > `TYPE_NOT_CANCELABLE` > `STATE_LOCKED` > `SUCCESSOR_EXISTS`?) 를 계약 문장으로 · `entity_type_registry` 에서 표를 읽어 **동적 SQL 을 짤지**(`$queryRawUnsafe` 식별자 — 안전 판정) vs 등록부는 «검증»에만 쓰고 Prisma 모델 매핑은 코드에 두는지 — §2 로 판정(plan-integration 663 「코드에 박지 않는다」 vs 식별자 주입 위험).
5. **조회 2건** — 9종 목록의 **한 형태**(`DocumentProgress` 칸을 각 표의 어느 칸에서 채우나 — 번호·상태·날짜·거래처·수량 요약) · 유니온을 SQL 로 하나 유형별 쿼리 후 합치나(페이징·정렬 축) · 미구현 유형(이동·조정·피킹 등 표는 있으나 데이터 0)의 취급 · `DocumentProgressDetail`/`Step` 의 체인 재구성(P/O → 입하 → 입고 → 출고 를 `source_document_*` 로 거슬러 올라가는 깊이) · 모델 배분 sonnet 가능 여부.
6. **취소 요청·실행 상태기계** — `document-request-cancel`(`REGISTERED`·`POSTED` → `CANCEL_REQUESTED`)·`document-cancel`(`CANCEL_REQUESTED` → `CANCELLED`) 을 **3표 3키**에 등록(`transitions.ts` — `goods_issue` 키는 I-4 R-2 대로 «안에» 더하고 `inbound_receipt`·`goods_receipt` 키는 새로 열되 I-3 R-12 ⓑ §7-4 자물쇠 재판정) · `:request-cancel` 이 `approval.request()` 를 부르는 모양(`approvalTypeCode` 3유형 이름 · 결재선 없는 유형/사업부면? · `assertNoOpenRequest` · **FK `approval_request_id` 를 안 채운다**(plan.md #12) → 응답의 `ApprovalRequestRef` 만) · `:cancel` 이 `assertApproved(tx, { targetTypeCode, targetId, approvalTypeCode: *_CANCEL })` 로 자물쇠를 풀고 **재판정 → `document_cancellation` INSERT → `POSTED` 였으면 `reverse()` → `CANCELLED`** 순서 · `previous_status_code` 채움 · 승인 거절 뒤 `CANCEL_REQUESTED` 를 되돌리는 경로가 계약에 있는가(없으면 §2 · 문의 후보) · `CANCEL_IN_PROGRESS` 가 두 번째 `:request-cancel` 을 막는 자리(J-7).
7. **유형별 되돌림 부수효과** — 입하 취소: `purchase_order_line.received_qty` 되돌림(I-3 R-12 ⓐ 오름차순 `FOR UPDATE` + 하한) · 라인의 `lot_id` LOT 처리(ⓒ — LOT 상태를 옮기나 그대로 두나 · 026 미회신이면 §2 잠정) · 입고 취소: 원장 역 + 입하 `status`? · 출고 취소: 원장 역(잔액 복원) + `approval_request` 업무 승인 흔적 보존(#12). 각각 「원장 되돌림 = 코어, 수량 되돌림 = 도메인 어댑터」 경계.
8. **횡단** — 403 등록(`manual-permissions.ts` 에 없는 것만 — 도출표 중복 금지 · 화면 `W-01-13`) · 멱등 2(`runIdempotent` 키) · If-Match 2 — ⛔ **토큰이 대상 문서의 `version_no`**(어댑터가 표·칸을 안다 · `ifMatchVersion` 재사용) · ETag **없음**(응답이 `CancelResult`·`ApprovalRequestRef`) · 404 선언 여부 · `TYPE_NOT_CANCELABLE` 이 400 인가 404 인가(계약 문장) · `screenId` 생략(`omitEmpty`).
9. **e2e 정리 순서** — ⭐ **M1 완료 조건 한 줄**(plan.md 82행: 입고 취소 요청 → 결재 승인 → 실행 → 역트랜잭션 → `balance` 원복)을 어느 PR 에 두나 · ⭐ **J-8 e2e**(승인 → 그 사이 후속 생성 → 실행 400 `SUCCESSOR_EXISTS` → 승인은 `APPROVED` 그대로) · `NEGATIVE_BALANCE` e2e(입고 취소 전에 출고로 잔액을 빼 둔다) · `REGISTERED` 전표 취소(원장 없음 · `reversed:false`) · 픽스처 재사용(goods-issue e2e 의 `makeLot()`·`stock()`) · cleanup 에 `document_cancellation`·역 `inventory_transaction(_line)`(복합 FK 자기참조 — 역행 먼저 지운다)·`approval_request(_step)` 순서.
10. **설계 미정 자리** — 각각 README §2 절차로 판정. 문의가 필요한 것은 제목만(요청서는 구현 시 작성, **032** 부터). 기존 문의(026·030·031)와 겹치면 「기존」. 후보: 역트랜잭션 `business_date` 출처(C-8) · 취소 승인 거절 뒤 상태 복귀 경로 · `goods_issue` 취소 3칸 vs `document_cancellation` 이중 흔적 · 9종 중 표만 있는 유형의 목록 취급 · `reversalTransactionNo` 채번 규칙.
11. **PR 분할** — 초안 3(plan.md·S06). 초안: ① `posting.reverse()` 코어(≤200 · opus 전용 PR · 단위 테스트만) ② 조회 2 + 후속 판정 함수(sonnet 가능? — 판정 함수는 opus) ③ `:request-cancel` + `transitions.ts` 3키 + 승인 연결 + e2e ④ `:cancel` + 유형별 어댑터 3 + `document_cancellation` + J-8·M1 e2e(opus). 비테스트 diff ≤400(코어 ≤200). 각 PR 의 파일 목록·예상 diff·**테스트 이름 목록을 unit 과 e2e 로 나눠 각각** — e2e 로 도달 못 하는 가드(이중 역처리·남의 id·`STATE_LOCKED`·경합)는 **단위 테스트 이름을 여기서 확정**한다. 스택 PR 로 낼 순서.
12. **I-8·I-13·I-14·I-23 인계 모양** — `reverse()` 시그니처가 정정(I-7)·조정 역분개(I-14)·출하 취소(I-23)에 그대로 쓰이는가 · 후속 판정 함수를 I-23 `shipments:cancel` 이 import 하는 위치(도메인이 다른 도메인의 service 를 부르지 않는다 → `src/core/` 인가 `src/logistics/document-progress/` 인가) · `entity_type_registry` 에 유형이 늘 때 시드만 바꾸면 되는 구조인지.
13. **통합 계획 대조** — `plan.md` §1 39행·§3 101~102·`plan-api.md` S06·B·`plan-integration.md` 244~250·`plan-uiux.md` ⑫·U18 과 다른 점. README §1-2 3조건 중 걸리는 것이 있으면 맨 위에 ⚠ 로.

금지: 구현 코드 작성 · 계약 파일 수정 · 다른 문서 수정 · 값 지어내기(값 목록이 없으면 「없다」고 적고 §2 로 판정) · `omf-mes`·`omf-mes-client` 저장소 쓰기 · `contracts:update`/`contracts:check`.
