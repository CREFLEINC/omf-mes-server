# I-7 개별 계획안 재검토 — **uiux 관점**

> 정본: `docs/coverage-100/plan-uiux.md` 58행(U24)·59행(U25)·89행(범례)·359~376(U24·U25 표)·631행·1096행·§9-3.
> 화면 사본 실측: `/Users/rangkim/projects/crefle/omf/apps/omf-mes/design/wiki/screens/02/` `P-02-04`·`P-02-06`·`W-02-05`·`W-02-08` · `06/W-06-15`.
> 실측일 2026-09-06 · worktree `docs/coverage-100-i7-plan` · 계약 사본 `a6a87e1` · 읽기만.

---

## ⛔ 먼저 — 이 관점에서 하나가 통째로 빠졌다: `Lot.progress`

`contracts/logistics-01자재창고.json` `LotProgress` 스키마 원문:
> ⌜이 LOT 의 생산 진척. **서버가 계산해 내린다 — 화면이 각자 계산하면 완료 화면과 라벨 화면의 값이 갈린다(공유계약 L-2). 근거: `P-02-04` §5 · `P-02-06` §5-2**⌝
> required 4: `goodQty`(⌜이 LOT 에 배분된 양품 합계⌝) · `achievementRate`(÷`initialQty`) · `varianceQty` · `completionJudgmentCode` enum `UNDER`·`NORMAL`·`OVER`.

- 실린 자리: `GET /trace/lots/{lotId}?withProgress=true` **하나뿐**(질의 실측 — 목록 `GET /trace/lots` 의 16질의에 `withProgress` 가 **없다**).
- 지금 **안 채운다**: `src/trace/lot/lot-view.ts:9-11` ⌜계약에 있으나 지금 채울 수 없는 칸이 셋 — `progress`(**실적 파생 · production 미구현**)⌝. ⇒ **I-7 이 그 「미구현」을 없애는 슬라이스**인데 계획 897줄에 `progress` 가 **0회** 나온다.
- 화면이 대신 셀 길이 **없다**(전부 실측): `GET /production/production-results` 질의 8개에 **`lotId` 가 없다** · `ProductionResult` 22칸에 **`lotAllocations` 가 없다**(배분은 쓰기 전용) · LOT 목록에 `withProgress` 없음.
- 그런데 `derived-permissions.ts:111` 은 `GET /production/production-results` 의 화면을 **`P-02-06`·`W-02-05`** 로 적었다 — P-02-06 이 실적 목록을 불러도 **LOT 로 좁힐 수 없다.**
- 걸리는 화면 자리: `P-02-06` §3 목록의 「480/500」·§5-2 누적 양품·§5-3 미달 판정·§7 `ProgressBar` / `P-02-04` §3 「잔여수량 380/500」·§5-1 저장 활성 조건「잔여수량 검증 통과」·§5-2 저장 후 분기(잔여>0 회귀 / =0 → P-02-06).
- ⇒ **`progress` 를 안 채우면 두 화면의 핵심 숫자와 버튼 활성 조건이 서지 않는다.** 서버가 `:complete` 안에서 **이미 같은 식을 계산한다**(I-7 §3-3 목표=`initial_qty` · 누적=`Σ allocated_qty` · 미달/정상/초과) — 계산해 놓고 버린다. `W-02-08` §5-1(⌜파생 값은 서버가 계산⌝·§L 조항 ②)과 계약 L-2 가 같은 말이다.
- 제안: **PR ④ 에 `LotView.progress`(+`withProgress` 질의)를 얹는다**(`lot-view.ts`+`lot.service.ts` ~+35줄 · ~200 → ~235 로 ≤350 안). 판정식은 `:complete` 와 **같은 함수**를 부른다(§3-3 과 갈리면 L-2 위반).

---

## 판정 1~9

### 1. 문의 041~044 가 새 문의인가 — ✏
- **041 ⓐ·ⓑ 한 장 묶음 ✅** — `W-02-05` §5-8 이 ⓐ(⌜상신 본문은 「정정 내용」을 실어 나르지 않는다 … ⛔ 상신이 정정을 «예약»하지 않는다⌝)와 §6 「B급인데 상신 시도 → 400 · ⭐ 정상 동선에서는 일어나지 않는다」를 **한 절에서** 적었다. 화면도 도달 불가로 본다 ⇒ I-7 의 「그 400 을 만들지 않는다」와 충돌 없음.
- **042 ✅ 문의가 맞다 · 다만 화면 근거를 하나 더 실어야 한다** — `W-02-05` §4-B ⌜정정 실적도 **같은 목록에 쌓이고** `corrects_production_result_id` 로 원본을 가리킨다⌝ 가 「잎만 센다」의 화면쪽 뒷받침이다(목록은 안 거르고 누계만 거른다 = I-7 §7-1 과 정확히 같다).
- **043 ✅ · 문안에 화면 파급 한 줄 추가** — `P-02-06` §8 #2 가 이미 ⌜LOT별 사유가 필요하면 `[docs→데이터모델]`. 지금은 W/O 단위로 기록⌝ 로 받아들였다. 남는 진짜 피해는 **표시**다 — `W-02-05` §4-A 가 **같은 칸**을 「미달·초과 사유」로 **마감 화면에 그린다**. 슬롯 N개를 순차 완료하면 마지막 LOT 사유가 마감 화면의 사유로 보인다. 요청서에 이 문장을 넣어야 「왜 칸이 더 필요한가」가 선다.
- **044 ✅ 별도 문의가 맞다(042 와 합치지 않는다)** — 042 는 W/O 축, 044 는 **LOT 축**이고 갈라지는 화면이 다르다(042→`W-02-08`·`W-02-05` / 044→`P-02-06` §5-2). 화면 증상도 다르다 — 양품을 낮추는 정정 뒤 `P-02-06` 의 달성률이 그대로라 **작업자가 「완료 처리」를 누를 수 있다**(§5-3 표).
- ✏ **새 문의 후보 하나가 빠졌다 — `PRODUCTION_RESULT_CORRECT_REASON` 값 0건**: `reasonCode` 는 계약 required 이고 `W-02-05` §4-D 가 ⌜값 목록은 `GET /mdm/code-values?codeGroupCode=PRODUCTION_RESULT_CORRECT_REASON` — ⛔ **화면이 값을 지어내지 않는다**⌝ 로 못박았다. I-7 §1-5 실측이 그 그룹 **0값**이다 ⇒ **정정 사유 드롭다운이 비어 정정 화면이 서지 않는다.** 서버는 대조를 안 걸어(I-6 선례) 막지 않지만, 이건 「알려둘 것」이 아니라 **시드/코드사전 요청**이다. 최소한 §9-3 에 한 줄(현재 0건이라 화면이 고를 값이 없다)을 넣는다.
- ⚠ 내 관점 정본과의 충돌 하나를 기록해야 한다: `plan-uiux.md` §9-4 Q 는 ⌜`F-6` 판정할 수 없음을 통과로 처리하지 않는다 — **코드 검증이 필요한 자리는 막는다**⌝ 인데 I-7 은 빈 그룹 둘(`LATE_ENTRY_REASON`·`PRODUCTION_RESULT_CORRECT_REASON`)에 대조를 안 건다. I-6 §9-1 #2 선례가 이기는 것에 동의하나 **3단계 흔적(주석)** 으로 이 충돌을 남겨야 한다.
- `resultSourceCode` **✅ 모순 아님** — `P-02-04` §8 #6 이 ⌜⛔ **화면이 고르는 값이 아니다 — 서버가 안다**⌝ 와 ⌜생성 타입(`api.d.ts`)이 값을 갖는다 · ⛔ `codeGroupCode=` 포인터를 적지 않는다⌝ 를 **같은 칸에** 적었다. 「받되 enum 검증」으로 닫힌다.

### 2. 마이그 D1·D2 — ✅ (칸 이름·상수값 모두 화면과 맞는다)
- **D1 ✅** — `P-02-04` §4 ~~교대~~ 행 ⌜⛔ 이 화면은 교대를 받지 않는다(사용자 확정 2026-08-18) · 「교대 필수」는 **물리 모델에서 올라온 것**이고 설계 결정을 앞설 수 없다 · **계약에서도 필수를 풀었다** · 모델 결손은 📨 작업 통지⌝ + §8 해소된 미결 #2. 화면이 D1 을 직접 지시한다.
- **D2 이름 `correct_reason_code` ✅** — 화면 라벨이 「**정정** 사유」(`W-02-05` §4-D)이고 코드 그룹이 `PRODUCTION_RESULT_CORRECT_REASON` 이다. 형제 `material_consumption.change_reason_code` 는 「변경」 축이라 이름을 따라갈 이유가 없다. nullable ✅(원본 행은 빈다 — `P-02-04` 의 일반 저장에는 이 칸이 없다).
- **`status_code = 'CONFIRMED'` 상수 ✅** — 화면 어디도 이 값을 **보이지 않는다**(《실적 목록》 표시 칸은 `W-02-05` §4-B 실측으로 5수량·`corrects…`·`result_sequence`·`occurred_at`/`recorded_at`·`worker_id` 뿐). `P-02-04` §8 #6 이 ⌜`status_code` 는 「코드가 아니다」로 판정됐다(2026-09-02)⌝ 라 **값 목록 조회도 없다** ⇒ 상수가 화면에 파급 0. nullable 대안은 응답 required 라 불성립.
- ✏ `plan-uiux.md` **58행 U24 의 「마이그 **불필요**」가 틀렸다** — D1·D2 둘이다. §11 대조표에 이 행이 없다(#1 은 `plan.md` §4 만 고친다).

### 3. 상태기계 — ✏ (한 갈래가 비었다: **누적 0**)
- W/O 상태표 ✅ — `CLOSED` 허용의 근거 `W-02-05` §5-4 규칙 3 원문 대조 확인(⌜✅ 마감된 W/O에 상쇄 실적을 넣을 수 있다 … 재오픈은 마감을 «되돌리는» 것이고 정정은 실적을 «덧붙이는» 것⌝ · ✓확정 2026-09-04). §6 예외표 「마감 후 정정 시도 → ⭐ 갈 곳이 생겼다」도 같은 방향.
- `:complete` 가 어느 상태 칸도 안 옮긴다 ✅ — `P-02-06` §5-5 인용이 **글자 그대로 정확**하고 §8 #1 이 ⌜완료·미달 마감은 **어느 코드 축에도 넣지 않는다** — `Lot.completedAt`(시각) + 서버 판정값으로 선다(`omf-mes#269`)⌝ 로 한 번 더 닫았다. `lot.completed_at` 실재 ✅(`lot-view.ts:31` `completedAt`).
- 대상 = `initial_qty` vs `Σ allocated_qty` ✅ — `P-02-06` §4-A·§4-C·§5-2 그대로.
- ✏ **빠진 갈래**: `P-02-06` §6 ⌜**누적 = 0 → 완료·미달 마감 둘 다 비활성** — 아무것도 안 만든 LOT은 마감할 것이 없다. **폐번 대상**(W/O 마감 소관)⌝. I-7 §3-3 은 미달/정상/초과 셋만 가른다 ⇒ 누적 0 이면 「미달 + 사유」로 **200 이 난다.** 그 LOT 은 L1 을 안 탔으니 `WAITING` 인 채 `completed_at` 을 얻고, 이어서 I-6 `:close` 의 **L2(대기→폐번)가 그 행을 폐번한다** — 「완료된 폐번 LOT」이 남는다. ⇒ **배분 0건(=`Σ allocated_qty` 0 · `lifecycle_status_code='WAITING'`)이면 400 `STATE_LOCKED`** 를 §3-3 에 더한다(기준 2 · 화면이 이미 그렇게 막고 있다). 단위 테스트 이름도 하나.
- ✏ 「두 번 완료 = 400 `STATE_LOCKED`」 ✅ 이나 근거를 화면으로 보강 가능 — `P-02-06` §6 「이미 완료된 LOT | **목록에서 제외**」 + `GET /trace/lots` 의 `completed` 질의(실측)가 `completed_at` 을 축으로 쓴다 ⇒ I-7 이 그 칸을 채우면서 **그 질의가 처음으로 살아난다**(알려둘 것 후보).

### 4. `POST` 코어 — ✅ · 두 가지 알려둘 것 추가
- `X-Worker-No` 400 `REQUIRED` ✅ — 화면이 정확히 그 전제다: `P-02-04` §4 작업자 행 ⌜⛔ 화면이 보내지 않는다 — **서버 파생** · 귀속 헤더 `X-Worker-No`(사번)에서 서버가 푼다(D-5) · ⛔ 작업자를 고르는 칸을 두지 않는다⌝ + v0.3 변경 이력(⌜`workerId`·`terminalId` 프로퍼티가 **제거**됐다⌝). `app_user_id` 도출 금지 ✅.
- `terminal_id` 영원히 NULL ✅(`P-02-04` §4 「단말 토큰에서 서버가 푼다(F-2)」).
- ✏ **알려둘 것 추가 — 단말 게이팅은 서버에 없다**: `P-02-04` §5-1 ⌜`mdm.terminal_process.**can_input_result**` 가 이 화면의 저장 활성 조건⌝ · `P-02-06` §5-1 ⌜`can_complete_work` 없으면 **두 버튼 모두 비활성**⌝(8플래그 중 이 화면 하나가 쓴다 · F-1). 서버는 단말 토큰이 없어 이 둘을 **검사하지 않는다** ⇒ 게이트가 화면에만 있다. `ⓓ`(terminal_id NULL)의 파급이므로 §9-3 에 한 줄로 잇는다.
- `Σ allocatedQty` vs `goodQty` 검사 안 함 ✅ — `P-02-04` §4 「대상 LOT … 진입 시 선택」 **단수**이고 §6 「초과 생산은 허용(✓확정 QA #27) → 차단하지 말고 확인 후 진행」이라 화면이 막을 자리도 아니다.
- `late_entry_reason_code` ✅ — `P-02-04` §4 ⌜⭐ **항상 선택 입력**(2026-08-31 확정 `#61`) · 임계 판정을 하지 않고 조건부 필수도 걸지 않는다⌝ · 값은 `GET /mdm/code-values?codeGroupCode=LATE_ENTRY_REASON`(고객이 늘린다) ⇒ 서버 대조 없음이 화면과 일치.

### 5. `:correct` — ✅ (등급식·게이트 모두 화면과 일치) · 에러 코드 한 건 추가
- 대체값 + A급 판정식 ✅ — `W-02-05` §4-D ⌜**정정 수량 5칸** … ⭐ 이 다섯 칸이 **등급 판정의 입력**이다 — 서버가 본다. 화면은 **등급을 보내지 않는다**⌝ · §5-8 ⌜⭐⭐ 등급은 «서버»가 판정한다 — 화면이 고르지도 보내지도 않는다 … 그 **수량이 바뀌는가**가 서버가 보는 입력이다⌝. 승계 뒤 전체 비교/준 칸만 비교는 화면 관점에서 결과가 같다(승계값=원본값).
- 승인 게이트가 `:correct` 에 선다 ✅ — §5-8 흐름도가 400 을 **`:correct`** 에서 낸다. 화면은 두 400 을 **다르게 다룬다**: 「A급인데 승인 전」→ **승인 상신 대화상자**(§6) / 「이미 진행 중」→ 「결재함(`W-CO-09`)에서 확인하십시오」(§6). ⇒ `APPROVAL_REQUIRED` ↔ `APPROVAL_IN_PROGRESS` 를 **반드시 갈라 내야 한다**(I-7 이 그렇게 했다 ✅ · `error-codes.ts:34-41` 주석도 같은 말).
- `X-Worker-No` 없음 ✅ — 계약 사본 `jq` 실측: `:correct` 의 parameters 는 `productionResultId`+`IdempotencyKey` **둘뿐**. `W-02-05` §5-8 표의 「`:correct` | 멱등 · 사번」은 **낡았다**(그 절 스스로 ⌜부르는 화면이 관리웹 하나로 확정되면서 이 헤더의 등급이 어긋났다 · 📄 기록만⌝). I-7 판정이 맞다.
- `note`→`remarks` ✅(§9-3 ⓗ). 정정본 목록 노출 ✅(§4-B).

### 6. `:request-approval` — ✏ **`ROUTE_AMBIGUOUS` 가 표에서 빠졌다**
- `W-02-05` §6 예외표가 갈래 **넷**을 그린다: 결재선 없음(`ROUTE_NOT_FOUND` — 「결재선 정의(`W-06-15`)에서 「실적 정정」 결재선을 먼저 등록하십시오」) · **결재선이 둘 이상 → `400 code=ROUTE_AMBIGUOUS` ⌜서버가 임의로 하나를 고르지 않는다(B-12)⌝** · 진행 중 요청 · B급 상신.
- 실측: `error-codes.ts:31` 에 `ROUTE_AMBIGUOUS` 가 있고 `approval.service.ts:188` `selectRoute` 가 **이미 던진다**. `W-06-15` §8 #1·§5-3 이 ⌜`approval_route` 에 유일 제약이 **없다** — 같은 유형·사업부로 활성 결재선 다중 생성 가능⌝ 라 **실제로 나는 갈래**다.
- ⇒ I-7 §1-6 에러표와 §6 표에 **한 행 추가**(새 코드 0건은 유지 — 코어가 던진다). 화면이 전용 문구를 갖고 있으므로 400 봉투의 `code` 가 그대로 나가야 한다.
- 나머지 ✅ — `businessUnitId` null(`W-06-15` §4-A 가 사업부를 nullable 로 그린다) · 202 · 반려 뒤 재상신(§6 ⌜반려된 요청은 진행 중이 아니라 다시 상신할 수 있고, 그때는 새 요청이 만들어진다(J-6)⌝) · 상신이 정정을 예약하지 않음(§5-8).

### 7. 조회 3 · `lot-lifecycle-events` — ✏
- 정렬 `occurred_at DESC` ✅ — `W-02-05` §4-B ⌜⭐ 《실적 목록》의 **정렬·식별 축** = `occurred_at`⌝.
- 목록에서 정정본을 안 거른다 ✅ — 같은 절(⌜정정 실적도 같은 목록에 쌓이고⌝) + §5-8 흐름(《실적 목록》에서 원본 1건 선택 → [정정]).
- ✏ **질의에 `lotId` 가 없다**를 「알려둘 것」에 넣는다 — `derived-permissions.ts:111` 이 이 목록의 화면에 **`P-02-06`** 을 적었는데, P-02-06 은 LOT 단위 화면이라 좁힐 축이 없다. ⇒ 위 ⛔(`Lot.progress`)와 같은 뿌리.
- `shiftId` 필터 영구 0건(ⓒ) ✅ — 화면이 교대를 안 보내니 화면 피해는 0(`P-02-04` §4).
- `lot-lifecycle-events` 모듈 신설 ✅ · 기간 required ✅ · 쪽 나눔 없음(ⓚ) ✅. 소유 화면은 `plan-uiux.md` 376행대로 `W-02-05`·`W-02-06`(폐번 이력 §5-3) — I-7 §7-3 의 「L1 만 보인다」와 어긋나지 않는다.

### 8. 횡단 · PR 분할 — ✏ (①의 모델 · ④의 예산)
- 403 추가 0 ✅ — `derived-permissions.ts:232-234·258` 실측 확인(`:correct` = `P-04-03`·`W-02-05` · `:complete` = `P-02-06`). `W-02-05` §5-8 이 ⌜현장 단말 둘은 스스로 거부했다 — `P-02-04` §6 · `P-04-03` §5-6⌝ 로 화면 소유를 닫아 두었다.
- PR **4** ✅ — 내 정본이 오히려 이 편이다: `plan-uiux.md` 58·59행이 U24 **PR 2** + U25 **PR 2** 로 이미 4를 셌다(`plan.md` §1 41행의 3 과 다르다). ⇒ §11 #9 의 근거로 **`plan-uiux` 58·59행을 인용**할 수 있다.
- ✏ **PR ④ 예산**: 위 ⛔ 대로 `LotView.progress`+`withProgress` 를 얹으면 ~200 → ~235. 그래도 ≤350. **①을 sonnet 으로 내리는 것에는 반대** — 마이그가 들어 있고(README §4) 조회 뷰가 `omitEmpty` 규칙(required 14 는 0 도 값으로)을 처음 세운다.
- ✏ 픽스처: `PRODUCTION_RESULT_CORRECT_REASON` 값이 0건이라 e2e 는 **아무 문자열이나** 넣게 된다 — 그 사실을 §10-1 픽스처 표에 한 줄로 남긴다(대조를 안 걸기 때문에 통과한다는 것이 테스트로 드러나야 한다).
- cleanup 자기참조 2단 삭제 ✅.

### 9. 자기 관점 계획서와 어긋난 자리(구현에 영향 주는 것만) — ✏ **3칸**
`plan-uiux.md` **58행 U24** ｜ 열은 `… 새 표 · 마이그 · posting · 상태기계 · PR`:
| 칸 | 적힌 것 | 실측 | 고칠 것 |
|---|---|---|---|
| 마이그 | **불필요** | D1+D2 **2건** | 「2(D1 `shift_id` 완화 · D2 `correct_reason_code`)」 |
| posting | **○ 생산** | ⛔ 원장 안 지난다(I-7 §4-1 · `plan.md` §0 151행) | 「—」. **§11 #2 는 `plan-api.md` S16 만 고치는데 같은 오류가 내 정본에도 있다** |
| PR | 2 | ①②③ 셋이 U24 의 5건을 진다 | 3 |

`plan-uiux.md` **59행 U25** 「상태기계 **○ 수명주기**」도 ✏ — L1 을 일으키는 것은 U24 의 `POST` 실적이고, U25 의 `:complete` 는 **어느 상태 칸도 안 옮긴다**(§3-3). 「— (완료는 `completed_at` 시각)」로 고친다.
✏ **§11 #11 의 지시 절반이 무효다** — 「U24 의 `:correct` 행에서 「사번」을 빼라」인데 **362행 실측상 그 행은 이미 「멱등」 하나뿐**이다. 뺄 것이 없다. `:complete`(U25 371행)의 「사번 — 받되 저장 칸 없음」 한 줄만 유효하다. §11 #10(ETag 표기)은 ✅ — 89행 범례가 ⌜`ETag` = `If-Match`⌝ 라 고칠 것 없음이 맞다.
✏ **알려둘 것 추가 2건**: ⓣ `:complete` 응답에 **ETag 가 없는데** 화면은 낙관적 잠금을 기대한다(`P-02-06` §6 「동시 완료 | `version_no` 낙관적 잠금 — B-1」) — `lot.version_no` 를 +1 하고도 새 토큰을 못 돌려주므로 화면은 완료 뒤 **다시 `GET /trace/lots/{lotId}`** 해야 한다. ⓤ 단말 게이팅 2플래그 서버 미검사(위 4번).
✅ `plan-uiux.md` §9-3 의 `achievementRate` 분모 행(LOT `initialQty` vs W/O `orderQty`)은 I-7 §9-1 #15 가 그대로 이어받았다 — `LotProgress.achievementRate` 의 계약 ⚠ 주석과 `W-02-08` §5-2(⌜분모는 지시 수량⌝)가 둘 다 실측으로 확인된다. 요청서를 I-7 이 쓰는 것 ✅.

---

## 재수립 결과 — 5줄

1. **I-7.md 에 반영할 수정 7건**: ① **`Lot.progress` 를 PR ④ 에 넣는다**(`withProgress` — P-02-04 §5-2·P-02-06 §5-2 의 유일한 원천이고 클라이언트가 대신 셀 길이 0) ② `:complete` 에 **누적 0(배분 0건) → 400 `STATE_LOCKED`** 갈래 추가(P-02-06 §6) ③ `:request-approval` 에 **`ROUTE_AMBIGUOUS`** 행 추가(W-02-05 §6 · `approval.service.ts:188` 이미 던진다) ④ 043 요청서에 「같은 칸을 `W-02-05` §4-A 가 마감 화면에 그린다」 파급 문장 ⑤ §9-3 에 **`PRODUCTION_RESULT_CORRECT_REASON` 0값 → 화면 드롭다운이 빈다** ⑥ §9-3 에 **단말 게이팅 2플래그(`can_input_result`·`can_complete_work`) 서버 미검사** ⑦ §9-3 에 `:complete` **응답 ETag 부재 ↔ 화면의 낙관적 잠금 기대**.
2. **`plan-uiux.md` 에 반영할 것 4건**: 58행 U24 의 「마이그 불필요→2」·「posting ○ 생산→—」·「PR 2→3」, 59행 U25 「상태기계 ○ 수명주기→—」. §11 #11 의 「U24 `:correct` 에서 사번 빼기」는 **무효**(362행에 이미 없다) — `:complete` 쪽 한 줄만 유효.
3. **문의 최종 건수 — 4 유지(041~044)** + 신규 후보 1(정정 사유 코드값 0건: 문의가 아니라 **시드·코드사전 요청**으로 §9-2 「기존으로 미루는 것」에 배치). 042·044 를 합치지 않는 데 동의 — 갈라지는 화면과 증상이 다르다(042=W/O 축/W-02-08 · 044=LOT 축/P-02-06).
4. **PR 분할 최종안 — 4 유지 · 전부 opus · 직렬**: ①~250(마이그+골격+조회3) ②~300(POST+L1+누계) ③~250(`:correct`+`:request-approval`) ④**~235**(`:complete` + **`Lot.progress`** + M1 마디 e2e). ①을 sonnet 으로 내리는 것에는 반대.
5. **관점 결론**: 계약·물리 대조는 촘촘하나 **「화면이 이 값을 어디서 받는가」를 한 번도 되묻지 않아** `Lot.progress` 가 통째로 빠졌다. 나머지 8항목은 화면 명세 원문과 글자 단위로 맞는다(특히 §3-3 `P-02-06` §5-5 인용·§5-5 A급 판정식·`CLOSED` 허용).
