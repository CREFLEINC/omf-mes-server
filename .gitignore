# OS
.DS_Store

# 로컬 도구 상태
**/settings.local.json

node_modules/
dist/
coverage/
.env
.env.prod
.env.*.local
*.log
.eslintcache
.swc/
*.tsbuildinfo

# 계약 사본(contracts/*.json)에서 만들어지는 파생물 — Prisma Client 와 같은 취급이다.
src/contracts/*.d.ts

# Postman 환경 파일 — 관리자 비밀번호가 들어간다(예시본은 *.example.json)
test/postman/local.postman_environment.json

# 산출물 생성 스크립트가 만드는 것
__pycache__/
